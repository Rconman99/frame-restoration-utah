# Frame Roofing Utah — Claude Master Context
> Last updated: 2026-06-03 (Wednesday AFTERNOON auto-refresh — POST-ACTIVITY) — **Three AEO-buildout PRs merged this afternoon (13:57–14:23 MDT, HEAD `fbdb501` → `0b697f8` = `origin/main` = `main`):** **#77** (`4c2c1ce`) flips the review-integrity gate to **BLOCKING** (no cloned `AggregateRating` on city pages, real reviewers only — `scripts/audit-review-integrity.mjs`); **#78** (`2b74264`) adds real per-location `Review` JSON-LD to the **top-10 traffic cities (2 → 12 pages)**; **#79** (`0b697f8`) completes **`hasMap` + `sameAs` on all 47 location pages** + an entity-consistency gate. **Pillar 2 (entity consistency) of the new `AEO-BUILDOUT-HANDOFF-2026-06-03.md` roadmap is now DONE** (hasMap 0→47, sameAs 0→47); **Pillar 3 (review velocity)** infrastructure is live and rolling 12 → 47. A new untracked `AEO-BUILDOUT-HANDOFF-2026-06-03.md` reframes the remaining AEO gain as **~80% OFF-page** (directory saturation + `site:` index verification + third-party consensus across BBB/Angi/Thumbtack/manufacturer locators/Reddit), records **GBP enforcement-watch CLEARED** (5.0★, reviews 20 → 27 flowing — Pillars 0+3 unblocked, no reinstatement appeal needed), and sets the win metric as **Perplexity/AIO money-query citations 1/10 → N, tracked weekly** in `frame-utah-aeo/ai-visibility/log.jsonl`. The earlier same-day 6/2-evening location-pages session is also captured below: **A productive 6/2-evening location-pages session: 3 PRs merged to `main` (#72–#74) plus a built-but-not-yet-merged 2-page feature branch.** `git log --since="7 days ago"` + HEAD show four commits past the 6/2 early-AM refresh (`74820c7`). **On `main` (HEAD `74820c7` → `00f273d` = `origin/main` = `main`):** **PR #72 (`48e840c`, 19:45 MDT) — `content(locations): deepen 15 thin location pages past the 500-unique-word floor`:** the advisory city-quality gate had flagged 15 pages at 486–499 unique words; each got **2 genuinely city-specific FAQ Q&As** added to **both** the FAQPage JSON-LD **and** the visible accordion (kept in sync), grounded in real geography/climate/housing — Bluffdale Point-of-the-Mountain gap winds, Santaquin 5,000-ft snow load + wildfire interface, Magna Kennecott-era housing + lakebed dust, Wallsburg 5,600-ft Deer Creek snow load, Syracuse Great Salt Lake/Antelope Island wind, etc. (no vocabulary padding, no fabricated jobs/names/numbers; built via 15 parallel agents, self-verified then independently re-verified). All 15 now clear the floor (560–622w). Pages: bluffdale, clearfield, kaysville, kearns, lindon, magna, north-salt-lake, santaquin, south-salt-lake, springville, syracuse, wallsburg, west-bountiful, west-valley-city, woods-cross. **This closes the long-running "17 thin location pages 484–498w" carry-forward** — all 45 now PASS depth. **PR #73 (`37d0eb1`, 19:48 MDT) — `ci(city-quality): flip doorway/thin-page guard from advisory to blocking`:** with all 45 pages clearing depth + local-substance + rating-integrity, the `city-quality` job lost its `continue-on-error` — a thin or `{city}`-swapped templated location page can **no longer silently ship** (same ratchet pattern as link-integrity). **Consequence: the recurring "merged PRs read UNSTABLE because city-quality is advisory" caveat from prior refreshes is now retired** — that gate is real. **PR #74 (`00f273d`, 21:50 MDT) — `fix(seo): consolidate duplicate about pages on /pages/about`:** two about pages were competing (sitemap pointed at `/about` = `about.html`, 1,424w, while nav + the richer content live at `/pages/about` = `pages/about.html`, 2,452w, self-canonical). Consolidated on the richer nav-linked page: `sitemap.xml` `/about`→`/pages/about`, `vercel.json` 301 `/about`→`/pages/about`. Everything (sitemap, nav, canonical, redirect) now points at one about URL — **closes the `about.html` canonical carry-forward.** **Built on the feature branch (NOT yet merged to `main`):** `e29541b` (23:20 MDT, on `feat/location-pages-fruit-heights-spanish-fork`) — `feat(locations): add Fruit Heights + Spanish Fork location pages`, the two served-area pages whose dead links were removed in #60. Both are **real, depth-complete** pages (not `{city}`-swapped templates), authored from the nearest sibling's structure (Kaysville→Fruit Heights 665w / Springville→Spanish Fork 705w): Fruit Heights carries the Davis County east-bench wind story (Sept 8 2020 100+ mph windstorm, Baer Canyon/Shepard Lane, ~4,800-ft snow/ice-dam load, Class-4 vs standing-seam); Spanish Fork the Spanish Fork Canyon wind story (the canyon that powers Utah's first commercial wind farm, Spanish Oaks/Canyon Hills/Palmyra benches vs the 1851 downtown grid). Full JSON-LD (BreadcrumbList/Article/RoofingContractor+GeoCoordinates/FAQPage ~14 Q&As), **NO AggregateRating** (would red-block the manifest-less CI gate), canonical identity from `business.json` (public 435-292-8802 only, write-review deep link, `/locations/<slug>` canonical), compliance-safe framing (deliberately did NOT copy the borderline adjusting language still in the Kaysville/Springville templates). Wiring: `sitemap.xml` both URLs, `index.html` Davis/Utah County chips, reciprocal nearby links (Kaysville+Farmington↔Fruit Heights, Springville+Payson↔Spanish Fork). **All gates green incl. the now-blocking city-quality (47/47 PASS) — corpus → 47 location pages once this merges.** So the "Fruit Heights/Spanish Fork pages" carry-forward is **built, pending a PR/merge to `main`** (near-closed). **New untracked artifacts:** `HANDOFF-2026-06-01.md` + `HANDOFF-2026-06-02.md` — both **left untracked intentionally** (handoff scratch docs, not site content). **Carry-forwards RESOLVED this window:** thin location pages (#72), city-quality advisory→blocking (#73), about.html canonical (#74); Fruit Heights/Spanish Fork (built, pending merge). **Open carry-forwards (unchanged):** ⛔ **GBP enforcement-watch dashboard fixes (owner login — the actual review-hold remedy** from #71: rename live "Insurance Claim Assistance"→"Storm & Hail Damage Roofing" + "Free Roof Estimate"→"Roof Estimate", stop review/star mentions in posts, then request reinstatement); Lighthouse agentic-browsing 67 (`agent-accessibility-tree` not well-formed); repo secrets `SERPAPI_KEY` (reviews-sync / cta-liveness leg B) + `SUPABASE_ACCESS_TOKEN` (edge-fn deploy); NAP/brand directory cleanup (DBA "Frame Roofing Utah" → GBP name edit → directory pass). **This refresh commits on `main` via an isolated `git worktree`** (the primary working tree is mid-session on `feat/location-pages-fruit-heights-spanish-fork` — worktree avoids disturbing live dev; the two untracked HANDOFF docs persist there untouched). Earlier metadata for 2026-06-02 (Tuesday early-AM auto-refresh — POST-ACTIVITY) — **One new merged PR since the evening refresh, and it reframes the review problem.** `git log --since="7 days ago"` and HEAD show exactly one commit past the 6/1 evening refresh: **PR #71 (`4e11152`, 2026-06-01 20:56 MDT — ~49 min after the `e755246` evening refresh at 20:07).** HEAD `e755246` → **`4e11152`** = `origin/main` = `main`; working tree clean except the one intentional untracked doc (`HANDOFF-2026-06-01.md`, unchanged from the evening refresh). Diffstat: **4 files, +21/−12, all internal `.vercelignored` GBP draft docs — zero deployed/page content.** **PR #71 — `fix(gbp): scrub adjusting language + internal phone from GBP drafts; flag live violations`.** The same investigation that produced #68–#70 (why customer reviews weren't showing) surfaced a second, bigger root cause: **Frame's GBP is on Google ENFORCEMENT WATCH** from a prior posting violation, and the **2026-05-11 session pushed policy-violating content LIVE to the profile** — the likely reason new reviews are being **held**. So the "reviews not showing" failure has TWO causes: (1) the dead-listing review CTA (fixed in prod by #68–#70), and (2) **the live profile itself is under enforcement, holding reviews** — which #71 only *documents*, it does **not** remedy. **What #71 actually changed (drafts only, internal, `.vercelignored`, NOT deployed):** (a) **9 instances of the internal-only phone `435-302-4422` → public `435-292-8802`** across the GBP draft docs (a leak in scratch copy, not on a live surface); (b) **unlicensed-adjusting language removed** per **Utah §31A-26-201** — "Don't fight your insurance alone", "we walk you through the claim from inspection to final invoice", "claim navigation", "supplement preparation", "start to invoice" → compliant **"we document the damage, provide a line-item scope your adjuster can read, meet your adjuster on site — the claim stays between you and your insurer; we don't adjust"**; (c) renamed the violating **"Insurance Claim Assistance" / "Insurance Claim Roofing" service drafts → "Storm & Hail Damage Roofing"**; (d) added **GBP-post rules** (NEVER mention reviews/stars in a post = **DPNB** violation; never the internal phone; no adjusting language); (e) `GBP-AUDIT-2026-05-11.md` gained a **CORRECTION banner** listing the LIVE dashboard fixes still required. Files touched: `data/GBP-AUDIT-2026-05-11.md`, `data/GBP-POST-BACKLOG.md`, `data/GBP-POST-PUBLISH-READY-2026-05-11.md`, `data/GBP-SERVICES-PROPOSAL-2026-05-27.md`. **⛔ NEW high-priority carry-forward (off-repo human task, owner login — this is the actual remedy):** the **LIVE GBP** still needs dashboard edits — (1) rename live service **"Insurance Claim Assistance" → "Storm & Hail Damage Roofing"** + reword to the compliant framing; (2) rename live service **"Free Roof Estimate" → "Roof Estimate"** (drop "Free" — promo language is an instant GBP rejection trigger); (3) stop mentioning reviews/stars in GBP posts (the DPNB trigger that caused the original violation); then **request reinstatement**. Until the dashboard edits land, the enforcement watch — and likely the review hold — persists; the committed draft cleanup is necessary hygiene but is **not** the fix. **Carry-forwards otherwise unchanged from the evening refresh** (17 thin location pages 484–498w, `about.html` canonical, Lighthouse agentic-browsing 67, Fruit Heights/Spanish Fork pages, repo secrets `SERPAPI_KEY`+`SUPABASE_ACCESS_TOKEN`, NAP/brand directory cleanup). **This refresh commits on `main`** (primary tree clean on `main`, HEAD `4e11152`). Earlier metadata for 2026-06-01 (Monday EVENING auto-refresh — POST-ACTIVITY) — **A third 6/1 session hardens the review-CTA path end-to-end: 3 more merged PRs (#68–#70).** HEAD `515d51e` → **`564dc95`** = `origin/main` = `main`, working tree clean except the one intentional untracked doc (`HANDOFF-2026-06-01.md`). Diffstat `da8a024..564dc95`: 7 files, +469/−2 (no page content — this is identity-integrity + CI tooling). **Root cause fixed:** a "Leave a Review" button had shipped pointing at a **dead Google listing** (`cid 16833…`), so customer reviews never reached Frame's real profile; the same audit surfaced **internal-phone (302-4422) leaks** on three deployed pages. **PR #68 (`b7262a5`) — CTA & external-link integrity gate (BLOCKING):** new `data/route-factory/business.json` = single source of truth for external identity (**canonical GBP `cid 8458659884566588108` + `data_id`, public phone 292-8802, internal-only phone 302-4422-never-publish, the known-dead listing `cid 16833…`**); new `scripts/audit-cta-integrity.mjs` blocks on internal-phone leak (any format), wrong/dead Google `cid`/`data_id`, placeholder links (`YOUR-…`, `${…}`, `cid=0`, `example.com`), and generic `maps/search` review CTAs — scoped to **deployed** (parses `.vercelignore`) AND **customer-facing** (sitemap + core routes) so owner tools + their `${…}` templates don't false-positive, strips HTML comments so docs can't trip it; wired into `compliance-gate.yml` as a blocking job; new `.vercelignore` stops deploying `backlink-playbook.html` + two internal `data/*.html` reports that carried 302-4422. **PR #69 (`c0aeba2`) — CTA liveness monitor (Layer 3), `cta-liveness.yml` + `scripts/check-cta-liveness.py`:** weekly prod watch — (A) GETs the deployed review page, asserts 200 + canonical `cid` present + no known-dead `cid` (catches 404s / CDN regressions / CTA drifting back to a wrong listing), no API key needed; (B) SerpAPI GBP liveness by `data_id`, asserts a real business with >0 reviews (catches a listing going dead/suspended — exactly the `cid 16833…` failure), skipped gracefully if `SERPAPI_KEY` unset. Verified green against live prod (review page 200, canonical `cid 8458659884566588108`). **PR #70 (`564dc95`) — one-tap write-review link:** `review.html` upgraded from a `maps?cid=` listing link (opens the profile) to a direct **writereview deep link** `https://search.google.com/local/writereview?placeid=ChIJCT6-aZD1TYcRzCpwX1kyY3U` (opens the star-rating dialog with **no owner login needed** — same outcome as Landon's official g.page "Ask for reviews" link); `place_id` resolved via SerpAPI 2026-06-01 (canonical GBP **"Frame Restoration Utah", 27 reviews**); `business.json` gains `gbp.place_id` + `review_form_link`; `audit-cta-integrity.mjs` adds a `WRONG-LISTING(placeid)` check; `check-cta-liveness.py` now accepts the canonical `cid` OR `place_id` on the live page. **Net:** the review-CTA bug class is now (1) fixed in prod, (2) un-mergeable going forward (merge-time gate), and (3) watched in production (weekly liveness monitor) — defense in depth on the single highest-leverage local-ranking signal (review velocity). **Carry-forwards unchanged from late-afternoon** (17 thin location pages 484–498w, `about.html` canonical, Lighthouse agentic-browsing 67, Fruit Heights/Spanish Fork pages, repo secrets `SERPAPI_KEY`+`SUPABASE_ACCESS_TOKEN` — now `cta-liveness` is a 3rd workflow that wants `SERPAPI_KEY` to run leg B, NAP/brand directory cleanup). **This refresh commits on `main`** (primary tree clean on `main`, HEAD `564dc95`). Earlier metadata for 2026-06-01 (Monday LATE-AFTERNOON auto-refresh — POST-ACTIVITY) — **A second productive ~3.5h afternoon session lands 5 more merged PRs to `main`.** HEAD now `515d51e` = `origin/main` = `main`, working tree clean except one intentional untracked doc (below). After the midday backlog-ship (HEAD `545598e` / PRs #59–#61), dev continued **12:18–15:52 MDT** shipping **PRs #62–#67** — note the midday refresh narrative pre-dated **PR #62 (`4324171`)**, which had already merged as its parent: **#62** put one accurate city-specific roofing paragraph on all **32 thin location pages**; **PR #63 (`9a984e6`/`c861cbb`, 12:18–12:36)** added a **second** local technical paragraph on **27** of those (now **15/32 clear the 500-unique-word floor**, 17 still at 484–498w); **PR #64 (`2cfe0ed`, 14:37)** corrected stale `handle-lead/DEPLOY.md` Twilio status — **SMS is LIVE (10DLC approved)**, NOT pending (all four `TWILIO_*` creds set, `handle-lead` v28 fires owner SMS + customer speed-to-lead auto-text, `send-message` v3 deployed; Verizon vtext now a redundant owner-alert path only); **PR #65 (`fa33dea`, 14:38)** repaired a truncated Heber blog meta description + harmonized Sandy meta; **PR #66 (`f4dc882`, 15:41)** fixed the homepage featured-video caption to **Orem, UT**; **PR #67 (`515d51e`, 15:52)** pointed the review CTA (`review.html` + `Landon-Action-Items-March2026.html`) at the **correct Google profile**. **New untracked artifact:** `HANDOFF-2026-06-01.md` (6.7KB, 14:09 MDT) — a cold-start session handoff (project facts + what-shipped table #59–#63 + site-health + gate-suite map + prioritized backlog + gotchas); **left untracked intentionally** (handoff scratch doc, not site content). **Canonical site health (per HANDOFF):** LCP 291ms / CLS 0.02 / SEO 100 / A11y 100 / JSON-LD 100% valid (364 blocks) / 0 broken links / FAQ on 102/116 pages / all **blocking** gates green. **Gate-suite note:** `compliance-gate.yml` blocking checks = compliance-words + jsonld-validity + link-integrity (`--strict`); `city-quality` is **advisory** (`continue-on-error`) and shows "fail" on the 17 thin pages — this is why merged PRs read `UNSTABLE` mergeStateStatus (expected, not a real failure). **Carry-forwards resolved/clarified:** SMS-live doc-drift (closed #64); `send-message` commit posture (confirmed DONE — tracked `57bd993` + deployed v3, no drift); canonical brand is **DECIDED = "Frame Roofing Utah"** (align GBP/directories *to* the site). **Open carry-forwards (well-scoped, from HANDOFF backlog):** (1) **17 location pages at 484–498 unique words** under the advisory floor — close each with a real city FAQ/project example (american-fork, bluffdale, clearfield, kaysville, kearns, lindon, magna, north-salt-lake, payson, santaquin, south-salt-lake, springville, syracuse, wallsburg, west-bountiful, west-valley-city, woods-cross); (2) `pages/about.html` self-canonical vs `/about` dup — owner decision; (3) Lighthouse a11y/console items + **agentic-browsing 67** (`agent-accessibility-tree` not well-formed); (4) **Fruit Heights + Spanish Fork** served-area pages (dead links removed in #60 — new-page candidates); (5) **off-repo human tasks:** set repo secrets `SERPAPI_KEY` (reviews-sync) + `SUPABASE_ACCESS_TOKEN` (edge-fn deploy) — both workflows skip gracefully until set; (6) **NAP/brand cleanup** per `data/NAP-DIRECTORY-CHECKLIST-2026-06-01.md` — confirm exact registered LLC name (BBB "Frame Restoration Utah LLC" vs state "Frame Restoration LLC" disagree), file DBA "Frame Roofing Utah", then GBP name edit (20 reviews persist) + directory NAP pass (BBB/Nextdoor/BuildZoom/GBP confirmed wrong-name; HomeAdvisor/Yelp/FB/IG unverified; `framerestorations.com/utah-home` 404 stale-indexed). **Methodology note (HANDOFF, verified primary source):** Google Search Central May-2026 — "**AEO/GEO is just SEO**" (no llms.txt / AI-files / special schema needed for Google's AI features); keep clean schema + citable FAQ + NAP consistency for non-Google engines (ChatGPT/Perplexity), don't chase AEO gimmicks. The earlier-AM cost-page-AIO + weekly-report stalls are **superseded by the day's ship cadence** (12 PRs merged across 6/1 total). **This refresh commits on `main`** (primary tree already clean on `main`, HEAD `515d51e`). Earlier metadata for 2026-06-01 (Monday MIDDAY auto-refresh — POST-ACTIVITY) — **The long-stalled untracked/unpushed backlog finally ships.** A major ~2.5h dev session (6/1 09:27–12:05 MDT, after the 00:13 early-AM quiet refresh) landed **three merged PRs to `main`** — HEAD now `4c47dac` = `origin/main` = `main`, working tree clean, **everything pushed** (untracked stack 28 → 0). **PR #59 (`b7ccc9f`) — Texas→Utah systems port + Utah-law compliance remediation:** the entire `feat/tx-systems-port` scaffolding is now committed (`PORT-PLAN.md`, `data/route-factory/` ×4, 6 `scripts/audit-*.mjs`, `.github/workflows/compliance-gate.yml`) **plus the whole long-untracked `data/` doc backlog** (the 5/17–5/27 AEO/handoff/session docs, `CITY-RANKING-ACTION-PLAN`, `GBP-SERVICES-PROPOSAL`, both NAP checklists, all three `data/weekly-reports/*.json`), the cost-page AIO tooling (`scripts/aeo-aio-sweep.py` + `scripts/serpapi-smoke-test.py`), and the late-5/27 outbound-messaging pipe (`scripts/cyborg-send.sh` + `supabase/functions/send-message/index.ts`). Same PR: **rating-integrity fix** (`816c280` + `scripts/fix-location-aggregate-rating.mjs` — removed the cloned `AggregateRating` from 45 location pages, schema now clean); **2 new CI workflows** (`google-reviews-sync.yml` cloud-scheduled review sync wrapping UT's scraper; `deploy-edge-function.yml` manual edge-fn deploy for the UT Supabase project); **Utah-law claim-language remediation + insurance-claims cornerstone** (`57bd993`, `pages/insurance-claims.html` +44 — "public adjuster"/"negotiate" language gated); **GBP `hasMap` + per-location Google Review schema** for Sandy + Millcreek (`7dc14a0`); **`llms.txt` single-public-number NAP fix** (`8cd0076` — 435-292-8802 only); and the **canonical brand decision** (`data/BRAND-DECISION-2026-06-01.md` — ✅ DECIDED: official public name = **"Frame Roofing Utah"** (Option B); website/schema/llms.txt already correct, so align GBP/BBB/~15 directories *to* the site via a **DBA-first** sequence — file "Frame Roofing Utah" assumed-name → edit GBP name (27 reviews stay attached) → directory pass per `data/NAP-DIRECTORY-CHECKLIST-2026-06-01.md`; do NOT rename the 161-file site). **PR #60 (`b1984f9`) — site-audit fixes:** full-site audit (`data/SITE-AUDIT-2026-06-01.md` + reusable `scripts/audit-full-site.mjs`) over all 116 sitemap pages — headline "**technically strong; gaps are content-metadata hygiene, not architecture**" (JSON-LD 0 invalid/364 blocks, SEO 100, A11y 100, LCP 291ms / CLS 0.02, 99/116 FAQPage); fixes shipped: **repaired all 17 live broken internal links** (incl. the literal `${item.url}` template-leak bug + `/pages/solar` → `/pages/solar-installation` + the ×9 missing-`/heber-city/`-segment snow-damage link) and **made the `audit-links` gate blocking** (`63a2200`); **titles + metas trimmed to length + 5 missing og:image added + sitemap orphan resolved** (`64d8f89`). **PR #61 (`4c47dac`) — thin-content fix:** FAQ + `FAQPage` schema added to the 3 thin service pages flagged by the audit (`general-contracting` 325w, `solar-installation` 449w, `water-fire-flood-restoration` 492w; each +62 lines). **Weekly-report writer is NOT broken — refutes the early-AM read:** `data/weekly-reports/2026-06-01.json` landed (generated 14:06 UTC) — 30d rollup: **725 pageviews, 82 google-organic, 527 direct (73%), 11 leads, 4 calls, 9 form-submits / 14 phone-clicks / 10 inbound-calls / 9 inbound-SMS, $142K job_value (same 2 won — Greg Atkinson $118K + Jan Hermansen $24K), 2.1% conversion, coverage 10/59 = 17%** (Cottonwood Heights + Holladay tied-lead at 8 views — both premium target ZIPs). **`phone_clicks` beacon fix CONFIRMED in production:** 14 phone-click + 10 inbound-call events now flowing — the 15-day DB hole (`80a59d7`) is closed. **Carry-forwards resolved this session:** untracked/unpushed stack (all 28 committed+merged), tx-port commit/PR posture, cost-page AIO scripts committed, weekly-report writer (working), `send-message`/`cyborg-send` now tracked. **Open carry-forwards (now mostly known-and-scoped, not mysteries):** brand execution (DBA filing → GBP name edit → directory NAP pass — name + the still-unconfirmed 5/27 GBP **phone** edit fold into one directory sweep); audit medium-tier hygiene backlog (49 over-length titles, 46 meta descriptions, 32 location pages below the 500-unique-word floor, 3 sitemap orphans, 3 dup meta groups, Lighthouse agentic-browsing 67 / `agent-accessibility-tree` not well-formed); cost-page AIO — the sweep scripts are committed but a **post-merge 26-query SerpAPI panel run still has not executed** (`cdee7de` now +12d, high-info recrawl band effectively closed — hypothesis likely expires unmeasured). **This refresh commits on `main` via an isolated `git worktree`** (the primary working tree was on an active feature branch mid-session — worktree avoids disturbing live dev). Earlier metadata for 2026-06-01 (Monday early-AM auto-refresh) — **Quiet ~24h window since the 5/31 port-scaffolding capture** (`9b85b5a`, 2026-05-31 00:09 MDT): zero new `main` commits (HEAD still `9b85b5a`), working tree byte-identical to 5/31 close — `find -newer` against the refresh commit returns empty, so the TX→UT systems-port scaffolding (5/30 17:59–18:10 MDT) remains the latest worktree activity and is **still untracked on local branch `feat/tx-systems-port`** (28 entries; branch still pointer-equal to `main` + **unpushed**, zero unique commits). **Weekly-report cadence — hypothesis refuted:** 6/1 Mon arrived with **still no new snapshot** (only `2026-05-24.json` + `2026-05-25.json`); the snapshot writer is now silent **7 days** (5/26–6/1), and with neither a 5/31 Sun nor a 6/1 Mon rollup landing the weekly-Sunday hypothesis is dead — **writer-broken is now the leading read** (the `/functions/v1/weekly-report` snapshot pipeline likely needs a direct edge-fn trace, not more waiting). Consequence: `80a59d7`'s phone_clicks beacon-fix production verification has been gated on this silent snapshot for a full week — it should be pulled **directly from the `phone_clicks` Supabase table** instead of via the weekly report. **GBP 72h phone-edit cusp now ~4.5 days past submission** (5/27 ~10:12 MDT) with no acceptance signal observable from the repo — escalation (GBP chat support / re-submit) is now **overdue**, not merely standing. **Debt clocks +1:** `.gitignore` `.env*.local` **16-day debt**; cost-page AIO **+12 days post-merge** (`cdee7de` 5/20) — the high-information recrawl-likely band (~6/3 close) is all but closed with **still zero post-merge 26-query SerpAPI sweep data points captured**, so the 30-day hypothesis is now effectively **expiring unmeasured** (the gating sweep never ran across the entire high-info window); refresh-stale fixer **26-day debt**. **All 5/31 carry-forwards intact + clock-ticked:** GBP edit escalation (overdue); `data/NAP-DIRECTORY-CHECKLIST-2026-05-27.md` execution (BBB/Yelp/Apple Maps still on 302-4422 — NAP-inconsistency local-pack penalty compounding 5 days now); DOPL `hasCredential` sitewide rollout (Midway still 1-of-45 LIVE); `data/CITY-RANKING-ACTION-PLAN-2026-05-27.html` slotting into build/PR queue; read `scripts/cyborg-send.sh` + `supabase/functions/send-message/index.ts` end-to-end; **tx-port posture** — decide commit/PR sequence on the local-only unpushed `feat/tx-systems-port` scaffolding (P1 advisory-gate is the next concrete build step) + review `data/route-factory/compliance-words.json` Utah re-authoring before it gates CI. **Pattern read:** five consecutive auto-refreshes (5/28→6/1) with HEAD effectively pinned (production-quiet on `main` since `2fe52d2` 5/27; the only intervening worktree event = the 5/30-evening port-scaffolding session) — the standout 6/1 signal is the **convergence of three stalls past their decision points**: weekly-report writer broken (7d silent), GBP edit unconfirmed (~4.5d), and the cost-page AIO sweep never run (hypothesis expiring) — all three now want **active intervention** rather than another observe-and-wait cycle. **This refresh commits on `main`** then restores the working branch to `feat/tx-systems-port` (per the 5/31 convention — refresh history lives on `main`; safe because `feat` has zero unique commits and only untracked files, which persist across checkout). Earlier metadata for 2026-05-31 (Sunday early-AM auto-refresh) — **First non-`main` HEAD in the refresh series:** working tree is now on feature branch **`feat/tx-systems-port`** (created 2026-05-30, points at `d2dbf81` — **zero unique commits vs `main`**, so the branch is currently `main` + untracked working files only). **A real 5/30-evening work session landed** (artifacts written 17:59–18:10 MDT, ~10h after the `d2dbf81` early-AM refresh) standing up the **Texas→Utah systems-port scaffolding**: `PORT-PLAN.md` (7-phase P0–P6 plan to port TX v2's systems layer into UT while *adapting data/law to Utah*, explicitly NOT a content copy — source-of-truth `~/projects/frame-restoration-texas-v2`, launched 5/30), `.github/workflows/compliance-gate.yml` (CI quality gate), `data/route-factory/` (4 files: `compliance-words.json`, `UTAH-COMPLIANCE-LAW.md`, `README.md`, `batch-manifest.example.json` — the multi-city page-factory + Utah legal re-authoring layer), and **6 `scripts/audit-*.mjs`** quality-gate runners (`audit-route-batch`, `-city-quality`, `-compliance-words`, `-jsonld`, `-links`, `-media-provenance` — headers still read "Frame Restoration TX", i.e. ported verbatim; Utah-adaptation is the P1 task). Untracked stack grows **19 → 28 entries** (+9). **Port guardrails (from PORT-PLAN.md, load-bearing):** do NOT overwrite UT's `handle-call` / `handle-sms` / `send-message` edge fns, AEO-measurement scripts (`aeo-aio-sweep`, `serpapi`, `reddit-scanner`, `indexnow`), `dashboard/`, `build-intelligence/`, or **the daily CLAUDE.md auto-refresh cron** — Utah is *ahead* of TX on call/SMS + AEO and must not regress; UT has **no TX Insurance Code §4102** (roofer-as-public-adjuster gate), so `compliance-words.json` is re-authored against Utah DOPL contractor licensing + UT advertising rules; P1 lands advisory-first (no day-1 red-block). This finally connects the long-running **Houston/TX multi-city-expansion thread to actual tooling — the "route factory" IS the multi-city page generator.** **5/30 cusps resolve:** (1) **GBP 72h phone-edit cusp passed** (~10:00 MDT 5/30) with no acceptance signal observable from the repo — now statistically late; escalation (GBP chat support / re-submit) is the standing carry-forward; (2) **weekly-report cadence — no new snapshot landed 5/30 or 5/31-so-far** (still only `2026-05-24.json` + `2026-05-25.json`); the writer is now silent **6 days** (5/26–5/31), refuting both daily and twice-weekly — the weekly-Sunday hypothesis is at its make-or-break test today, with 6/1 Mon the last window before a writer-broken read (also still the first unverified opportunity to confirm the `80a59d7` phone_clicks beacon fix in production). **Debt clocks +1:** `.gitignore` `.env*.local` **15-day debt**; cost-page AIO **+11 days post-merge** (~3 days high-info recrawl band left, still **zero** post-merge SerpAPI sweep data points — now at the edge of "missed window"); refresh-stale fixer **25-day debt**. **This refresh commits on `main`** (HEAD was switched `main`→`feat/tx-systems-port`-back around the commit per the daily-refresh convention that refresh history lives on `main`; safe because `feat` has zero unique commits and no tracked modifications exist — only untracked files, which persist across checkout — and the working branch is restored to `feat/tx-systems-port` afterward). Earlier metadata for 2026-05-30 (Saturday early-AM auto-refresh) — **Quiet ~18h window** since yesterday's `c665916` auto-refresh (5/29 07:45 MDT), zero new `main` commits, HEAD still at `c665916` (parent still `2fe52d2`), working tree byte-identical to 5/29 close — latest worktree mtime remains `scripts/cyborg-send.sh` at 5/27 23:52 MDT, confirming yesterday's corrected discovery-sweep methodology (compare worktree mtimes against PRIOR refresh's commit timestamp, not own clock) caught up the late-evening 5/27 backlog and no new artifacts have landed since. Untracked stack flat at **19 entries** (18 files + `data/weekly-reports/` directory). **Debt clocks tick +1:** `.gitignore` `.env*.local` edit at **14-day debt** (a full week past the 1-week threshold, no resolution path proposed since 5/20); cost-page AIO experiment clock at **+10 days post-merge** (`cdee7de` 2026-05-20) — now one-third through the 30-day hypothesis window, only ~4 days of high-information recrawl-likely band remaining through ~6/3, still **zero post-merge 26-query SerpAPI sweep data points captured** (the gating issue rolls another day past its 5/28 "now urgent" flag and is approaching "missed window" territory — if no panel run lands in the next 3-4 days the hypothesis effectively expires without measurement); refresh-stale fixer at **24-day debt**. **Two cusps land inside the next ~24h:** (1) **GBP primary phone edit 72h cusp ~10:00 MDT today** — submitted 5/27 ~10:12 MDT, 24h passed 5/28 night, 48h passed 5/29 night, 72h cusp hits ~8h from this refresh; if no Google acceptance confirmation by mid-morning Saturday this becomes the escalation point (chat support / re-submit); (2) **first weekly-report cadence test window opens today** — last snapshot `2026-05-25.json`, weekly hypothesis points to next snapshot ~5/31 Sun or 6/1 Mon (1-2 days out), and a 5/30 Sat snapshot landing would refute the weekly read in favor of twice-weekly — either way this is the first hard cadence data point since the 5/24 Sun + 5/25 Mon pair. **All 5/29 carry-forwards intact + clock-ticked:** (1) GBP edit verification — cusp lands today, see above; (2) `data/NAP-DIRECTORY-CHECKLIST-2026-05-27.md` execution (BBB/Yelp/Apple Maps still on 302-4422 — NAP-inconsistency local-pack penalty compounds each day the directory listings disagree with GBP + website); (3) DOPL `hasCredential` sitewide rollout decision (Midway still 1-of-45 LIVE); (4) phone_clicks recovery confirmation gated on next weekly-report snapshot (1-2 days out per above — first opportunity to verify the `80a59d7` `text/plain` beacon fix actually closed the 15-day DB hole in production); (5) `data/GBP-SERVICES-PROPOSAL-2026-05-27.md` action; (6) `data/CITY-RANKING-ACTION-PLAN-2026-05-27.html` end-to-end read + slot recommendations into build/PR queue (Park City citations / SLC-valley chamber + BBB-local / CWH-Holladay review collection / Utah County GAF+CertainTeed + Alpine Chamber bundle / review velocity campaign on 20-review base / ~1000× Ahrefs DR gap puts SLC-valley local-pack wins on a 60-90d horizon minimum); (7) read `scripts/cyborg-send.sh` + `supabase/functions/send-message/index.ts` end-to-end to characterize the late-evening 5/27 outbound messaging pipe + decide `.gitignore`/commit posture (uncharacterized surface area in the messaging layer for 3+ days now). **Pattern read:** three consecutive auto-refreshes (5/28 + 5/29 + 5/30) on a quiet `main` branch with HEAD pinned at `2fe52d2` since 5/27 10:12 MDT — ~63-hour gap, the longest dev-quiet stretch since the 5/24-5/27 four-day window that preceded the 5/27 burst. Two cusps converging today (GBP 72h + weekly-snapshot test window) make this the highest-information ~24h since the 5/27 production burst — both outcomes are externally-gated, neither needs new dev work to land. Earlier metadata for 2026-05-29 (Friday auto-refresh) — **Quiet ~22h window** since yesterday's `d9f7960` auto-refresh (5/28 08:11 MDT), zero new `main` commits, HEAD still at `2fe52d2`, working tree byte-identical to 5/28 close (`git diff` empty re: tracked files). **One newly-discovered untracked artifact missed by yesterday's discovery sweep:** `scripts/cyborg-send.sh` (2,758 bytes, executable `-rwxr-xr-x`, written 2026-05-27 23:52 MDT — 29 min after `data/CITY-RANKING-ACTION-PLAN-2026-05-27.html` at 23:23 MDT, 89 min after the `1be4803` evening refresh). Pairs with `supabase/functions/send-message/index.ts` (6,069 bytes, written 5/27 23:49–23:51 MDT — that file WAS captured by `d9f7960`, the shell script was not). Naming pattern + 3-minute write delta suggest a single late-evening session that stood up an outbound messaging pipe (new `send-message` Supabase edge fn + a CLI wrapper to drive it from the workstation — likely a Cowork/Codex notification or SMS-out channel; not yet read end-to-end, untouched in this refresh). Net effect: untracked stack corrects upward to **18 files + `data/weekly-reports/` directory = 19 entries** (+1 vs `d9f7960`'s 18). No new `data/weekly-reports/` snapshot landed overnight (still just `2026-05-24.json` + `2026-05-25.json` — Friday is **4 days past** the last weekly snapshot 5/25, still fits the weekly-cadence theory pointing to next snapshot ~5/31 Sun or 6/1 Mon, **2–3 days away**, becomes the first real test of weekly-vs-twice-weekly). `.gitignore` `.env*.local` edit at **13-day debt** (+1 day; 6 days past 1-week threshold, no resolution path proposed since 5/20). Cost-page AIO experiment clock at **+9 days post-merge** (`cdee7de` 2026-05-20) — still inside recrawl-likely window but tightening (~5 days of high-information band remaining through ~6/3), still zero post-merge 26-query SerpAPI sweep data points captured — gating issue rolls another day past its 5/28 "now urgent" flag. Refresh-stale fixer at **23-day debt**. **All 5/28 carry-forwards intact + clock-ticked:** (1) GBP primary phone edit accept verification — 24h mark passed last night ~22:00 MDT, **72h cusp hits tomorrow ~10:00 MDT 5/30** (escalation point if no Google confirmation by Saturday morning); (2) `data/NAP-DIRECTORY-CHECKLIST-2026-05-27.md` execution (BBB/Yelp/Apple Maps NAP updates) still pending — the longer 302-4422 lives in those listings, the worse the NAP-inconsistency local-pack signal; (3) DOPL `hasCredential` sitewide rollout decision (Midway still 1-of-45 LIVE); (4) phone_clicks recovery confirmation gated on next weekly-report snapshot ~5/31/6/1; (5) `data/GBP-SERVICES-PROPOSAL-2026-05-27.md` action; (6) the 5/28 carry-forward — read `data/CITY-RANKING-ACTION-PLAN-2026-05-27.html` end-to-end and slot recommendations into build/PR queue — still open (Park City citations / SLC-valley chamber + BBB-local citations / CWH-Holladay review collection / Utah County GAF+CertainTeed contractor listings + Alpine Chamber for Alpine-Highland bundle / review velocity campaign on existing 20-review base — defines the next 2–4 weeks of SEO work, ~1000× DR gap puts local-pack wins outside Heber Valley on a 60-90d horizon at minimum). **NEW carry-forward from today's discovery:** read `scripts/cyborg-send.sh` + `supabase/functions/send-message/index.ts` end-to-end to characterize the new messaging pipe — if it's the outbound channel for the AEO/Reddit/digest pipelines or a generic SMS-out, it changes the surface area of the `handle-sms` / `handle-call` lead-creation flow and may want a writer-trace + `.gitignore`/commit decision. **Reframed risk read:** two consecutive auto-refreshes (5/28 + 5/29) discovered late-evening 5/27 artifacts that `1be4803` (the 12:09 MDT 5/27 refresh) couldn't see — confirms the discovery sweep should compare worktree mtimes against the prior refresh's commit timestamp, not its own clock, OR add a same-day evening pass when significant work lands after the morning refresh. Earlier metadata for 2026-05-28 (Thursday auto-refresh) — **Quiet 18-hour window** following yesterday's two-commit production burst (`80a59d7` + `2fe52d2`); zero new `main` commits since `1be4803` (2026-05-27 12:09 MDT auto-refresh, itself metadata-only). **One new artifact landed in worktree late last night:** `data/CITY-RANKING-ACTION-PLAN-2026-05-27.html` (~14KB, written 5/27 22:23 MDT — ~10h after the evening refresh, so not captured in `1be4803`). The doc is a **city-by-city ranking diagnostic + action plan** for the SLC-valley invisibility gap — confirms Frame ranks **#2 Heber City / #3 Midway organically** (in-pack at Heber, partial at Midway) but is **NOT in top 10 for Park City / Cottonwood Heights / Holladay / Alpine / Highland** (all absent from local pack); ChatGPT also doesn't cite Frame for "best roofer in Park City" — same competitor set (Bartlett, On Top, Alta Vista, FBC, Whitaker) dominates AI answers; explicitly identifies bottleneck as **authority + local-pack proximity, NOT page quality** (Frame's location pages are 2,400–2,900 words / 4 schemas / FAQ / 16–32 internal links — already excellent); root-cause gaps: **Ahrefs DR 6 vs competitors 104–6,140K linked domains (~1000× backlink deficit)**, 4-of-38 directory presence, 20 Google reviews vs 120–300+, zero city-specific backlinks for Park City / CWH / Holladay, 0 AI engine citations confirmed via Chrome ChatGPT test 5/27. PostHog 30-day pageview proof points: Heber City 12 views (top, ranking working), Park City 4, CWH 8, Holladay 8, Alpine 0, Highland 0. No new `data/weekly-reports/` snapshot landed overnight (still just `2026-05-24.json` + `2026-05-25.json` — Thursday is **3 days past** the last weekly snapshot 5/25, fits the weekly-cadence theory that next snapshot lands ~5/31 Sun or 6/1 Mon — first real test of weekly-vs-twice-weekly hypothesis). Untracked stack grows to **17 files + `data/weekly-reports/` directory ≈ 18 entries** with the new CITY-RANKING-ACTION-PLAN (replaces last refresh's 16+dir count). `.gitignore` `.env*.local` edit at **12-day debt** (+1 day; 5 days past the 1-week threshold). Cost-page AIO experiment clock at **+8 days post-merge** (`cdee7de` 2026-05-20) — solidly inside recrawl-likely window (~6 days of high-information band remaining through ~6/3), still no gating 26-query SerpAPI sweep run. Refresh-stale fixer at **22-day debt**. **All 5/27 evening carry-forwards intact:** (1) verify GBP primary phone edit accepted by Google (submitted 5/27 ~10:12 MDT — 24h mark hits ~tonight 10 PM MDT, 72h cusp ~5/30 morning); (2) work `data/NAP-DIRECTORY-CHECKLIST-2026-05-27.md` (BBB/Yelp/Apple Maps NAP updates — NAP inconsistency = local-pack ranking penalty); (3) decide DOPL `hasCredential` sitewide rollout (Midway now 1-of-45 LIVE per `80a59d7`); (4) confirm phone_clicks recovery in next weekly snapshot (~5/31 / 6/1); (5) read + act on `data/GBP-SERVICES-PROPOSAL-2026-05-27.md`. **NEW carry-forward from today's artifact discovery:** read `data/CITY-RANKING-ACTION-PLAN-2026-05-27.html` end-to-end and slot recommendations into build/PR queue — the doc effectively defines the next 2–4 weeks of SEO work (Park City citations + 2-3 PC case studies w/ neighborhood proper nouns / SLC-valley chamber + BBB-local citations + CWH-Holladay review collection / Utah County play: GAF + CertainTeed contractor listings + Alpine Chamber for Alpine-Highland bundle / review velocity campaign on existing 20-review base / closing the ~1000× DR gap is a months-long backlink push, not a weeks-long fix — sets realistic expectation that local-pack wins outside Heber Valley are 60-90d horizon at minimum). Notable absence: still no `scripts/aeo-aio-sweep.py` panel run (the 5/20 30-day cost-page AIO hypothesis is at day +8 of 30 with zero post-merge data points captured — gating issue is now urgent, not just open). Earlier metadata for 2026-05-27 (Wednesday evening auto-refresh — POST-ACTIVITY) — **Quiet streak broken: two production commits land today** flipping a major piece of architecture. `80a59d7` (09:19 MDT) **root-causes + fixes the 15-day `phone_clicks` DB hole** (`track-clicks.js` v1 sent the beacon as `application/json` Blob, triggering a CORS preflight that the browser dropped when the tel:/sms: dialer handoff fired before preflight completed → server saw 0 rows despite PostHog capturing 14 phone_click events incl. 4 google.com + 2 ChatGPT referrers in the same 60d). Fix: switch primary path to `fetch()` + `keepalive` + `text/plain` (CORS simple, no preflight); keep `navigator.sendBeacon()` with `text/plain` Blob as page-unload fallback; cache-bust `/track-clicks.js?v=2` across **124 files** because `vercel.json` sets `/*.js` to `max-age=31536000 immutable`. Server-side unchanged (edge fn already calls `req.json()` which parses regardless of declared Content-Type). **13 historical PostHog phone_click rows replayed into `public.phone_clicks`** via separate Supabase migration with original timestamps + source classification. Then `2fe52d2` (10:12 MDT) — **public NAP phone number swapped from 435-302-4422 → 435-292-8802 across 164 files** (1,341 insertions / 1,341 deletions: every HTML display string, every `schema.org` `telephone` field, `handle-lead` customer-facing auto-reply copy). **Architectural significance: the Twilio tracking number IS now the public NAP** — every Google-initiated call (GBP / organic / ads / referral) routes through Twilio → `handle-call` → `call_logs` + auto-lead creation, closing the GBP-Call-button attribution gap that bypassed the website entirely. Landon's direct cell 435-302-4422 is now **internal-only** (preserved as `LANDON_PHONE` constant in `handle-call` / `handle-lead` / `handle-sms` as the forwarding destination — never expose publicly). Companion actions: GBP primary phone edit submitted 2026-05-27 (pending Google review); directory listings (BBB, Yelp, Apple Maps, etc.) still need manual updates — see new `data/NAP-DIRECTORY-CHECKLIST-2026-05-27.md`. **Two carry-forward debts closed today as side-effects:** (1) the 3 uncommitted location-page niche-differentiation edits (Alpine / Highland / Midway, 3-day debt) got swept into `80a59d7` as part of the cache-bust pass on those pages — `git status` confirms they're no longer in worktree; (2) the inbound-call webhook health check (open since 5/25) is now moot — once GBP picks up the new NAP, calls will flow through the Twilio webhook that just got its tracking fix shipped. Two new untracked artifacts landed today: `data/GBP-SERVICES-PROPOSAL-2026-05-27.md` + `data/NAP-DIRECTORY-CHECKLIST-2026-05-27.md`. Untracked stack unchanged in count (still 16 files + `data/weekly-reports/` directory ≈ 17 entries, just with 2 new today replacing the 2 location-edit candidates that got committed). `.gitignore` `.env*.local` edit at **11-day debt**. Cost-page AIO clock at **+7 days post-merge** (no SerpAPI sweep run yet — still gating). Refresh-stale fixer at **21-day debt**. **NEW carry-forwards from today's NAP swap:** (1) verify GBP primary phone edit accepted by Google (24-72h typical); (2) work `data/NAP-DIRECTORY-CHECKLIST-2026-05-27.md` for BBB/Yelp/Apple Maps citation updates (NAP inconsistency = local-pack ranking penalty); (3) ⛔ **update CLAUDE.md CRITICAL RULES Phone numbers section** to reflect the flipped architecture (display + tel:href both = 292-8802, 302-4422 = internal LANDON_PHONE only) — done in this same refresh. Earlier metadata for 2026-05-27 (Wednesday morning auto-refresh) — Second consecutive quiet 24-hour window: zero new `main` commits since `f379483` (2026-05-26 20:08 MDT auto-refresh — itself metadata-only), zero worktree changes (working tree byte-identical to 5/26 close), no 5/26 or 5/27 `data/weekly-reports/` snapshot landed — strengthens the 5/26 "writer is NOT daily" read (two-blank-day streak effectively rules out a daily cron; weekly cadence per the 5/24 Sun + 5/25 Mon pair points to next expected snapshot ~5/31 Sun or 6/1 Mon); same 3 uncommitted location-page niche-differentiation edits (`locations/alpine.html` Bench-Side Estate Reroofs, `locations/highland.html` Custom-Home Reroofs on Larger Lots, `locations/midway.html` Heber Valley Resort & Swiss-Style Homes + DOPL `hasCredential` JSON-LD license 14256097-5501) — now **3-day-old debt**; untracked stack flat at **17 files**; `.gitignore` `.env*.local` edit at **10-day debt** (3 days past 1-week threshold, no resolution path proposed since 5/20); cost-page AIO experiment clock at **+7 days post-merge** (cdee7de 2026-05-20) — solidly inside recrawl-likely window (~3-14 days) with ~7 days of high-information band remaining (~6/3 close), still no gating 26-query SerpAPI sweep run; refresh-stale fixer now **20-day debt**. Earlier metadata for 2026-05-26 (Tuesday auto-refresh) — Quiet 24-hour window: no new `main` commits since `50a98f6` (2026-05-25 20:07 MDT auto-refresh); no new `data/weekly-reports/` snapshot landed overnight (still just `2026-05-24.json` + `2026-05-25.json` — confirms writer cadence is **NOT daily**, likely weekly or twice-weekly so today fits the gap rather than signaling a writer break); working tree byte-identical to Monday's 09:30 MDT snapshot — same 3 uncommitted location-page niche-differentiation edits (`locations/alpine.html` Bench-Side Estate Reroofs, `locations/highland.html` Custom-Home Reroofs on Larger Lots, `locations/midway.html` Heber Valley Resort & Swiss-Style Homes + DOPL `hasCredential` JSON-LD license 14256097-5501) — now **2-day-old debt**; untracked stack unchanged at **17 files**; `.gitignore` `.env*.local` edit still uncommitted (**9-day debt — past the 1-week threshold**); cost-page AIO experiment clock at **+6 days post-merge** (cdee7de 2026-05-20) — well into the recrawl-likely window; refresh-stale fixer now **19-day debt**. Earlier metadata for 2026-05-25 (Monday morning auto-refresh) — Second `data/weekly-reports/` snapshot (`2026-05-25.json`, ~6KB, written 09:30 MDT) confirms a weekly cadence is running on the writer; week-over-week deltas vs 2026-05-24 snapshot: pageviews 614 → 588 (-26), google-organic 61 → 60 (flat), direct 487 → 462 (-25), leads 4 → 4 (flat), inbound-calls **1 → 0 (regression flag — 0 calls despite 8 phone_click events; weekly-report's own notes flag a possible inbound webhook break through Twilio)**, total job_value $142K → $142K (same two won leads still drive everything: Greg Atkinson $118K offline-signed-contract + Jan Hermansen $24K homepage form), conversion 0.7% (flat), location coverage 11/59 = 19% (flat; Heber City still leading with 12 views; 48 location pages still zero-traffic). No new `main` commits since `129ce1f` (2026-05-24 evening auto-refresh). Working tree unchanged from last night: same 3 uncommitted location-page niche-differentiation edits (`locations/alpine.html` Bench-Side Estate Reroofs, `locations/highland.html` Custom-Home Reroofs on Larger Lots, `locations/midway.html` Heber Valley Resort & Swiss-Style Homes + DOPL `hasCredential` JSON-LD license 14256097-5501) — now **1-day-old debt**. Untracked stack grows to 17 with the new `2026-05-25.json` snapshot. `.gitignore` `.env*.local` edit still uncommitted (**8-day debt**). Carry-forwards intact: commit-or-revert decision on the 3 location-page edits + DOPL credential 1-of-45 sitewide rollout decision + first post-merge 26-query SerpAPI sweep (cost-page AIO hypothesis 30-day clock at +5 days post-merge, well into the recrawl-likely window) + Track E v0 revenue tile implementation + all carry-forwards from 5/17→5/24. Earlier metadata for 2026-05-24 (evening): 3 uncommitted niche-differentiation edits surfaced in worktree: `locations/alpine.html` (Bench-Side Estate Reroofs), `locations/highland.html` (Custom-Home Reroofs on Larger Lots), `locations/midway.html` (Heber Valley Resort & Swiss-Style Homes + new DOPL `hasCredential` JSON-LD — license 14256097-5501). Each page now carries a distinct visible-text snippet (Estate-Grade / Custom-Home / Resort-Grade) applying the 5/20 visible-text-snippet AIO hypothesis to the location-page tier. Working-tree state: 3 tracked-file modifications + 11 untracked artifacts + 7-day-old `.gitignore` `.env*.local` debt. No new `main` commits since morning refresh (`479d2f4`). Earlier metadata for the morning quiet-maintenance run: 2026-05-24 (Quiet maintenance window — no `main` commits since 2026-05-20 cost-page AIO experiment (PR #58 / `cdee7de`). One new artifact landed in worktree: `data/weekly-reports/2026-05-24.json` — first snapshot in a NEW `data/weekly-reports/` directory, week-ending 2026-05-24 / 30d rollup pulled live from the `/functions/v1/weekly-report` edge fn (614 pageviews, 61 organic, 487 direct, 4 leads, 8 phone-clicks, 1 inbound-call, 2 inbound-SMS, $142K total job_value, 0.7% conversion, top page = homepage 298 views). 30-day clock on the cost-page AIO hypothesis is still running — first post-merge 26-query `aeo-aio-sweep.py` panel run remains the gating data point. Carry-forwards from 5/17 + 5/18 + 5/19 + 5/20 all still open: 7 AEO/CODEX/COWORK handoff docs untracked, 2 `scripts/aeo-aio-sweep.py` + `scripts/serpapi-smoke-test.py` untracked, `data/aeo-master-digest-2026-05-18.html` untracked, `data/SESSION-2026-05-17-hf-supply-chain-and-classifier-bench-handoff.md` + `data/REVENUE-TILE-DESIGN-2026-05-17.md` untracked, `.gitignore` `.env*.local` edit uncommitted (**7-day-old debt**), `.env.local` present in worktree. Earlier metadata for 2026-05-20: AIO citation experiment shipped to production: cost-page structural rewrite merged to `main` via PR #58 → `cdee7de` — 3 minimal edits per Codex-audited `data/AEO-EDIT-PROPOSAL-2026-05-20.md`: question-form title/meta/H1, $8,000–$30,000 lead paragraph, visible 5-Q&A FAQ block; falsifiable hypothesis that visible-text-snippet shape — not schema — drives `homerroofing.net`'s 5/7 AIO citation rate vs Frame's 0/7. Same-day 10-query SerpAPI baseline sweep confirms 7/10 AIO surfaced, 0/7 cite Frame (`data/AEO-AIO-SWEEP-2026-05-20.md`). New 26-query measurement script `scripts/aeo-aio-sweep.py` v1.2 (Codex-audited) + `scripts/serpapi-smoke-test.py`. Architecture pivots off DataForSEO plan v1.0 onto existing SerpAPI Free Plan per v1.1 handoff. 7 new `data/` docs + 2 new `scripts/` untracked. Carry-forwards from 5/17 + 5/18 + 5/19 still open) | Auto-refreshed via Cowork scheduled task

---

## ⛔ CRITICAL RULES — READ BEFORE ANY EDIT

### DO NOT add false trust claims. These are NOT true:
- ~~**BBB A+ Rated**~~ — NOW CONFIRMED. BBB accreditation approved 2026-04-07. A+ BBB badge on site is accurate.
- **NRCA Member** — Frame Roofing is NOT an NRCA member. Never add this.
- **GAF Master Elite / OC Preferred** — Not verified. Do not claim.
- Any certification, membership, or accreditation that hasn't been confirmed by Ryan or Landon.

### These ARE true and can be used:
- Licensed & Insured in Utah
- Free Roof Inspections
- 24/7 Storm Response
- Financing Available
- 10-Year Workmanship Warranty
- BBB Accredited (confirmed by Landon 2026-04-07)
- A+ BBB Rating

### Phone numbers: ARCHITECTURE FLIPPED 2026-05-27 (commit 2fe52d2) — Twilio number IS now the public NAP
- **Public NAP / display / tel: href / sms: / schema.org / GBP / directories → 435-292-8802** (Twilio A2P 10DLC registered number)
  - **As of 2026-05-27 (commit 2fe52d2, 164 files / 1,341 swaps):** every visible display string AND every `tel:` href now uses 435-292-8802. Display text "(435) 292-8802" and "435-292-8802", `schema.org` `telephone`, `handle-lead` customer-facing auto-reply strings, footer "Call:" line, GBP primary phone, directory listings — ALL point here.
  - **Why the flip:** GBP "Call" button bypasses the website entirely; tracking only worked for clicks that came through the site. Making the Twilio number the public NAP funnels every Google-initiated call (GBP / organic / ads / referral) through `handle-call` → `call_logs` + auto-lead creation, closing the attribution gap.
  - Customer dials 435-292-8802 → Twilio → forwards to Landon's cell (435-302-4422) via `handle-call` TwiML → Landon's phone rings showing the Twilio caller ID. Customer experience unchanged from their side; Landon sees Twilio number on incoming call (already familiar — has been the SMS sending number since 4/24).
  - This is also the number Twilio sends outbound SMS from (10DLC live). Required to be published on site + consent copy + privacy + terms for **TCR (The Campaign Registry) CTA verification** — carriers (T-Mobile/AT&T) reject campaigns that don't disclose the sending number.
  - Appears in: every `tel:` href + every visible display string (2026-05-27); `sms:` links (footer "Text:" line, Contact → Text card on index); SMS consent checkbox copy on hero + contact + modal forms; privacy.html §SMS; terms.html §SMS; `schema.org` `telephone`; `handle-lead` auto-reply copy.
- **Internal-only / forwarding destination / never expose → 435-302-4422** (Landon's direct Verizon cell)
  - **Preserved ONLY as the `LANDON_PHONE` constant** in `supabase/functions/handle-call/index.ts`, `supabase/functions/handle-lead/index.ts`, `supabase/functions/handle-sms/index.ts` — used as the Twilio forwarding destination.
  - **Never put this number on a public surface again.** Not in HTML, not in schema, not in GBP, not in directories, not in customer-facing auto-reply copy. If you find it on a public surface, it's a regression — remove it.
- **Companion follow-ups still in progress (carry-forwards from 2026-05-27):**
  - **GBP primary phone edit:** submitted 2026-05-27, pending Google review (24-72h typical). Verify acceptance in GBP merchant panel.
  - **Directory updates:** BBB, Yelp, Apple Maps, etc. need manual NAP updates — checklist tracked in `data/NAP-DIRECTORY-CHECKLIST-2026-05-27.md` (NAP inconsistency across citations = local-pack ranking penalty).
- **Historic context (no longer the active architecture):** From 2026-04-24 → 2026-05-16, 292-8802 was Twilio-only (SMS + selectively published). From 2026-05-16 → 2026-05-27, `tel:` hrefs dialed 292-8802 but visible display still said "302-4422" (split-brain — display ≠ dial). 2026-05-27 unified everything onto 292-8802.

### Before generating or editing ANY page:
1. Read this CLAUDE.md first
2. Do NOT invent certifications, awards, or memberships
3. Do NOT re-add things that were previously removed
4. If unsure whether a claim is true, ASK — don't guess

### 2026-05-11 directives (active rules — do NOT violate):
- **NO DRONE in marketing.** Landon ruled it out 2026-05-11 (commit 191ef15). Drone inspection is NOT a marketed service. Do not list under GBP Products, GBP Services, Roofing Contractor services-to-add, or in the review-language audit attribute taxonomy. Existing aerial drone *photography* (homepage video, hero shots) is fine — what's banned is "drone inspection" as a service offering.
- **Heber Valley is ONE market** (commits 37e8d7e / cf6c269). Heber City + Midway + Wallsburg + Charleston + Daniel = same elevation (5,600+ ft), same snowload, same drive radius. Do NOT fragment content into Midway-only or Wallsburg-only posts. Use the umbrella "Heber Valley" brand. Existing `/blog/heber-city/` posts stay (already indexed) but cross-link UP to any future `/blog/heber-valley/` umbrella content.
- **"Valley" = Salt Lake Valley** (commit c6ce67c). Ryan clarified 2026-05-11: when current strategy says "focus on the valley," that means **Salt Lake City + surrounding premium ZIPs** — 84093/84121 (Cottonwood Heights), 84117/84121/84124 (Holladay), 84106/84109 (Millcreek east), 84092/84094 (Sandy east bench), 84020 (Draper east bench), 84102/84103/84105/84108 (SLC east bench). Heber Valley / Park City / Utah Valley / Davis County are **demoted** for the current 6-post calendar — Frame already has visibility in those markets.
- **Every GBP post must include a photo** (Ryan directive 2026-05-11, commit ec1171e). Text-only GBP posts underperform on Frame's profile — never queue or publish text-only.
- **Hours have changed.** Memory and any older docs may say Mon–Sat 8AM–6PM — actual hours are **Mon–Fri 8AM–7PM, Sat 8AM–6PM**. Use the corrected hours.

## PEOPLE

- **Ryan Conwell** (ryanconwell99@gmail.com) — manages site, SEO, gets 10% commission on leads. Intermediate vibe coder using Claude Code, Cowork, Antigravity, and Google models. Also an Enterprise Platform AE at his day job.
- **Landon Yokers** (landon@framerestorations.com) — business owner. Wants leads via email + SMS to 435-302-4422 (his direct number, Verizon — internal forwarding destination only, no longer public NAP as of 2026-05-27).
- **Business:** Frame Restoration Utah LLC, DBA Frame Roofing Utah
- **Address:** 142 S Main St, Heber City, UT 84032
- **Business phone (public NAP):** 435-292-8802 (Twilio tracking number — flipped from 302-4422 on 2026-05-27 via commit 2fe52d2; see CRITICAL RULES Phone numbers section for full rationale)
- **Hours:** Mon–Fri 8AM–7PM, Sat 8AM–6PM (corrected 2026-05-11 from live GBP merchant panel — site schema + thank-you copy fixed in commit 23ae38a; was Mon–Sat 8AM–6PM)
- **Utah DOPL license #:** 14256097-5501 (in GBP description, canonicalized 2026-05-11)
- **GBP merchant profile ID:** 9902208163385139295
- **Email:** landon@framerestorations.com (Ryan override 2026-05-09 — info@framerestorationutah.com was never set up to receive mail, so leads were dying. Accepted Texas-brand cross-resolution risk in exchange for working inbox. NEVER use sales@framerestorations.com — that one was swept out 2026-04-22 for the same Frisco TX cross-resolution reason.)
- **Company entity (Vercel billing):** Acme Inc.

---

## DOMAINS & HOSTING

### Production (Landon's Vercel)
- **Live URL:** https://frameroofingutah.com / https://www.frameroofingutah.com
- **Vercel account:** landon-4824s-projects (Ryan is admin)
- **Deploys from:** GitHub repo Rconman99/frame-restoration-utah, main branch
- **Auto-deploys:** YES — every git push to main triggers production deploy

### Sandbox (Ryan's Vercel)
- **Team:** Ryan's projects (team_YY9hStNZc86Fgkf5Sin8Z0V0)
- **Project ID:** prj_zcwGmh7wUxHvqtRoHRmoRp441ePY
- **Domains:** framerestorationutah.com (OLD domain, still active), frame-restoration-utah.vercel.app
- **Use:** Testing new ideas, sandbox builds. NOT the live production site.
- **Design sandbox branch:** `design-refresh`

### Important Domain Notes
- frameroofingutah.com = PRODUCTION (Landon's Vercel)
- framerestorationutah.com = OLD/SANDBOX (Ryan's Vercel) — 301 domain forwarding to frameroofingutah.com ACTIVE (set up 2026-03-30 by Landon, path forwarding enabled)
- framerestorations.com = TEXAS Squarespace site (Landon's other market, Frisco TX) — causes SEO brand confusion. We do NOT control this.
- All canonical URLs, OG tags, and schema in the codebase reference www.frameroofingutah.com

### GitHub
- **Repo:** https://github.com/Rconman99/frame-restoration-utah
- **Branch:** main = production
- **Connected to BOTH Vercel accounts** — pushing to main deploys to both Landon's (production) and Ryan's (sandbox)

### Vercel MCP Access
- The Vercel MCP token only authenticates to Ryan's team (team_YY9hStNZc86Fgkf5Sin8Z0V0)
- Cannot see/manage Landon's Vercel project via MCP — but git push auto-deploys there
- Ryan's other Vercel projects: tradeworker-site, listingkit, template, taylor-creek, theology-app, conwell-agentic-seo, rc-digital, taylor-creek-site

---

## TECH STACK

- **Frontend:** Static HTML site (NOT React/Next.js — plain HTML files)
- **Hosting:** Vercel (static deployment)
- **Backend:** Supabase (lead handling)
- **Styling:** global.css (shared across all pages)
- **JS:** global-modal.js (booking form modal)
- **Config:** vercel.json (cleanUrls, security headers, caching, redirects)

---

## SITE STRUCTURE (~120+ HTML pages, 105 sitemap URLs as of 2026-04-22 — pruned from 126 during audit fix; removed dupes, robots-disallowed paths, Vercel-redirected routes)

- `/` — Homepage (index.html), H1: "Mountain-Grade Roofing, Valley-Wide" (rebrand 2026-04-17; previously "The Gold Standard In Utah Roofing"). Hero badge reads **"★★★★★ 5.0 from 20 Google reviews · A+ BBB · Serving 45+ Utah Cities"** (PR #41, 2026-05-11 — was "5.0 Rating" / "40+ Utah Cities", CR-driven specific-fact lift). Structure (top → bottom): Hero → `.quick-answer-strip` → **#fresh-aerials "Just Shipped — May 2026 Wasatch Front" (PR #40, 2026-05-11 — 3 responsive AVIF/WEBP/JPG photos via `<picture>` at 800/1200/1600 widths: timpanogos-wide, stone-home, craftsman-balcony; plus 1 featured looping WEBM/MP4 1080p ~5.7MB mid-install underlayment video w/ JPG poster; IntersectionObserver gates playback >55% in view + fades `.sticky-cta-side` gold "Free Inspection" half only while video on screen — navy tap-to-call half always visible; `prefers-reduced-motion` honored. All alt text says "aerial drone photograph" — never "drone inspection")** → **#watch-in-action 60s Heber Valley reroof showcase (added 2026-04-20, click-to-play MP4+WebM, VideoObject JSON-LD)** → Gallery → Video Showcase (drone loop) → Trust Bar → …
- `/about.html` — Thin root-level About page (1,424w). **2026-06-02 (PR #74):** now **301-redirects to `/pages/about`** (`vercel.json`) and removed from `sitemap.xml`; the richer 2,452-word self-canonical `/pages/about` is the single canonical about URL (sitemap + nav + canonical + redirect all point there).
- `/pages/` — 14 pages (residential-roofing, commercial-roofing, roof-repair, roof-replacement, storm-damage, storm-damage-restoration [redirects to storm-damage], emergency-tarping, gutters, solar-installation, insurance-claims, general-contracting, water-fire-flood-restoration, gallery, **about** [NEW — owner bio, headshot, E-E-A-T signals])
- `/locations/` — **45 location pages on `main`** (all Utah cities along the Wasatch Front); **all 45 now clear the BLOCKING `city-quality` depth gate** (≥500 unique words — last 15 deepened in PR #72, gate flipped advisory→blocking in PR #73, 2026-06-02). **+2 built on `feat/location-pages-fruit-heights-spanish-fork`, pending merge → 47:** `fruit-heights.html` (665w, Davis County) + `spanish-fork.html` (705w, Utah County), the served-area pages whose dead links were removed in #60 (`e29541b`).
- `/blog/` — Blog index + ~54 posts total (10 new spokes added 2026-05-09 afternoon + 1 flagship SLV reroof guide added 2026-05-11 + 1 storm-chaser pillar added 2026-05-12; all have BlogPosting + FAQPage + HowTo JSON-LD schema):
  - **`/blog/salt-lake-valley/` — NEW 2026-05-11.** Umbrella valley-as-brand folder (parallels new `/blog/heber-valley/` pattern). First post: `salt-lake-valley-premium-roof-replacement-2026.html` — 3,057-word flagship covering premium SLV ZIPs (Cottonwood Heights / Holladay / Millcreek east / Sandy east bench / Draper east bench / SLC east bench), Wasatch-snowmelt + tree-debris failure modes, material decision matrix, $22K-$65K cost ranges, HOA + permit realities. Real-Landon Mt. Olympus aerial hero (`images/projects/cities/salt-lake-valley-east-bench-aerial-2026.webp`). Round-1 Post #1 of the SLV blog calendar (`data/BLOG-CALENDAR-LOW-TRAFFIC-HIGH-REVENUE-2026-05-11.md`).
  - **`/blog/heber-valley/` — folder reserved (cf6c269, 2026-05-11).** Per c6ce67c the planned Heber Valley umbrella post was demoted off the active 6-post calendar in favor of SLV refocus, but the folder convention is established for future Heber-Valley-as-brand content.
  - `/blog/utah/` — 16 statewide SEO posts (roof-replacement-cost, emergency-roof-repair, how-long-does-roof-last, how-to-choose-a-roofer, best-roofing-materials, signs-you-need-new-roof, utah-hail-season, utah-roof-insurance-claims, utah-roof-maintenance-checklist, utah-roof-ventilation-guide, april-2026-wasatch-storm-roof-checklist, commercial-roofing-utah, gutter-installation-guide-utah, roof-financing-options-utah, spring-roof-inspection-utah, **roof-replacement-quote-checklist-utah-2026** [NEW 2026-05-09 — Reddit content-gap close, compounds aeo-76a17e0e], **local-roofer-vs-storm-chaser-utah** [NEW 2026-05-12, PR #56 — 2,836-word storm-chaser-disambiguation pillar, FAQPage 6 Q&As, EEAT Andrew Best pattern + Utah DOPL license-lookup + Utah AG + Utah Insurance Dept primary sources, dual-number CTA, `heber-custom-reroof-front-view.webp` og:image first use; closes ~15 of 22 local-service AI-fan-out gaps from `verticals/local-service/QUERY-FANOUT-AUDIT.md`. PR #57 wired hub-spoke linking — 13 storm-cluster spokes (bountiful/draper/herriman/holladay/lehi/murray/ogden/provo/riverton/slc/sandy + statewide april-storm-checklist + utah-hail-season-guide) gained a red-tinted `.storm-chaser-warning` aside linking up with anchor "Local Utah Roofer vs Storm Chaser guide"; blog/index.html pillar card inserted at top of grid])
  - City subdirectories (~36 city posts as of 2026-05-09 afternoon): salt-lake-city/5 (added storm-damage-roofing-salt-lake-city today — first post shipped through Higgsfield-aware blog generator pipeline), heber-city/5, park-city/3, **bountiful/2** (davis-county-wind-damage-guide), **murray/2** (added storm-damage-roofing-murray-utah today — extends storm cluster), **orem/2** (added roof-replacement-orem-utah today — new cluster), **provo/2** (added roof-replacement-provo-utah today — new cluster), sandy/2, plus 1 each in **alpine** [NEW 2026-05-09], **cottonwood-heights** [NEW 2026-05-09 — new cluster], draper, herriman, **highland** [NEW 2026-05-09], **holladay** [NEW 2026-05-09 — canyon-outflow wind / east-bench guide], layton, lehi, ogden, riverton, **south-jordan** [NEW 2026-05-09], west-jordan, west-valley-city. Two clusters expanded heavily today: roof-replacement (Alpine + Highland + South Jordan + Orem + Provo + Cottonwood Heights = 6 new spokes) and storm-damage (SLC + Holladay + Murray = 3 new spokes).
- `/projects/` — 1 case study (heber-valley-roof)
- `pages/gallery.html` — Now leads with **FEATURED IN-PROGRESS PROJECT** section (Heber Valley custom reroof): aerial panorama hero + 3-up grid + drone flyover video player; 4 new residential cards added at top of filtered grid (added 2026-04-20)
- `/seo-report/` — Live SEO & Lead Attribution Tracker (PIN-gated, multi-user admin panel, 30-day default view, Chart.js rendering). **2026-04-27:** Now ships alongside `/dashboard/` (productized successor) as fallback during ~7-day soak; planned 301 to /dashboard/ once verified.
- `/dashboard/` — **NEW 2026-04-27.** Productized client-dashboard module vendored from RCBuild-Kit v0.8.0. Config-driven via `window.DASHBOARD_CONFIG` (reads `data/dashboard-config.json` for Supabase URL + anon key + campaign key + branding). Same edge function (`/functions/v1/weekly-report`), same PIN system, same Supabase tables — existing PINs continue to work. Files: `dashboard/index.html`, `dashboard/dashboard.css` (--brand-accent CSS var for white-label), `dashboard/dashboard.js` (PIN gate + KPI grid + charts + admin panel). Hotfix 801bc59: absolute paths for CSS/JS (Vercel cleanUrls+trailingSlash:false strips /dashboard/ → /dashboard, breaking relative path resolution). **2026-04-27/28 polish stack:** chart canvases capped at 280px max-height + `maintainAspectRatio:false` (96f2c56 — fixed full-screen-height doughnuts); time-range buttons changed 7/14/30/90 → 7/30/60/90 with parallel current+prior fetches and per-KPI/per-page growth-vs-prior-period indicators (ed493ea); 🚀 Biggest Movers section (Top Growers / Fresh Wins / Needs Attention, top 3 each) + ⛈️ Storm Watch tile polling NWS api.weather.gov for storm/wind/hail/winter alerts in UT, ties active alerts to the $800/mo storm-trigger reserve recommendation in MARKETING-BUILD-CONTEXT.md (7a42f1d). **2026-04-28 Phase 2 Site Health tiles (50320e1, d083be1, b6f817c):** new "Site Health" section between Biggest Movers and Top Pages with three tiles — ⭐ **Reviews Velocity** (reads `/reviews.json`, shows total + rating + days-since-last-review w/ green≤14d / orange≤30d / red color coding + competitor-gap line vs roofingutah.com), 📝 **Content Freshness** (parses `/sitemap.xml` lastmod, shows total indexed pages + days-since-most-recent-update + 7d/30d/90d update counts to catch stale content drift), ⚡ **Core Web Vitals** (PageSpeed Insights API integration with 60-min sessionStorage cache, shows LCP/CLS/INP/TTFB origin field data + Lighthouse lab perf score; graceful fallbacks for missing key + insufficient CrUX data — Frame Utah currently in latter state, 587 impressions/30d too low). Live PSI API key restricted to PSI API + HTTP referrer www.frameroofingutah.com/* in Google Cloud Console. Each tile silent-fails if endpoint unreachable. All client-side, no edge function changes. Deploy-stamp now `v0.11.2` in `dashboard.js` to force Vercel byte-size change after the 2026-04-28 silent-webhook-failure incident (PR #6 merged but didn't deploy until commit d083be1 forced redeploy).
- `/scripts/` — **NEW 2026-04-27.** Wave 2 marketing-intel runners: `market-intel.mjs` + `market-intel-config.json` (45-city × 4-tier scoring), `market-intel-kit/` (bundled scoring + allocator + sources from RCBuild-Kit v0.7.0), `competitor-ads.mjs` (SerpAPI sweep), `competitor-tiktok.mjs` (Apify scraper), `supabase-pipeline-query.sql` (per-city job-value + close-rate from leads table). All dry-run when env vars missing. Vercelignored. **2026-04-28:** added `verify-deploy.sh` — compares local file size to production after a configurable wait, fails loud on drift; should run after every PR merge that touches `/dashboard/*` until the squash-merge → main → Vercel webhook chain is fully trusted (added after PR #6 merged but didn't actually deploy until forced re-deploy in PR #7). **2026-05-06 (evening):** AEO automation stack committed — `aeo-citation-monitor.py`, `aeo_actions.py`, `aeo-file-issues.py`, `aeo-fix-internal-links.py`, `aeo-citation-cron.sh`, `update-google-reviews-cron.sh` plus `Library/LaunchAgents/com.ryan.frame-roofing-aeo.plist` (monthly cron 5th of month 8:23am). Pure-cron replacement for the prior claude.ai/code AEO monitor trigger. SerpAPI sweep → leverage-scored actions (severity × lead-value) → auto-filed GitHub issues → optional PR-gated auto-fixer powered by local `mistral-nemo:12b`. `data/aeo-citations/` vercelignored. See SESSION LOG 2026-05-06 (evening).
- `/supabase/` — **NEW 2026-05-09 (PR #23), expanded 2026-05-10 (d9bd585), expanded again 2026-05-11 evening.** Edge function source-of-truth, previously deployed-only. Now holds: (a) `functions/handle-lead/index.ts` — **v8** (Resend email backend, retains v7 OpenRouter / Gemini 2.0 Flash tier classifier — emergency / urgent / scheduled / general / spam routing); (b) `functions/lead-crm/index.ts` — **v5 as of 2026-05-11 (71eef60 + 6559fcf + 67caae9)**, ~230 lines, PIN-gated reads + writes for the `/leads` CRM page (auth mirrors `weekly-report`). v5 endpoints: LIST (returns margin + city in row shape), UPDATE (accepts margin/city, removed commission write — column is GENERATED), `?action=clicks` (30d phone/SMS totals + by-source breakdown), `action=create` (validates + stamps won_at, used by the new "+ Add Lead" panel for offline contracts/walk-ins); (c) `functions/track-click/index.ts` — **NEW 2026-05-11 evening (PR #55, 112 lines)**, persists `tel:`/`sms:` clicks from `track-clicks.js` into the new `phone_clicks` table with full attribution (`gclid`/`fbclid`/`utm_*`/`referrer`/`source`); 6-bucket source classifier (`gbp`/`organic_google`/`google_ads`/`bing`/`social`/`direct`); (d) `functions/handle-lead/DEPLOY.md` (deploy + secret-rotation notes); (e) `migrations/20260510_add_attribution_columns.sql` — adds gclid / fbclid / utm_{source,medium,campaign,term,content} / won_at / uploaded_to_{google_ads,meta_ads} cols to `leads` + `leads_status_check` CHECK constraint + partial indexes for offline-conversion sync. **2026-05-11 evening DB migrations** applied via Supabase MCP (verify matching migration files land in `supabase/migrations/`): `leads.margin` (numeric) + `leads.city` (text) cols + `commission` redefined as GENERATED column (CASE: Heber/Midway 5% else 10%, off margin) + `website_leads_report` view DROP/CREATE; new `phone_clicks` table. `.gitignore` excludes `supabase/.temp/` (CLI cache); `.vercelignore` should keep `supabase/` off the static CDN.
- `/leads.html` — **NEW 2026-05-10 (d9bd585), expanded 2026-05-11 evening (71eef60 + 6559fcf + 459bfda + 67caae9).** Public deliverable (PIN-gated) at `/leads` (cleanUrls). Dark-themed CRM dashboard, now ~550 lines: KPIs (new untouched / last 30d / open pipeline / won MTD / commission MTD), filter+sort+search, click-to-edit panel for status / notes / job_value / **margin** / **city** / **address** + auto-commission display ("Commission (auto)" read-only recalculates live from `margin × city-aware rate`, 5% Heber/Midway else 10% — null margin contributes $0 to MTD rollup), auto-stamps `won_at` on status=`won`. **New 2026-05-11 evening:** "+ Add Lead" gold button in header opens a slide-in panel mirroring edit-panel style for offline-signed contracts + walk-ins + phone-only leads (calls `lead-crm` `action=create`, refreshes list + auto-opens new lead); "Phone & SMS Clicks" panel showing recent click feed + 6-bucket source breakdown (gbp/organic_google/google_ads/bing/social/direct); toolbar `.sp` flex-spacer renamed to `.spacer` to fix overload with the `.sp` spinner class (459bfda hotfix — was rendering as a gold spinning circle wedged between "Lost" and "Tier" chips). Auth: same PIN system as `/seo-report` (key=`frame-roofing-2026` + per-user PIN against `report_access` table — existing PINs work). `seo-report.html` user-bar has a "Leads CRM →" button linking to it. **Do NOT add `leads.html` to `.vercelignore` reflexively** — header comment in `.vercelignore` flags this.
- `/track-attribution.js` — **NEW 2026-05-11 evening (PR #39).** Phase 0 closed-loop ROAS instrumentation. Captures ad click IDs (`gclid`/`fbclid`/`msclkid`/`gbraid`/`wbraid`) into 90-day localStorage + UTM params (first-touch sessionStorage) + `landing_page` + `referrer` on every page load. Exposes `window.FrameAttribution.get()` for downstream consumers. Site-wide injection via `<script src="/track-attribution.js" defer></script>` in `<head>` of **130 HTML pages**. Hero/lead/modal forms merge the payload into `fetch` body before submit + redirect to `/thank-you` on success. Unblocks Google Ads Offline Conversion Import + Meta CAPI uploads.
- `/track-clicks.js` — **NEW 2026-05-11 evening (PR #55, 158 lines).** Auto-instruments every `tel:`/`sms:` link site-wide. On click: PostHog event (`phone_click`/`sms_click`) + Beacon POST to `/track-click` edge fn + Google Ads `gtag` conversion (gated by `window.FrameAds.send_to`) + `dataLayer.push` for GTM. Beacon-based delivery survives dialer page-nav on mobile. Depends on `track-attribution.js` loading first; falls back gracefully if absent. Injected into **122 live pages** via `scripts/inject-track-clicks.py`. `window.FrameAds.send_to` (Conversion ID/Label) is unset — pending Landon's Google Ads conversion action creation.
- `/review/` — Review landing page + `review-request` edge function (automated post-job review collection)
- `backlink-playbook.html` — 12 backlink acquisition opportunities with step-by-step instructions
- `directory-blitz-tool.html` — Directory blitz tool v4 with full blitz results (Angi marked Already Done 2026-04-13)
- `Frame-Innovation-Audit-2026-05-16.html` — **NEW 2026-05-16 (06ee582, 272 lines).** Strategic-thinking artifact (NOT action) pulled from live Supabase + scheduled tasks + edge fns + recent commits: 6 leads / 0 phone clicks 4d post-launch / 0 won deals with margin / 254 reddit signals 0 actioned / 8 inbound calls 5 SMS none becoming leads / 13 active scheduled tasks / 11 edge fns. Diagnoses the structural state: enterprise-grade infrastructure, anemic throughput. Surfaces 12 gaps + sequenced fix stack. Three of the highest-impact gaps (#2 phone_clicks plumbing severed, #3 inbound calls/SMS not creating leads, #7 review-request agent filtering on impossible status) were closed the same day (commits 72b09fb / 8a8063f / 1b95df5). Live at `/Frame-Innovation-Audit-2026-05-16` (cleanUrls).
- `Frame-Reddit-AEO-Playbook.html` — **NEW 2026-05-16 (c957cfe).** Strategy doc for the Phoenix-pattern Reddit-AEO classifier rollout: rules of engagement, persona guidelines, measurement framework, 90-day targets, the Landon sign-off ask. Companion to the `reddit-aeo-classifier` + `reddit-aeo-digest` edge fns (see LEAD PIPELINE). Shareable as standalone HTML.
- `Frame-AEO-Master-Play.html` — **NEW 2026-05-16 evening (9467596, 260 lines).** Strategy doc for the unified 8-surface AEO distribution system (Reddit / YouTube / Nextdoor / Yelp / LinkedIn / GBP / local news / Quora). Architecture diagram, surface-coverage matrix, 90-day targets, the Landon sign-off ask. Pairs with the `aeo_distribution_queue` + `aeo_citation_log` tables and the `youtube-script-drafter` / `aeo-master-digest` / `aeo-citation-tracker` edge fns (see LEAD PIPELINE). **Baseline 0% citation rate** logged at first run (Gemini, "Best roofing company in Heber City Utah" — competitor "Premier Roofing" cited instead). Shareable as standalone HTML.
- `/build-intelligence/` — **NEW 2026-05-16 (8a8063f), expanded 2026-05-16 evening (9467596).** Scratch/feedback dir holding the build-intelligence loop scaffolding committed alongside handle-call v2 / handle-sms v5: `build-intelligence-loop.md` (467 lines), `contractor-growth-category-knowledge-map.md` (1697 lines), `implementation-queue.md`, plus paired JSON shapes (`build_intelligence.json`, `build_intelligence_loop.json`, `contractor_growth_category_knowledge_map.json`). **9467596 added** `customer-request-batch.json` (2073 lines), `customer-request-board.md` (70 lines), `customer-request-ledger.json` (2088 lines) — customer-request intake scaffolding, plus `feedback-ledger.json` / `feedback-row.json` / `feedback-row.md` (the prior-untracked feedback artifacts now committed). Vercelignored.
- `/archive/` — Old docs, sub-location service pages, eval files
- `/images/` — Photos, OG image, brand source files (/brand-source/, /projects/cities/, /services/), landon-yokers-headshot.jpg (About page)
- `/assets/` — Hero video/poster, gutters video

### Key Files
- `vercel.json` — Redirects (storm-damage-restoration → storm-damage, /:city/storm-damage-restoration → /:city/storm-damage, /home → /, **NEW 2026-04-22:** /privacy-policy → /privacy, /terms-of-service → /terms after duplicate page deletion)
- `.vercelignore` — **NEW 2026-04-22.** Blocks deploying CLAUDE.md (contains Supabase anon key + internal rules), *.md strategy docs, *.py scripts, drive-photos.zip (8.5 MB customer photos), archive/, tmp-*/, internal audit/scratch HTML files
- `sitemap.xml` — 105 URLs (regenerated 2026-04-22 — pruned dupes, removed robots-disallowed paths and Vercel-redirected routes, fixed root trailing slash; was 126 → 105)
- `robots.txt` — Allows AI crawlers (GPTBot, Claude, Perplexity), blocks Bytespider. **2026-04-22:** Removed Disallow for /privacy and /terms (already noindex via meta — let Google crawl + honor noindex cleanly)
- `llms.txt` — AI/LLM optimization file
- `directory-tracker.html` — Interactive React-based directory tracker (38 directories)
- `global-modal.js` — Booking form modal → Supabase edge function
- `global.css` — Shared styles (self-hosted Archivo Black + Archivo WOFF2 already wired here — duplicate Google Fonts <link> removed from index 2026-04-22)
- `data/audits/` — **NEW 2026-05-04.** Lighthouse audit drops from cross-project sessions. Current: `lighthouse-2026-05-04.md` (rendered summary) + paired `-mobile.json` / `-desktop.json` (raw, ~1 MB each). Currently untracked — open question: commit raw JSONs for trend tracking vs `.gitignore data/audits/lighthouse-*.json` and only commit summaries. See SESSION LOG 2026-05-04.
- `data/route-factory/business.json` + `scripts/audit-cta-integrity.mjs` + `.github/workflows/cta-liveness.yml` + `scripts/check-cta-liveness.py` + `.vercelignore` — **NEW 2026-06-01 (evening), MERGED to `main` via PRs #68 / #69 / #70 (`b7262a5` / `c0aeba2` / `564dc95`).** Review-CTA + external-identity integrity system. `business.json` = single source of truth for external identity (canonical GBP `cid 8458659884566588108` / `data_id` / `place_id ChIJCT6-aZD1TYcRzCpwX1kyY3U`, public phone 292-8802, internal-only 302-4422-never-publish, known-dead listing `cid 16833…`). `audit-cta-integrity.mjs` = **blocking** gate in `compliance-gate.yml` (internal-phone leaks, wrong/dead listing `cid`/`data_id`/`placeid`, placeholder/generic review CTAs; scoped deployed+customer-facing). `cta-liveness.yml` + `check-cta-liveness.py` = **weekly prod monitor** (review page 200 + canonical cid/place_id present + no dead cid; SerpAPI GBP >0-reviews liveness, `SERPAPI_KEY`-gated). `.vercelignore` extended to stop deploying `backlink-playbook.html` + 2 internal `data/*.html` reports (302-4422 leak). See SESSION LOG 2026-06-01 (evening).
- `PORT-PLAN.md` + `data/route-factory/` + `scripts/audit-*.mjs` + `.github/workflows/compliance-gate.yml` — **NEW 2026-05-30, COMMITTED + MERGED to `main` via PR #59 (`b7ccc9f`) on 2026-06-01** (was untracked/unpushed on local branch `feat/tx-systems-port` from 5/30 until the 6/1 midday burst). Companion CI workflows merged the same day: `.github/workflows/google-reviews-sync.yml` (cloud-scheduled review sync) + `.github/workflows/deploy-edge-function.yml` (manual UT Supabase edge-fn deploy). Audit toolchain now also includes `scripts/audit-full-site.mjs` (crawl/on-page/citability, PR #60) + `scripts/fix-location-aggregate-rating.mjs` (rating-integrity remediation). The 6 `scripts/audit-*.mjs` headers may still read "Frame Restoration TX" (ported verbatim) — Utah-adaptation remains the P1 task. Texas→Utah systems-port scaffolding (P0/P1). `PORT-PLAN.md` = P0–P6 plan to port the TX v2 systems layer into UT, adapting data + law to Utah (Utah has no TX Insurance Code §4102 — fence is DOPL contractor licensing + UT advertising rules). `data/route-factory/` = coordinator/worker/audit worktree workflow for static-HTML route-depth parity (the multi-city page-factory) + Utah-re-authored `compliance-words.json` ("public adjuster" + "negotiate" banned site-wide). `scripts/audit-*.mjs` = 6-script quality-gate toolchain (route-batch, city-quality, jsonld, links, media-provenance, compliance-words; headers still read "Frame Restoration TX" — Utah-adaptation is the P1 task). `.github/workflows/compliance-gate.yml` = CI gate. **DO NOT TOUCH during the port:** UT `handle-call` / `handle-sms` / `send-message` edge fns, AEO-measurement scripts, `dashboard/`, `build-intelligence/`, the CLAUDE.md auto-refresh cron. See SESSION LOG 2026-06-01.
- `scripts/audit-review-integrity.mjs` — **NEW 2026-06-03, MERGED to `main` via PR #77 (`4c2c1ce`).** **Blocking** review-integrity gate: forbids cloned `AggregateRating` on city/location pages (single GBP → homepage/About only) and requires real, individually-attributable reviewers on any `Review` JSON-LD. The merge-time fence that makes scaling per-location `Review` schema (PR #78, 12/47 → 47) safe. See SESSION LOG 2026-06-03 (afternoon).
- `AEO-BUILDOUT-HANDOFF-2026-06-03.md` — **NEW 2026-06-03, UNTRACKED** (per the handoff-doc convention; supersedes the on-page checklist in `AEO-GROWTH-HANDOFF-2026-06-01.md`, pairs with `GENAI-REPORT-HANDOFF-2026-06-03.md`). The "really build AEO perfect" roadmap: thesis = on-page AEO is table-stakes already paid (Utah cited 1/10 on Perplexity for money queries, more schema = no uplift), so ~80% of remaining gain is OFF-page consensus. 4 pillars in priority order — 0 (unblock GBP ✅ cleared), 1 (off-page citation substrate — directory saturation + `site:` index verification + manufacturer locators + Reddit), 2 (entity consistency ✅ done via #79), 3 (review velocity — gate live + 12/47), 4 (weekly AI-visibility cron). Win metric: money-query citations 1/10 → N in `~/projects/frame-utah-aeo/ai-visibility/log.jsonl`. See SESSION LOG 2026-06-03 (afternoon).

---

## SEO STATUS (as of 2026-03-28)

### Google Search Console (Baseline 2026-03-30)
- **Property:** https://www.frameroofingutah.com/ (verified, ryanconwell99@gmail.com)
- **Performance (first 10 days):** 10 clicks, 587 impressions, 1.7% CTR, avg position 23.2
- **Indexed pages:** 19
- **Not indexed:** 89 (88 "Discovered - currently not indexed", 1 redirect error on storm-damage — FIXED 2026-03-30)
- **Sitemap:** Submitted 2026-03-26, 101 pages discovered, status: Success
- **Indexing requests submitted (2026-03-30):** roof-repair, residential-roofing, roof-replacement, commercial-roofing, insurance-claims, gutters, emergency-tarping, solar-installation (8 total, hit daily quota)
- **Still need indexing requests:** general-contracting, water-fire-flood-restoration, storm-damage (after fix deploys), plus ~40 location pages
- **Pages missing from sitemap:** insurance-claims, emergency-tarping (need to add)

### Completed
- Meta descriptions on ALL 45 location pages, 12 service pages, blog pages
- Canonical tags (self-referencing) on ALL pages
- Open Graph + Twitter card tags on ALL pages
- Schema markup: RoofingContractor, FAQPage, HowTo, BlogPosting, BreadcrumbList, AggregateRating, Article, **SpeakableSpecification (homepage FAQPage, added 2026-04-22 for AEO + voice assistants)**, **6 standalone `@type:Service` nodes for each homepage tile (added 2026-05-12, PR #56 — Residential / Commercial / Storm / Water-Fire / Solar / General Contracting; previously nested as Offer/Service inside hasOfferCatalog, extracted to top-level so each is independently rich-result eligible + AI-citable as a discrete entity)**
- **2026-04-22 audit fix:** Texas-brand entity leak lockdown — purged sales@framerestorations.com from index/legal/10 blog posts, removed parentOrganization block from LocalBusiness schema, deleted broken "Part of Frame Roofing Utah" Texas-domain footer link, changed about page foundingLocation from Dallas-Fort Worth → Heber City, narrowed homepage services sub from "all of Utah" → "Wasatch Front + Heber Valley"
- **2026-04-22 audit fix:** FAQPage JSON-LD + visible Quick Answer passages added to 4 service pages (roof-replacement, storm-damage, insurance-claims, residential-roofing)
- **2026-04-22 audit fix:** AggregateRating (20 reviews, 5.0 stars) added to all 45 location pages (was previously only on homepage + about)
- **2026-04-22 audit fix:** Visible "Last updated" stamp under H1 on 87 blog + location pages (freshness signal visible to readers, not just in schema)
- **2026-04-22 audit fix:** Backfilled dateModified on two 2026 posts that still showed 2026-03-27; fixed malformed parentOrganization block on west-valley-city.html
- **2026-04-22 audit fix:** Removed two `EDITOR CHECK` placeholder paragraphs that had shipped to production on blog/park-city/ice-dam-winter-roof-damage.html (rest of blog grep-swept clean)
- **2026-04-22 audit fix:** Standardized "24/7 emergency response" copy across homepage FAQ/schema and services.html (was inconsistent — some "rapid," some "24/7")
- **2026-04-22 audit fix:** Deleted privacy-policy.html and terms-of-service.html duplicates; added 301 redirects in vercel.json; fixed privacy.html/terms.html canonicals to use www subdomain; removed robots Disallow on /privacy + /terms
- **2026-04-22 (afternoon) audit follow-up:** Hail cluster hub-spoke internal linking — 8 city posts (bountiful, draper, herriman, lehi, ogden, provo, riverton, sandy) now link up to the Utah hail pillar via "Statewide context" callout; pillar links down to all 8 cities via spoke block. Eliminates pillar-vs-city cannibalization without sacrificing long-tail city traffic.
- **2026-04-22 (afternoon) brand-safe review sync:** Hardened `update-google-reviews.py` — brand-leak filter drops bare "Frame Restoration" reviewers, city metadata preserved on author merge, empty-result fallback to existing feed. Aggregate verified against GBP data_id: 20 reviews / 5.0 stars.
- **2026-04-27 perf:** Blog hero images switched from `loading="lazy"` → `loading="eager" fetchpriority="high"` across all 23 blog posts using the figure.blog-featured-image template. Hero was below the dark header but still in the initial viewport — lazy was deferring it past first paint and dragging LCP / showing a visible blank gap. Web-standard above/near-the-fold pattern restored.
- **2026-05-01 CRITICAL SEO unblock (PR #10):** Fixed 5-week-silent `noindex,nofollow` bug on `pages/storm-damage.html` (highest-CPL service page in Frame's vertical) and `pages/general-contracting.html`. Both were in sitemap.xml + canonical to themselves but tagged noindex since 2026-03-28. Now `index,follow,max-snippet:-1,max-image-preview:large`.
- **2026-05-01 form CRO (PR #10):** All 3 primary lead funnels (index hero, index #contact, global-modal.js booking modal) cut from 6 required fields → 3 (Name + Phone + ZIP). Optional fields collapsed into `<details>` "Add details (optional)" disclosure. Hero subhead: "3 fields, 30 seconds. We'll call you back within 15 minutes during business hours."
- **2026-05-01 visible FAQ blocks (PR #11):** 4 visible Q&A blocks added to storm-damage / roof-replacement / insurance-claims service pages, near-verbatim to existing JSON-LD (closes the schema-content mismatch that disqualified them from FAQ rich-results, opens AEO citation surface).
- **2026-06-02 location-depth close-out + gate hardening + about consolidation (PRs #72 / #73 / #74):** (a) **last 15 thin location pages deepened** past the 500-unique-word floor (`48e840c`) — 2 city-specific FAQ Q&As each, synced across FAQPage JSON-LD + visible accordion, grounded in real geography (bluffdale, clearfield, kaysville, kearns, lindon, magna, north-salt-lake, santaquin, south-salt-lake, springville, syracuse, wallsburg, west-bountiful, west-valley-city, woods-cross; all now 560–622w). All 45 location pages now PASS depth. (b) **`city-quality` gate flipped advisory → BLOCKING** (`37d0eb1`) — `continue-on-error` removed; thin / `{city}`-swapped templated location pages can no longer silently ship (link-integrity-style ratchet). (c) **duplicate about pages consolidated on `/pages/about`** (`00f273d`) — `sitemap.xml` `/about`→`/pages/about` + `vercel.json` 301; the richer 2,452-word self-canonical nav page is the single about URL (the thin 1,424-word `about.html` now redirects). **Built but not yet merged** (`e29541b`, branch `feat/location-pages-fruit-heights-spanish-fork`): **Fruit Heights (665w) + Spanish Fork (705w)** depth-complete served-area pages (full JSON-LD, no AggregateRating, `business.json` identity, compliance-safe framing, sitemap + index chips + reciprocal links) — all gates green incl. now-blocking city-quality (47/47); corpus → 47 location pages on merge.
- **2026-06-01 full-site audit pass (PRs #59 / #60 / #61):** (a) **rating-integrity** — removed the cloned `AggregateRating` from 45 location pages (`816c280` via `scripts/fix-location-aggregate-rating.mjs`); audit now reports 364 JSON-LD blocks, 0 invalid, rating-integrity clean. (b) **broken-link sweep** — repaired all 17 live broken internal links (literal `${item.url}` template-leak bug, `/pages/solar`→`/pages/solar-installation`, ×9 missing-`/heber-city/` snow-damage path, nonexistent per-city service subpages) and flipped the `audit-links` gate to **blocking** (`63a2200`). (c) **metadata hygiene** — over-length titles + meta descriptions trimmed, 5 missing `og:image` added, sitemap orphan resolved (`64d8f89`). (d) **thin-content** — FAQ + `FAQPage` schema added to the 3 sub-500-word service pages: general-contracting, solar-installation, water-fire-flood-restoration (`e223660`, PR #61). (e) **Utah-law compliance remediation** — claim-language gated ("public adjuster"/"negotiate") + insurance-claims cornerstone rewrite (`57bd993`); compliance gate green, 0 blockers. (f) **GBP `hasMap` + per-location Google Review schema** for Sandy + Millcreek (`7dc14a0`). (g) **`llms.txt`** uses 435-292-8802 as the single public number (`8cd0076`). Audit headline: site is technically strong (SEO 100 / A11y 100 / LCP 291ms / CLS 0.02 / 99/116 FAQPage); remaining work is mechanical metadata hygiene (49 over-length titles, 46 meta descriptions, 32 thin location pages) tracked in `data/SITE-AUDIT-2026-06-01.md`.
- **2026-06-01 GBP compliance scrub + enforcement-watch discovery (PR #71, `4e11152`):** the review-not-showing investigation surfaced that **Frame's GBP is on Google enforcement watch** (a prior post that mentioned reviews/stars = **DPNB** violation) and that the 2026-05-11 session pushed **unlicensed-adjusting language live** to the profile (Utah **§31A-26-201**) — both plausibly **holding new reviews**. This PR scrubbed the *internal, `.vercelignored`* GBP draft docs (`GBP-AUDIT-2026-05-11.md`, `GBP-POST-BACKLOG.md`, `GBP-POST-PUBLISH-READY-2026-05-11.md`, `GBP-SERVICES-PROPOSAL-2026-05-27.md`): 9 internal-phone (302-4422) leaks → 292-8802; adjusting language → compliant "we document the damage, provide a line-item scope your adjuster can read, meet your adjuster on site — the claim stays between you and your insurer; we don't adjust"; renamed "Insurance Claim Assistance"/"Insurance Claim Roofing" drafts → "Storm & Hail Damage Roofing"; added GBP-post guardrails (no review/star mentions, no internal phone, no adjusting language) + a CORRECTION banner in the audit doc. **NOT yet fixed (owner-login dashboard task, the actual remedy):** rename live services "Insurance Claim Assistance" → "Storm & Hail Damage Roofing" and "Free Roof Estimate" → "Roof Estimate" (drop "Free"), stop review-mention posts, then request reinstatement.
- **2026-06-01 review-CTA integrity hardening (PRs #68 / #69 / #70):** root-caused a "Leave a Review" button that shipped pointing at a **dead Google listing** (`cid 16833…`) — customer reviews never reached Frame's real profile — plus internal-phone (302-4422) leaks on 3 deployed pages. (a) **single source of external identity** — new `data/route-factory/business.json` (canonical GBP `cid 8458659884566588108` + `data_id` + `place_id ChIJCT6-aZD1TYcRzCpwX1kyY3U`, public phone 292-8802, internal-only 302-4422-never-publish, known-dead `cid 16833…`). (b) **merge-time blocking gate** — `scripts/audit-cta-integrity.mjs` wired into `compliance-gate.yml`; blocks internal-phone leaks, wrong/dead listing `cid`/`data_id`/`placeid`, placeholder links, and generic `maps`/`search` review CTAs; scoped to deployed+customer-facing pages. (c) **`.vercelignore`** now excludes `backlink-playbook.html` + 2 internal `data/*.html` reports that leaked 302-4422. (d) **weekly prod liveness monitor** — `cta-liveness.yml` + `scripts/check-cta-liveness.py`: asserts the deployed review page is 200 + carries the canonical `cid`/`place_id` + no dead `cid`, and (SerpAPI, `SERPAPI_KEY`-gated) that the GBP listing is live with >0 reviews. (e) **one-tap writereview** — `review.html` now uses `https://search.google.com/local/writereview?placeid=ChIJCT6-aZD1TYcRzCpwX1kyY3U` (opens the star dialog, no owner login). Net: the review-CTA bug class is fixed, un-mergeable going forward, and watched in production.
- **2026-06-01 brand-name decision (`data/BRAND-DECISION-2026-06-01.md`):** canonical public name DECIDED = **"Frame Roofing Utah"** (Option B). Site/schema/`llms.txt` already correct and stay as-is; GBP + BBB + ~15 directories get aligned *to* the site via DBA-first sequence (file assumed name → GBP name edit, 27 reviews preserved → directory NAP pass per `NAP-DIRECTORY-CHECKLIST-2026-06-01.md`).
- **2026-05-01 storm hub-spoke (PR #12 + #14):** Complete bidirectional cluster — `/pages/storm-damage` now links to all 11 city storm/hail/wind blog posts; each of those 11 posts has a city-personalized CTA card linking back to `/pages/storm-damage`.
- **2026-05-01 review-acquisition surface area (PR #9 + #13):** Gold "★ Leave a Review" link added to homepage footer + all 45 location-page footers, plus thank-you page review card. Surface area went ~2 → ~48 paths to `/review.html`.
- **2026-05-01 thank-you primary CTA flip (PR #13):** Primary visual CTA changed from "Back to Home" (wasted action) → gold ☎️ Call 435-302-4422 (tel:). New PostHog event `post_lead_call_now` for submitter→immediate-call attribution.
- **2026-05-01 neighborhood depth (PR #14):** "{City} Neighborhood-Specific Roofing Notes" section (4 callout cards each, sourced from real planning code + IRC + manufacturer specs) added to top 3 location pages (heber-city, park-city, salt-lake-city). Each closes with sources attribution citing planning code, IRC R905, manufacturer specs, Frame's Utah DOPL license #14256097-5501.
- **2026-05-06 mobile-perf partial fix (commits 660ead1 + 64128a4):** Homepage video stack reweighted to address the 2026-05-04 Lighthouse audit's 6.1 MB mobile page weight / 7.8 s LCP finding. `assets/drone-footage.mp4` 13.2 MB → 5.2 MB (H.264 720p, 24.5 s, moov-first faststart) and `assets/videos/frame-restoration-showcase.{mp4,webm}` 22.0/10.6 MB → 5.4/7.5 MB. The above-the-fold "Watch Us Work" section now autoplay-muted-loops a 5.4 MB silent aerial instead of a 22 MB controls-only narrative, and the original branded 60s music showreel was re-homed to a click-to-play element below the fold (commit 4009ffe) — audio is preserved for engaged scrollers without paying the LCP cost. Cachebuster `?v=20260506` on all three URLs. Re-audit pending. Two of the three audit-recommended fixes (PostHog `disable_surveys: true` + `index.html:1671` JS TypeError) still queued.
- **2026-05-06 reviews JSON sync (commit 21edf38, auto-update):** `data/google-reviews.json` + `reviews.json` ticked 19 → **20 reviews**. AggregateRating copy on LocalBusiness JSON-LD + all 45 location pages was already showing 20/5.0 stars since the 2026-04-22 audit fix — this commit catches the underlying JSON up to the schema claim. Performed by Review Bot automation identity (`review-bot@frameroofingutah.com`).
- **2026-05-12 storm-chaser pillar + hub-spoke + schema entity-split (PRs #56 + #57, commits d03cda7 + 6e17d96):** New 2,836-word `blog/utah/local-roofer-vs-storm-chaser-utah.html` pillar (BlogPosting + FAQPage 6 Q&As, EEAT Andrew Best pattern, Utah DOPL + AG + Insurance Dept primary sources, dual-number 302-4422/292-8802 CTA per 10DLC rule, `digitalSourceType: humanWritten`, `heber-custom-reroof-front-view.webp` og:image — first use). Closes ~15 of 22 universal local-service AI-fan-out gaps from RCBuild-Kit's `verticals/local-service/QUERY-FANOUT-AUDIT.md`. Hub-spoke wired in #57: 13 existing storm-cluster blog posts (bountiful/draper/herriman/holladay/lehi/murray/ogden/provo/riverton/slc/sandy + statewide april-storm-checklist + utah-hail-season-guide) gain a red-tinted `.storm-chaser-warning` aside linking up with explicit anchor "Local Utah Roofer vs Storm Chaser guide" (11 posts use `</article>` insertion point, 2 use `div.blog-cta` marker). Pillar card inserted at TOP of `blog/index.html` post grid. Sitemap registered (priority 0.8). LocalBusiness JSON-LD `dateModified` bumped 2026-04-24 → 2026-05-12; description corrected "40+ communities" → "45 communities" (catches drift PR #41 hero-badge fix didn't cover). llms.txt appended 10 direct-answer Q&A blocks (30-50 words each: storm-chaser disambig / hire checklist / hail payout averages / insurance denial / ACV vs RCV / photo documentation / wind vs hail / tear-off vs overlay / permit-by-city / seasonality) + hours-drift fix Mon-Sat 8AM-6PM → Mon-Fri 8AM-7PM / Sat 8AM-6PM + dual-number AI guidance clarification. Index.html homepage tiles extracted from nested `hasOfferCatalog` → 6 standalone `@type:Service` JSON-LD nodes (Residential / Commercial / Storm / Water-Fire / Solar / General Contracting) so each is independently rich-result eligible + AI-citable as a discrete entity.
- **2026-05-06 (evening) AEO automation infra (commits 3adaf0f → 762b9b8):** Pure-cron AEO citation monitor stack committed (was untracked, local-only earlier today). 5-query SerpAPI sweep → repo-aware action engine (`leverage = severity × lead_value`, smart classification across freshness-anchor / authority-boost / refresh-stale / internal-link-boost / create-content) → GitHub-issue auto-filer (`gh` CLI, idempotent) → Level-2 PR-gated auto-fixer (`aeo-fix-internal-links.py`, local `mistral-nemo:12b`, confidence ≥ 0.75 + topicality check, NEVER auto-merges, 24h race-condition guard skips files Cowork may be editing). LaunchAgents plist `com.ryan.frame-roofing-aeo.plist` runs monthly (5th, 8:23am). **First run today:** score 1/5, Frame **#1 in local pack for "best roofer Heber City Utah"**, 3 medium-priority actions filed as issues #15-17, demo PR #18 added 3 internal links to the cost-2026 guide. Anchor-quality filters tightened in 762b9b8 after PRs #18-20 produced 3/8 borderline anchors (confidence threshold 0.6 → 0.75 + new `_topic_overlap()` rejects domain-stop-word-only anchors like "Utah" / "Salt Lake City" / "Frame Roofing Utah"). Token savings vs prior claude.ai/code monitor trigger: ~55K tokens/run = ~$0.23/month. Replaces the trigger entirely; trigger-side now retired.
- **2026-04-22 perf:** Removed double-loading Google Fonts <link> (self-hosted WOFF2 already wired in global.css — kills render-blocking + double FOUT); changed drone-loop video preload from "metadata" → "none" (13 MB clip was loading eagerly on mobile); added width/height to 6 service-card imgs (CLS fix)
- **2026-04-24 (afternoon) AEO gap closure:** Homepage hero `<p>` now leads with "Frame Roofing Utah is a family-owned, licensed roofing contractor based in Heber City" (definition pattern for AI direct-answer extraction); new `.quick-answer-strip` between hero and #watch-in-action carries visible "Last updated: April 24, 2026" stamp (87 blog/location pages already had it — homepage was the gap); `dateModified: 2026-04-24` added to index LocalBusiness JSON-LD (maintenance-layer was 0/2, now 2/2); "10-year" → "10 years" in hero so number matches AEO audit regex. llms.txt Texas-brand disambiguation rewritten to "not to be confused with … distinct from" phrasing (was "is a different business", bypassed entity-disambiguation heuristic). Perfect Stack audit: 76/B → 88/A- projected.
- **2026-04-24 (afternoon) llms.txt A-grade polish:** Blog Library + Owner line converted from `- Title — url` → `- [Title](url)` markdown link format (0 → 11 markdown links) to match llmstxt.org spec; audit llms_txt_quality 2/5 → 5/5 projected. Expected overall: 88 → 91 (A-).
- **2026-04-22 (evening) mobile polish (3 tiers + carousel):** Tier 1 perf/touch — removed Mapbox 4s timeout fallback (was loading 7s of JS into Lighthouse audit window on non-scrolling viewports), null-safed mobile nav toggle, SMS consent checkbox upgraded to WCAG 44px tap target (22×22 box, 14px font). Tier 2 type/visibility — hero-badge 10→12px, hero p 14→16px, hero-points 13→15px at ≤600px; Quick Answer boxes on 4 service pages swapped cream-on-cream → navy-tint (#EEF4F9) with 6px border + shadow; hamburger button 33×24 → min 44×44 (WCAG 2.5.5 / Fitts). Tier 3 legibility/thumb-zone — .last-updated inside dark-navy heroes forced to rgba white 0.82 (was invisible grey), .content-section p unified at 16px mobile, homepage hero subhead trimmed 5 sentences → 2 (was crowding CTA out of first scroll). Reviews carousel on mobile — removed min-height on slides so short reviews don't render as tall empty boxes, 7-line clamp on review body to stop 3 long quotes from stretching slides past 500px, hid under-sized 32×32 prev/next arrows, added native touchstart/touchend swipe handler (40px threshold, horizontal-only so vertical page scroll still works), touch-action: pan-y on viewport.
- Homepage H1 rebranded 2026-04-17: "Mountain-Grade Roofing, Valley-Wide"
- Storm-damage / storm-damage-restoration consolidated (301 redirect + canonical)
- Duplicate blog URLs cleaned from sitemap
- Yelp footer link corrected to /biz/frame-restoration-heber-city
- Directory tracker: Yelp marked as Done; Angi marked Done 2026-04-13
- robots.txt with AI crawler allowances
- llms.txt upgraded 2026-04-17 for AEO: 10 passage-citable Q&A, 45 service areas by county, verified-credentials-only
- Full rebrand from "Frame Restoration Utah" to "Frame Roofing Utah" (site-wide re-swept 2026-04-08)
- WebP conversion site-wide (88 images converted 2026-04-12, earlier 24 compressed 2026-04-07)
- IndexNow integration for bulk URL indexing (2026-04-12)
- HowTo JSON-LD on all 39 blog posts (2026-04-10)
- Author bios + comparison tables + digitalSourceType on blog + service pages (2026-04-08)
- FAQ schemas expanded 5→12 city-specific per location page (2026-04-08)
- Internal crosslinks: blog→location and location→location on all 45 location pages (2026-04-10)
- PostHog analytics wired on 107 pages + click tracking on 47 (2026-04-10)
- Live SEO & Lead Attribution Tracker launched with PIN gate + multi-user admin (2026-04-10)
- Review landing page + automated review-request edge function (2026-04-10)
- 301 redirects for old domain framerestorationutah.com → www.frameroofingutah.com (2026-04-07)
- HSTS header + PostHog reverse proxy (2026-04-08)
- A11y pass: aria-label roles, heading order, select label, sr-only class (2026-04-07)
- **2026-06-03 AEO (Pillar 2 — entity consistency, PR #79 `0b697f8`):** `hasMap` (GBP map URL) + `sameAs` anchors [GBP, Facebook, BBB 90056184, DOPL #14256097-5501, CertainTeed/Tamko locators] added to **all 47 location pages** (was 0/47), NAP matched to the Pillar-1 directories + CI entity-consistency gate so cross-source agreement can't drift
- **2026-06-03 AEO (Pillar 3 — review velocity, PR #78 `2b74264`):** real per-location `Review` JSON-LD on the **top-10 traffic cities (2 → 12 location pages)** — the individual reviews AI Overviews quote; rolling to all 47 next, gated by #77
- **2026-06-03 compliance (PR #77 `4c2c1ce`):** `scripts/audit-review-integrity.mjs` flipped advisory → **BLOCKING** — no cloned `AggregateRating` on city pages (single GBP → homepage/About only), real/individually-attributable reviewers only

### Competitors
- roofingutah.com — exact-match domain, 54 Google reviews
- utahroofingcompany.com — 50 Google reviews
- frameroofingutah.com — 19 Google reviews (us)

### Audit Reports (in project folder)
- Frame-Roofing-SEO-Audit-March2026.html — Full interactive SEO audit
- Frame-Roofing-GEO-Audit-March2026.html — GEO/AI optimization audit
- SEO-BRUTAL-AUDIT-March-2026.md — Honest audit scoring 38/100
- Frame-Roofing-SEO-Automation-Playbook.html — Automation strategy
- Frame-Restoration-Blog-Content-Strategy.xlsx — 23 topics x 44 cities keyword map

---

## LEAD PIPELINE (Supabase)

- **Project:** frame-roofing-utah (ID: hdcflshhomzildwqlmwh, region: us-west-1)
- **Edge function:** `handle-lead` **v8** (PR d9bd585, 2026-05-10) — receives JSON form submissions, saves to DB, emails via **Resend** (replaces Formspree as of v8), sends SMS via Verizon email-to-SMS gateway + Twilio (parallel). v7 added OpenRouter / Gemini 2.0 Flash tier classifier (emergency / urgent / scheduled / general / spam routing); v8 swaps the email backend.
- **Edge function:** `lead-crm` **v6 as of 2026-05-16 (1b95df5)** — PIN-gated reads + writes for the `/leads` CRM page; auth mirrors `weekly-report` (key=`frame-roofing-2026` + per-user PIN against `report_access` table — existing PINs work unchanged). v6 adds the 8 post-won lifecycle fields to LIST + UPDATE (deposit_received_at/deposit_amount/install_scheduled_for/job_started_at/job_completed_at/balance_due/contract_url/warranty_doc_url/product). v5 added `?action=clicks` + `action=create`. Source at `supabase/functions/lead-crm/index.ts`.
- **Edge function:** `handle-call` **v2 as of 2026-05-16 (8a8063f)** — Twilio inbound voice webhook. v2 auto-creates a `leads` row for any unknown caller (`source_page=inbound-call`, `tier=general`, normalizes phone to E.164, 90-day dedupe against `leads.phone`), stamps `call_logs.lead_id` so the CRM can show the call thread on the lead detail. Closes audit gap #3.
- **Edge function:** `handle-sms` **v5 as of 2026-05-16 (8a8063f)** — Twilio inbound SMS webhook. v5 mirrors handle-call behavior: first inbound SMS from an unknown number auto-creates a lead with the message body captured as `leads.message` (so Ryan sees customer intent immediately). Skips messages from Landon (operator commands). 7 historic inbound contacts (6 calls + 1 SMS) backfilled; cold-pitch SEO solicitation +18333380180 auto-classified spam.
- **Edge function:** `review-request` **v2 as of 2026-05-16 (1b95df5)** — daily cron, was previously DOA filtering on `status='completed'` (impossible — nothing set that value). v2 now filters on `status='won' AND job_completed_at IS NOT NULL +3d`. Verified GET returns "No review requests due" cleanly with the new query shape. Closes audit gap #7.
- **Edge functions:** `reddit-aeo-classifier` + `reddit-aeo-digest` **NEW 2026-05-16 (c957cfe)** — LLM-powered multi-class classifier (gemini-2.0-flash) inspired by the xai-org x-algorithm Phoenix multi-action-prediction pattern. Predicts independent probabilities per axis (`p_utah_lead`/`p_aeo`/`p_competitor`/`p_storm`/`p_spam`), weighted-sum into `aeo_score`. Digest fn produces sms/markdown/json/html formats. Reclassified all **254 historic reddit_signals rows**: before 0 leads / 254 noise (0 actionable); after 6 utah_lead + 55 aeo_opportunity + 3 competitor_intel + 1 storm_signal + 150 noise + 39 spam (65 actionable).
- **Edge functions:** `youtube-script-drafter` + `aeo-master-digest` + `aeo-citation-tracker` **NEW 2026-05-16 evening (9467596)** — the unified 8-surface AEO distribution stack. `youtube-script-drafter` converts a Frame pillar/spoke URL into a 60-90s Landon-voice video brief (hook + talking points + shot list + voiceover + hashtags + thumbnail idea + YouTube description); **18 scripts already drafted** from the storm-chaser pillar + 13 spokes and queued. `aeo-master-digest` replaces the reddit-only digest — pulls top-per-surface picks across `aeo_distribution_queue`, formats `sms`/`markdown`/`html`/`json`; the `reddit-aeo-monday-digest` scheduled task now fires this instead. `aeo-citation-tracker` queries Perplexity Sonar + Gemini Flash + Claude Haiku with 20 Utah roofing prompts, parses for Frame citations, logs to `aeo_citation_log`. **Baseline measured 2026-05-16:** 0% Frame citation rate on "Best roofing company in Heber City Utah" (Gemini) — competitor "Premier Roofing" cited instead. Architecture inspired by xai-org/x-algorithm Phoenix component; plug-and-play surface drafters — each new platform (Nextdoor / Yelp / LinkedIn / Quora) drops in as one new edge fn + queue inserts without changing digest or queue shape.
- **DB tables (Master AEO Play 2026-05-16 evening, 9467596):** `aeo_distribution_queue` — unified queue across all citation surfaces (columns: `priority_score`, `status` workflow, `expires_at`, `performance_data` slot, surface-tag). Seeded by migrating all **65 actionable `reddit_signals` rows** (carries `aeo_score` + `recommended_url` + `reply_draft` + `persona_voice` from the morning's Phoenix classifier) + 18 YouTube scripts from `youtube-script-drafter` — **83 drafts cooked** as of close, ~2+ months of weekly posting backlog. `aeo_citation_log` — measurement-loop foundation; rows written by `aeo-citation-tracker`. First row logs the 0% baseline.
- **DB table:** `leads` (id, created_at, name, email, phone, address, service, message, source_page, status, job_value, commission GENERATED [5% Heber/Midway, 10% else, off margin], margin, city, 2026-05-10 attribution columns: gclid, fbclid, utm_{source,medium,campaign,term,content}, won_at, uploaded_to_google_ads, uploaded_to_meta_ads, **plus 2026-05-16 post-won lifecycle columns: deposit_received_at, deposit_amount, install_scheduled_for, job_started_at, job_completed_at, balance_due, contract_url, warranty_doc_url, product (1b95df5)**. `leads_status_check` CHECK constraint + partial indexes for offline-conversion sync — migration `20260510_add_attribution_columns.sql`). Greg Atkinson lead backfilled with deposit $10k on 2026-05-09, balance_due $108k, product Brava Aged Cedar.
- **DB tables (Reddit-AEO 2026-05-16, c957cfe):** `reddit_signals` gains 12 new columns (`aeo_score`, `p_utah_lead`/`p_aeo`/`p_competitor`/`p_storm`/`p_spam`, `recommended_url`, `reply_draft`, `persona_voice`, `digest_sent_at`, `posted_to_reddit_at`, `posted_url`). New table `frame_content_inventory` seeded with 21 Frame pillar/spoke URLs + `topic_tags` + `question_patterns` for classifier URL matching.
- **CRM dashboard:** `/leads` (`leads.html`, **NEW 2026-05-10**, expanded 2026-05-16) — PIN-gated dark-themed CRM. KPIs (new untouched / last 30d / open pipeline / won MTD / commission MTD), filter+sort+search, click-to-edit panel. **2026-05-16 (1b95df5):** new "Job pipeline" sub-panel inside the edit drawer that only renders when `status=won` (keeps the form lean for new leads); shows deposit/install/balance/start/complete dates inline + a "Review request status" line stating exactly when the review-request auto-fire will go. Auto-stamps `won_at` when status flips to `won`. `/seo-report` user-bar links to it.
- **Supabase anon key:** eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImhkY2Zsc2hob216aWxkd3FsbXdoIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzM4ODQzNzcsImV4cCI6MjA4OTQ2MDM3N30.GO5CU5dRBfeoC5ZEl2U143cTXbKdV5ZUhq4ucwBICoI
- **Edge function URLs:** https://hdcflshhomzildwqlmwh.supabase.co/functions/v1/handle-lead · https://hdcflshhomzildwqlmwh.supabase.co/functions/v1/lead-crm · https://hdcflshhomzildwqlmwh.supabase.co/functions/v1/handle-call · https://hdcflshhomzildwqlmwh.supabase.co/functions/v1/handle-sms · https://hdcflshhomzildwqlmwh.supabase.co/functions/v1/review-request · https://hdcflshhomzildwqlmwh.supabase.co/functions/v1/reddit-aeo-classifier · https://hdcflshhomzildwqlmwh.supabase.co/functions/v1/reddit-aeo-digest · https://hdcflshhomzildwqlmwh.supabase.co/functions/v1/youtube-script-drafter · https://hdcflshhomzildwqlmwh.supabase.co/functions/v1/aeo-master-digest · https://hdcflshhomzildwqlmwh.supabase.co/functions/v1/aeo-citation-tracker
- **Email:** WORKING via Resend (as of 2026-05-10, PR d9bd585) — Resend free tier 100/day, 3000/mo (vs Formspree's 50/mo). Domain `frameroofingutah.com` to verify in Resend; `RESEND_API_KEY` stored as a Deno secret. Landon notification: `leads@frameroofingutah.com` → `landon@framerestorations.com`, CC `ryanconwell99@gmail.com`. Verizon SMS gateway sender: `noreply@frameroofingutah.com` → `{digits}@vtext.com`. Formspree (account meeroaqa, ryanconwell99@gmail.com) retired but not yet deleted — keep ~30 days as rollback option.
- **SMS:** WORKING — sends via Verizon email-to-SMS gateway (4353024422@vtext.com) via Resend as primary + Twilio as secondary. Twilio outbound still blocked by 10DLC (error 30034) but Verizon gateway delivers.
- **Google Sheet:** NOT YET SET UP — env var ready, needs Google Apps Script webhook (lower priority now that `/leads` CRM exists)

---

## YELP

- **Listing:** https://www.yelp.com/biz/frame-restoration-heber-city
- **Display name:** FRAME RESTORATION (Yelp shortens LLC names — normal)
- **Free trial:** $17/day ($13 ads + $4 upgrade), ends 4/27/2026
- **Account email:** landon@framerestorations.com
- **Footer link:** Corrected and live on frameroofingutah.com

---

## BRAND

- **Colors:** Navy #0B4060, Gold #E1B969, Off-white #FAF9F5
- **Fonts:** Archivo Black (headings), Archivo (body) — Google Fonts, need to self-host
- **Logo:** Still says "Frame Restoration" in PNG — needs designer update to "Frame Roofing"
- **Logo file:** /images/logo-rc-darkblue.png (85px height, 100px nav bar)
- **OG image:** /images/og-image.jpg (branded aerial drone shot)

---

## PRIORITY TODO (as of 2026-04-17)

### Immediate
1. ~~**Ship Lighthouse audit fixes (from `data/audits/lighthouse-2026-05-04.md`)**~~ ✅ DONE 2026-05-10. All three fixes shipped: (a) `index.html:1692` IIFE-wrapping-addEventListener TypeError fixed in commit 0199efd; (b) LCP was already solved by 2026-05-06 video swap (fresh audit 2026-05-10 reports 99/100 mobile perf, LCP 1.5s — no image work needed); (c) PostHog `disable_surveys: true` added in 0199efd, saves 27 KB unused JS. Best Practices projected 96 → 100. `data/audits/` commit policy still open.
2. Set up Twilio — Utah phone number, wire into edge function for lead SMS (10DLC consent checkbox now in place 2026-04-17, error 30923 resolved)
3. Build speed-to-lead AI bot — Twilio + Claude API + Supabase
4. Google Sheet lead tracker — Apps Script webhook (lower priority now that `/leads` CRM exists, 2026-05-10)
5. ~~Direct email (replace Formspree with Resend/SendGrid)~~ ✅ DONE 2026-05-10 (PR d9bd585 — handle-lead v8 swaps Formspree → Resend; Resend free tier 100/day vs Formspree's 50/mo)
6. ~~301 redirects framerestorationutah.com → frameroofingutah.com~~ ✅ DONE (Landon domain forwarding 2026-03-30; in-code 301s also added 2026-04-07)
7. ~~Google Search Console verification + sitemap for frameroofingutah.com~~ ✅ DONE
8. Update Google Business Profile with new URL
9. Reddit-scanner: response parsing hardened 2026-04-17 (HTTP status, empty body, retry, typed errors) — monitor reliability

### Short-term
9. ~~Self-host fonts (Archivo Black + Archivo)~~ ✅ DONE (already wired in global.css; double-loading Google Fonts <link> removed 2026-04-22)
10. ~~Convert images to WebP~~ ✅ DONE (88 images converted 2026-04-12 + 24 compressed 2026-04-07)
11. ~~Add author bios + publication dates to blog posts~~ ✅ DONE (2026-04-08)
12. Directory submissions (HomeAdvisor, Thumbtack, local chambers) — Angi + Yelp already done
13. Get updated logo from designer ("Frame Roofing" not "Frame Restoration")
14. ~~IndexNow integration~~ ✅ DONE (2026-04-12)
15. ~~HowTo schema coverage on blog~~ ✅ DONE (all 39 posts, 2026-04-10)

### Medium-term
13. Weather-triggered blog automation (NWS API, architecture in FRAMEROOFINGUTAH-BUILD-PLAN.md)
14. Rewrite 44 location pages with unique per-city content
15. Build more project case studies

---

## 💰 MARKETING BUILD (Active April 2026)

**Entry point for all marketing work:** [`data/MARKETING-BUILD-CONTEXT.md`](data/MARKETING-BUILD-CONTEXT.md)

That file is the canonical "start here" for any agent (Cowork, Claude Code, future-you) working on Frame Utah marketing. It contains the active $15K/mo allocation, Wave 1 findings already integrated, Wave 2 fire commands, free actions today, KPI scoreboard, confidence scorecard, and full file index.

### Quick reference

**Active $15K/mo allocation (Wave 1 deltas applied):**
- $7,500 — Tier-1 priority cities (Park City, Heber, Draper, Salt Lake, Cottonwood Heights, Midway)
- $2,500 — Tier-2/3 volume layer (West Valley, Sandy, Bountiful, Lehi)
- **$1,700 — Reviews acquisition** ⭐ (closes 20→60+ vs roofingutah.com's 54)
- $1,300 — YouTube + TikTok organic + light boost (Heber drone reuse)
- $700 — Content moat (narrowed scope after Wave 1)
- $800 — Storm-trigger reserve (May-Sept hail/wind + Dec-Feb ice dam)
- $500 / $0 — Yelp (decision pending — trial expired 2026-04-27)

**Critical Wave 1 finding:** roofingutah.com has not published a blog post since 2024-05-31 (sitemap-verified). Frame Utah's content moat is essentially already built; the actual gap is reviews + GBP. Allocation reflects this.

### Wired npm scripts (in `package.json`)

| Command | Purpose | Cost (live mode) |
|---|---|---|
| `npm run market-intel` | Offline scoring of 45 cities (smoke test) | $0 |
| `npm run market-intel:dataforseo` | Real per-city SERP + KW + paid-density | ~$0.30 / 45 cities |
| `npm run competitor-ads` | SerpAPI scan on roofingutah.com + utahroofingcompany.com + reignroofing.com | ~$0.15 |
| `npm run competitor-tiktok` | Apify TikTok scrape on UT competitor handles | ~$1.50 |

All scripts dry-run when env vars (`SERPAPI_KEY`, `APIFY_TOKEN`, `DATAFORSEO_*`, `CENSUS_API_KEY`) are missing — they print queries + estimated spend, exit 0, no API calls.

**Total Wave 2 spend:** ~$33 one-time across both Frame projects (UT + TX). Pending company CC.

### Free actions (no CC needed — do these first)

1. Pull Yelp 30-day dashboard → today's $510/mo decision
2. Pull GSC last-30-days → refresh March-30 baseline
3. Run `scripts/supabase-pipeline-query.sql` in Supabase SQL Editor → real per-city job-value + close-rate
4. Pull PostHog form-submission count last 30 days
5. Google Keyword Planner: Park City + Heber + Draper
6. 15-min Birdeye / NiceJob / Podium pricing calls
7. Send 5 manual review-requests via existing edge function
8. Storm-trigger fire drill via iMessage to Landon

### Key marketing files
- `data/MARKETING-BUILD-CONTEXT.md` — **canonical entry point**
- `data/30-day-lead-sprint-2026-05-01.md` — **NEW 2026-05-01.** May 2026 lead-velocity sprint plan (5 highest-EV plays — reviews velocity, speed-to-lead, form CRO, pre-built storm landing page, AI-Overview citation moat — w/ file paths, effort scores, expected impact, source citations, weekly cadence). PRs #9-#14 executed Day 1 of this plan.
- `data/RYAN-VALUE-MEMO-2026-05-01.md` — **NEW 2026-05-01.** Landon-facing build receipt + 10% commission cadence ask (SMS-friendly TLDR + long form). Frames the 5-week build, current GSC baseline, May sprint roadmap.
- `data/market-intel-audit-2026-04-27.md` — full $15K deep-dive audit
- `scripts/market-intel-kit/README.md` — bundled scoring + allocator kit docs
- `scripts/supabase-pipeline-query.sql` — ready-to-paste SQL for Supabase
- Cross-project research framework: `~/projects/frame-roofing-research-plan-to-A-grade-2026-04-27.md`
- Cross-project Wave 2 punch-list: `~/projects/frame-roofing-wave-2-readiness-2026-04-27.md`

### Operational separation reminder

Frame TX (`framerestoration.com` singular) and Frame Utah (`frameroofingutah.com`) are independent operational entities. **Never share runtime state, scheduled triggers, Twilio numbers, or GBP listings between projects.** Cross-project research artifacts at `~/projects/frame-roofing-*.md` are documentation only — not a license to cross-trigger. See § "Operational Separation from Frame Utah (enforced 2026-04-27)" in this CLAUDE.md.

---

## GIT WORKFLOW

- All work in `/Users/agenticmac/projects/frame-restoration-utah/` (NOT tradeworker-site/frame-restoration-utah — that is a stale obsolete copy)
- Push to `main` auto-deploys to BOTH Vercel accounts
- Commit format: descriptive message + `Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>`
- Watch for `.git/index.lock` — delete before committing if stale
- Desktop Commander read_file returns empty for HTML — use `start_search` with contextLines or `start_process` with `cat`

---

## SESSION LOG

### 2026-06-03 (Wednesday AFTERNOON Auto-Refresh — POST-ACTIVITY) — AEO Buildout Session: 3 PRs Merged (#77–#79) Land Pillars 2 & 3 of the New AEO Roadmap — Review-Integrity Gate Flipped to BLOCKING (#77), Per-Location `Review` JSON-LD Expanded on Top-10 Traffic Cities 2→12 (#78), `hasMap` + `sameAs` Completed on All 47 Location Pages + Entity-Consistency Gate (#79) — Plus New Untracked `AEO-BUILDOUT-HANDOFF-2026-06-03.md` Reframing ~80% of Remaining AEO Gain as OFF-Page (Directory Saturation + Index Verification + Third-Party Consensus) and Recording GBP Enforcement-Watch CLEARED (5.0★ / Reviews 20→27 Flowing)
Three feature PRs merged to `main` this afternoon (13:57–14:23 MDT), advancing HEAD `fbdb501` (10:03 AM early-AM auto-refresh) → `0b697f8` (= `origin/main` = `main`, clean tree apart from one new untracked handoff doc). All three execute directly against the freshly-written `AEO-BUILDOUT-HANDOFF-2026-06-03.md` roadmap (the "really build AEO perfect" plan whose thesis is that on-page AEO is table-stakes already paid and did NOT move the needle — Utah is cited only 1/10 on Perplexity for money queries — so the remaining gain is consensus across independent, indexed third-party sources). **PR #77 (`4c2c1ce`, 13:57 MDT) — `feat(compliance): blocking review-integrity gate — no cloned ratings, real reviewers only`:** `scripts/audit-review-integrity.mjs` loses advisory status and becomes a **blocking** gate. Enforces the standing rule that city/location pages NEVER carry `AggregateRating` (single GBP → homepage/About only) and that any `Review` JSON-LD references real, individually-attributable reviewers (no cloned/templated ratings). This is the merge-time fence that makes Pillar 3 (review velocity) safe to scale. **PR #78 (`2b74264`, 14:15 MDT) — `feat(schema): real per-location Review JSON-LD on top-10 traffic cities (2→12)`:** real per-location `Review` nodes added to the top-10 traffic cities, taking coverage from **2/47 → 12/47** location pages. AI Overviews cite *individual* GBP reviews, so this is the surface AI quotes — rolls toward all 47 next, gated by #77. **PR #79 (`0b697f8`, 14:23 MDT) — `feat(aeo): hasMap + sameAs on all 47 location pages + entity-consistency gate (Pillar 2)`:** closes the entity-linking gap the handoff doc flagged (`hasMap` 0/47, `sameAs` 0/47). Adds the GBP map URL (`hasMap`) plus `sameAs` anchors [GBP, Facebook, BBB 90056184, DOPL #14256097-5501, CertainTeed/Tamko locators] to **all 47** location pages, matching the exact NAP used in the Pillar-1 directories, plus a CI **entity-consistency gate** so the cross-source agreement can't silently drift. **This completes Pillar 2 (entity consistency): hasMap 0→47, sameAs 0→47.** Net roadmap state after the session: **Pillar 0 (unblock GBP) ✅ CLEARED** (validated against the live public listing — 5.0★, reviews 20→27 flowing not held, open/not suspended; the prior reviews-mention-post DPNB + adjusting-language services that put it on enforcement watch were scrubbed in PR #71 and the auto-post moved to manual-Monday — no reinstatement appeal needed); **Pillar 2 ✅ DONE** (this session); **Pillar 3 🟡 in progress** (gate live + 12/47 Review schema, rolling to 47); **Pillar 1 (off-page citation substrate — the ~80%) ⬜ owner's next move** (directory claims: BBB ✅ A+ 90056184, Angi/Thumbtack/Nextdoor/Yelp/Houzz + local "best roofer in {city}" listicles, each verified indexed via `site:` check — the step everyone skips — plus manufacturer installer-locator listings as high-authority `sameAs` anchors and genuine Reddit/forum answers); **Pillar 4 (measurement loop) ⬜ this week** (turn `~/projects/frame-utah-aeo/ai-visibility/check.mjs` into a weekly cron over a bridge Chrome :9222, extend to count third-party indexed mentions, watch 1/10 climb in `log.jsonl`). **New untracked artifact:** `AEO-BUILDOUT-HANDOFF-2026-06-03.md` (~86 lines) — supersedes the on-page checklist in `AEO-GROWTH-HANDOFF-2026-06-01.md`, pairs with `GENAI-REPORT-HANDOFF-2026-06-03.md`, written against verified Utah state 2026-06-03. Left **untracked** consistent with the prior handoff-doc convention (HANDOFF-2026-06-01/02.md also left untracked). **Win metric:** Perplexity/AIO money-query citations **1/10 → N**, measured weekly. **Don't-do (from the handoff):** ❌ more schema for its own sake (no uplift proven) · ❌ `llms.txt` polishing (17.6 KB, don't touch) · ❌ `AggregateRating` on city pages · ❌ AI opt-out toggle · ❌ spam Reddit/directories. **Carry-forwards:** roll Review schema 12→47 (gated by #77); execute Pillar 1 directory saturation + `site:` index verification (owner); stand up the Pillar 4 weekly AI-visibility cron; wire the US GSC gen-AI report when it lands; the 6 ported `scripts/audit-*.mjs` headers may still read "Frame Restoration TX" (Utah-adaptation P1); repo secrets `SERPAPI_KEY` + `SUPABASE_ACCESS_TOKEN`.

### 2026-06-03 (Wednesday early-AM Auto-Refresh — POST-ACTIVITY) — Location-Pages Session: Last 15 Thin Pages Deepened Past the 500-Word Floor (#72), City-Quality Gate Flipped Advisory → BLOCKING (#73), Duplicate About Pages Consolidated on `/pages/about` (#74) + Two New Depth-Complete Served-Area Pages (Fruit Heights, Spanish Fork) Built on a Feature Branch, Pending Merge

**What shipped to `main` (HEAD `74820c7` → `00f273d`, three commits 19:45–21:50 MDT 6/2):**

- **PR #72 (`48e840c`) — `content(locations): deepen 15 thin location pages past the 500-unique-word floor`.** The advisory city-quality gate had flagged 15 pages at 486–499 unique words; each gained **2 genuinely city-specific FAQ Q&As**, added to **both** the FAQPage JSON-LD **and** the visible accordion (kept in sync), grounded in each city's real geography/climate/housing — not vocabulary padding (Bluffdale Point-of-the-Mountain gap winds, Santaquin 5,000-ft snow load + wildfire interface, Magna Kennecott-era housing + lakebed dust, Wallsburg 5,600-ft Deer Creek snow load, Syracuse Great Salt Lake/Antelope Island wind, …). Built via 15 parallel agents grounded with real city facts to prevent fabrication, self-verified against the gates, then independently re-verified. All 15 now clear the floor (560–622 unique words). Pages: bluffdale, clearfield, kaysville, kearns, lindon, magna, north-salt-lake, santaquin, south-salt-lake, springville, syracuse, wallsburg, west-bountiful, west-valley-city, woods-cross. Verified: city-quality PASS (45 pages, 0 depth red-copy) · JSON-LD 100% valid · compliance 0 blockers · no unlicensed-adjusting language (Utah §31A-26-201). **Closes the long-running "thin location pages 484–498w" carry-forward.**
- **PR #73 (`37d0eb1`) — `ci(city-quality): flip doorway/thin-page guard from advisory to blocking`.** With all 45 pages clearing depth (500-unique-word), local-substance, and rating-integrity (#72 deepened the last 15), `continue-on-error` was removed from the `city-quality` job in `compliance-gate.yml` — a thin or `{city}`-swapped templated location page can **no longer silently ship** (same ratchet pattern as link-integrity). **Retires the recurring "merged PRs read UNSTABLE because city-quality is advisory" caveat** from the late-May / early-June refreshes — that gate is now real and blocking.
- **PR #74 (`00f273d`) — `fix(seo): consolidate duplicate about pages on /pages/about`.** Two about pages were competing: `sitemap.xml` pointed at `/about` (`about.html`, 1,424 words) while the nav + the richer content live at `/pages/about` (`pages/about.html`, 2,452 words, self-canonical). Consolidated on the richer nav-linked page — `sitemap.xml` `/about`→`/pages/about`; `vercel.json` 301 `/about`→`/pages/about` (the thin duplicate now funnels to the real page). Sitemap, nav, canonical, and redirect all point at one about URL. **Closes the `about.html` canonical carry-forward.**

**Built on a feature branch, NOT yet merged to `main`:**

- **`e29541b` (23:20 MDT, branch `feat/location-pages-fruit-heights-spanish-fork`) — `feat(locations): add Fruit Heights + Spanish Fork location pages`.** The two served-area pages whose dead links were removed in #60. Both **real, depth-complete** pages (not `{city}`-swapped templates), authored from the nearest sibling's structure: **Fruit Heights** (665 unique words — Davis County east-bench wind story: Sept 8 2020 100+ mph windstorm, Baer Canyon/Shepard Lane, orchard-era canopy, ~4,800-ft snow/ice-dam load, Class-4 vs standing-seam specs) and **Spanish Fork** (705 unique words — Spanish Fork Canyon wind story: the canyon that powers Utah's first commercial wind farm, Spanish Oaks/Canyon Hills/Palmyra bench subdivisions vs the 1851 downtown grid). Each carries full JSON-LD (BreadcrumbList, Article, RoofingContractor+GeoCoordinates, FAQPage ~14 Q&As), **NO AggregateRating** (would red-block the manifest-less CI gate), canonical identity from `business.json` (public 435-292-8802 only, write-review deep link, `/locations/<slug>` canonical), and compliance-safe framing (deliberately did NOT copy the borderline adjusting language — "work directly with your adjuster", "for maximum recovery" — still present in the Kaysville/Springville templates). Wiring: `sitemap.xml` both URLs (alphabetical, lastmod 2026-06-02); `index.html` Davis/Utah County service-area chips; reciprocal nearby-city links from the two closest neighbors each (Kaysville+Farmington↔Fruit Heights; Springville+Payson↔Spanish Fork). **All gates green incl. the now-blocking city-quality (47/47 PASS) — corpus → 47 location pages once merged.** The "Fruit Heights/Spanish Fork pages" carry-forward is **built, pending a PR/merge to `main`** (near-closed).

**New untracked artifacts:** `HANDOFF-2026-06-01.md` + `HANDOFF-2026-06-02.md` — both **left untracked intentionally** (cold-start handoff scratch docs, not site content; consistent with the prior-refresh decision to leave HANDOFF-*.md untracked).

**Carry-forwards RESOLVED this window:** thin location pages (#72); city-quality advisory→blocking (#73); about.html canonical (#74); Fruit Heights/Spanish Fork (built, pending merge). **Open carry-forwards (unchanged):** ⛔ GBP enforcement-watch **dashboard** fixes (owner login — the actual review-hold remedy from #71: rename live "Insurance Claim Assistance"→"Storm & Hail Damage Roofing" + "Free Roof Estimate"→"Roof Estimate", stop review/star mentions in posts, then request reinstatement); Lighthouse agentic-browsing 67 (`agent-accessibility-tree` not well-formed); repo secrets `SERPAPI_KEY` + `SUPABASE_ACCESS_TOKEN`; NAP/brand directory cleanup (DBA "Frame Roofing Utah" → GBP name edit → directory pass).

- **Write decisions for this refresh:** "Last updated" top-paragraph prepended (the 6/2 early-AM entry demoted to "Earlier metadata"); this SESSION LOG entry added at top; SEO STATUS → Completed gained a `#72/#73/#74` location-depth + gate-hardening + about-consolidation milestone block; SITE STRUCTURE updated (`/locations/` count note for the now-blocking depth gate + the 2 feature-branch pages; `/about.html` + `/pages/` reflect the #74 consolidation). **Commit landed on `main` via an isolated `git worktree`** (the primary working tree is mid-session on `feat/location-pages-fruit-heights-spanish-fork`; the two untracked HANDOFF docs persist there untouched).

### 2026-06-02 (Tuesday early-AM Auto-Refresh — POST-ACTIVITY) — One New Merged PR (#71) Reframes "Reviews Not Showing": Frame's GBP Is on Google ENFORCEMENT WATCH (Prior Posting Violation + 2026-05-11 Pushed Policy-Violating Content LIVE) — Likely Holding New Reviews; #71 Scrubs Unlicensed-Adjusting Language (Utah §31A-26-201) + Internal-Phone Leaks from the GBP Draft Docs and Flags the LIVE Dashboard Fixes Still Owed (Owner Login)

**What shipped (HEAD `e755246` → `4e11152`, single commit since the evening refresh; diffstat: 4 files, +21/−12, all internal `.vercelignored` GBP draft docs — zero deployed/page content):**

- **PR #71 (`4e11152`, 2026-06-01 20:56 MDT) — `fix(gbp): scrub adjusting language + internal phone from GBP drafts; flag live violations`.** Investigating why customer reviews weren't surfacing (the #68–#70 thread) found a second root cause beyond the dead-listing CTA: **Frame's GBP is on Google enforcement watch** from a prior posting violation, and the **2026-05-11 GBP session pushed policy-violating content LIVE** — the probable reason new reviews are being held. This PR cleans the *draft* docs (internal, `.vercelignored`, not deployed) and documents the live remedy:
  - **Internal-phone leak:** 9 instances of `435-302-4422` → public `435-292-8802` in the GBP draft copy.
  - **Unlicensed-adjusting language removed (Utah §31A-26-201):** "Don't fight your insurance alone", "we walk you through the claim from inspection to final invoice", "claim navigation", "supplement preparation", "start to invoice" → compliant **"we document the damage, provide a line-item scope your adjuster can read, meet your adjuster on site — the claim stays between you and your insurer; we don't adjust."**
  - **Renamed violating service drafts:** "Insurance Claim Assistance" / "Insurance Claim Roofing" → **"Storm & Hail Damage Roofing"**.
  - **New GBP-post rules:** never mention reviews/stars in a post (**DPNB** violation — this is what triggered the original enforcement), never the internal phone, no adjusting language.
  - **`GBP-AUDIT-2026-05-11.md` CORRECTION banner** listing the LIVE fixes still required.
  - Files: `data/GBP-AUDIT-2026-05-11.md`, `data/GBP-POST-BACKLOG.md`, `data/GBP-POST-PUBLISH-READY-2026-05-11.md`, `data/GBP-SERVICES-PROPOSAL-2026-05-27.md`.

**Why it matters / what's NOT fixed:** the committed change is draft hygiene + documentation. **The enforcement watch (and likely the review hold) is remedied only in the GBP dashboard (owner login):** (1) rename live service "Insurance Claim Assistance" → "Storm & Hail Damage Roofing" + reword to the compliant framing; (2) rename live service "Free Roof Estimate" → "Roof Estimate" (drop "Free" — promo language is an instant GBP rejection trigger); (3) stop review/star mentions in GBP posts; then **request reinstatement**. This is now the top GBP carry-forward and is logically prior to expecting review velocity to recover (it gates the same review-path the #68–#70 work just hardened from the website side).

- **Write decisions for this refresh:** "Last updated" top-paragraph prepended (evening entry demoted to "Earlier metadata"); this SESSION LOG entry added at top; SEO STATUS → Completed gained a `#71` GBP-compliance milestone block; **no SITE STRUCTURE change** (no new files/pages — #71 touched four already-tracked `data/` draft docs). Commit landed directly on `main` (primary working tree clean on `main`, HEAD `4e11152`). The untracked `HANDOFF-2026-06-01.md` left untracked intentionally (handoff scratch doc, not site content).

### 2026-06-01 (Monday EVENING Auto-Refresh — POST-ACTIVITY) — Review-CTA Path Hardened End-to-End: 3 Merged PRs (#68–#70) — Root-Cause Fix for a "Leave a Review" Button Pointing at a DEAD Google Listing (`cid 16833…`) So Customer Reviews Never Reached the Real Profile + Internal-Phone (302-4422) Leaks on 3 Deployed Pages; Defense-in-Depth: Merge-Time Blocking Gate (`audit-cta-integrity.mjs`) + Weekly Prod Liveness Monitor (`cta-liveness.yml`); One-Tap Writereview Deep Link on `review.html`; New `business.json` Single Source of External Identity (Canonical GBP `cid 8458659884566588108` / `place_id ChIJCT6-aZD1TYcRzCpwX1kyY3U` / "Frame Restoration Utah" / 27 Reviews)

**What shipped (HEAD `515d51e` → `564dc95`, diffstat `da8a024..564dc95` = 7 files, +469/−2, zero page-content changes — identity-integrity + CI tooling only):**

- **PR #68 (`b7262a5`) — CTA & external-link integrity gate, BLOCKING.** Root-cause fix for the review-CTA bug class. New `data/route-factory/business.json` is the single source of truth for external identity: canonical GBP `cid 8458659884566588108` + `data_id`, public phone 292-8802, the internal-only phone 302-4422 (never-publish), and the known-dead listing `cid 16833…`. New `scripts/audit-cta-integrity.mjs` blocks: internal-phone leak (any format), wrong/dead Google `cid`/`data_id`, placeholder links (`YOUR-…`, `${…}`, `cid=0`, `example.com`), and generic `maps`/`search` review CTAs. Scope = **deployed** (parses `.vercelignore`) AND **customer-facing** (sitemap + core routes), so operational/owner tools and their client-side `${…}` templates don't false-positive; strips HTML comments so documentation can't trip it. Self-tested: clean on `main`, catches all four of this session's failure modes. Wired as a blocking job in `compliance-gate.yml` (+ path triggers for the script, `business.json`, `.vercelignore`, `sitemap.xml`). New `.vercelignore` stops deploying `backlink-playbook.html` + two internal `data/*.html` reports that carried 302-4422 (surfaced by the new gate).
- **PR #69 (`c0aeba2`) — CTA liveness monitor (Layer 3).** `.github/workflows/cta-liveness.yml` + `scripts/check-cta-liveness.py`, weekly schedule. The merge-time gate can't tell that a syntactically-valid listing is actually dead or that the live page broke post-deploy — this watches the real world: (A) GETs the deployed review page, asserts 200 + canonical `cid` present + NO known-dead `cid` (catches 404s, deploy/CDN regressions, CTA drifting back to a wrong listing), no API key needed; (B) SerpAPI GBP liveness by canonical `data_id`, asserts a real business with >0 reviews (catches the listing going dead/suspended — exactly what `cid 16833…` was), skipped gracefully if `SERPAPI_KEY` unset. A red run = a real customer review path is broken → GitHub notifies. Verified green against live prod (review page 200, carries canonical `cid 8458659884566588108`). Reads identity from `business.json`.
- **PR #70 (`564dc95`) — one-tap write-review link on `review.html`.** Upgrades the review CTA from a `maps?cid=` listing link (which just opens the profile) to a direct **writereview deep link** (`https://search.google.com/local/writereview?placeid=ChIJCT6-aZD1TYcRzCpwX1kyY3U`) that opens the star-rating dialog — **no owner login needed**, same outcome as Landon's official g.page "Ask for reviews" link without needing owner access to the listing. `place_id` resolved via SerpAPI 2026-06-01 (canonical GBP "Frame Restoration Utah", 27 reviews). `business.json` gains `gbp.place_id` + filled `review_form_link`; `audit-cta-integrity.mjs` adds a `WRONG-LISTING(placeid)` check (a writereview link whose `placeid` ≠ canonical now blocks); `check-cta-liveness.py` accepts the canonical `cid` OR `place_id` on the live page.

**Why it matters:** review velocity + response rate is the top local-pack ranking factor, and a review CTA silently pointing at a dead listing is a zero-feedback failure — every "leave a review" click was lost. The bug class is now fixed in prod, made un-mergeable (merge-time gate), and watched in production (weekly monitor). New identity facts captured for downstream tooling: **canonical GBP `cid 8458659884566588108`, `place_id ChIJCT6-aZD1TYcRzCpwX1kyY3U`, canonical GBP name "Frame Restoration Utah", 27 reviews, known-dead listing `cid 16833…`.**

- **Write decisions for this refresh:** "Last updated" top-paragraph prepended (late-afternoon entry demoted to "Earlier metadata"); this SESSION LOG entry added at top; SEO STATUS → Completed gained a CTA-integrity milestone block; SITE STRUCTURE Key Files gained a `business.json` + CTA-gate/monitor entry. Commit landed directly on `main` (primary working tree clean on `main`, HEAD `564dc95`). The untracked `HANDOFF-2026-06-01.md` left untracked intentionally (handoff scratch doc, not site content), consistent with the late-afternoon refresh.

### 2026-06-01 (Monday MIDDAY Auto-Refresh — POST-ACTIVITY) — The Stalled Backlog Ships: 3 Merged PRs to `main` in One ~2.5h Session (09:27–12:05 MDT) — TX→UT Systems Port + Full-Site Audit Fixes + Thin-Content FAQ; Untracked Stack 28 → 0, All Pushed; Weekly-Report Writer Confirmed WORKING (6/1 Snapshot Landed — Early-AM "Broken" Read Refuted); `phone_clicks` Beacon Fix Confirmed in Production (14 Clicks + 10 Inbound Calls Flowing)

**State of play (machine-readable summary):**
- HEAD: `4c47dac` (Merge PR #61) = `main` = `origin/main`; working tree **clean**, **fully pushed**. The early-AM refresh found the tree on `feat/tx-systems-port`; by midday the work was committed/merged and `main` advanced through the burst. Untracked porcelain entries: **28 → 0**.
- **What landed (newest→oldest within the burst):** `4c47dac` Merge PR #61 ← `e223660` feat(content): FAQ + FAQPage schema on 3 thin service pages · `b1984f9` Merge PR #60 ← `64d8f89` titles/metas-to-length + og:image + sitemap orphan ← `63a2200` repair all live broken internal links + make link gate blocking ← `ba105a9` full-site audit report + `audit-full-site.mjs` · `b7ccc9f` Merge PR #59 ← `b0d0e9b` brand decision + directory checklist ← `8cd0076` llms.txt single public number ← `7dc14a0` GBP `hasMap` + per-location Google Review schema (Sandy, Millcreek) ← `57bd993` Utah-law claim-language remediation + insurance-claims cornerstone ← `1565ed2` manual edge-fn deploy workflow ← `1725055` cloud-scheduled Google reviews sync ← `816c280` rating-integrity (remove cloned AggregateRating from 45 location pages) ← `874432b` port TX compliance-gate tooling, re-authored for Utah law.
- **PR #59 (TX→UT port + compliance):** committed the full `feat/tx-systems-port` scaffolding (`PORT-PLAN.md`, `data/route-factory/` ×4, 6 `scripts/audit-*.mjs`, `compliance-gate.yml`) **plus the entire long-untracked `data/` doc backlog** (5/17–5/27 AEO/handoff/session docs, `CITY-RANKING-ACTION-PLAN`, `GBP-SERVICES-PROPOSAL`, both NAP checklists, all 3 `data/weekly-reports/*.json`), cost-page AIO tooling (`aeo-aio-sweep.py` + `serpapi-smoke-test.py`), and the 5/27 outbound-messaging pipe (`cyborg-send.sh` + `send-message/index.ts`). Net new tracked: `+158 files, ~6,904 insertions`.
- **Rating-integrity fix (`816c280`):** the cloned `AggregateRating` previously stamped on 45 location pages is removed via `scripts/fix-location-aggregate-rating.mjs` — closes a latent review-schema-integrity risk; audit now reports rating-integrity clean.
- **2 new CI workflows:** `google-reviews-sync.yml` (cloud-scheduled review sync wrapping UT's existing scraper — removes the local-cron/LaunchAgent dependency) + `deploy-edge-function.yml` (manual edge-fn deploy for UT Supabase project `hdcflshhomzildwqlmwh`).
- **Brand decision (`data/BRAND-DECISION-2026-06-01.md`):** ✅ DECIDED — canonical public name = **"Frame Roofing Utah"** (Option B). Website/schema/`llms.txt` already correct (3,345 refs / 161 files) so they stay; align GBP + BBB + ~15 directories *to* the site. **DBA-first execution:** confirm registered LLC name (sources disagree — BBB "Frame Restoration Utah LLC" vs state "FRAME RESTORATION LLC") → file "Frame Roofing Utah" assumed name with UT Division of Corporations → edit GBP name (27 reviews stay attached, expect re-verify) → directory pass per `NAP-DIRECTORY-CHECKLIST-2026-06-01.md`. Domain + `LANDON_PHONE` internal constant unchanged. Honest caveat in-doc: name consistency removes one suppressor; citations + reviews + proximity are the rest.
- **PR #60 (site audit):** `data/SITE-AUDIT-2026-06-01.md` over all 116 sitemap pages. Headline: **technically strong, gaps are content-metadata hygiene not architecture** (JSON-LD 0 invalid/364 blocks; Lighthouse SEO 100 / A11y 100 / Best-Practices 92 / Agentic-Browsing 67; LCP 291ms / CLS 0.02 / TTFB 35ms; 99/116 FAQPage; compliance gate 0 blockers). Fixed this PR: 17 broken internal links repaired (incl. literal `${item.url}` template-leak bug, `/pages/solar`→`/pages/solar-installation`, ×9 missing-`/heber-city/` snow-damage link) + `audit-links` flipped to blocking; over-length titles/metas trimmed; 5 missing `og:image` added; sitemap orphan resolved.
- **PR #61 (thin content):** FAQ + `FAQPage` schema added to the 3 service pages the audit flagged <500w — `general-contracting` (was 325w), `solar-installation` (449w), `water-fire-flood-restoration` (492w), each +62 lines.
- **Weekly-report writer WORKING (refutes early-AM "broken 7d" read):** `data/weekly-reports/2026-06-01.json` landed (generated 2026-06-01 14:06 UTC). 30d: 725 pageviews / 82 google-organic / 527 direct (73%) / 11 leads / 4 calls / 9 form-submits / 14 phone-clicks / 10 inbound-calls / 9 inbound-SMS / $142K (same 2 won: Greg Atkinson $118K + Jan Hermansen $24K) / 2.1% conversion / coverage 10/59 = 17% (Cottonwood Heights + Holladay tied at 8 views). The earlier "writer broken" hypothesis was a disk-snapshot-cadence artifact — the snapshots were committed in the same burst, not written daily.
- **`phone_clicks` beacon fix CONFIRMED:** lead_actions shows `phone_click: 14` + `inbound_call: 10` this period — the 15-day DB hole fixed by `80a59d7` (text/plain + keepalive fetch) is closed in production. This carry-forward is **resolved**.
- **Carry-forwards RESOLVED this session:** untracked/unpushed 28-entry stack (committed+merged); tx-port commit/PR posture; cost-page AIO tooling now tracked; weekly-report writer (working); `send-message`/`cyborg-send` now tracked + characterized; phone_clicks production verification; broken-internal-links (audit-fixed).
- **OPEN carry-forwards (scoped, not mysteries):** (1) **brand execution** — DBA filing → GBP name edit → directory NAP pass (fold the still-unconfirmed 5/27 GBP *phone* edit into the same directory sweep); (2) **audit medium-tier hygiene backlog** — 49 over-length titles, 46 meta descriptions, 32 location pages below the 500-unique-word floor, 3 sitemap orphans (`blog/salt-lake-valley/...premium-roof-replacement-2026`, `pages/about.html` dup of `/about`, `pages/storm-damage-restoration.html` dup), 3 dup meta groups, Lighthouse agentic-browsing 67 / `agent-accessibility-tree not well-formed`; (3) **cost-page AIO** — scripts committed but the post-merge 26-query SerpAPI panel run **still never executed** (`cdee7de` +12d, recrawl band closed — hypothesis likely expires unmeasured); (4) DOPL `hasCredential` sitewide rollout (still ~1-of-45 LIVE).
- **Write decisions for this refresh:** "Last updated" top-paragraph prepended (early-AM entry demoted to "Earlier metadata"); this SESSION LOG entry added at top; SEO STATUS → Completed gained the 6/1 milestone block; SITE STRUCTURE Key Files tx-port note updated from "untracked/unpushed" → "committed + merged via PR #59". **Commit landed on `main` via an isolated `git worktree`** because the primary working tree was mid-session on an active feature branch (`fix/location-content` at commit time) — the worktree advances `main` without checking it out in the live tree or disturbing the active dev branch.

### 2026-06-01 (Monday Auto-Refresh) — Quiet ~24h Window Since the 5/31 Port-Scaffolding Capture (`9b85b5a`) — Zero New `main` Commits, Worktree Byte-Identical, TX→UT Port Scaffolding Still Untracked + Unpushed on `feat/tx-systems-port` (28 Entries); Three Stalls Converge Past Their Decision Points: Weekly-Report Writer Broken (7 Days Silent — Weekly Hypothesis Refuted), GBP Phone-Edit Unconfirmed (~4.5 Days), Cost-Page AIO Sweep Never Ran (+12 Days Post-Merge, Hypothesis Expiring Unmeasured)

**State of play (machine-readable summary):**
- HEAD: `9b85b5a` (auto-refresh metadata, 2026-05-31 00:09 MDT); parent `d2dbf81`. Production-commit quiet streak on `main`: ~4.5 days since `2fe52d2` (5/27 ~10:12 MDT). Working branch as found = `feat/tx-systems-port` (at `d2dbf81`, one commit behind `main` after the 5/31 refresh committed to `main`); this refresh again commits the refresh to `main` and restores `feat/tx-systems-port` afterward.
- Worktree diff vs 5/31 refresh: **byte-identical** — `find . -newer CLAUDE.md` (vs the 5/31 commit) returned empty; the 5/30-evening port scaffolding (12 files) remains the latest activity, untracked, **28 porcelain entries**. Branch `feat/tx-systems-port` still pointer-equal to `main` (`main..feat` empty) and **not pushed** to origin.
- **Weekly-report cadence — REFUTED:** `data/weekly-reports/` still holds only `2026-05-24.json` + `2026-05-25.json`. 6/1 Mon was the 5/31 entry's flagged make-or-break window; it arrived with no snapshot. Writer now silent **7 days** (5/26–6/1) — neither a 5/31 Sun nor 6/1 Mon rollup landed, killing the weekly-Sunday hypothesis. **Leading read: the `/functions/v1/weekly-report` snapshot writer is broken** and needs a direct edge-fn trace. Knock-on: `80a59d7` phone_clicks beacon-fix has had **zero production verification for a full week** because it was gated on this snapshot — pull it **directly from the `phone_clicks` Supabase table** instead.
- **GBP 72h phone-edit cusp:** submitted 5/27 ~10:12 MDT; now ~4.5 days elapsed, no acceptance signal observable from the repo. Escalation (GBP chat support / re-submit) is **overdue**.
- **Cost-page AIO experiment:** `cdee7de` (5/20) now **+12 days post-merge**; the high-information recrawl-likely band (~6/3 close) is all but exhausted with **still zero post-merge 26-query SerpAPI sweep data points captured** — the gating `scripts/aeo-aio-sweep.py` panel run never executed across the entire high-info window, so the 30-day falsifiable hypothesis is **expiring unmeasured**. If the sweep is ever to yield signal it must run in the next ~2 days.
- **Debt clocks:** `.gitignore` `.env*.local` edit **16-day debt**; refresh-stale fixer **26-day debt**; cost-page AIO **+12 days** (above).
- **Carry-forwards (all 5/31 intact + clock-ticked):** GBP edit escalation (overdue); `data/NAP-DIRECTORY-CHECKLIST-2026-05-27.md` execution (BBB/Yelp/Apple Maps still on 302-4422); DOPL `hasCredential` sitewide rollout (Midway still 1-of-45); `data/CITY-RANKING-ACTION-PLAN-2026-05-27.html` slotting; read `scripts/cyborg-send.sh` + `supabase/functions/send-message/index.ts` end-to-end. **tx-port-specific:** (1) decide commit/PR posture on the local-only unpushed `feat/tx-systems-port` scaffolding — P1 (advisory-first quality-gate tooling) is the next concrete build step; (2) review `data/route-factory/compliance-words.json` Utah re-authoring before it gates CI; (3) the 6 `scripts/audit-*.mjs` headers still read "Frame Restoration TX" (ported verbatim) — Utah-adaptation is the P1 task.
- **6/1 read:** the three stalls (weekly-report writer broken 7d / GBP edit unconfirmed ~4.5d / AIO sweep never run) have all crossed their decision points and now want **active intervention**, not another observe-and-wait refresh cycle. The single highest-leverage move is tracing the silent weekly-report edge fn, since it simultaneously unblocks phone_clicks verification and restores the dashboard's data spine.
- **Write decisions for this refresh:** CLAUDE.md top-paragraph "Last updated" prepended; this SESSION LOG entry added at top; SITE STRUCTURE Key Files gained a tx-port tooling note (omitted by the 5/31 pass). No git operations beyond the auto-refresh commit on `main` + branch restore; the 12 tx-port files left untracked (per-phase-PR posture is a Ryan call per `PORT-PLAN.md`).

### 2026-05-31 (Sunday Early-AM Auto-Refresh) — TX→UT Systems-Port Scaffolding Lands (5/30 Evening Session) + Working Tree Now on `feat/tx-systems-port` (First Non-`main` HEAD in the Series) + Both 5/30 Cusps Resolve Negatively (GBP Edit Past 72h Unconfirmed / Weekly-Report Writer Silent 6 Days)

**State of play (machine-readable summary):**
- HEAD at refresh start: `feat/tx-systems-port` (created 2026-05-30) → `d2dbf81`. `git log main..feat/tx-systems-port` is **empty** — zero unique commits; the branch is `main` + untracked working files only. `main` also at `d2dbf81`, in sync with `origin/main`.
- **First real development activity since the 5/27 production burst.** A 5/30-evening session (artifacts 17:59–18:10 MDT, ~10h after the `d2dbf81` early-AM refresh) stood up the **Texas→Utah systems-port scaffolding**. All untracked (no commits yet — Phase work ships as PRs through the new gate).
- Untracked stack: **19 → 28 entries** (+9 today): `PORT-PLAN.md`, `.github/` (`workflows/compliance-gate.yml`), `data/route-factory/` (`compliance-words.json` + `UTAH-COMPLIANCE-LAW.md` + `README.md` + `batch-manifest.example.json`), and 6× `scripts/audit-*.mjs` (`route-batch`, `city-quality`, `compliance-words`, `jsonld`, `links`, `media-provenance`).

**What the port is (from `PORT-PLAN.md`):**
- Premise: TX v2 (`~/projects/frame-restoration-texas-v2`, launched 2026-05-30) was cloned from UT, then gained a **systems layer UT never received**. This ports those systems back into UT and **adapts the data/law to Utah — it is NOT a content copy.** Utah is explicitly *ahead* on call/SMS edge fns + AEO measurement and those must not be overwritten.
- **7 phases, each ships as its own PR through the new compliance gate:** P0 baseline+branch (✅ done), P1 quality-gate tooling (port 8 `scripts/*.mjs` + `compliance-gate.yml`, re-author `route-factory/compliance-words.json` for Utah, **advisory-first / no day-1 red-block**), P2 schema/rating integrity (strip business-wide `AggregateRating` cloned across 45 UT location pages; keep only on canonical home/org entity), P3 reviews automation (port `update-google-reviews.mjs` + `google-reviews-sync.yml`, repoint to UT GBP place-ID), P4 lead-pipeline hardening (port `verify-pin` + `_shared` **without touching call/SMS/send-message**), P5 insurance-claims cornerstone (build UT `services/insurance-claims/` rewritten for Utah law), P6 verify & ship.
- **Hard adaptation constraints (load-bearing — do not violate when this work proceeds):** Utah has **no TX Insurance Code §4102.163** (the "roofers ≠ public adjusters / negotiate forbidden" gate); UT fence = DOPL contractor licensing + Utah roofing/insurance advertising rules → `compliance-words.json` re-authored for UT (see `data/route-factory/UTAH-COMPLIANCE-LAW.md`). City copy does NOT port (45 UT cities already have pages); only template/schema improvements port. Public NAP stays **435-292-8802**; UT Vercel project `frame-restoration-utah` (`prj_zcwGmh7wUxHvqtRoHRmoRp441ePY`), UT Supabase ref + GBP place-ID + `reviews.json` are Utah's own.
- **Explicit do-NOT-touch list:** UT's `handle-call` / `handle-sms` / `send-message` edge fns, AEO scripts (`aeo-aio-sweep`, `serpapi`, `reddit-scanner`, `indexnow`), `dashboard/`, `build-intelligence/`, and **the daily CLAUDE.md auto-refresh cron** (this task). The 6 ported audit scripts still self-identify as "Frame Restoration TX" in their header comments — they are verbatim ports, Utah-localization is P1 work, not yet done.
- **Strategic significance:** this is the **Multi-City Page Generator / "route factory"** thread (Tier-2, flagged "secretly most urgent" for the Frame Houston expansion) finally producing tooling. Build-once-scale-every-city tooling now exists as a branch; gates-first sequencing (P1 before content) means every later phase is verified automatically.

**5/30 cusps — both resolved negatively this refresh:**
1. **GBP primary-phone edit** — 72h cusp (~10:00 MDT 5/30) has **passed** with no Google acceptance signal observable from the repo. Now past the statistically-typical 24–72h GBP review window → **escalate** (GBP chat support, then re-submit). Until accepted, GBP's primary phone may still route Google-initiated calls to Landon's 302-4422 cell directly, bypassing Twilio → `handle-call` → `call_logs` → auto-lead creation (the exact attribution gap the 5/27 NAP flip was meant to close).
2. **Weekly-report cadence** — **no new snapshot landed 5/30 or 5/31-so-far**; `data/weekly-reports/` still holds only `2026-05-24.json` + `2026-05-25.json`. Writer is now silent **6 days (5/26–5/31)**, refuting both daily and twice-weekly. Weekly-Sunday hypothesis is at make-or-break **today**; if 5/31 + 6/1 both stay blank, this becomes a writer-trace task (broken or moved to bi-weekly). The next snapshot is still the first unverified chance to confirm the `80a59d7` `text/plain` phone_clicks beacon fix replenished rows in production.

**Debt clock summary (all +1 day from 5/30):**

| Item | Debt | Threshold | Status |
|---|---|---|---|
| `.gitignore` `.env*.local` edit uncommitted | 15d | 1 week | 1+ week past threshold, no resolution path |
| Cost-page AIO post-merge 30-day clock | +11d | 30d hypothesis | ~3d high-info band left, **0 SerpAPI sweep data points** — edge of "missed window" |
| Refresh-stale fixer | 25d | — | Long-running, low-urgency |
| GBP phone-edit acceptance (now escalation) | past 72h | 24–72h | Statistically late — escalate via GBP chat / re-submit |
| Weekly-report writer trace | 6d quiet | weekly? | 6/1 Mon is the last window before a writer-broken read |
| `scripts/cyborg-send.sh` + `send-message` end-to-end read | 4d | — | Uncharacterized outbound messaging surface |
| `data/CITY-RANKING-ACTION-PLAN-2026-05-27.html` read + queue | 4d | — | Defines next 2–4 weeks of SEO work |

**Carry-forwards (5/30 set, plus new):** (1) GBP edit verify → now escalate; (2) NAP directory checklist execution (BBB/Yelp/Apple Maps still on 302-4422); (3) DOPL `hasCredential` sitewide rollout decision (Midway 1-of-45 LIVE); (4) phone_clicks recovery confirm in next weekly snapshot; (5) GBP services proposal action; (6) CITY-RANKING-ACTION-PLAN end-to-end + queue; (7) characterize cyborg-send/send-message pipe. **NEW:** (8) the TX→UT port is now live work — P1 quality-gate tooling needs Utah-localization (the 6 audit scripts + `compliance-words.json` still carry TX identity/law); track P0–P6 progress and confirm none of the do-NOT-touch UT systems get clobbered as PRs land.

**Write decisions for this refresh:** CLAUDE.md top-paragraph "Last updated" prepended; this SESSION LOG entry added at top. **Branch handling:** HEAD was on `feat/tx-systems-port` at refresh start; switched `main` for the refresh commit (refresh history lives on `main` by convention) — **safe because `feat/tx-systems-port` has zero unique commits and the tracked tree was clean (only untracked files, which persist across checkout)** — then restored working branch to `feat/tx-systems-port`. The 9 new port artifacts were **NOT committed** (they ship as gated PRs per PORT-PLAN; staging them here would bypass the gate and isn't in this task's mandate). No edits to PRIORITY TODO / SEO STATUS / SITE STRUCTURE beyond this log (no pages shipped, no TODOs completed).

---

### 2026-05-30 (Saturday Early-AM Auto-Refresh) — Quiet ~18h Window Since `c665916` (Zero `main` Commits, HEAD Still `c665916` / Parent Still `2fe52d2`, Working Tree Byte-Identical to 5/29 Close — Latest Worktree Mtime Still `scripts/cyborg-send.sh` at 5/27 23:52 MDT, Confirming Yesterday's Corrected Discovery-Sweep Methodology Caught Everything) + Debt Clocks +1 (`.gitignore` `.env*.local` 14 Days — Full Week Past 1-Week Threshold / Cost-Page AIO +10 Days Post-Merge — One-Third Through 30-Day Hypothesis Window With Zero Sweep Data Points Captured, Approaching "Missed Window" / Refresh-Stale Fixer 24 Days) + Two Externally-Gated Cusps Land Inside ~24h: (1) GBP Phone-Edit 72h Cusp ~10:00 MDT Today (Escalation Point If No Google Acceptance by Mid-Morning Saturday); (2) First Weekly-Report Cadence Test Window Opens (Next Snapshot Expected 5/31 Sun or 6/1 Mon Per Weekly Hypothesis — a 5/30 Sat Snapshot Would Refute Weekly Read in Favor of Twice-Weekly; Either Way First Hard Cadence Data Point Since 5/24+5/25 Pair, Also First Opportunity to Verify `80a59d7` Phone-Click Beacon Fix Actually Closed the 15-Day DB Hole in Production)

**State of play (machine-readable summary):**
- HEAD: `c665916` (auto-refresh metadata, 2026-05-29 07:45:33 MDT); parent: `2fe52d2` (last production commit, 2026-05-27 ~10:12 MDT). Branch up-to-date with `origin/main`.
- Quiet streak on `main` (production commits): ~63 hours since `2fe52d2` — longest dev-quiet stretch since the 5/24-5/27 four-day window that preceded the 5/27 burst.
- Worktree diff vs 5/29 refresh: **byte-identical** — `find -newer .git/refs/heads/main` returned empty, all 19 untracked entries unchanged in count and content, no tracked-file modifications.
- Untracked stack (19 entries, mtime-sorted): `.env.local` (5/17) → 4× `data/AEO-*-2026-05-20.md` + 2× `data/CODEX-HANDOFF-*-2026-05-20.md` + `data/COWORK-HANDOFF-2026-05-20.md` + `scripts/aeo-aio-sweep.py` + `scripts/serpapi-smoke-test.py` (all 5/20) → `data/REVENUE-TILE-DESIGN-2026-05-17.md` + `data/SESSION-2026-05-17-*.md` (5/17) → `data/weekly-reports/{2026-05-24.json, 2026-05-25.json}` → `data/aeo-master-digest-2026-05-18.html` (mtime 5/27 10:17, contents 5/18) → `data/GBP-SERVICES-PROPOSAL-2026-05-27.md` + `data/NAP-DIRECTORY-CHECKLIST-2026-05-27.md` (5/27) → `data/CITY-RANKING-ACTION-PLAN-2026-05-27.html` (5/27 23:23) → `supabase/functions/send-message/index.ts` (5/27 23:51) → `scripts/cyborg-send.sh` (5/27 23:52, last write in tree).

**Today's two cusps (both externally-gated, no dev required):**
1. **GBP primary phone edit 72h cusp ~10:00 MDT 5/30** — the public NAP swap (`2fe52d2`, 1,341 string replacements across 164 files) closes the GBP-Call-button attribution gap only if Google accepts the edit. Submitted 5/27 ~10:12 MDT; 24h passed 5/28 night (no acceptance email); 48h passed 5/29 night (no acceptance email); 72h hits ~8h from this refresh. Google's typical GBP-edit review is 24-72h, so anything past mid-morning Saturday is statistically late — escalation path: GBP chat support, then re-submit. The longer this hangs, the longer the website + directories + GBP carry the new 292-8802 NAP while GBP's primary phone still routes calls to Landon's 302-4422 cell directly (bypassing Twilio → `handle-call` → `call_logs` → auto-lead creation — i.e., the exact attribution gap this whole flip was supposed to close).
2. **Weekly-report cadence test window** — `data/weekly-reports/` writer landed 2 snapshots so far: `2026-05-24.json` (Sun, ~12:45 MDT) + `2026-05-25.json` (Mon, ~10:30 MDT). 4 quiet days since (5/26-5/29) → strongly suggests not-daily. If today (5/30 Sat) lands a snapshot, writer cadence is twice-weekly or irregular. If tomorrow (5/31 Sun) lands one, weekly-Sunday hypothesis holds. If 6/1 Mon also lands one, twice-weekly read. If 5/31 + 6/1 both blank, writer is broken or moved to bi-weekly — would surface a writer-trace task. Critically, the next snapshot is also the first post-`80a59d7` opportunity to verify the `text/plain` beacon fix actually replenished `phone_clicks` rows in production (4 phone-click events were captured during the broken window per PostHog; if the next snapshot shows phone-click count rising on a 7-day trailing basis, the fix is confirmed end-to-end).

**Debt clock summary (all +1 day from 5/29):**

| Item | Debt | Threshold | Status |
|---|---|---|---|
| `.gitignore` `.env*.local` edit uncommitted | 14d | 1 week | 1 full week past threshold, no resolution path |
| Cost-page AIO post-merge 30-day clock | +10d | 30d hypothesis | One-third through, 0 SerpAPI sweep data points, ~4d high-info band left |
| Refresh-stale fixer | 24d | — | Long-running, low-urgency |
| `data/NAP-DIRECTORY-CHECKLIST-2026-05-27.md` execution | 3d | — | BBB/Yelp/Apple Maps still on 302-4422 = NAP inconsistency penalty |
| `scripts/cyborg-send.sh` + `send-message` edge fn end-to-end read | 3d | — | Uncharacterized outbound messaging surface area |
| `data/CITY-RANKING-ACTION-PLAN-2026-05-27.html` end-to-end read + queue | 3d | — | Defines next 2-4 weeks of SEO work |

**Carry-forwards (all 5/29 intact, none closed today):** (1) GBP edit verify (cusp lands today); (2) NAP directory checklist execution; (3) DOPL `hasCredential` sitewide rollout decision (Midway 1-of-45 LIVE); (4) phone_clicks recovery confirm in next weekly snapshot; (5) GBP services proposal action; (6) CITY-RANKING-ACTION-PLAN end-to-end + queue slotting; (7) characterize cyborg-send/send-message pipe.

**Pattern read:** three consecutive auto-refreshes (5/28 + 5/29 + 5/30) on a quiet `main`, working tree progressively immobile (5/28 found a new file, 5/29 found one more, 5/30 found none). Three convergent signals point to today being the highest-information ~24h since 5/27 — both cusps (GBP + weekly snapshot) resolve externally, neither needs new dev work to land. If both land cleanly today/tomorrow, two distinct debts close: GBP-edit verification + phone_clicks recovery proof. If either misses, escalation paths kick in (GBP chat support / writer-trace).

---

### 2026-05-29 (Friday Auto-Refresh) — Quiet ~22h Window Since `d9f7960` (Zero `main` Commits, HEAD Still `2fe52d2`, Working Tree Byte-Identical to 5/28 Close) + One Newly-Discovered Untracked Artifact Missed by Yesterday's Sweep: `scripts/cyborg-send.sh` (Executable, Written 5/27 23:52 MDT — Pairs With Already-Captured `supabase/functions/send-message/index.ts` From 5/27 23:49–23:51 MDT to Form a New Outbound Messaging Pipe That Stood Up in a Single Late-Evening 5/27 Session) + Debt Clocks +1 (`.gitignore` `.env*.local` 13 Days / Cost-Page AIO +9 Days Post-Merge / Refresh-Stale Fixer 23 Days) + GBP Phone-Edit 72h Cusp Hits Tomorrow ~10:00 MDT + Weekly-Report Cadence Test Window Opens (~5/31 / 6/1 Snapshot Now 2–3 Days Out)

**22-hour quiet window.** Zero new `main` commits since `d9f7960` (2026-05-28 08:11 MDT auto-refresh — itself metadata-only). HEAD = `2fe52d2`, up to date with `origin/main`. `git diff` empty re: tracked files (working tree byte-identical to 5/28 close). The window's only signal is a discovery-sweep correction on artifacts from the 5/27 late-evening session that yesterday's pass missed.

- **Newly-discovered untracked artifact: `scripts/cyborg-send.sh`** — 2,758 bytes, executable (`-rwxr-xr-x`), written **2026-05-27 23:52 MDT**. Not in yesterday's `d9f7960` untracked enumeration. Discovered via `git status --short | wc -l` returning 19 (vs `d9f7960`'s asserted 18) + mtime cross-check.
  - **Paired companion (already captured 5/28):** `supabase/functions/send-message/index.ts` — 6,069 bytes, written **2026-05-27 23:49–23:51 MDT** (3-minute write delta to the shell script).
  - **Inferred function:** new outbound messaging pipe — a Supabase edge function (`/functions/v1/send-message`) plus a CLI shell wrapper to drive it from the workstation. The `cyborg-` prefix matches the project's Codex/automation-handoff lineage (cf. `CODEX-HANDOFF-*` docs). Probable use cases: outbound SMS via Twilio for the AEO digest / Reddit-classifier / lead-routing pipelines, OR a generic Cowork-Codex notification channel.
  - **Not yet read end-to-end** — untouched in this refresh per "produce a report of what you found" guidance for write actions. Carry-forward to next working session.
  - **Why both files matter even though only one is "new" today:** they were written as a unit ~12 hours AFTER the 5/27 morning NAP+tracking burst and ~10 hours AFTER `1be4803` (the 5/27 evening refresh). `d9f7960` (5/28 morning) caught the edge fn directory but not the shell script — likely because the 5/28 discovery sweep walked `data/` thoroughly but only listed top-level scripts already on its radar. Today's pass corrects.
  - **Net untracked count:** 18 files + `data/weekly-reports/` directory = **19 entries** (+1 vs `d9f7960`'s 18).

- **Working tree at refresh time:** Branch `main` clean re: tracked refs. HEAD = `2fe52d2`. Untracked enumeration (full, 19 entries):
  - `.env.local` (worktree-local secret; un-staged `.gitignore` edit needs to land before this is safe).
  - 7 `data/` design+handoff docs from 5/17–5/20 (`AEO-AIO-SWEEP-2026-05-20.md`, `AEO-COMPETITOR-MODEL-HOMERROOFING-2026-05-20.md`, `AEO-EDIT-PROPOSAL-2026-05-20.md`, `AEO-FRAME-COST-PAGE-AUDIT-2026-05-20.md`, `CODEX-HANDOFF-DATAFORSEO-2026-05-20.md`, `CODEX-HANDOFF-SERPAPI-2026-05-20.md`, `COWORK-HANDOFF-2026-05-20.md`) — unchanged.
  - 2 `data/` 5/27 morning docs (`GBP-SERVICES-PROPOSAL-2026-05-27.md`, `NAP-DIRECTORY-CHECKLIST-2026-05-27.md`) — unchanged.
  - 1 `data/` 5/27 evening doc (`CITY-RANKING-ACTION-PLAN-2026-05-27.html`) — captured 5/28, unchanged.
  - 2 5/17 docs (`REVENUE-TILE-DESIGN-2026-05-17.md`, `SESSION-2026-05-17-hf-supply-chain-and-classifier-bench-handoff.md`) — unchanged.
  - `data/aeo-master-digest-2026-05-18.html` — unchanged.
  - `data/weekly-reports/` directory — still just `2026-05-24.json` + `2026-05-25.json` inside, no new snapshot.
  - 2 scripts captured pre-5/29 (`scripts/aeo-aio-sweep.py` 5/20, `scripts/serpapi-smoke-test.py` 5/20) — unchanged.
  - **NEW THIS REFRESH:** `scripts/cyborg-send.sh` (5/27 23:52).
  - `supabase/functions/send-message/` directory (captured 5/28).
  - Plus the un-staged `.gitignore` `.env*.local` edit (**13-day debt, +1**).

- **Debt clocks (+1 each):**
  - `.gitignore` `.env*.local` edit: **13-day debt** (6 days past 1-week threshold). No resolution path proposed since 5/20.
  - Cost-page AIO experiment clock: **+9 days post-merge** (`cdee7de` 2026-05-20). Recrawl-likely window: ~5 days of high-information band remaining through ~6/3. Still **zero** post-merge 26-query SerpAPI sweep data points captured — the gating issue `d9f7960` flagged as "now urgent" rolls another day past urgency.
  - Refresh-stale fixer: **23-day debt**.

- **Weekly-report cadence test window opens.** Last snapshot = `2026-05-25.json` (Monday, 09:30 MDT write). Today = Friday 5/29 — **4 days past last snapshot**. The 5/24 Sun + 5/25 Mon pair → weekly-cadence hypothesis predicts next snapshot **2026-05-31 Sun or 2026-06-01 Mon** (2–3 days out). If a snapshot lands tomorrow 5/30 Sat, that would falsify weekly cadence in favor of ~5-day or twice-weekly. First real test of the hypothesis runs through this weekend.

- **GBP primary phone edit accept verification — 72h cusp tomorrow.** Edit submitted 2026-05-27 ~10:12 MDT (per `2fe52d2`). 24h mark was last night ~22:00 MDT (passed). **72h cusp hits ~2026-05-30 10:12 MDT** (~28h from now). If no Google confirmation by Saturday morning, escalate via GBP merchant support — this is on the critical path for the NAP unification to take effect on the GBP Call button surface (which is where the 1 → 0 inbound-calls regression in the 5/25 weekly snapshot was traced from).

- **Carry-forwards (all 5/28 entries intact, ticked):**
  - GBP primary phone edit accept verify — 72h cusp tomorrow ~10:12 MDT 5/30 (escalation point).
  - Work `data/NAP-DIRECTORY-CHECKLIST-2026-05-27.md` (BBB / Yelp / Apple Maps NAP updates) — NAP inconsistency = local-pack ranking penalty; every day 302-4422 lives in directories is a day of conflicting signal vs the now-unified 292-8802 on-site + GBP.
  - DOPL `hasCredential` sitewide rollout decision — Midway still 1-of-45 LIVE per `80a59d7`. License 14256097-5501. Either commit to sitewide rollout or revert Midway for consistency.
  - Confirm phone_clicks recovery in next weekly snapshot (~5/31 / 6/1) — the 80a59d7 fix has been in production 2.5 days; first weekly-report snapshot covering the fix window is the validation point.
  - Read + act on `data/GBP-SERVICES-PROPOSAL-2026-05-27.md` — still untouched.
  - Read `data/CITY-RANKING-ACTION-PLAN-2026-05-27.html` end-to-end and slot recommendations into build/PR queue — the doc effectively defines the next 2–4 weeks of SEO work (Park City citations + 2–3 PC case studies w/ neighborhood proper nouns; SLC-valley chamber + BBB-local citations; CWH-Holladay review collection; Utah County play: GAF + CertainTeed contractor listings + Alpine Chamber for Alpine-Highland bundle; review velocity campaign on existing 20-review base; the ~1000× DR gap is a months-long backlink push, not a weeks-long fix — local-pack wins outside Heber Valley are 60-90d horizon at minimum).
  - **NEW:** Read `scripts/cyborg-send.sh` + `supabase/functions/send-message/index.ts` end-to-end to characterize the new outbound messaging pipe; decide commit/`.gitignore` policy + whether it affects the `handle-sms` / `handle-call` lead-creation surface area.

- **Reframed risk read on the discovery sweep itself.** Two consecutive auto-refreshes (5/28 `d9f7960` + 5/29 today) have surfaced late-evening 5/27 artifacts that the same-day evening refresh `1be4803` (5/27 12:09 MDT) couldn't see. Yesterday's refresh caught one of three (`CITY-RANKING-ACTION-PLAN`). Today caught a second (`cyborg-send.sh`). The pattern suggests the discovery sweep should (a) compare worktree mtimes against the **prior refresh's commit timestamp**, not against its own clock, and (b) add a same-day evening pass when significant production work lands after the morning refresh. Cheap to fix in the next iteration of this scheduled task.

- **Notable absences (unchanged from 5/28):** still no `scripts/aeo-aio-sweep.py` panel run; the 5/20 30-day cost-page AIO hypothesis is at day **+9 of 30** with zero post-merge data points captured. ~5 days of high-information recrawl-likely band remain through ~6/3. Beyond that, signal degrades fast.

### 2026-05-28 (Thursday Auto-Refresh) — Quiet 18-Hour Window After Yesterday's Two-Commit Production Burst + One New Untracked Artifact Discovered: `data/CITY-RANKING-ACTION-PLAN-2026-05-27.html` (Written 5/27 22:23 MDT, ~10h After Evening Refresh, So Not Captured in `1be4803`) — City-by-City Diagnostic + Action Plan That Effectively Defines the Next 2–4 Weeks of SEO Work (Park City / Cottonwood Heights / Holladay / Alpine / Highland All Absent From Top 10; Bottleneck = Authority + Local-Pack Proximity, NOT Page Quality; ~1000× Ahrefs DR Gap Sets 60–90d Horizon)

**18-hour quiet window since yesterday's NAP + tracking burst.** Zero new `main` commits since `1be4803` (5/27 12:09 MDT — itself metadata-only). HEAD still `2fe52d2`. Working tree carries one new untracked artifact that didn't make the evening refresh and was the only meaningful change in the window.

- **New artifact: `data/CITY-RANKING-ACTION-PLAN-2026-05-27.html`** — ~14KB, written 5/27 22:23 MDT (~10h after `1be4803` at 12:09, so structurally invisible to that refresh — first time captured here).
  - **Format:** standalone HTML report with inline CSS (matches the visual signature of prior strategy docs — diagnostic header, color-coded city matrix table, root-cause section, action-ranked recommendations).
  - **Snapshot date:** 2026-05-27. **Data sources:** live Google SERP via Chrome MCP (2026-05-27), PostHog pageviews last 30d, on-page audit of `/locations/[city].html`.
  - **30-second diagnosis (lifted from doc):** "Frame ranks well in Heber Valley (the GBP home turf — #2 organic Heber, #3 Midway) and is invisible everywhere else on the priority list (Park City, Cottonwood Heights, Holladay, Alpine, Highland — none in top 10). ChatGPT also doesn't cite Frame for 'best roofer in Park City' — same competitor set dominates AI answers. Your on-page content is excellent (2,400–2,900 words per location page, 4 schemas, FAQ, 16–32 internal links). The bottleneck is authority + local-pack proximity, not page quality."
  - **City matrix:**

    | City | Organic rank (roofer + city) | Local pack | PostHog views 30d | Page quality | Top action |
    |---|---|---|---|---|---|
    | Heber City | #2 | in pack | 12 | strong | Defend — keep posting weekly, gather reviews |
    | Midway | #3 | partial | 1 | strong | Build Midway-specific backlinks; mention in Heber Valley publications |
    | Park City | not in top 10 | absent | 4 | strong | Build Park City citations + 2-3 PC case studies w/ neighborhood proper nouns |
    | Cottonwood Heights | not in top 10 | absent | 8 | strong | SLC-valley authority signals: chamber + bbb-local citations, reviews from CWH jobs |
    | Holladay | not in top 10 | absent | 8 | strong | Same as CWH — bundle into one SLC-valley plan |
    | Alpine | not in top 10 | absent | 0 | strong | Utah County play: GAF/CertainTeed contractor listings, Alpine Chamber |
    | Highland | not in top 10 | absent | 0 | strong | Bundled with Alpine — Utah County backlink push |

  - **Root-cause gap table (lifted from doc):**

    | Signal | Frame | Top competitors | Gap |
    |---|---|---|---|
    | Domain authority (Ahrefs DR) | 6 | 104–6,140K linked domains | 1000× behind on backlinks |
    | GBP physical address | Heber City only | Have offices in target cities or established service-area history | Local-pack only fires within ~5–10 mi of GBP pin |
    | Directory presence | 4 of 38 | Yelp + BBB + Angi + Houzz + HomeAdvisor + Thumbtack at minimum | Citation count drives local rank |
    | Reviews (volume + recency) | 20 Google reviews | 120–300+ Google + Yelp | Review velocity is a top-3 local-pack factor |
    | City-specific backlinks | 0 known for Park City / CWH / Holladay | Local chamber, neighborhood blogs, real-estate sites | "Topical relevance to city" tips a non-local biz into rankings |
    | AI engine citations | 0 (per ChatGPT test 2026-05-27) | Bartlett, On Top, Alta Vista, FBC, Whitaker | AI models cite directory aggregators + high-DR roofer sites |

  - **Strategic significance:** First time the Park City / SLC-valley invisibility has been quantified into a concrete action plan rather than left as a vague TODO. Confirms what 5/20 cost-page AIO experiment hinted at — Frame's *content* is competitive (often better) but the *authority signals* aren't there. The ~1000× DR gap is the headline number: it sets realistic horizon expectations (local-pack wins outside Heber Valley are **60–90d minimum**, not weeks) and reframes the next sprint priorities away from more pages and toward citations + reviews + backlinks.
  - **NEW carry-forward:** read end-to-end and slot recommendations into build/PR queue. Most actionable near-term items: (1) Park City citations + 2-3 PC case studies; (2) SLC-valley chamber + BBB-local citations for CWH/Holladay bundle; (3) Utah County GAF + CertainTeed contractor listings for Alpine/Highland bundle; (4) review velocity campaign on existing 20-review base.

- **Working tree at refresh time:** Branch `main` clean re: tracked refs. HEAD = `2fe52d2`, up to date with `origin/main`. **Untracked (17 files + 1 directory ≈ 18 entries — +1 vs 5/27 evening):** `.env.local`; 7 `data/` design+handoff docs from 5/17→5/20 (unchanged); 2 5/27 morning docs (`GBP-SERVICES-PROPOSAL-2026-05-27.md` + `NAP-DIRECTORY-CHECKLIST-2026-05-27.md`); **NEW** `data/CITY-RANKING-ACTION-PLAN-2026-05-27.html`; `REVENUE-TILE-DESIGN-2026-05-17.md`; `SESSION-2026-05-17-hf-supply-chain-and-classifier-bench-handoff.md`; `aeo-master-digest-2026-05-18.html`; `data/weekly-reports/` directory (still just the 2 snapshots inside — no new 5/26 / 5/27 / 5/28 file); `scripts/aeo-aio-sweep.py`; `scripts/serpapi-smoke-test.py`; `supabase/functions/send-message/`. Plus the un-staged `.gitignore` `.env*.local` edit (**12-day debt, +1 day; 5 days past the 1-week threshold**).

- **Clock check:**
  - Cost-page AIO experiment: **+8 days post-merge** (`cdee7de` 5/20). Recrawl-likely band wide open through ~6/3 (~6 days remaining). **First post-merge 26-query SerpAPI sweep STILL NOT RUN.** At day +8 of 30 with zero data points captured, this is now urgent — the 30-day measurement window closes 6/19 and there's no point running it on day +29.
  - Weekly-reports cadence: **3 days past** the last snapshot (5/25). Per the weekly hypothesis, next file should land 5/31 Sun or 6/1 Mon. Thursday is the first day of the second test cycle for that theory.
  - GBP primary phone edit acceptance: submitted 5/27 ~10:12 MDT. **24h cusp hits tonight ~10 PM MDT, 72h cusp ~5/30 morning.** No verify-check run today (carry-forward intact).
  - `.gitignore` `.env*.local` debt: **12 days.**
  - Refresh-stale fixer: **22-day debt.**

- **All 5/27 evening carry-forwards intact (none worked today):**
  - Verify GBP primary phone edit accepted by Google.
  - Work `data/NAP-DIRECTORY-CHECKLIST-2026-05-27.md` (BBB / Yelp / Apple Maps NAP updates).
  - Decide DOPL `hasCredential` sitewide rollout (Midway now 1-of-45 LIVE per `80a59d7`).
  - Confirm phone_clicks recovery in next weekly snapshot.
  - Read + act on `data/GBP-SERVICES-PROPOSAL-2026-05-27.md`.
  - All carry-forwards from 5/17 / 5/18 / 5/19 / 5/20 / 5/24 / 5/25 / 5/26 / 5/27 morning still open.

- **Write decisions for this refresh:** CLAUDE.md top-paragraph "Last updated" prepended; this SESSION LOG entry added at top; no git operations beyond the auto-refresh commit; no edits to PRIORITY TODO / SEO STATUS / SITE STRUCTURE (no completed items, no new pages). One untracked artifact discovered but **not committed** — it's a working strategy doc that may evolve, and the scheduled task's mandate doesn't extend to staging unrelated worktree files. Commit-vs-keep-untracked decision is a Ryan call.

---

### 2026-05-27 (Wednesday Evening Auto-Refresh — POST-ACTIVITY) — Quiet Streak Broken: Two Production Commits Flip a Major Architectural Decision in 53 Minutes — (1) `80a59d7` Root-Causes + Fixes the 15-Day `phone_clicks` DB Hole (CORS Preflight Drop on `application/json` Beacon → Switch to `text/plain` + `keepalive` fetch, Cache-Bust `?v=2` Across 124 Files, Backfill 13 Historical Rows from PostHog) + (2) `2fe52d2` Public NAP Swapped from 435-302-4422 → 435-292-8802 Across 164 Files (1,341 Insertions / 1,341 Deletions — Twilio Tracking Number IS Now the Public NAP; Closes the GBP-Call-Button Attribution Gap That Bypassed the Website Entirely) + 3 Location-Page Niche-Diff Edits Carry-Forward CLOSED (Swept Into `80a59d7` Cache-Bust Pass) + 2 New Untracked Artifacts: `data/GBP-SERVICES-PROPOSAL-2026-05-27.md` + `data/NAP-DIRECTORY-CHECKLIST-2026-05-27.md`
**Two commits landed today after a two-blank-day quiet streak**, both authored by `Review Bot <review-bot@frameroofingutah.com>` 53 minutes apart (09:19 + 10:12 MDT), and together they flip a piece of architecture that had been split-brain since 5/16.

- **`80a59d7` (09:19 MDT) — `fix(tracking): phone-click beacon was silently dropped — switch to text/plain Blob, cache-bust ?v=2`.**
  - **Diagnosis:** PostHog had captured 14 phone_click events in last 60d (4 from google.com/utm=google referrers + 2 from ChatGPT yesterday), but the Supabase `phone_clicks` table had **0 rows** for the same period.
  - **Root cause:** `track-clicks.js` v1 sent the beacon as a `Blob` with `type: application/json`, which is NOT a CORS "simple" request — it triggers a preflight. On `tel:`/`sms:` clicks the browser begins dialer handoff before the preflight + POST can complete, so the actual POST is dropped. `navigator.sendBeacon()` still returns `true` (it queued the preflight) — that's why the fallback `fetch()` never ran. Insidious silent failure for 15 days.
  - **Fix:** Switch primary path to `fetch()` with `keepalive: true` and `Content-Type: text/plain` (CORS simple, no preflight). Keep `navigator.sendBeacon()` with a `text/plain` Blob as fallback for page-unload safety on older iOS Safari. Edge function uses `req.json()` which parses regardless of declared Content-Type → server-side unchanged.
  - **Cache bust:** `vercel.json` sets `/*.js` to `max-age=31536000 immutable`. Updated every script tag from `/track-clicks.js` to `/track-clicks.js?v=2` so existing visitors get the new file on next page load — 124 HTML files touched.
  - **Side-effect (carry-forward CLOSED):** The 3 uncommitted location-page niche-differentiation edits (Alpine = Bench-Side Estate Reroofs, Highland = Custom-Home Reroofs on Larger Lots, Midway = Heber Valley Resort & Swiss-Style Homes + DOPL `hasCredential` JSON-LD license 14256097-5501) got swept into this cache-bust commit. Confirmed by diff stat: `locations/alpine.html` (16 lines vs 2 for peers), `locations/highland.html` (16 lines), `locations/midway.html` (29 lines incl. JSON-LD). The 3-day commit-or-revert decision is resolved by inclusion; the 1-of-45 DOPL schema-inconsistency exposure is now LIVE in production (carry-forward: decide sitewide rollout vs strip-from-Midway-for-parity now that Midway is shipped).
  - **Backfill:** Separate Supabase migration replayed 13 historical PostHog phone_click rows into `public.phone_clicks` with original timestamps and source classification. Closes the 15-day data hole retroactively.

- **`2fe52d2` (10:12 MDT) — `chore(nap): swap public phone from +14353024422 → +14352928802 across all live surfaces`.**
  - **Architectural significance:** The Twilio tracking number is now the public NAP. Every Google-initiated call (GBP "Call" button / organic / ads / referral) now routes through Twilio → `handle-call` → `call_logs` + auto-lead creation. Previously the GBP Call button bypassed the website entirely (clicked the listed display number directly), so half the call attribution was structurally invisible. Even with the same-day `80a59d7` tracking fix, that GBP-bypass would still cap data quality. This commit closes it.
  - **Scope:** 164 files / 1,341 insertions / 1,341 deletions. Every HTML display string (`(435) 302-4422` and `435-302-4422` → `(435) 292-8802` / `435-292-8802`), every `schema.org` `telephone` field, `handle-lead` customer-facing auto-reply strings. `tel:` hrefs were already pointed at +14352928802 in most places (5/16 swap) — this fixes the display-vs-dial mismatch.
  - **Preserved as internal-only:** `LANDON_PHONE` constant (+14353024422) in `handle-call` / `handle-lead` / `handle-sms` stays — that's the Twilio forwarding destination, not public NAP. **Never expose this number again.**
  - **Companion actions (carry-forwards):** GBP primary phone edit submitted 2026-05-27, pending Google review (24-72h typical). Directory listings (BBB, Yelp, Apple Maps, etc.) need manual updates — tracked in new artifact `data/NAP-DIRECTORY-CHECKLIST-2026-05-27.md`.

- **CLAUDE.md CRITICAL RULES updated this same refresh** — the Phone numbers section that previously read "Visible display = 302-4422, all `tel:`/`sms:` hrefs = 292-8802" is now stale and has been rewritten to reflect the unified architecture: public NAP = 292-8802 (display + tel: + sms: + schema + GBP + directories), internal-only LANDON_PHONE = 302-4422 (forwarding destination only). PEOPLE section "Business phone" line also updated.

- **New untracked artifacts (2, both 2026-05-27):**
  - `data/GBP-SERVICES-PROPOSAL-2026-05-27.md` — likely a GBP Services taxonomy update proposal (not yet read).
  - `data/NAP-DIRECTORY-CHECKLIST-2026-05-27.md` — the directory-update checklist referenced by `2fe52d2`'s commit message.

- **Working tree at refresh time (post-commit, evening):** Branch `main` clean re: tracked refs (no modified files — the 3 location-page edits got committed via `80a59d7`). HEAD = `2fe52d2`, up to date with `origin/main`. **Untracked (16 files + 1 directory ≈ 17 entries):** `.env.local`; 7 `data/` design+handoff docs from 5/17→5/20 (unchanged); 2 new 5/27 docs (GBP-SERVICES-PROPOSAL + NAP-DIRECTORY-CHECKLIST); `REVENUE-TILE-DESIGN-2026-05-17.md`; `SESSION-2026-05-17-hf-supply-chain-and-classifier-bench-handoff.md`; `aeo-master-digest-2026-05-18.html`; `data/weekly-reports/` directory (still just the 2 snapshots inside — no new 5/26 or 5/27 file); `scripts/aeo-aio-sweep.py`; `scripts/serpapi-smoke-test.py`. Plus the un-staged `.gitignore` `.env*.local` edit (**11-day debt**).

- **Carry-forwards CLOSED today:**
  - **3 uncommitted location-page niche-diff edits** — swept into `80a59d7` cache-bust pass. Live in production.
  - **Inbound-call webhook health check** (open since 5/25, 0 calls / 8 phone clicks regression) — now moot: the phone-click beacon was the regression root cause (DB couldn't see clicks), and `80a59d7` fixed it. Once GBP picks up the new NAP, calls will flow through the Twilio webhook that just got its tracking fix shipped — the next weekly snapshot should show recovery.
  - **15-day `phone_clicks` DB hole** — root caused, fixed, 13 rows backfilled.

- **Carry-forwards STILL OPEN + NEW:**
  - **(NEW) Verify GBP primary phone edit accepted by Google** — 24-72h typical, check merchant panel.
  - **(NEW) Work `data/NAP-DIRECTORY-CHECKLIST-2026-05-27.md`** — BBB / Yelp / Apple Maps / other citation updates. NAP inconsistency across citations is a measurable local-pack ranking penalty; the longer 302-4422 lives in those listings while the site + GBP say 292-8802, the worse the inconsistency signal.
  - **(NEW) Decide DOPL `hasCredential` sitewide rollout** — Midway is now LIVE in production with the 1-of-45 schema variant via `80a59d7`. Either roll out to the other 44 locations (consistency win) or strip from Midway (parity rollback). Decision should land within a week before search engines normalize the inconsistency into a negative signal.
  - **(NEW) Confirm phone_clicks recovery** — next weekly-report snapshot (~5/31 Sun or 6/1 Mon per cadence theory) should show non-zero phone_clicks for the first time in 60d. If it doesn't, the fix has a second-order issue.
  - **(NEW) Read `data/GBP-SERVICES-PROPOSAL-2026-05-27.md`** — untouched in this refresh; bring into next session's working set.
  - **Cost-page AIO experiment clock at +7 days post-merge** — `cdee7de` merged 2026-05-20; today 5/27 = day +7 of 30-day hypothesis window. Recrawl-likely band still wide open (~7 days of high-information band remaining through ~6/3). First post-merge 26-query SerpAPI sweep still gating; not run today either.
  - **Commit-batch the 16-file + 1-directory untracked stack + `.gitignore` edit** — gitignore at 11-day debt now.
  - All carry-forwards from 5/17 + 5/18 + 5/19 + 5/20 + 5/24 + 5/25 + 5/26 + 5/27-morning entries remain open: Landon sign-off on `Frame-Reddit-AEO-Playbook.html` + `Frame-AEO-Master-Play.html`; Supabase Functions ACTIVE confirm for `reddit-aeo-classifier` / `reddit-aeo-digest` / `youtube-script-drafter` / `aeo-citation-tracker`; migration mirrors for `aeo_distribution_queue` + `aeo_citation_log`; 9 remaining innovation-audit gaps; revenue tile implementation by Codex; AEO Level-2 fixer model audit; SLV Phase B hero swaps × 5; SLV blog Posts #2–6; Closed-loop ROAS Phase 1; GTM container; `FrameAds.send_to`; form-abandonment fn; gallery expansion; refresh-stale fixer (**21-day debt**); `data/audits/` policy; Formspree wind-down; GBP manual post; GBP Products; density wave 4+; trace `data/weekly-reports/` writer; add 3 city+service queries to `aeo-aio-sweep.py` v1.2 `QUERY_PANEL_V1`.

- **Net effect:** Highest-impact-per-line-of-code day in two weeks. In 53 minutes of commit activity, the project closed a 15-day silent attribution bug AND unified the public NAP onto the tracking number — together these two changes are the difference between "we have phone-click data but it doesn't match calls" and "every Google-driven call from any surface is attributable end-to-end". The CRITICAL RULES section needed surgery to match (done in this same refresh). The 3 location-page niche-diff edits got committed as a side effect, closing a 3-day debt without explicit decision — fine outcome but the DOPL credential sitewide-rollout question moves from "decide before commit" to "decide before the inconsistency calcifies in search-engine indexes". Next high-information actions are now (a) GBP edit acceptance verification (passive — just check), (b) NAP-DIRECTORY-CHECKLIST execution (high-impact for local-pack consistency), (c) the still-not-run cost-page AIO SerpAPI sweep (7 days of recrawl-likely window remaining).

---
### 2026-05-27 (Wednesday Morning Auto-Refresh) — Second Consecutive Quiet 24-Hour Window: Zero `main` Commits + Zero Worktree Changes Since 5/26 Refresh + No 5/27 (or 5/26) Weekly-Report Snapshot (Strengthens 5/26 "Writer NOT Daily" Read — Two-Blank-Day Streak Effectively Rules Out Daily Cron) + Debt Clocks Tick Forward (Location-Page Edits 3 Days / `.gitignore` `.env*.local` 10 Days / Refresh-Stale Fixer 20 Days / Cost-Page AIO Clock +7 Days Post-Merge — Solidly Inside Recrawl-Likely Window, ~7 Days of High-Information Band Remaining)
No new commits on `main` since `f379483` (Tuesday 2026-05-26 20:08 MDT auto-refresh — itself a metadata-only commit that captured the previous quiet-window snapshot). HEAD = `f379483`, up to date with `origin/main`. Working tree is **byte-identical** to 5/26 close: `git diff --stat` shows the same three files with identical line counts — `locations/alpine.html` (+7/-7), `locations/highland.html` (+7/-7), `locations/midway.html` (+13/-8), totaling 33 insertions / 22 deletions, unchanged for the third refresh in a row. Untracked stack flat at 17 files (no new `data/weekly-reports/2026-05-26.json` or `2026-05-27.json` landed overnight).
- **Writer-cadence read (consolidated).** Two consecutive blank days (5/26 Tue + 5/27 Wed) on top of the prior 5/24 Sun + 5/25 Mon pair effectively rules out a daily cron. Most likely candidates now: (a) a weekly cron firing Sun/Mon by timezone coincidence — next due ~5/31 Sun or 6/1 Mon, or (b) a manual trigger that Landon/Ryan ran twice in a row that may not fire again without prompt. Don't expect a snapshot today; do expect one this weekend if the cron theory holds. Writer-trace task still cheap (3-day debt) and worth closing before the next session — a single `grep -rn weekly-report` across the repo + `crontab -l` + `launchctl list | grep weekly` would likely pin it.
- **Working tree at refresh time (Wednesday morning):** Branch `main` clean re: tracked refs. **Modified (uncommitted, now 3-day debt):** `locations/alpine.html`, `locations/highland.html`, `locations/midway.html` — identical diff to Monday morning. **Untracked (17 files):** `.env.local`; 7 `data/` design+handoff docs from 5/17→5/20 (`AEO-AIO-SWEEP-2026-05-20.md`, `AEO-COMPETITOR-MODEL-HOMERROOFING-2026-05-20.md`, `AEO-EDIT-PROPOSAL-2026-05-20.md`, `AEO-FRAME-COST-PAGE-AUDIT-2026-05-20.md`, `CODEX-HANDOFF-DATAFORSEO-2026-05-20.md`, `CODEX-HANDOFF-SERPAPI-2026-05-20.md`, `COWORK-HANDOFF-2026-05-20.md`); `REVENUE-TILE-DESIGN-2026-05-17.md`; `SESSION-2026-05-17-hf-supply-chain-and-classifier-bench-handoff.md`; `aeo-master-digest-2026-05-18.html`; `data/weekly-reports/` (still just the 2 snapshots inside — `2026-05-24.json` + `2026-05-25.json`, no new file); `scripts/aeo-aio-sweep.py`; `scripts/serpapi-smoke-test.py`. Plus the un-staged `.gitignore` `.env*.local` edit (**10-day debt — 3 days past 1-week threshold, no resolution path proposed since 5/20**).
- **Cost-page AIO experiment clock at +7 days post-merge.** `cdee7de` (PR #58 cost-page structural rewrite) merged 2026-05-20 15:40 MDT; today 5/27 = day +7 of the 30-day hypothesis window. Recrawl-likely band (~3-14 days for an indexed page with internal links) is wide open and a full week has now been lost to inaction. Running the gating 26-query `python scripts/aeo-aio-sweep.py` panel today would still produce informative data; SerpAPI 50-search reserve preflight unchanged. Every day past +7 without a measurement compounds the post-merge-signal-lost cost: at +14 (one week from today) the recrawl-likely window starts to close and the experiment moves into the "rank-stable" half of the band where movement signal weakens.
- **Carry-forwards (nothing closed today — zero-activity day, third refresh in a row):**
- **Commit-or-revert decision on the 3 uncommitted location-page niche-differentiation edits.** Now 3-day-old. Same risk profile as 5/26: all three pages sit in the 48-zero-traffic location-coverage bucket so there's no demoted-rank downside to shipping; the only open question remains the 1-of-45 schema-inconsistency exposure from Midway's DOPL `hasCredential` JSON-LD.
- **DOPL `hasCredential` JSON-LD 1-of-45 schema inconsistency** — sitewide rollout vs strip-from-Midway-for-parity decision still gating the Midway commit.
- **Inbound-call webhook health check** — open since 5/25 (0 calls / 8 phone clicks regression flag), now 2 days open. Still cheap to verify: `curl https://hdcflshhomzildwqlmwh.supabase.co/functions/v1/handle-call` + check Twilio Console webhook config for 435-292-8802 voice URL drift. Risk compounds with each day: if the webhook is genuinely broken, every additional 24 hours = more missed-call attribution loss the weekly report won't recover.
- **Trace + label the `data/weekly-reports/` writer** — now 3 days open (since 5/24 morning). Two-blank-day pattern (5/26 + 5/27) narrows the search further: definitely not a daily cron. Find it before the next weekend window so the next snapshot can be expected (or its absence diagnosed) confidently.
- **First post-merge 26-query SerpAPI sweep** — still gating cost-page AIO hypothesis; recrawl window open through ~6/3 (~7 days of high-information band remaining).
- **Commit-batch the 17-file untracked stack + `.gitignore` edit** — debt now 10 days on the gitignore line. The 5/17→5/20 design/handoff docs remain clean candidates for a single vercelignored batch commit.
- **Add 3 city+service queries to `aeo-aio-sweep.py` v1.2 `QUERY_PANEL_V1`** — gates on the location edits shipping first.
- All carry-forwards from 5/17 + 5/18 + 5/19 + 5/20 + 5/24 + 5/25 + 5/26 entries remain open: Landon sign-off on `Frame-Reddit-AEO-Playbook.html` + `Frame-AEO-Master-Play.html`; Supabase Functions ACTIVE confirm for `reddit-aeo-classifier` / `reddit-aeo-digest` / `youtube-script-drafter` / `aeo-citation-tracker`; migration mirrors for `aeo_distribution_queue` + `aeo_citation_log`; 9 remaining innovation-audit gaps; revenue tile implementation by Codex; AEO Level-2 fixer model audit; SLV Phase B hero swaps × 5; SLV blog Posts #2–6; Closed-loop ROAS Phase 1; GTM container; `FrameAds.send_to`; form-abandonment fn; gallery expansion; refresh-stale fixer (**20-day debt**); `data/audits/` policy; Formspree wind-down; GBP manual post; GBP Products; density wave 4+.
- **Net effect:** Third consecutive refresh capturing a quiet day. No tracked-file churn, no new worktree files, no new commits. The strongest new signal is the second blank day in a row for the weekly-report writer — combined with the 5/24+5/25 paired-snapshot baseline, this effectively rules out a daily cron and points to a weekly cadence with next snapshot due ~5/31 Sun or 6/1 Mon. Debt is mechanically aging across every clock: location-page edits 2d→3d, gitignore edit 9d→10d (passes the "1-week was the threshold" marker by 3 days), refresh-stale fixer 19d→20d, cost-page AIO +6→+7 days post-merge. The cost-page AIO informative-measurement window has now consumed half its recrawl-likely band (+7 of ~14) without anyone running the gating SerpAPI sweep. Cheapest, highest-information next-session priorities (in order): (1) inbound-call webhook curl check (3 minutes, surfaces a real attribution bug if present), (2) writer-trace one more pass (`grep -rn weekly-report` + `crontab -l`), (3) decide commit-vs-revert on the 3 location-page edits before they hit a full week.
---
### 2026-05-26 (Tuesday Auto-Refresh) — Quiet 24-Hour Window: Zero `main` Commits, Zero New Worktree Activity, No 5/26 Weekly-Report Snapshot (Writer Cadence Confirmed NOT Daily — Likely Weekly/Twice-Weekly Per 5/24 + 5/25 Pattern) + Location-Page Niche-Diff Edits Hit 2-Day Debt + `.gitignore` `.env*.local` Past 1-Week Threshold at 9 Days + Cost-Page AIO Clock at +6 Days Post-Merge (Recrawl-Likely Window Still Open)
No new commits on `main` since `50a98f6` (Monday 2026-05-25 20:07 MDT auto-refresh — itself a metadata-only commit that captured the Monday-morning snapshot). HEAD = `50a98f6`, up to date with `origin/main`. Working tree is **byte-identical** to Monday's 09:30 MDT state — `git diff --stat` shows the same three files with identical line counts: `locations/alpine.html` (+7/-7), `locations/highland.html` (+7/-7), `locations/midway.html` (+13/-8), totaling 33 insertions / 22 deletions. Untracked stack still 17 files (no new `data/weekly-reports/2026-05-26.json` landed overnight). This is the cleanest possible signal that no human or scheduled-writer activity touched the repo in the last 24 hours.
- **Writer-cadence read.** The absence of a 5/26 snapshot — when 5/24 (Sun) + 5/25 (Mon) both produced one — is itself informative. The writer is **not daily**. Likely candidates: (a) weekly cron firing Sunday morning + Monday morning by coincidence of timing/timezone (suggests next snapshot due ~5/31 Sun or 6/1 Mon), or (b) a manual trigger that Landon/Ryan ran twice in a row, in which case the writer is human-driven and may not fire again without prompt. Until the writer-trace task closes, treat the 5/24+5/25 pair as the only ground truth and don't expect a 5/26 file. Worth tracing because the snapshots are clean weekly-pulse data and a working writer is a free dashboard.
- **Working tree at refresh time:** Branch `main` clean re: tracked refs. **Modified (uncommitted, now 2-day debt):** `locations/alpine.html`, `locations/highland.html`, `locations/midway.html` — identical diff to Monday morning. **Untracked (17 files):** `.env.local`; 7 `data/` design+handoff docs from 5/17→5/20 (`AEO-AIO-SWEEP-2026-05-20.md`, `AEO-COMPETITOR-MODEL-HOMERROOFING-2026-05-20.md`, `AEO-EDIT-PROPOSAL-2026-05-20.md`, `AEO-FRAME-COST-PAGE-AUDIT-2026-05-20.md`, `CODEX-HANDOFF-DATAFORSEO-2026-05-20.md`, `CODEX-HANDOFF-SERPAPI-2026-05-20.md`, `COWORK-HANDOFF-2026-05-20.md`); `REVENUE-TILE-DESIGN-2026-05-17.md`; `SESSION-2026-05-17-hf-supply-chain-and-classifier-bench-handoff.md`; `aeo-master-digest-2026-05-18.html`; `data/weekly-reports/` (2 snapshots inside); `scripts/aeo-aio-sweep.py`; `scripts/serpapi-smoke-test.py`. Plus the un-staged `.gitignore` `.env*.local` edit (**9-day debt — first day past 1-week threshold**).
- **Cost-page AIO experiment clock at +6 days post-merge.** `cdee7de` (PR #58 cost-page structural rewrite) merged 2026-05-20 15:40 MDT; today 5/26 = day +6 of the 30-day hypothesis window. Recrawl-likely band (≈3-14 days for an indexed page with internal links) is wide open. Running the gating 26-query `python scripts/aeo-aio-sweep.py` panel today would produce informative data; still requires the SerpAPI 50-search reserve preflight. Decision pressure increasing: every day past +6 without a measurement is a day of post-merge signal lost.
- **Carry-forwards (nothing closed today — zero-activity day):**
- **Commit-or-revert decision on the 3 uncommitted location-page niche-differentiation edits.** Now 2-day-old. Same risk profile as Monday: all three pages sit in the 48-zero-traffic location-coverage bucket so there's no demoted-rank downside to shipping; the only open question is the schema-inconsistency exposure from Midway's 1-of-45 DOPL `hasCredential` JSON-LD.
- **DOPL `hasCredential` JSON-LD 1-of-45 schema inconsistency** — sitewide rollout vs strip-from-Midway-for-parity decision still gating the Midway commit.
- **Inbound-call webhook health check** — open since 5/25 (0 calls / 8 phone clicks regression flag). Still cheap to verify: `curl https://hdcflshhomzildwqlmwh.supabase.co/functions/v1/handle-call` + check Twilio Console webhook config for 435-292-8802 voice URL drift.
- **Trace + label the `data/weekly-reports/` writer** — now 2 days open (since 5/24 morning). Today's no-snapshot data point narrows the search: writer is not a daily cron. Likely a weekly or twice-weekly cron, or a manual trigger. Find it before next session.
- **First post-merge 26-query SerpAPI sweep** — still gating cost-page AIO hypothesis; recrawl window still open through ~6/3.
- **Commit-batch the 17-file untracked stack + `.gitignore` edit** — debt now solidly past 1 week on the gitignore line. The 5/17→5/20 design/handoff docs are clean candidates for a vercelignored batch commit.
- **Add 3 city+service queries to `aeo-aio-sweep.py` v1.2 `QUERY_PANEL_V1`** — gates on the location edits shipping first.
- All carry-forwards from 5/17 + 5/18 + 5/19 + 5/20 + 5/24 + 5/25 entries remain open: Landon sign-off on `Frame-Reddit-AEO-Playbook.html` + `Frame-AEO-Master-Play.html`; Supabase Functions ACTIVE confirm for `reddit-aeo-classifier` / `reddit-aeo-digest` / `youtube-script-drafter` / `aeo-citation-tracker`; migration mirrors for `aeo_distribution_queue` + `aeo_citation_log`; 9 remaining innovation-audit gaps; revenue tile implementation by Codex; AEO Level-2 fixer model audit; SLV Phase B hero swaps × 5; SLV blog Posts #2–6; Closed-loop ROAS Phase 1; GTM container; `FrameAds.send_to`; form-abandonment fn; gallery expansion; refresh-stale fixer (**19-day debt**); `data/audits/` policy; Formspree wind-down; GBP manual post; GBP Products; density wave 4+.
- **Net effect:** Pure-quiet day. No tracked-file churn, no new worktree files, no new commits. The only signal-bearing observation is the *absence* of a 5/26 weekly-report snapshot — which confirms the writer is not daily and helps narrow the writer-trace search. Debt is mechanically aging across the board (location-page edits 1d→2d, gitignore edit 8d→9d crossing 1-week threshold, refresh-stale fixer 18d→19d). The cost-page AIO experiment is now squarely in the informative-measurement window without anyone having run the gating SerpAPI sweep yet.
---
### 2026-05-25 (Monday Auto-Refresh) — Second Weekly-Report Snapshot Lands (`data/weekly-reports/2026-05-25.json`, 09:30 MDT) Confirms Weekly Cadence + Inbound-Call Regression Flag (1 → 0 Calls Despite 8 Phone-Click Events; Weekly-Report Notes Suspect Twilio Webhook) + Working Tree Unchanged from 5/24 Evening (Same 3 Location-Page Niche-Differentiation Edits Still Uncommitted — Now 1-Day Debt) + Cost-Page AIO 30-Day Clock at +5 Days Post-Merge (Recrawl-Likely Window Open)
No new commits on `main` since `129ce1f` (2026-05-24 evening auto-refresh). The only new on-disk activity overnight is the second `data/weekly-reports/` snapshot — `2026-05-25.json`, ~6KB, written 09:30:09 MDT today by whatever cron/scheduled-task wrote the 5/24 one (writer-trace task still carries forward — open since 5/24 morning entry). Pulled from the same `/functions/v1/weekly-report` edge fn URL; same shape; week-ending 2026-05-25 / trailing-30-day rollup.
- **Week-over-week deltas vs 2026-05-24 snapshot (one rolling-30d window slid forward by 1 day):** pageviews 614 → 588 (-26 / -4.2%); google-organic 61 → 60 (-1, effectively flat); direct 487 → 462 (-25); total leads 4 → 4 (flat — same four rows: Dan Richardson 5/12 new, Greg Atkinson 5/12 won $118K offline-signed, George Warner 5/11 new, Jan Hermansen 5/9 won $24K homepage); form_submitted events 5; phone_click events 8; **inbound_call 1 → 0** (regression flag — see below); inbound_sms 2 (same); email_click 1; total job_value $142K → $142K (the two pre-existing wins still anchor the entire period); conversion_rate_pct 0.7% (flat); commission still $0 (DB column shape issue from prior weeks unchanged).
- **Inbound-call regression flag.** Snapshot shows **0 inbound calls vs 8 phone_click events** over the same trailing window. Last week's snapshot showed 1 call. The weekly-report's own embedded `notes` field calls this out verbatim: *"0 inbound calls tracked despite 8 phone_click events — worth checking whether the inbound webhook is still firing through Twilio."* This is exactly the kind of break the 5/16 Twilio plumbing (commits 72b09fb + 8a8063f + `handle-call` v2 + `handle-sms` v5) was built to surface. Probable fault zones, in order of likelihood: (1) Twilio Console → 435-292-8802 voice webhook URL drifted or Supabase URL rotated, (2) Supabase `handle-call` edge fn deployed but not ACTIVE / 5xx-ing silently, (3) `call_logs` insert path broken upstream of the leads auto-create. Cheap first check next session: `curl https://hdcflshhomzildwqlmwh.supabase.co/functions/v1/handle-call` (expect TwiML / non-error response) + check Twilio Console webhook config. Worth confirming before assuming Landon missed real inbound traffic — could equally be that genuinely zero calls landed in the trailing 30 (8 phone clicks ≠ 8 actual dials; people often tap and bounce).
- **Top-pages mix (588 PV / 30d) — same-shape as 5/24, no rank reshuffle:** `/` 288 (49% of traffic, homepage dominance intact) → `/about` 39 → `/pages/about` 38 → `/blog` 22 → `/pages/gallery` 20 → `/blog/utah/utah-hail-season-roof-guide-2026` 18 → `/locations/heber-city` 12 → `/pages/storm-damage` 12 → `/pages/general-contracting` 11 → `/blog/sandy/spring-hail-damage-roof` 10 → `/thank-you` 10 → `/locations/cottonwood-heights` 8 → `/locations/holladay` 8 → `/dashboard` 6 → `/locations/salt-lake-city` 5 → `/locations/park-city` 4. Location coverage stuck at **11 / 59 = 19%** (48 location pages zero-traffic — Alpine + Highland + Midway, the three pages with uncommitted niche-differentiation edits, all sit in the zero-traffic 48, so the niche edits would be testing into a pre-traffic baseline if they ship).
- **Traffic-source mix:** $direct 462 (79%) · google 60 (10%) · vercel sandbox 16 · bing 15 (12 + 3) · yelp 14 (8 + 6) · self-ref 6 · instagram 5 (4 + 1). Direct-dominance unchanged from 5/24 (similar ~80% direct shape).
- **Working tree state at refresh time (09:30 MDT):** Branch: `main`, up to date with `origin/main` at `129ce1f`. **Modified (still uncommitted from 5/24 evening — now 1-day debt):** `locations/alpine.html` (+7/-7), `locations/highland.html` (+7/-7), `locations/midway.html` (+13/-8) — 33 insertions / 22 deletions across the three (unchanged from 5/24 close — no further edits or partial reverts overnight). **Untracked grows to 17** (was 16 at 5/24 close): adds only `data/weekly-reports/2026-05-25.json` on top of the existing stack (the same 11 5/17→5/20 artifacts + `data/weekly-reports/2026-05-24.json` + `.env.local` + 3 scripts). `.gitignore` `.env*.local` edit still uncommitted (**8-day-old debt** and rolling — hits 1-week-debt threshold today).
- **Cost-page AIO experiment clock: +5 days post-merge (`cdee7de` was 2026-05-20 15:40 MDT).** Recrawl-likely window for an indexed blog post with internal-link presence is ~3-14 days, so today (Monday morning, day +5) sits squarely inside the window where a 26-query `python scripts/aeo-aio-sweep.py` run could start to produce informative data. Quota preflight still requires a 50-search reserve; the SerpAPI Free Plan budget supports this. Running it is still the gating decision for whether the visible-text-snippet hypothesis is bearing out at the cost-page tier. Mid-July 2026 first-citation target from `data/COWORK-HANDOFF-2026-05-20.md` remains on calendar.
- **Carry-forwards rolling into next session (nothing closed today — pure measurement-window day):**
  - **Commit-or-revert decision on the 3 uncommitted location-page niche-differentiation edits.** Now 1-day-old. Either commit + plan the 42-page rollout (or 2-week-rank-watch first), or `git checkout HEAD -- locations/{alpine,highland,midway}.html`. Worth noting these 3 pages all currently sit in the 48-zero-traffic location-coverage bucket, so the edits are testing into a pre-traffic baseline — there's no demoted-rank risk from shipping them, just the schema-inconsistency risk from Midway's DOPL `hasCredential` being 1-of-45.
  - **DOPL `hasCredential` JSON-LD 1-of-45 schema inconsistency** — same call still gates a clean sitewide rollout plan vs strip-from-Midway-for-parity.
  - **Inbound-call webhook health check** — new today. `0 calls / 8 phone clicks` flagged by the weekly-report's own notes; cheap to verify before assuming customer behavior change.
  - **Trace + label the `data/weekly-reports/` writer** — open since 5/24. Two snapshots now exist, both timestamped during the 09:30 MDT window (5/24 = 11:45 but 5/25 = 09:30 — slight cadence drift worth noting; not strictly weekly-Sunday). Find the cron or scheduled-task that wrote them so future refreshes know whether to git-track or vercelignore.
  - **First post-merge 26-query SerpAPI sweep** — still gating; recrawl-likely window now open.
  - **Commit-batch the 17-file untracked stack + `.gitignore` edit** — debt now 1-week-plus on the gitignore line; the design/handoff docs and weekly-reports snapshots are clean candidates for a vercelignored batch commit.
  - **Add 3 city+service queries to `aeo-aio-sweep.py` v1.2 `QUERY_PANEL_V1`** — to instrument the location-page tier (Alpine bench-side estate / Highland custom-home larger-lot / Midway resort Heber Valley) alongside the cost-page sweep IF the location edits ship.
  - All carry-forwards from 5/17 + 5/18 + 5/19 + 5/20 + 5/24 entries remain open: Landon sign-off on `Frame-Reddit-AEO-Playbook.html` + `Frame-AEO-Master-Play.html`; Supabase Functions ACTIVE confirm for `reddit-aeo-classifier` / `reddit-aeo-digest` / `youtube-script-drafter` / `aeo-citation-tracker`; migration mirrors for `aeo_distribution_queue` + `aeo_citation_log` as `supabase/migrations/*.sql`; 9 remaining innovation-audit gaps; revenue tile implementation by Codex (per `data/REVENUE-TILE-DESIGN-2026-05-17.md`); AEO Level-2 fixer model audit; SLV Phase B hero swaps × 5; SLV blog Posts #2–6; Closed-loop ROAS Phase 1; GTM container; `FrameAds.send_to`; form-abandonment fn; gallery expansion; refresh-stale fixer (**18-day debt**); `data/audits/` policy; Formspree wind-down; GBP manual post; GBP Products; density wave 4+.
- **Net effect:** Zero tracked-file churn day. Pure-measurement Monday. The only new on-disk signal is the second weekly-report snapshot, which (a) validates the writer is running on a roughly-weekly cadence and (b) surfaces a concrete actionable item (inbound-call webhook regression flag) on top of the still-deferred 5/24-evening location-page commit decision. The cost-page AIO experiment enters its informative-measurement window today. Carry-forward debt is aging — gitignore line hits 8 days, location-page edits become 1-day-old, refresh-stale fixer hits 18 days.

---

### 2026-05-24 (Evening Auto-Refresh) — 3 Uncommitted Location-Page Niche-Differentiation Edits Surface in Worktree (Alpine / Highland / Midway): Generic "Premium Reroofing" Titles → Niche Headlines + Midway Adds DOPL License JSON-LD Credential
~8 hrs after the morning quiet-maintenance auto-refresh (`479d2f4`, 12:07 MDT), the working tree now shows three previously-clean location pages with uncommitted edits — `locations/alpine.html`, `locations/highland.html`, `locations/midway.html` — 33 insertions / 22 deletions across the three. No new `main` commits since `cdee7de` (PR #58, 2026-05-20 cost-page AIO experiment). Pattern is a Wave-1-style niche-differentiation pass on three premium-market locations:
- **`locations/alpine.html`** (+7/-7) — Title/meta/OG/Twitter reframed from generic "Premium Reroofing Alpine UT" to **"Alpine UT Roofing — Bench-Side Estate Reroofs"**; meta description swapped to lead with "bench-side estates & luxury homes" + "Snow-load engineering, premium materials"; H1 changed from "Premium Reroofing in Alpine" to **"Estate-Grade Reroofing in Alpine"**. Targets the Alpine Lone-Peak custom-home / luxury-bench audience explicitly rather than the generic "premium roofing contractor" phrasing.
- **`locations/highland.html`** (+7/-7) — Title/meta/OG/Twitter reframed to **"Highland UT Roofing — Custom-Home Reroofs on Larger Lots"**; meta swapped to "Highland, UT roof replacement for custom & semi-custom homes on larger lots. HOA-savvy crews, full material warranties, free estimates."; H1 changed from "Premium Reroofing in Highland" to **"Custom-Home Reroofing in Highland"**. Differentiates from the Alpine niche (estate/luxury) by leaning on Highland's larger-lot semi-custom build pattern + HOA workflow positioning.
- **`locations/midway.html`** (+13/-8) — Title/meta/OG/Twitter reframed to **"Midway UT Roofing — Heber Valley Resort & Swiss-Style Homes"**; meta swapped to "Midway, UT reroofing for Swiss-style historic homes, resort properties & Heber Valley custom builds. Local Heber City crews."; H1 changed from "Premium Reroofing in Midway" to **"Resort-Grade Reroofing in Midway"**. Also adds a **`hasCredential` JSON-LD block** to the LocalBusiness graph — `EducationalOccupationalCredential` → Utah DOPL Contractor License `14256097-5501`, recognized by Utah Division of Professional Licensing (https://dopl.utah.gov). This is the first appearance of a structured-data DOPL license credential on a location page in the worktree; if shipped, it adds an entity-graph trust signal directly to the Midway LocalBusiness schema that all other 44 locations currently lack.
- **Pattern signal — three premium-market east-bench / Heber Valley locations get unique niche headlines on the same working-tree pass.** Each page now has a distinct visible-text snippet shape: Alpine = "Estate-Grade" / "bench-side estates", Highland = "Custom-Home" / "larger lots / HOA-savvy", Midway = "Resort-Grade" / "Swiss-style historic / Heber Valley". This directly applies the visible-text-snippet hypothesis from the 5/20 `data/AEO-COMPETITOR-MODEL-HOMERROOFING-2026-05-20.md` audit (Homer's cost page got 5/7 AIO citations on distinct concrete language, not schema) to the location-page tier rather than the cost-page tier. Open question for next session: are the other 42 locations being held back deliberately (niche-differentiation pilot on 3 first), or did the working session stop mid-pass?
- **Working-tree state at close (evening 2026-05-24).** Branch: `main`, up to date with `origin/main` at `cdee7de`. **Now tracked changes** (new since morning refresh): `locations/alpine.html`, `locations/highland.html`, `locations/midway.html` — all uncommitted, all niche-differentiation edits. Untracked stack unchanged from morning: same 11 artifacts + `.gitignore` `.env*.local` edit (now **7-day-old debt** + rolling). `.env.local` still present in worktree.
- **Open items added to carry-forward.**
  - **Commit-or-revert decision on Alpine / Highland / Midway niche edits.** If these are intentional Wave-2 niche-differentiation pilot — commit + decide whether to roll the same pattern across remaining 42 locations or run a 2-week-rank-watch before scaling. If accidental/stale — revert via `git checkout HEAD -- locations/alpine.html locations/highland.html locations/midway.html`.
  - **Decide whether DOPL `hasCredential` JSON-LD ships across all 45 locations.** Midway is the only page in the worktree currently carrying it. The credential is a verifiable real-world trust signal — Utah DOPL Contractor License `14256097-5501` — and adding it sitewide is a cheap entity-graph win. Either commit Midway's pattern as the template + plan a 45-page rollout, or strip it from Midway for parity. Don't ship a 1-of-45 schema inconsistency to production.
  - **Compare niche-differentiation pattern against 5/20 AIO hypothesis.** The cost-page experiment tests whether visible-text-snippet shape drives AIO citation; these location-page edits apply the same hypothesis to local-pack / rank-tracker keyword surfaces. If Codex/Ryan's intent matches that read, the location-page tier becomes a second falsifiable test running in parallel to the cost-page 30-day clock — worth instrumenting alongside the cost-page sweep in `scripts/aeo-aio-sweep.py` v1.2 by adding the 3 city+service queries to `QUERY_PANEL_V1`.

---

### 2026-05-24 — Quiet Maintenance Window: No `main` Activity Since 2026-05-20 + First Weekly-Report Disk Snapshot Lands (`data/weekly-reports/2026-05-24.json`) + 30-Day Cost-Page AIO Hypothesis Clock Still Running
No new commits on `main` since the 2026-05-20 auto-refresh (`0caeab5`) — the 4-day window between PR #58 (`cdee7de`, cost-page structural rewrite for AIO citation eligibility) and today has been deliberate hold-time on the experiment: the falsifiable hypothesis (Frame's visible-text-snippet shape, not its schema, is what gates AIO citation) needs Google to recrawl the rewritten cost page before the first post-merge 26-query SerpAPI panel run becomes useful baseline data. No human action on the repo today.
- **New artifact: `data/weekly-reports/2026-05-24.json` (untracked, ~5 KB).** First file in a NEW `data/weekly-reports/` directory — appears to be a fresh weekly auto-export firing for the first time against the existing `weekly-report` edge fn. Snapshot week ending 2026-05-24, 30-day rollup: 614 total pageviews · 61 google-organic / 487 direct / 16 sandbox-referer / 12 bing / 8+6 yelp / 6 same-site self-ref / 4 instagram · 4 total leads · 8 phone clicks · 1 inbound call · 2 inbound SMS · $142K total job_value · 0% commission realized (no won leads with margin populated yet) · 0.7% form conversion · top page = homepage 298 views (49% of total), then `/pages/about` 40, `/about` 39, `/blog/utah/utah-hail-season-roof-guide-2026` 32, `/blog` 22, `/pages/gallery` 20. Direct-traffic dominance (487/614 = 79%) tracks with brand-search + GBP-tap + direct-recall behavior typical of local-service roofing — organic search at 10% of pageviews indicates Frame is still in the early-impression / pre-rank-pack phase, consistent with the 5/16 `aeo-citation-tracker` 0% baseline and the 5/20 AIO 0/7 sweep. The new `data/weekly-reports/` directory is the kind of disk-snapshot history layer the `seo-report.html` + `/dashboard/` lacked — useful for future trend math against the prior-period delta logic already in `dashboard.js`. Open question: which scheduled task or cron wrote this snapshot? Worth tracing on next session — likely a Coworker scheduled task or a new cron in `Library/LaunchAgents/`.
- **Working tree state at close.** Branch: `main`, up to date with `origin/main` at `cdee7de`. No tracked changes. Untracked: same set as 2026-05-20 close + 1 new (`data/weekly-reports/2026-05-24.json`). Uncommitted: same `.gitignore` `.env*.local` edit from 5/17 — now **7-day-old debt** and rolling.
- **Cost-page AIO hypothesis status (`cdee7de` was 2026-05-20, +4 days):** No measurement run today. The first post-merge 26-query SerpAPI panel run (`python scripts/aeo-aio-sweep.py`) is still the next data point and remains gating. Google needs to have recrawled + re-rendered the cost page since the structural rewrite for the AIO model to even potentially cite it; typical recrawl latency for an indexed blog post with internal links is ~3-14 days, so a meaningful measurement run any day from ~2026-05-23 onward should start to be informative. Mid-July 2026 first-citation target from `data/COWORK-HANDOFF-2026-05-20.md` remains on calendar.
- **Open items rolling forward to next session (carry-forward debt growing):**
  - **Trace + label the new `data/weekly-reports/` writer.** Find the scheduled task or cron that wrote `2026-05-24.json` and document its cadence in this CLAUDE.md so future refreshes know whether to commit these snapshots or vercelignore them. Default to vercelignore + git-track if it's a Sunday-weekly cadence so trend data accumulates without bloating the production deploy.
  - **First post-merge 26-query SerpAPI sweep** — gating data point for the cost-page AIO hypothesis. Quota-aware (preflight reserves 50-search buffer), Codex-audited.
  - **Commit the 11-file untracked stack** (was 9 at 5/20 close, now 11 with `weekly-reports/2026-05-24.json` + the previously-untracked `data/aeo-master-digest-2026-05-18.html` still pending): batch the 7 `data/AEO-*` / `data/CODEX-HANDOFF-*` / `data/COWORK-HANDOFF-*` 5/20 docs + 2 5/20 scripts + 2 carry-over digest/snapshot artifacts + the `.gitignore` `.env*.local` edit. Likely vercelignored.
  - **All carry-forward items from 2026-05-20 entry remain open:** Landon sign-off on `Frame-Reddit-AEO-Playbook.html` + `Frame-AEO-Master-Play.html`; Supabase Functions ACTIVE confirm for `reddit-aeo-classifier` / `reddit-aeo-digest` / `youtube-script-drafter` / `aeo-citation-tracker`; `aeo_distribution_queue` + `aeo_citation_log` migration mirrors as `supabase/migrations/*.sql`; remaining 9 innovation-audit gaps from `Frame-Innovation-Audit-2026-05-16.html`; revenue tile implementation by Codex (per `data/REVENUE-TILE-DESIGN-2026-05-17.md`); AEO Level-2 fixer model audit; SLV Phase B hero swaps × 5; SLV blog Posts #2–6; Closed-loop ROAS Phase 1; GTM container; `FrameAds.send_to`; form-abandonment fn; gallery expansion; refresh-stale fixer (now **17-day debt**); `data/audits/` policy; Formspree wind-down; GBP manual post; GBP Products; density wave 4+.
- **Net effect:** Zero tracked-file churn day. The structural cost-page experiment is in its measurement window. The single observable signal is the new `data/weekly-reports/` snapshot — confirming that whatever cron/scheduled-task is producing it has gone live and is now writing weekly disk artifacts of the same 30-day rollup `seo-report.html` + `/dashboard/` render live. Carry-forward debt continues to age; nothing is on fire, but the untracked-artifact + uncommitted-`.gitignore` stack is starting to look like a Monday-morning cleanup pass.


### 2026-05-20 — AIO Citation Experiment Shipped to Production (Cost-Page Structural Rewrite Merged to `main` via PR #58 → `cdee7de`) + 10-Query SerpAPI AIO Baseline Sweep (7/10 Surface AIO, 0/7 Cite Frame — Confirms 5/16 `aeo-citation-tracker` 0% Gemini Baseline Across 7 Surfaces) + homerroofing.net Competitor Audit (5/7 AIO Cited — Visible-Text-Snippet Pattern Identified) + Production 26-Query Measurement Script (`aeo-aio-sweep.py` v1.2, Codex-Audited) + Architecture Pivots Off DataForSEO v1.0 Onto Existing SerpAPI Free Plan (v1.1)
One commit lands on `main` today (`cdee7de`, 15:40 MDT, GitHub merge of PR #58 — feature branch `aeo/cost-page-aio-rewrite-2026-05-20`, original Review Bot commit was `bbc20d3`). The commit ships a 3-edit structural rewrite to `blog/utah/roof-replacement-cost-utah-2026.html` (+26/-9 lines): (1) title/meta/H1/OG/Twitter reframe to question form ("How Much Does a Roof Replacement Cost in Utah?") with $ range in meta description (132 chars, under Google truncation threshold); (2) opening paragraph leads with concrete $8,000–$30,000 range plus a $12,000–$19,200 architectural-shingle anchor — sourced from the page's existing HowTo schema material tiers (no new claims, no fabrication); (3) visible FAQ block added between The Real Answer and Sources sections with 5 Q&A pairs copied verbatim from existing JSON-LD `acceptedAnswer.text`. No URL changes. No JSON-LD changes. No phone-number changes. Dual-number rule preserved (visible 435-302-4422, hrefs +14352928802). Day's arc: turn the 2026-05-16 `aeo-citation-tracker` 0% Gemini baseline into a structurally-grounded falsifiable experiment with a 30-day measurement clock; build production-grade measurement substrate; pivot architecture off a planned DataForSEO subscription onto an already-paid SerpAPI Free Plan after live validation proves it covers Phase 1.
- **`data/AEO-AIO-SWEEP-2026-05-20.md` — baseline sweep.** SerpAPI `engine=google` + `engine=google_ai_overview` async resolver. 10 queries, 16 SerpAPI calls, 227 quota remaining. **7/10 (70%) surfaced an AIO block** (1 inline, 6 async). **0/7 (0%) of AIO blocks cited Frame.** Fully confirms the 2026-05-16 `aeo-citation-tracker` 0% Gemini baseline across 7 independent AIO surfaces. The headline gap the experiment is designed to close.
- **`data/AEO-COMPETITOR-MODEL-HOMERROOFING-2026-05-20.md` — competitor model.** Deconstructs why `homerroofing.net` cited 5/7 in the same sweep. Three URLs audited: `/learning-center/roof-replacement-cost-utah/` (cited #2 for cost AIO), `/pricing/` (cited #10), `/faqs/` (cited #14). Pattern identified: 3 distinct concrete $ ranges in first 100 words (`$8,500–$25,000`, `$11,000–$14,000`, metal premium), 8 visible Q&A pairs, question-form H1 ("How Much Does a New Roof Cost in Utah in 2026?"), `/learning-center/{topic}/` slug pattern (trailing slash, no `.html`). Schema NOT detected in fetched HTML — confirms schema is not the differentiator; visible-text shape is.
- **`data/AEO-FRAME-COST-PAGE-AUDIT-2026-05-20.md` — Frame pre-edit audit.** Read-only audit of `blog/utah/roof-replacement-cost-utah-2026.html`: ~1,800 words body, title was "Roof Replacement Cost in Utah (2026 Guide) | What You'll Actually Pay", meta description led with factor explanation ("No prices locked in, just the factors that matter") not a $ range, no visible FAQ section on page, JSON-LD schemas (BlogPosting + FAQPage + HowTo) richer than Homer's. The exact 3 gaps the Edit Proposal targets.
- **`data/AEO-EDIT-PROPOSAL-2026-05-20.md` — Codex-audited edit proposal.** Status 🟡 ship-with-changes: Edit 1 + Edit 3 wording revised to source-aligned phrasing (anchors pulled verbatim from existing HowTo schema material tiers — not fabricated); Edit 2 approved as-written. Codex direction: skip pre-merge 26-query sweep; use May 20 10-query baseline as reference; first post-merge 26-query run becomes the full panel baseline. This is the proposal `cdee7de` (PR #58) implements verbatim. Hypothesis being tested: **Frame's visible-text-snippet shape — not its schema, authority, or content depth — is the reason Frame is 0/7 cited in Google AI Overview blocks across its market.** Falsification path: if Frame stays 0/N cited through the July sweep, the visible-text-shape hypothesis is wrong.
- **`scripts/aeo-aio-sweep.py` v1.2 + `scripts/serpapi-smoke-test.py` — measurement substrate.** Production-grade Phase 1 measurement script (Codex-audited 2026-05-20). Implements a locked **26-query panel** (`QUERY_PANEL_V1`): 5 production + 5 already-tested + 16 new across cost / insurance / storm / materials / process. Same location/device/day across runs for apples-to-apples comparison. Codex hardening applied: quota preflight with hard-stop unless run can preserve a 50-search reserve; durable `run_id` + hash-suffixed filenames (no slug-truncation collisions); `no_cache=true` on all SerpAPI calls (measurement integrity); first-class `resolve_status` enum (success/empty/error/expired_token); dual output (markdown report + machine-readable JSON); `.last_run.json` for cron observability; account quota delta tracking before/after. NO DB writes — disk-only for two stable monthly cycles before adding migrations + LaunchAgent cron. Reads `SERPAPI_KEY` from `~/.config/frame-roofing-utah/.env` (canonical secrets file, same path used by `update-google-reviews-cron.sh` + `aeo-citation-monitor.py`). Companion `serpapi-smoke-test.py` validates AIO + Local Pack parsing (~6 calls ≈ $0.10–$0.30) before any vendor migration.
- **`data/CODEX-HANDOFF-SERPAPI-2026-05-20.md` v1.1 — architecture pivot.** Supersedes `data/CODEX-HANDOFF-DATAFORSEO-2026-05-20.md` v1.0 (parked as fallback). v1.0 was: subscribe to DataForSEO as the data substrate for Frame Utah + Frame TX + BioChargeLabs + cbp multi-tenant agency SaaS. Then we discovered Ryan already has a working SerpAPI Free Plan; the two real validation runs (smoke test + 10-query AIO sweep) prove SerpAPI covers Phase 1 of the original plan at $0/mo recurring. AEO play and local-pack play are architecturally separate. The pivot keeps what works; DataForSEO is fallback if cbp client count makes SerpAPI Free Plan budget insufficient (~3 clients × current monthly usage = budget pressure). LLM Mentions substrate stays on existing OpenRouter polling in `aeo-citation-tracker` edge fn (no migration needed).
- **`data/COWORK-HANDOFF-2026-05-20.md` — execution boundary doc.** Locked rule: Cowork executes the cost-page HTML edit; Claude Code does NOT touch HTML in this repo (race-condition risk; this is the locked boundary). Roofing=Utah lane only — do NOT cross to Restoration=TX. ~60-90 min Cowork time estimate. Hypothesis target: first Frame cost-query AIO citation by mid-July 2026 if the structural pattern is causal. The actual edit (`bbc20d3` → merged as `cdee7de` via PR #58) is the executed result of this handoff.
- **Working tree state at close.** Branch: `main`, up to date with `origin/main` at `cdee7de`. Feature branch `aeo/cost-page-aio-rewrite-2026-05-20` consumed by the PR merge. Untracked: 7 new `data/AEO-*` / `data/CODEX-HANDOFF-*` / `data/COWORK-HANDOFF-*` docs from today + 2 new `scripts/aeo-aio-sweep.py` + `scripts/serpapi-smoke-test.py` + 3 carry-over untracked artifacts from 5/17 + 5/18 (`data/REVENUE-TILE-DESIGN-2026-05-17.md`, `data/SESSION-2026-05-17-hf-supply-chain-and-classifier-bench-handoff.md`, `data/aeo-master-digest-2026-05-18.html`). Uncommitted: same `.gitignore` `.env*.local` edit from 5/17, still pending (now **3-day-old debt**).
- **Open items rolling forward to next session:**
  - **First post-merge 26-query SerpAPI sweep run** is the next data point — locks in the full `QUERY_PANEL_V1` baseline against which the cost-page edits get measured 30 days post-crawl. Run via `python scripts/aeo-aio-sweep.py` after Google recrawls the cost page.
  - **Commit the new untracked design/handoff docs + scripts.** 7 `data/` docs (AEO-AIO-SWEEP / AEO-COMPETITOR-MODEL-HOMERROOFING / AEO-EDIT-PROPOSAL / AEO-FRAME-COST-PAGE-AUDIT / CODEX-HANDOFF-DATAFORSEO / CODEX-HANDOFF-SERPAPI / COWORK-HANDOFF) + 2 `scripts/` (aeo-aio-sweep.py / serpapi-smoke-test.py) — batch in with the 3 carry-over untracked artifacts + `.gitignore` edit still pending from 5/17 + 5/19. Likely vercelignored worthy for the design/handoff docs (internal-only).
  - **Local `.git` housekeeping note (2026-05-20):** discovered + removed two stale duplicate ref files (`.git/refs/heads/main (1)` and `.git/refs/remotes/origin/main (1)` from 2026-05-12) that had been masking `git fetch origin` with "bad object refs/heads/main" errors for ~8 days. Likely macOS Finder/iCloud sync artifact. `git fsck` now clean apart from harmless dangling objects.
  - All carry-forward items from 2026-05-19 entry remain open: Landon sign-off on `Frame-Reddit-AEO-Playbook.html` + `Frame-AEO-Master-Play.html`; Supabase Functions ACTIVE confirm for `reddit-aeo-classifier` / `reddit-aeo-digest` / `youtube-script-drafter` / `aeo-citation-tracker` (digest validated 5/18); `aeo_distribution_queue` + `aeo_citation_log` migration mirrors as `supabase/migrations/*.sql`; remaining 9 innovation-audit gaps from `Frame-Innovation-Audit-2026-05-16.html`; revenue tile implementation by Codex (per `data/REVENUE-TILE-DESIGN-2026-05-17.md`); AEO Level-2 fixer model audit; SLV Phase B hero swaps × 5; SLV blog Posts #2–6; Closed-loop ROAS Phase 1; GTM container; `FrameAds.send_to`; form-abandonment fn; gallery expansion; refresh-stale fixer (now **13-day debt**); data/audits/ policy; Formspree wind-down; GBP manual post; GBP Products; density wave 4+.
- **Net effect:** The day converts the 2026-05-16 `aeo-citation-tracker` 0% Gemini baseline into a structurally-grounded falsifiable experiment with a 30-day clock, shipped to production via PR #58. Three artifacts hold the chain of evidence end-to-end: baseline sweep proves the gap (0/7 AIO blocks cite Frame), competitor audit identifies the candidate cause (visible-text-snippet pattern in Homer's cost page), edit proposal becomes the executed merged commit (`cdee7de`). The 26-query script (`aeo-aio-sweep.py` v1.2) locks in a measurement panel ready for the first post-merge run. The architecture-lane pivot off DataForSEO removes a recurring subscription decision from the critical path. The cost page is now in production with question-form title + concrete $ range in sentence one + visible 5-Q&A block — the clock on the hypothesis is running.


### 2026-05-19 — Master AEO Monday Digest Cron Validated (First Production Run 2026-05-18 — 10 Items Across 2 Surfaces) + No `main` Activity Since 2026-05-17
No new commits on `main` since the 2026-05-17 auto-refresh. Working tree state: same two design/handoff docs from 2026-05-17 still untracked (`data/SESSION-2026-05-17-hf-supply-chain-and-classifier-bench-handoff.md` + `data/REVENUE-TILE-DESIGN-2026-05-17.md`), same uncommitted `.gitignore` `.env*.local` add, and one **new** artifact: `data/aeo-master-digest-2026-05-18.html` — the output of the first production run of the upgraded `reddit-aeo-monday-digest` cron firing the new `aeo-master-digest` edge fn instead of the deprecated reddit-only digest. This validates end-to-end the unification layer shipped 2026-05-16 evening (commit 9467596).
- **First Monday Master Digest fired 2026-05-18.** Output dated `2026-05-19` inside the HTML body (likely written at run time on Monday 8:30am MDT, body-date stamp landed in next-day UTC window depending on render path). 10 actionable items selected from `aeo_distribution_queue`: **5 Reddit (r/Roofing)** + **5 YouTube** (top-per-surface picks per the digest spec). Reddit picks include high-aeo-score threads: "Should I ask this flashing to be fixed?" (score 100, no recommended URL — generic-helpful reply), "Roofing Sales rep questions" (score 84 → `/blog/utah/how-to-choose-a-roofer-utah.html`), "Considering weathered wood shingles" (score 75 → `/blog/utah/best-roofing-materials-utah.html`), "Not sure what to do. Can these panels be fixed?" (score 75 → `/blog/utah/best-roofing-materials-utah.html`), "Repair or remove skylights?" (score 70). Every Reddit draft contains the persona-voice signature `"I'm a contractor in the Wasatch Front"` and a Frame URL where `recommended_url` was set by the Phoenix classifier. Draft format intact: `<details>` collapsible with pre-formatted reply text per the digest HTML template.
- **What this closes from 2026-05-17 carry-forward:** ☑ "Confirm first Monday master digest fires 2026-05-18 8:30am via the upgraded `reddit-aeo-monday-digest` cron" — confirmed by the existence + correct shape of the output file. The 83-draft queue cooked on 2026-05-16 (65 Reddit + 18 YouTube) is now being drained at 10/week (5+5). At that cadence the cooked queue runs ~8 weeks before needing fresh upstream signals — long enough that the classifier-side cadence (Mac Monday 8:30am for the digest, but no documented refill cadence for new `reddit_signals` rows feeding the classifier) is the next thing to verify.
- **Still open (no movement today):**
  - **Landon sign-off** on `Frame-Reddit-AEO-Playbook.html` + `Frame-AEO-Master-Play.html` — both shipped 2026-05-16, both gated on owner approval before any `posted_to_reddit_at` rows can be stamped. The digest is producing drafts but no external posting is happening until sign-off.
  - **Verify deploys ACTIVE in Supabase Functions panel** for `reddit-aeo-classifier` / `reddit-aeo-digest` / `youtube-script-drafter` / `aeo-master-digest` / `aeo-citation-tracker` (digest run validates `aeo-master-digest` is ACTIVE; the other 4 still want a visual confirm in the panel).
  - **Migration files** for `aeo_distribution_queue` + `aeo_citation_log` — mirror as `supabase/migrations/*.sql` for repo parity (still applied via MCP only).
  - **Commit the 3 untracked `data/` docs + the `.gitignore` change** (now 2-day-old debt — `aeo-master-digest-2026-05-18.html` may be vercelignored worthy depending on whether each Monday's digest should live in git or in Supabase storage; default to vercelignored if committed).
  - All 9 remaining innovation-audit gaps from `Frame-Innovation-Audit-2026-05-16.html`.
  - All other carry-forward items from the 2026-05-17 entry remain open (revenue tile implementation by Codex; AEO Level-2 fixer model audit; SLV Phase B hero swaps × 5; SLV blog Posts #2-6; Closed-loop ROAS Phase 1; GTM container; FrameAds.send_to; form-abandonment fn; gallery expansion; refresh-stale fixer — now **12-day debt**; data/audits/ policy; Formspree wind-down; GBP manual post; GBP Products; density wave 4+).
- **Net effect:** Quiet maintenance day on the repo, but the AEO unification system shipped 3 days ago just proved itself in production. The path from `reddit-scanner.py` v5 → `reddit-aeo-classifier` (Gemini Flash, Phoenix-pattern multi-axis scoring) → `aeo_distribution_queue` → `aeo-master-digest` Monday cron → HTML artifact in `data/` is now end-to-end validated. The next compounding play remains owner-gated: until Landon signs off the playbook, the system can keep cooking drafts but nothing gets posted to Reddit, so `aeo_citation_log` will continue reading 0% against the 2026-05-16 baseline.


### 2026-05-17 — Supply-Chain Defense (HF Ollama-Pull Gate) + Reddit-Classifier 3-Way Bench (Gemini Flash Validated vs Qwen 3.6 27B vs Claude Haiku) + Track E v0 Revenue Attribution Tile Design Draft for /dashboard/
No new commits land on `main` today, but two substantive design/handoff artifacts ship to `data/` (untracked, to be committed next session) + a `.gitignore` edit. Day's arc: respond to the 2026-05-16 `/innovate Hugging Face` finding by hardening the Ollama-pull pipeline against HF supply-chain attacks AND bench-validate that Gemini Flash via OpenRouter is still the right choice for `reddit-classifier.py` vs the newer Qwen 3.6 27B. Parallel architecture lane drafts the Track E v0 revenue attribution tile for `/dashboard/` — the single artifact that turns Frame Utah's SoM gains (76 → 85) into a defensible $ claim for the growth retainer pricing.
- **`data/SESSION-2026-05-17-hf-supply-chain-and-classifier-bench-handoff.md` (untracked).** 3-way OpenRouter classifier bench (Gemini 2.0 Flash vs Qwen 3.6 27B vs Claude Haiku) over 50 rows of `reddit_signals`. Verdict: **keep `reddit-classifier.py` on Gemini Flash — no code change needed.** 91-98% inter-candidate agreement, Qwen ~10×/token + 14× slower p50 latency (10.77s vs 0.74s) for ~zero quality gain on short-prompt classification. Cost delta: Gemini Flash $0.075/M in + $0.30/M out vs Qwen $0.32/M + $3.20/M = ~3× per typical classification. All seven historical `aeo-v1`-tagged "leads" in `reddit_signals` confirmed as false positives (patio attachments, greenhouse builds, solar complaints, ALLATRA climate spam) — the current Gemini Flash prompt + Utah-gate are correctly downgrading them. Supply-chain trust gate installed: `verify-hf-model.sh` wrapper at `~/projects/local-llm-toolkit/scripts/`, `hfp <ref>` alias in `~/.zshrc`, 29-owner allowlist at `~/.config/hf-trust/allowlist.txt` (qwen, meta-llama, mistralai, deepseek-ai, anthropic, sentence-transformers, etc.). Tested live against the real `Open-OSS/privacy-filter` typosquat — gate caught all three signals (HF-disabled / 9d/9 likes shape / owner not in allowlist). One-line null-content fix landed in `~/projects/local-llm-toolkit/scripts/reddit-classifier.py:222` (defensive guard for `"content": null` from OpenRouter — previous code crashed instead of falling back; no behavior change). Bench results + per-row CSV saved at `~/Documents/bench-reddit-classifier-2026-05-16.{md,csv}`, reusable via `~/projects/local-llm-toolkit/scripts/bench-reddit-classifier.py` (~$0.05/run, 10 min) for future model-class changes. README "Before Pulling New Models" section added to local-llm-toolkit.
- **`data/REVENUE-TILE-DESIGN-2026-05-17.md` (untracked).** Track E v0 design draft for the `/dashboard/` revenue attribution tile. Frame Utah is the v0 prototype client; when validated, propagates to Frame TX + future cbp clients via Track D. Author: Claude (architecture lane). Implementer: Codex (TypeScript/SQL lane) when back from 5/19 outage. Goal: surface per-page revenue attribution so any dashboard viewer can answer (1) which pages produced won leads in last N days, (2) how much $ paid (realized) vs booked vs pipeline, (3) which traffic source drove each $ stream, (4) lead-to-pay ratio + avg time-to-pay — plus a 30-day forecast (jobs completing × margin). Scope v0: Frame Utah only, last-touch attribution (form-submit page OR tel:-clicked page), 8-bucket channel rollup (paid_search / paid_social / organic_google / organic_bing / llm_chatgpt / llm_perplexity / llm_gemini / llm_claude / llm_copilot / direct / other), 4 time windows (7/30/60/90d to match existing dashboard toggle), current + prior-period delta, `phone_clicks → leads` join for manually-created inbound-call leads.
- **`.gitignore` modified (uncommitted):** adds `.env*.local` — local env files now ignored (likely from prompt/credential work in the local-llm-toolkit lane spilling adjacent into this repo's worktree).
- **Net effect:** Two infrastructure-grade decisions get baked in without shipping code in this repo: (1) the production reddit-classifier stays on Gemini Flash with empirical backing (no impulsive Qwen swap), and (2) the dashboard's revenue tile gets a written design contract before implementation — so the Codex lane can pick it up on 5/19 without re-deriving the architecture. The supply-chain gate sits one level above this repo (in `~/projects/local-llm-toolkit`) but applies to any future Ollama-pull this project might trigger.
- **Open items rolling forward to next session:**
  - Check `scripts/aeo-fix-internal-links.py` classifier model — if still on `nemotron-mini:4b`, swap to Gemini Flash via OpenRouter (mirror `reddit-classifier.py:49-50`). If already Gemini Flash, no-op. Quick grep: `grep -n "MODEL\|model=\|nemotron\|gemini\|haiku" scripts/aeo-fix-internal-links.py`.
  - Optional cleanup (5 min): re-tag 7 `aeo-v1` false-positive rows in Supabase `reddit_signals` (`verdict in ('utah_lead','storm_signal') AND verdict_reason LIKE 'aeo-v1%'`) → `verdict='noise'`, `verdict_classifier='aeo-v1-rollup-2026-05-17'`. Removes pollution from Coworker pickup queries.
  - Standing rule: use `hfp <ref>` for any new Ollama HF pulls — bare `ollama pull hf.co/<x>/<y>:tag` is a supply-chain risk vector.
  - Commit the two untracked `data/` design docs + the `.gitignore` change next session (3 separate logical commits — supply-chain handoff, revenue tile design, gitignore env-local).
  - All carry-forward items from the 2026-05-16 (evening) Master AEO Play entry remain open (no commits on `main` today closed any of them): Landon sign-off on `Frame-Reddit-AEO-Playbook.html` + `Frame-AEO-Master-Play.html`; verify deploys ACTIVE in Supabase Functions panel for `reddit-aeo-classifier` / `reddit-aeo-digest` / `youtube-script-drafter` / `aeo-master-digest` / `aeo-citation-tracker`; confirm first Monday master digest fires 2026-05-18 8:30am via the upgraded `reddit-aeo-monday-digest` cron; migration files for `aeo_distribution_queue` + `aeo_citation_log` mirrored as `supabase/migrations/*.sql` for repo parity; Closed-loop ROAS Phase 1 (Google Ads OCI + Meta CAPI uploader); GTM container; `FrameAds.send_to`; SLV Phase B hero swaps × 5 (Holladay / Millcreek / Sandy / Draper / SLC); SLV blog calendar Posts #2-6; form-abandonment fn; gallery expansion; refresh-stale fixer (now 10-day-old debt); data/audits/ policy; Formspree wind-down clock; GBP manual post; GBP Products; density wave 4+; remaining 9 innovation-audit gaps from `Frame-Innovation-Audit-2026-05-16.html`.

### 2026-05-16 (evening) — Master AEO Play: Unified 8-Surface Distribution System (aeo_distribution_queue + aeo_citation_log tables + youtube-script-drafter + aeo-master-digest + aeo-citation-tracker edge fns + Monday Digest Cron Upgrade + Citation-Rate Baseline Measured 0% + 83-Draft Queue Cooked)
One commit (9467596, 17:09 MDT) builds the unification layer on top of the morning's Reddit-AEO Phoenix classifier. Thesis: one Frame pillar page should fan out automatically to every citation surface AI engines lean on (Reddit / YouTube / Nextdoor / Yelp / LinkedIn / GBP / local news / Quora). One Monday master digest unifies all the drafts so Landon sees a single top-of-week feed instead of one per surface. Architecture inspired by xai-org/x-algorithm Phoenix component: multi-axis classification → weighted scoring → candidate isolation → unified pipeline. Plug-and-play surface drafters: each new platform = one new edge fn + queue inserts.
- **9467596 — feat(aeo): Master AEO Play — unified 8-surface distribution system.** Ships seven artifacts to git (4,829 insertions): `Frame-AEO-Master-Play.html` (260 lines — strategy doc, architecture diagram, surface-coverage matrix, 90-day targets, the Landon ask, shareable as standalone HTML); `build-intelligence/customer-request-batch.json` + `customer-request-board.md` + `customer-request-ledger.json` (intake scaffolding for the build-intelligence loop); `build-intelligence/feedback-ledger.json` + `feedback-row.json` + `feedback-row.md` (untracked feedback shapes moved tracked per the AEO Play rollout). Backend (remote-only in Supabase, mirrors reddit-aeo-classifier deploy pattern): **2 new tables** — `aeo_distribution_queue` (unified queue across all surfaces, columns: priority_score / status workflow / expires_at / performance_data slot) + `aeo_citation_log` (measurement-loop foundation); **3 new edge fns** — `youtube-script-drafter` (converts pillar → 60-90s Landon-voice video brief: hook + talking points + shot list + voiceover + hashtags + thumbnail idea + YouTube description; 18 scripts already drafted from the storm-chaser pillar + 13 spokes), `aeo-master-digest` (top-per-surface picks across all queues, formats sms/markdown/html/json — replaces reddit-only digest), `aeo-citation-tracker` (baseline measurement: queries Perplexity Sonar + Gemini Flash + Claude Haiku with 20 Utah roofing prompts, parses for Frame citations, logs to `aeo_citation_log`). Migrated all **65 actionable `reddit_signals` rows → `aeo_distribution_queue`** (carries `aeo_score` + `recommended_url` + `reply_draft` + `persona_voice` from the morning's Phoenix classifier). Upgraded the `reddit-aeo-monday-digest` scheduled task to fire the master digest instead of reddit-only.
- **Baseline measured 2026-05-16:** `aeo-citation-tracker` returned **0% Frame citation rate** on the seed prompt "Best roofing company in Heber City Utah" (Gemini). Competitor "Premier Roofing" cited in Frame's place. This is the headline gap the system is designed to close — `aeo_citation_log` now has its first row to compare against future runs.
- **Queue state at close:** 83 drafts ready in `aeo_distribution_queue` (65 Reddit replies seeded from this morning's classifier + 18 YouTube scripts from youtube-script-drafter). **2+ months of weekly posting backlog cooked already** before Landon writes the first reply.
- **Net effect:** This morning's Phoenix classifier turned noise into actionable signals; the evening commit gives those signals a unified destination + a measurement loop. The Monday digest is now the single weekly artifact Landon sees — all surfaces, top-per-surface picks. New surfaces (Nextdoor / Yelp / LinkedIn / Quora) drop in as one-fn-each additions without changing the digest or the queue shape. The 0% baseline gives every future commit a measurable lift target. Companion to the morning's `Frame-Reddit-AEO-Playbook.html` (rules of engagement + persona guidelines) — both await Landon sign-off before any `posted_to_reddit_at` rows get stamped.
- **Carry-forward (still open from earlier today + prior sessions — not cleared by this commit):**
  - **Landon sign-off** on `Frame-Reddit-AEO-Playbook.html` AND `Frame-AEO-Master-Play.html` — both shipped, both gated on owner approval before any external posting fires.
  - **Verify deploys ACTIVE in Supabase Functions panel:** `reddit-aeo-classifier`, `reddit-aeo-digest`, `youtube-script-drafter`, `aeo-master-digest`, `aeo-citation-tracker`. Confirm first Monday master digest fires 2026-05-18 8:30am via the upgraded `reddit-aeo-monday-digest` cron.
  - **Migration files for `aeo_distribution_queue` + `aeo_citation_log`** — applied via Supabase MCP; mirror as `supabase/migrations/*.sql` files for repo parity (same pattern as the 2026-05-11 evening migrations).
  - All 9 remaining innovation-audit gaps from `Frame-Innovation-Audit-2026-05-16.html` (only #2/#3/#7 closed earlier today).
  - All other carry-forward items from the morning's session log entry remain open (Closed-loop ROAS Phase 1, GTM container, FrameAds.send_to, SLV Phase B hero swaps × 5, SLV blog calendar Posts #2-6, form-abandonment fn, gallery expansion, refresh-stale fixer, data/audits/ policy, Formspree wind-down clock, GBP manual post, GBP Products section, density wave 4+).

### 2026-05-16 — Innovation Audit (Strategic Diagnostic) + CRM Post-Won Lifecycle (8 New Lead Cols + lead-crm v6 + Job Pipeline Drawer) + Inbound Call/SMS Auto-Lead-Creation (handle-call v2 + handle-sms v5, 7 Historic Contacts Backfilled) + Tel: Twilio Tracking Plumbing (542 hrefs Across 124 Pages) + Review-Request v2 (Filter Fix Unblocks Daily Cron) + Reddit-AEO Phoenix-Pattern Classifier (12 New Cols + frame_content_inventory + 2 Edge Fns + Monday Digest Cron + 254 Rows Reclassified)
Five commits land on `main` between 11:33 MDT → 13:20 MDT, all authored under the `review-bot@frameroofingutah.com` automation identity (direct-to-main code-sync from this session's Supabase MCP work). Day's arc: ship the diagnostic first (innovation audit pulls live state from Supabase + scheduled tasks + edge fns + recent commits, surfaces 12 gaps + sequenced fix stack), then close the three highest-leverage gaps in the same session (#3 inbound calls/SMS bypassing the CRM, #2 phone-click attribution loop structurally severed, #7 review-request agent DOA on impossible filter status). Companion play in the afternoon ships a multi-class Reddit signal classifier inspired by xai-org/x-algorithm's Phoenix multi-action-prediction pattern — reclassifies all 254 historic reddit_signals rows from "0 leads, 254 noise (0 actionable)" → "65 actionable across 4 categories." Coherent compounding theme: turn enterprise-grade infrastructure already built into actual throughput.
- **06ee582 — docs: innovation audit 2026-05-16 (real state inventory + 12 gaps + sequenced stack).** Strategic-thinking artifact (NOT action). New `Frame-Innovation-Audit-2026-05-16.html` (272 lines, single file). Pulls live state from Supabase (`6 leads, 0 phone_clicks 4d post-launch, 0 won deals with margin, 254 reddit_signals 0 actioned, 8 inbound calls 5 SMS none becoming leads`), `scheduled_tasks` (13 active), edge fns (11), recent commits. Cross-references to memory. Surfaces the structural diagnostic: enterprise-grade infrastructure, anemic throughput. 12 gaps surfaced + sequenced fix stack. Live at `/Frame-Innovation-Audit-2026-05-16` (cleanUrls). Three of the highest-leverage gaps (#2, #3, #7) get closed in this same session — see the three commits below.
- **8a8063f — feat(crm): inbound calls + SMS auto-create lead rows (closes audit gap #3).** `handle-call` v2 — when an unknown phone calls the Twilio tracking number, auto-creates a `leads` row (`source_page=inbound-call`, `tier=general`) and stamps `call_logs.lead_id` so the CRM can show the call thread on the lead detail. `handle-sms` v5 — mirrors handle-call behavior for first inbound SMS from an unknown number; captures the first message body as `leads.message` so Ryan sees customer intent immediately. Both check `leads.phone` for a match in the prior 90 days before creating to avoid duplicates. Normalizes phone to E.164 (+1XXXXXXXXXX). Skips messages from Landon (operator commands). **Backfilled 7 historic inbound contacts** (6 calls + 1 SMS) into the `leads` table — the cold-pitch SEO solicitation from +18333380180 auto-classified spam. Saves the source for `handle-call` / `handle-sms` / `review-request` locally so future edits can be diffed against repo state. Also commits the `build-intelligence/` scratch dir scaffolding (build-intelligence-loop.md, contractor-growth-category-knowledge-map.md, implementation-queue.md + paired JSON shapes; vercelignored).
- **72b09fb — fix(tracking): route every site tel:/sms: through Twilio tracking number (closes audit gap #2).** Root cause: every `tel:` link on the live site dialed `+14353024422` (Landon's direct cell). Customer calls from the website bypassed Twilio entirely. Result: `call_logs` only captured GBP / directory calls — never the site; `phone_clicks` tracker (PR #55, 5 days ago) fired on click but the actual call disappeared, leaving the attribution loop structurally severed and 0 `phone_clicks` rows. Fix: swap every `<a href="tel:+14353024422">` to `tel:+14352928802` (Twilio). **542 swaps across 124 live pages.** Visible branded display text "(435) 302-4422" preserved everywhere — only the `href` changes. Customer experience: identical. Customer dials 435-292-8802 → Twilio → forwards to Landon's cell via `handle-call` TwiML → Landon's phone rings showing the Twilio caller ID. The call is now logged + auto-creates a lead via `handle-call` v2 (paired play from the 8a8063f commit 4 minutes earlier). Idempotent helper: `scripts/swap-tel-to-twilio.py` (safe to re-run if any new page lands with a 302-4422 href). **Plumbing finally connected to faucet.** Updates the CLAUDE.md "Phone numbers" critical rule: visible/branded NAP = 302-4422 (display only); every `tel:`/`sms:` href = 292-8802 (Twilio tracking + 10DLC SMS).
- **1b95df5 — feat(crm): post-won workflow + unblock review-request agent (closes audit gap #7).** `leads` schema gains 8 new lifecycle columns: `deposit_received_at`, `deposit_amount`, `install_scheduled_for`, `job_started_at`, `balance_due`, `contract_url`, `warranty_doc_url`, `product`. `job_completed_at` already existed but was dormant. Greg Atkinson lead backfilled: deposit $10k on 2026-05-09, balance_due $108k, product Brava Aged Cedar. `lead-crm` **v6**: `?action=update` accepts the 8 new lifecycle fields; `?action=list` returns them. `/leads` dashboard gains a new "Job pipeline" sub-panel inside the edit drawer that only renders when `status=won` — keeps the form lean for new leads. Shows deposit/install/balance/start/complete dates inline and a "Review request status" line stating exactly when the auto-fire will go. `review-request` edge fn **v2**: was previously DOA filtering on `status='completed'` (impossible — nothing in the pipeline ever set that value); v2 now filters on `status='won' AND job_completed_at IS NOT NULL +3d`. Verified GET returns "No review requests due" cleanly with the new query shape. Daily cron will actually do something now.
- **c957cfe — feat(reddit-aeo): multi-class classifier + AEO digest + playbook (Phoenix pattern).** Inspired by xai-org/x-algorithm's Phoenix multi-action prediction: predict independent probabilities per axis, weighted-sum into `aeo_score`. `reddit_signals` schema gains 12 new columns (`aeo_score`, `p_utah_lead`/`p_aeo`/`p_competitor`/`p_storm`/`p_spam`, `recommended_url`, `reply_draft`, `persona_voice`, `digest_sent_at`, `posted_to_reddit_at`, `posted_url`). New `frame_content_inventory` table seeded with 21 Frame pillar/spoke URLs + `topic_tags` + `question_patterns` for classifier URL matching. New edge fn `reddit-aeo-classifier` (LLM-powered, `gemini-2.0-flash`). New edge fn `reddit-aeo-digest` (`sms` / `markdown` / `json` / `html` formats). New scheduled task `reddit-aeo-monday-digest` (Mac, Monday 8:30am). `reddit-scanner.py` bumped to v5 with hard-blocklist for garbage spam subs. **Reclassified all 254 historic rows:** before (old classifier) 0 leads / 254 noise (0 actionable); after (Phoenix pattern) 6 `utah_lead` + 55 `aeo_opportunity` + 3 `competitor_intel` + 1 `storm_signal` + 150 noise + 39 spam — **65 actionable signals were always there**, the old classifier threw them away. Playbook doc shipped at `/Frame-Reddit-AEO-Playbook.html` covering strategy, rules of engagement, persona guidelines, measurement framework, 90-day targets, and the Landon sign-off ask. Shareable as a standalone HTML.
- **Net effect:** The day's commits convert the diagnostic surfaced at 11:33am into an executed fix stack by mid-afternoon. The `tel:` → Twilio plumbing fix (542 hrefs across 124 pages) reconnects the attribution loop that PR #55's click tracking depends on — `phone_clicks` should start populating from organic website traffic immediately, and every unknown caller now becomes a `leads` row automatically via `handle-call` v2 (paired commit). The CRM gains a real post-won lifecycle (8 new lead columns + drawer sub-panel only visible when relevant) and finally unblocks the daily review-request cron (was DOA on an impossible filter). The Reddit-AEO Phoenix-pattern classifier converts 254 rows of noise into 65 actionable signals — 55 of which are AEO opportunities Frame's content moat can address (Q&A pattern → recommended_url against `frame_content_inventory`'s 21 seeded URLs). Three audit gaps closed in 3 hours; the audit itself remains live as the rolling diagnostic for the remaining 9.
- **Carry-forward (rolling, still open from 2026-05-12 and earlier — not cleared today):**
  - **Closed-loop ROAS Phase 1** — wire actual Google Ads Offline Conversion Import + Meta CAPI uploads (gclid/fbclid + won_at captured; partial indexes ready; uploader job not written).
  - **GTM container** + `track-attribution.js` site-wide rollout — script live; container config still needs creation in Tag Manager per `SETUP-GTM-CONTAINER.md`.
  - **`FrameAds` Google Ads conversion config** — `window.FrameAds.send_to` unset pending Landon's Google Ads conversion action creation. (Now more urgent post-72b09fb — call tracking unblocked.)
  - **`phone_clicks` table + `/track-click` edge fn** — verify migration file exists in `supabase/migrations/` matching the live DB shape (applied via Supabase MCP, not `supabase db push`). Watch for the first organic rows now that tel: hrefs route through Twilio.
  - **`reddit-aeo-classifier` + `reddit-aeo-digest` deploy verification** — both committed; confirm ACTIVE in Supabase Functions panel + first Monday digest fires 2026-05-18 8:30am.
  - **Landon sign-off on `Frame-Reddit-AEO-Playbook.html`** — playbook shipped, but the rules-of-engagement + persona guidelines need owner approval before any `posted_to_reddit_at` rows get stamped.
  - **9 remaining innovation-audit gaps** — `Frame-Innovation-Audit-2026-05-16.html` lists the full set; only #2/#3/#7 were closed today.
  - **SLV Phase B hero swaps** (Holladay / Millcreek / Sandy / Draper / SLC) — CH done in PR #47, 5 cities remaining. Tracker: `data/SLV-LOCATION-WAVE-TRACKER.md`.
  - **SLV blog calendar Posts #2-6 drafts** — hero photos pre-staged (b017481, 2026-05-11); queue: CH mountain-modern, Holladay Mt. Olympus, SLC East Bench, Sandy East Bench, Millcreek+Murray mid-tier.
  - **Phase 1 form-abandonment recovery** Supabase scheduled fn — `form_start` emits site-wide (PR #42); fn itself not yet written.
  - Gallery expansion (5 more photos + 3 process videos + "Inside a Frame Install" sub-section) — deferred from PR #40.
  - **Level-3 AEO `refresh-stale` fixer** (`scripts/aeo-fix-refresh-stale.py`) — now **9-day-old debt** (uncommitted since 2026-05-07).
  - `data/audits/` raw Lighthouse JSON commit policy still open.
  - Formspree (account `meeroaqa`) — retired but not yet deleted; ~30 days as rollback (clock 2026-05-10, ~24 days into window).
  - Manual publish of GBP service-area-expansion post (`data/GBP-POST-PUBLISH-READY-2026-05-11.md`) — still needs Landon or Ryan at the GBP browser.
  - GBP **Products section** still empty in customer view — Landon photos to add.
  - **Density audit wave 4+** — `data/content-density-audit-2026-05-11.{json,md}` lists remaining LOW pages queued for waves 4+.
  - **Untracked feedback artifacts in `build-intelligence/`** (feedback-ledger.json, feedback-row.json, feedback-row.md) — left untracked intentionally per the build-intelligence loop pattern.

### 2026-05-12 (mid-morning) — Storm-Chaser Pillar (2,836-word disambiguation post) + 10 Q&A llms.txt Append + 6 Standalone Service JSON-LD Nodes + 13-Spoke Hub-Spoke Integration + Schema Freshness Bump
Two PRs land back-to-back on `main` 09:54 → 10:01 MDT, both authored by Rconman99 (squash-merge from feature branches). Coherent compounding play: ship a high-leverage pillar post that closes the largest AI fan-out gap competitors won't write, then immediately wire hub-spoke internal linking so every existing storm-cluster page gets a clear PageRank pipe to the pillar. Companion plays bake in the 2026-05-12 Substack query-fan-out research (`feedback_canonical_entrypoint_pattern`) at three more surfaces: llms.txt direct-answer expansion, schema entity-split on the homepage tiles, and freshness/accuracy fixes that close stale drift carried over from earlier rebrand layers.
- **PR #56 / d03cda7 — feat(aeo,content): storm-chaser pillar + 10 Q&A llms.txt + 6 standalone Service nodes.** Three compounding plays in one squash:
  - **NEW post `blog/utah/local-roofer-vs-storm-chaser-utah.html`** (2,836 words, 322 lines). Pillar closes 4 high-value AI fan-out gaps competitors won't write — storm chaser vs local roofer, red flags during door knock, 8 documents every legitimate roofer can produce in 5 minutes, national-franchise disambiguation. BlogPosting + FAQPage (6 Q&As) JSON-LD with `digitalSourceType: humanWritten`. **First use of `heber-custom-reroof-front-view.webp` as og:image** (matches the pillar card thumbnail wired in PR #57 — unified visual identity across hub + spoke + index). EEAT credentials in opening 100 words (Andrew Best pattern). Primary sources cited: Utah DOPL license-lookup, Utah Attorney General, Utah Insurance Department. Dual-number CTA respects the 2026-04-24 10DLC rule (call 302-4422 / text 292-8802 — never swap). Sticky-call bar inherited from global template.
  - **llms.txt — 10 Q&A blocks appended** (30-50 words each, direct-answer pattern for AI verbatim extraction): storm chaser disambiguation, hire checklist, hail payout averages, insurance denial process, ACV vs RCV, photo documentation, wind vs hail, tear-off vs overlay, permit-by-city, seasonality. **Hours-drift fix** in the same file: Mon-Sat 8AM-6PM → Mon-Fri 8AM-7PM / Sat 8AM-6PM (matches 2026-05-11 CLAUDE.md correction + index.html openingHoursSpecification fix in 23ae38a). AI usage guidance clarified for the dual-number 302-4422/292-8802 split. +35 lines net.
  - **index.html — 6 standalone `@type:Service` JSON-LD nodes** for each homepage tile (Residential / Commercial / Storm / Water-Fire / Solar / General Contracting). Previously nested as `Offer/Service` inside `hasOfferCatalog` — extracted to top-level so each service is independently rich-result eligible and an AI-citable discrete entity. +91 lines.
  - **sitemap.xml — new pillar registered**, lastmod 2026-05-12, priority 0.8. +6 lines (likely a `<url>` block).
  - Cumulative: 4 files / +452 lines / -2 lines.
- **PR #57 / 6e17d96 — feat(content): integrate storm-chaser pillar — 13 storm-cluster spokes + blog index card + freshness bump.** Hub-spoke integration for PR #56's pillar. Every existing storm/hail/wind blog post gets a clear PageRank pipe to the disambiguation guide.
  - **13 city/statewide storm-cluster blog posts** gain a red-tinted `.storm-chaser-warning` aside with explicit anchor text "Local Utah Roofer vs Storm Chaser guide" linking to the pillar: `blog/bountiful/davis-county-wind-damage-guide`, `blog/draper/insurance-claims-storm-damage`, `blog/herriman/hail-damage-new-homes`, `blog/holladay/storm-damage-roofing-holladay`, `blog/lehi/hail-damage-roof-repair`, `blog/murray/storm-damage-roofing-murray-utah`, `blog/ogden/wind-damage-roof-repair`, `blog/provo/storm-damage-roof-repair`, `blog/riverton/hail-damage-roof-inspection`, `blog/salt-lake-city/storm-damage-roofing-salt-lake-city`, `blog/sandy/spring-hail-damage-roof`, `blog/utah/april-2026-wasatch-storm-roof-checklist`, `blog/utah/utah-hail-season-roof-guide-2026`. **Template variance:** 11 posts use the `</article>` insertion point, 2 (ogden + riverton) use the `div.blog-cta` marker — script-aware insertion handles both.
  - **blog/index.html — pillar card inserted at TOP of the post grid** (above the SLV flagship and the quote-checklist 2026-05-09 spoke). Uses `heber-custom-reroof-front-view` thumbnail (matches pillar's og:image — unified visual identity). +10 lines.
  - **index.html schema freshness + accuracy bump:** LocalBusiness JSON-LD `dateModified` bumped 2026-04-24 → 2026-05-12 (reflects today's schema + content changes), description corrected "40+ communities" → "45 communities" matching the actual location-page count (catches drift the 2026-05-11 hero-badge "40+ → 45+" PR #41 didn't fully close — that one touched only the hero badge, this catches the schema description).
  - Cumulative: 15 files / +55 lines / -4 lines. Spoke-side diffs are uniform 3-line `.storm-chaser-warning` insertions; provo + lehi run +5/-1 because their existing intra-article CTA had to be repositioned cleanly.
- **Net effect:** Frame's storm-cluster content moat now has a canonical entry-point pillar that's simultaneously (a) discoverable via 13 internal-link paths from existing high-intent storm/hail/wind pages, (b) elevated in the blog index grid, (c) registered in the sitemap for crawl priority, and (d) backed by ~15 of the 22 universal local-service AI-fan-out gaps closed in a single 2,836-word entity. The llms.txt expansion gives LLM crawlers 10 fresh direct-answer Q&A passages in addition to the prior 10 (Q&A count effectively doubled in one commit). Six homepage service tiles now appear as independent `@type:Service` schema entities — addressable, citable, individually rich-result eligible. Hours-drift and city-count drift get one more closing pass (llms.txt + LocalBusiness description). All three plays compound on the 2026-04-22 + 2026-04-24 AEO foundation laid earlier (FAQPage on services, SpeakableSpecification, llms.txt markdown polish).
- **Carry-forward (rolling, still open from 2026-05-11 evening — none cleared by today's commits):**
  - **Closed-loop ROAS Phase 1** — wire actual Google Ads Offline Conversion Import + Meta CAPI uploads (gclid/fbclid + won_at captured; partial indexes ready; uploader job not written).
  - **GTM container** + `track-attribution.js` site-wide rollout — script live; container config still needs creation in Tag Manager per `SETUP-GTM-CONTAINER.md`.
  - **`FrameAds` Google Ads conversion config** — `window.FrameAds.send_to` unset pending Landon's Google Ads conversion action creation.
  - **`phone_clicks` table + `/track-click` edge fn** — verify migration file exists in `supabase/migrations/` matching the live DB shape (applied via Supabase MCP, not `supabase db push`).
  - **`lead-crm` v5 deploy** — verify ACTIVE in next session.
  - **SLV Phase B hero swaps** (Holladay / Millcreek / Sandy / Draper / SLC) — CH done in PR #47, 5 cities remaining. Tracker: `data/SLV-LOCATION-WAVE-TRACKER.md`.
  - **SLV blog calendar Posts #2-6 drafts** — hero photos pre-staged (b017481, 2026-05-11); queue: CH mountain-modern, Holladay Mt. Olympus, SLC East Bench, Sandy East Bench, Millcreek+Murray mid-tier.
  - **Phase 1 form-abandonment recovery** Supabase scheduled fn — `form_start` emits site-wide (PR #42); fn itself not yet written.
  - Gallery expansion (5 more photos + 3 process videos + "Inside a Frame Install" sub-section) — deferred from PR #40.
  - **Level-3 AEO `refresh-stale` fixer** (`scripts/aeo-fix-refresh-stale.py`) — now **6-day-old debt** (uncommitted since 2026-05-07).
  - `data/audits/` raw Lighthouse JSON commit policy still open.
  - Formspree (account `meeroaqa`) — retired but not yet deleted; ~30 days as rollback (clock 2026-05-10).
  - Manual publish of GBP service-area-expansion post (`data/GBP-POST-PUBLISH-READY-2026-05-11.md`) — still needs Landon or Ryan at the GBP browser.
  - GBP **Products section** still empty in customer view — Landon photos to add.
  - **Density audit wave 4+** — `data/content-density-audit-2026-05-11.{json,md}` lists remaining LOW pages queued for waves 4+.

### 2026-05-11 (evening) — Closed-Loop ROAS Phase 0: Attribution Capture Site-Wide + Phone/SMS Click Tracking + Commission Rule Migration (5% Heber/Midway, 10% else, off MARGIN) + CRM Polish (Add Lead, Address Field, Spacer Hotfix) + Mobile Exit-Intent CRO + Density Wave 3 + CH Hero Swap (SLV Phase B #1) + SLV Wave Tracker + Cowork Handoff Doc
Eleven commits land on `main` between 16:16 MDT → 19:47 MDT, spanning a ~3.5-hour late-afternoon-into-evening session that closes both the ad-attribution loop (Phase 0 of the closed-loop ROAS engine) and the CRM-write loop (Ryan's commission rule + manual lead entry for offline contracts/walk-ins). Authors split: PRs #39/#47/#49/#50/#51/#52 land under Ryan's GitHub identity (squash-merges from feature branches), the four post-#52 commits (71eef60 / 6559fcf / f895a83 / 459bfda / 67caae9) land under the `review-bot@frameroofingutah.com` automation identity (direct-to-main code sync from tonight's edge-fn deploys).
- **PR #39 / 6472b2c — feat(attribution): wire gclid/fbclid/utm capture + thank-you redirect site-wide.** Phase 0 of the closed-loop ROAS engine. New `/track-attribution.js` captures ad click IDs (`gclid` / `fbclid` / `msclkid` / `gbraid` / `wbraid`) into 90-day localStorage + UTM params (first-touch sessionStorage) + `landing_page` + `referrer` on every page load. Hero/lead/modal forms merge the attribution payload into `fetch` body before submit, then redirect to `/thank-you` on success instead of inline-replacing the form card. Site-wide injection: `<script src="/track-attribution.js" defer></script>` added to `<head>` of **130 HTML pages** (composes cleanly above the prior SLV `fetchpriority` + mid-page CTA work from PRs #37/#38). `handle-lead` edge fn already accepts the new fields server-side (committed prior session, v8). Migration `20260510_add_attribution_columns.sql` already in repo; apply via `supabase db push --project-ref hdcflshhomzildwqlmwh` before deploying the edge function update. Unblocks Google Ads Offline Conversion Import + Meta CAPI uploads in Phase 1. Docs landed alongside: `ADS-LAUNCH-AND-OPTIMIZATION-PLAYBOOK.md` + `LANDON-LAUNCH-CHECKLIST.md` + `SETUP-GTM-CONTAINER.md` + `DEPLOY-attribution.md` (all `*.md`-vercelignored, not deployed publicly).
- **PR #47 / 701e75a — Density refresh wave 3 (Heber + Sandy + Layton 3 LOW → GOOD) + Cottonwood Heights hero swap (SLV Phase B PR #1).** Two-part squash: (a) **CH hero swap** — replaces legacy `frame-restoration-03.webp` (generic stone-home placeholder, zero CH signal) with authentic Cottonwood Heights iPhone 13 capture (golden-hour alpenglow on Mt. Olympus + snow-capped Wasatch foothills, neighborhood roofs foreground). Center-cropped 16:9 from 4032×2268 master so power lines + chain-link fence drop out; Mt. Olympus dome remains unmistakable. 9 asset files (AVIF 18-57 KB, WEBP 63-225 KB, JPG 150-532 KB across 800/1200/1600 widths). `locations/cottonwood-heights.html` touches: 4 lines (img tag + og:image meta). Honest alt framing: "east-bench SLV homes Frame Roofing Utah reroofs and repairs across CH ZIP codes 84047, 84093, and 84121" — does NOT claim visible roofs are Frame's work (no CH project completed yet; when Landon books one + Drive-pulls a real install aerial, this gets demoted to atmospheric supporting position). (b) **Density refresh wave 3** — surgical stat-wrap + outbound citation pass on Heber `seasonal-roof-maintenance.html` + Sandy `roof-leak-repair-sandy-84070.html` + Layton `emergency-roof-tarping.html`, moving 3 LOW-density blog spokes → GOOD per content-density audit metrics. 5 of 6 remaining SLV pages still pending Phase B Drive-pull swaps.
- **PR #49 / d1d5f06 — chore: add SLV location-page wave tracker doc.** New `data/SLV-LOCATION-WAVE-TRACKER.md` (90 lines) captures live state of the 6-page SLV rebuild kicked off 2026-05-11. Tracks Phase A foundation (PRs #37/#38 merged), Phase B hero swaps (CH done in PR #47, 5 cities remaining), asset inventory in `/images/projects/cities/`, and the hard rules gating every SLV PR (no drone-inspection language, og:image must match img src, surgical 2-line diff, 9 image files per page). Vercelignored via `*.md` glob. Lives separately from CLAUDE.md so it can be edited frequently without bloating master context. Pattern matches earlier-this-week `data/COWORK-HANDOFF-*` + `data/BLOG-CALENDAR-*` operational docs.
- **PR #50 / 1fb0a4c — feat(cro): mobile exit-intent slide-up with scroll-reversal trigger.** Closes mobile-side gap on lead recovery — desktop has `mouseleave-top` exit intent baked into browsers, mobile has nothing. One-shot per-session slide-up at the bottom of phone-sized viewports (`≤820px` via `matchMedia` gate) when the user scrolls past 40% depth and then reverses direction by ≥80px within 600ms ("read enough, leaving" gesture). Backstop on `visibilitychange→hidden` + `pagehide` for tap-and-leave users. Sits above the dual `.sticky-call` bar (`bottom: 56px` when present, no overlap). Two CTAs: gold `☎️ Call now` (`tel:+14353024422`) + navy outlined `Free inspection` (reuses existing `openModal()` — same code path as nav CTA + hero form + modal trigger). Close × dismisses + records `exit_intent_dismiss`. ARIA: `role=dialog` + `aria-hidden` state + `aria-label`. Suppressed on `/thank-you`, `/privacy`, `/terms`, `/review` paths + after any form-submit this session (sessionStorage `fr_lead_submitted` flag). Analytics: PostHog + dataLayer events `exit_intent_show` / `_call` / `_form` / `_dismiss`. All inlined into `global-modal.js` (+184 lines) so it deploys site-wide with no new asset request; style block injected once on init, scoped to `.fr-exit-intent`. Scroll-velocity-reversal thresholds (80px / 600ms / 40%) tuned to reject normal scroll-up reading and trigger only on the "back-button-with-fingers" gesture. Re-uses existing `closeModal()` / `openModal()` — zero net new modal logic.
- **PR #51 / 774c18c — docs(handoff): cowork handoff 2026-05-11 (evening session close).** New `data/COWORK-HANDOFF-2026-05-11.md` (150 lines) captures the day's full ship sequence + open carry-forwards for any agent picking up the session in the morning. Matches the Frame-TX naming convention (`COWORK-HANDOFF-{YYYY-MM-DD}.md`). Lives in `data/`, vercelignored. The PR description bundles the exit-intent feature summary (PR #50 already-merged content re-quoted as session context — not a re-deploy).
- **PR #52 / 12473a9 — feat(forms): add optional Street Address field to hero/lead/modal forms.** Closes lead-location-tracking gap — ZIP gives city/area but not the home, so adding an optional Street Address lets Landon roll the inspection truck to the right address on first call without a separate "what's your address?" callback step. Placement: between **Mobile Phone** and **ZIP Code** on all 3 primary funnels (`index.html #heroForm`, `index.html #leadForm`, `global-modal.js #frModalForm`). Optional — preserves PR #10's prior 6→3-required-field CRO win. Wiring: `handle-lead` v8 already destructures `address` (line 359) + falls back to `city + zip` when missing (line 401), so **no edge fn change** needed; Supabase `leads.address` column already exists, **no migration needed**. `autocomplete="street-address"` enables mobile autofill. Hero subhead copy bumped: "3 fields, 30 seconds (address optional)" so users see the required-field count is unchanged. Touches `global-modal.js` (+1 line) + `index.html` (+6 lines net).
- **71eef60 — feat(crm): apply Ryan's commission rule — 5% Heber/Midway, 10% else, off MARGIN.** Source-code sync for tonight's commission migration + `lead-crm` v4 deploy (direct-to-main; not a PR). **Rule (Ryan 2026-05-11):** Heber City or Midway → 5% commission; everywhere else → 10% commission; base is **MARGIN** (profit), not `job_value` (revenue/build). **DB migration** applied tonight via Supabase MCP (already executed against `hdcflshhomzildwqlmwh` before commit): `ALTER TABLE leads ADD COLUMN margin numeric (nullable)` + `ADD COLUMN city text (nullable)` + DROP/ADD `commission numeric GENERATED ALWAYS AS (CASE WHEN lower(trim(coalesce(city,''))) IN ('heber','heber city','midway') THEN coalesce(margin,0)*0.05 ELSE coalesce(margin,0)*0.10 END) STORED`; plus DROP/CREATE `public.website_leads_report` view (depends on commission; recreated identical column shape, no downstream impact). **Edge fn** (`supabase/functions/lead-crm/index.ts`, deployed as **v4 ACTIVE**): LIST select returns `margin` + `city` in row shape; UPDATE patch accepts `margin` (numeric) + `city` (text); removed `commission` write entirely (replaced by NOTE comment — GENERATED column rejects writes with PG `428C9`). **Dashboard** (`leads.html`): drawer form replaces "Your 10% commission" input with two new fields — **Margin ($)** + **City** — plus a "Commission (auto)" read-only display that recalculates live from `margin × city-aware rate`. `openPanel` populates margin + city from the lead, calls `recalcCommission` to render display; `save()` body sends margin + city instead of commission. KPI rollup `commMtd` no longer falls back to flat 10% × `job_value` when commission is null (that fallback produced wrong-shape numbers under the new rule); now null commission contributes $0 to monthly total — filling in margin per won lead fixes the rollup retroactively. Verified end-to-end before commit: `margin=10000` + Heber/Midway → `commission=500` ✓; `margin=10000` + Holladay → `commission=1000` ✓; `margin=10000` + NULL city → `commission=1000` ✓ (default 10%).
- **PR #55 / 6559fcf / f895a83 — Track `tel:` / `sms:` clicks site-wide with source attribution + `/leads` panel.** New `track-clicks.js` (158 lines) auto-instruments every `tel:` / `sms:` link on the page. On click: (1) PostHog event (`phone_click` or `sms_click`); (2) Beacon POST to `/track-click` edge fn (persists to new `phone_clicks` table); (3) Google Ads `gtag` conversion (gated by `window.FrameAds.send_to` config); (4) `dataLayer.push` for GTM. Beacon-based delivery survives dialer page-nav on mobile. Depends on `track-attribution.js` loading first (`window.FrameAttribution.get()` — exposed by PR #39) — falls back gracefully if not present. **New edge fn** `supabase/functions/track-click/index.ts` (112 lines) persists clicks. **New table** `phone_clicks` with full attribution (`gclid` / `fbclid` / `utm_*` / `referrer` / `source`). **Source classifier** maps referrer + URL params into 6 buckets: `gbp` / `organic_google` / `google_ads` / `bing` / `social` / `direct`. **`lead-crm` v5** gains `?action=clicks` endpoint returning 30-day totals + by-source breakdown. **`leads.html`** gains a new "Phone & SMS Clicks" panel with recent feed (matches existing CRM styling). Injected `/track-clicks.js` into **122 live pages** (all locations + blog + services + root + dashboard public) via new `scripts/inject-track-clicks.py` (81 lines). New `scripts/content-density-audit.py` (363 lines) + `data/content-density-audit-2026-05-11.{json,md}` (1567 + 89 lines) landed in the same squash — provides the LOW/GOOD density classification that drove PR #47's wave-3 pick.
- **459bfda — fix(leads): toolbar 'spacer' was rendering as a spinner.** Hotfix: `.sp` class was overloaded — used as both flex-spacer in the `/leads` filter toolbar AND loading spinner. The toolbar override set `flex:1` without resetting the border + animation, so a gold spinning circle was wedged between "Lost" and "Tier" filter chips. Rename the toolbar spacer to `.spacer`. 2-line touch in `leads.html`.
- **67caae9 — feat(crm): Add Lead form on /leads dashboard.** New `action=create` endpoint on `lead-crm` edge fn (validates + stamps `won_at` when `status=won`). Gold "+ Add Lead" button in `/leads` header. Slide-in panel mirrors edit-panel style — name (required) + phone + email + address + zip + city + status + tier + job_value + margin + notes. On save: refreshes list, auto-opens the new lead for further edits. Backdrop click closes whichever panel is open (Add or Edit). Use case: offline-signed contracts (Greg Atkinson 5/7 reference) + walk-ins + phone-only leads that bypass the website form. Touches: `leads.html` (+115 lines), `supabase/functions/lead-crm/index.ts` (+63 lines).
- **Net effect:** Phase 0 of the closed-loop ROAS engine is now end-to-end live — every page captures `gclid`/`fbclid`/UTMs into 90-day localStorage, every form submit merges that payload into the `handle-lead` body + redirects to `/thank-you` (unblocking GCLID upload to Google Ads Offline Conversions and FBC/FBP upload to Meta CAPI in Phase 1), every `tel:`/`sms:` click is logged with source attribution and surfaced in the `/leads` CRM with a 6-bucket source breakdown, and the `/leads` CRM itself is now a full read+write surface (status / tier / margin / city / job_value / notes / commission-auto + manual lead creation for offline contracts). Ryan's actual commission rule (5% Heber/Midway, 10% else, off margin) is encoded as a Postgres `GENERATED` column so every lead's commission is mechanically correct without dashboard math. Mobile CRO gets its first dedicated exit-intent surface (scroll-reversal trigger, one-shot per session, suppression rules + form-submit guard). The 6-page SLV location-page wave moves from "Phase A foundation done" → "Phase B in flight" with Cottonwood Heights becoming the first city with both the perf foundation (PR #37) + mid-page CTA (PR #38) + an authentic real-Landon hero (PR #47). Density audit wave 3 takes 3 more low-density blog posts to GOOD. Operational docs land for both the in-flight SLV wave (`SLV-LOCATION-WAVE-TRACKER.md`) and the day's session close (`COWORK-HANDOFF-2026-05-11.md`).
- **Carry-forward (rolling, still open):**
  - **Closed-loop ROAS Phase 1** (deferred from PR #39 description) — wire actual Google Ads Offline Conversion Import + Meta CAPI uploads, using the now-captured `gclid` / `fbclid` / `won_at` + the partial indexes seeded in `20260510_add_attribution_columns.sql`. Per-lead `uploaded_to_google_ads` + `uploaded_to_meta_ads` boolean columns already exist; uploader job not yet written.
  - **GTM container** + `track-attribution.js` site-wide rollout — script is live (PR #39); GTM container config (per `SETUP-GTM-CONTAINER.md`) still needs to be created in the Tag Manager account.
  - **`FrameAds` Google Ads conversion config** — `track-clicks.js` looks for `window.FrameAds.send_to` (Conversion ID + Label) but the value isn't set in any page yet; pending Landon's Google Ads conversion action creation.
  - **`phone_clicks` table** + **`/track-click` edge fn** were deployed via Supabase MCP tonight; source commit landed but apply was via direct MCP (not `supabase db push`). Verify migration file exists in `supabase/migrations/` matching the live DB shape.
  - **`lead-crm` v5 deploy** — edge fn source updated for `?action=clicks` + `action=create`; v5 deployed ACTIVE tonight (parallel to commit). Verify in next session.
  - SLV location-page **Phase B (hero swaps for Holladay / Millcreek / Sandy / Draper / SLC)** — CH done in PR #47, 5 cities remaining. Tracker doc: `data/SLV-LOCATION-WAVE-TRACKER.md`. Gated on Ryan's Drive pulls landing in `/images/projects/cities/`; Holladay alt rewritten in PR #37 will need a second rewrite when its hero swaps.
  - SLV blog calendar **Posts #2-6 drafts** — hero photos pre-staged (b017481 earlier today), pipeline ready; queue: #2 Cottonwood Heights mountain-modern, #3 Holladay Mt. Olympus architectural vs metal, #4 SLC East Bench historic, #5 Sandy East Bench premium, #6 Millcreek + Murray mid-tier upgrade.
  - Phase 1 **form-abandonment recovery** Supabase scheduled fn (matches `form_start` → no `form_submit` within 5 min → Twilio SMS to partial-fill leads with valid Name+Phone) — `form_start` emits site-wide (PR #42); fn itself not yet written.
  - Gallery expansion (5 more photos + 3 process videos + "Inside a Frame Install" sub-section) — deferred from PR #40 earlier today.
  - Level-3 AEO `refresh-stale` fixer (`scripts/aeo-fix-refresh-stale.py`) — **now 5-day-old debt** (uncommitted since 2026-05-07).
  - `data/audits/` raw Lighthouse JSON commit policy still open.
  - Formspree (account `meeroaqa`) — retired but not yet deleted; ~30 days as rollback (clock 2026-05-10).
  - Manual publish of the GBP service-area-expansion post (`data/GBP-POST-PUBLISH-READY-2026-05-11.md`) — still needs Landon or Ryan at the GBP browser.
  - GBP **Products section** still empty in customer view — Landon photos to add.
  - **Density audit wave 4+** — `data/content-density-audit-2026-05-11.{json,md}` lists every blog post with density classification; wave 3 (PR #47) cleared Heber + Sandy + Layton, ~remaining LOW pages queued for waves 4+.

### 2026-05-11 (afternoon) — SLV Location-Page Wave (Phase A1 + A2 + A3) + Homepage Hero Trust-Signal Tighten + Form-Start Analytics + Fresh-Aerials Homepage Section + Blog #2-6 Hero Photos Pre-Staged
Six commits land on `main` between mid-morning auto-refresh (9353784) → ~late afternoon, all authored by the `review-bot@frameroofingutah.com` automation identity. This is the productionization layer that follows this morning's SLV strategic refocus (commits c6ce67c / 34eb621 / ef749ab): the 6 SLV location pages get a wave of conversion + perf upgrades, the homepage hero gets two CR-driven trust-signal corrections, the missing `form_start` PostHog micro-conversion ships site-wide, and the homepage gains a real-Landon May 2026 "Just Shipped" aerial section. Plus 5 hero photos for the queued SLV blog calendar Posts #2-6 land pre-staged from a Drive pull.
- **b017481 — feat: stage 5 hero photos from Landon Drive pull for SLV blog calendar Posts #2-6.** Pulled 14 JPGs from Landon's "Pics for Website" Drive folder via iframe-download (logged-in session), visually reviewed each, locked hero assignments + converted to 1600×900 webp 85% quality for the queued SLV premium blog posts: `cottonwood-heights-mountain-modern-reroof-2026.webp` (323 KB, IMG_77967352729, mountain-modern home + Frame yard sign), `holladay-mt-olympus-architectural-install-2026.webp` (343 KB, IMG_2532, Mt. Olympus + CertainTeed Landmark), `salt-lake-city-east-bench-tearoff-2026.webp` (501 KB, IMG_1905, open sheathing tear-off / historic-home fit), `sandy-east-bench-architectural-install-2026.webp` (516 KB, IMG_6144, steep premium install + snowy canyon backdrop), `millcreek-murray-rambler-reroof-2026.webp` (402 KB, IMG_1892, brick rambler + crew + Frame yard sign). New `data/BLOG-HERO-ASSIGNMENTS-2026-05-11.md` documents source→hero mapping, why each photo fits its post, 8 backup/round-2 candidates, build-rule compliance. Hero files are pre-staged so Posts #2-6 drafting only needs to wire references — no Drive round-trip in the critical path.
- **PR #37 / 7bc40fb — feat(slv): add fetchpriority=high to 6 SLV location heroes + rewrite Holladay alt (A1 + A3 of the SLV location-page wave).** `fetchpriority="high"` added to the hero `<img>` on `locations/cottonwood-heights.html` + `holladay.html` + `millcreek.html` + `sandy.html` + `draper.html` + `salt-lake-city.html`, bringing location-page heroes inline with the blog-hero pattern from commit a89192e (`fetchpriority="high"` on `.blog-featured-image` figures). Holladay alt rewritten per the playbook's 4-clause recipe (38 words, mentions "Frame Roofing Utah" + env signal: autumn canopy + Wasatch range). Other 5 alts intentionally left as-is — they'll be rewritten in Phase B alongside the Drive-pulled hero swaps so new alt describes new photo. Invariants verified: og:image still matches visible img src on all 6, no TX-brand leak, no "drone inspection" service language.
- **PR #38 / 159a23e — feat(slv): add mid-page CTA (.page-cta-inverted) across 6 SLV location pages (A2 of the wave).** `global.css` gains `.page-cta-inverted` modifier (+19 lines) composing against existing `.page-cta` selectors — navy bg / gold borders / gold buttons inverting the base palette, `.btn-navy` reused (no new inner-element classes), `!important` parity with the base block. 6 location pages each gain +7 lines: a `<section class="page-cta page-cta-inverted">` inserted immediately before the existing gold `.page-cta`, two `.btn-navy` CTAs (tel: + #heroForm anchor). Per c6ce67c SLV-focus framing, Salt Lake City uses "the Salt Lake Valley" in the H2 instead of "Salt Lake City". Conversion thesis: 3-CTA pages outperform 1-CTA pages — each SLV page now has hero CTA + mid-page navy CTA + bottom gold CTA. Playwright headless smoke test confirmed computed styles on Cottonwood Heights + SLC.
- **PR #41 / 749f5f4 — feat(home): replace generic "5.0 Rating" with specific "5.0 from 20 Google reviews" + 40→45 cities (hero badge).** Surgical 1-line edit on `index.html`. Was `★★★★★ 5.0 Rating · A+ BBB · Serving 40+ Utah Cities`, now `★★★★★ 5.0 from 20 Google reviews · A+ BBB · Serving 45+ Utah Cities`. CR research finding: specific-fact trust signals lift quote requests ~40% vs generic claims (Robben Media 2026 + multiple CR studies). Review count verified via the bi-monthly SerpAPI scheduled trigger pinned to GBP `data_id 0x874df59069be3e09:0x756332595f702acc`. "40+" → "45+" corrects pre-rebrand drift — site has 45 location pages live; GBP service area now covers 20 cities post-bda5310 expansion.
- **PR #42 / 887744a — feat(analytics): wire form_start PostHog event on hero/lead/modal forms.** Adds the missing micro-conversion event from `data/ADS-LAUNCH-AND-OPTIMIZATION-PLAYBOOK.md` §6 "Required conversion events". Pattern: `focusin` listener on the form element (focusin bubbles, focus doesn't), `dataset.formStarted` flag prevents re-fire, `posthog.capture('form_start', {form, page})`, silent no-op if PostHog isn't loaded. Three forms wired: `hero_inspection` (index.html heroForm), `lead_form` (index.html leadForm bottom-of-home), `modal_inspection` (global-modal.js frModalForm site-wide). +~30 lines across 2 files, no HTML changes — inline JS only. Powers Phase 1 form-abandonment recovery (planned Supabase scheduled fn: matches form_start events without subsequent form_submit within 5 min → fires Twilio SMS to partial-fill leads with valid Name+Phone). Existing PostHog taxonomy preserved unchanged (phone_click, email_click, form_submitted, cta_click, post_lead_call_now, lead_thank_you_view, review_cta_click).
- **PR #40 / 749cc0e — feat(home): add #fresh-aerials section — Just Shipped May 2026 Wasatch Front.** New homepage section between `.quick-answer-strip` and `#watch-in-action`. Contents: 3-photo responsive strip (AVIF/WEBP/JPG via `<picture>` at 800/1200/1600 widths — timpanogos-wide establishing aerial w/ Mt. Timpanogos backdrop, stone+brick custom luxury home w/ Wasatch foothills, close craftsman w/ stone accents + balcony) + 1 featured looping video (WEBM vp9 + MP4 h264 + JPG poster, 1080p ~5.7MB, April 2026 mid-install underlayment + crew) + "See all aerials →" link to #gallery (gallery expansion deferred to separate PR). **Performance:** IntersectionObserver gates video playback (plays only when >55% in view) — prevents two simultaneous video decoders since #watch-in-action also autoplays above. **Mobile CR fix shipped same branch:** original PR faded the entire dual `.sticky-call` bar when video was on screen, removing tap-to-call for ~10s — fix narrows the fade to `.sticky-cta-side` (the gold "Free Inspection" half only), navy `.sticky-call-side` (tap-to-call) stays at full opacity throughout. `prefers-reduced-motion` honored: video never plays for opted-out users (poster only). All photos `loading="lazy"` + `decoding="async"`. Mobile strip stacks 1-column at ≤820px. Asset weights: 3 AVIF photos browser-selected at viewport size ~50-200KB total, featured video `preload="metadata"` only — full WEBM loads when in viewport. **Photo authenticity:** all 12 candidate aerials thumbnailed + visually verified before encoding, 6 of 12 used (audit dropped 2 duplicate-house shots + 4 reserved for gallery-expansion PR). Per Ryan's no-drone-inspection rule: alt text says "aerial drone photograph" — never "drone inspection by Frame". 31 files staged: 1 `index.html` (+127 lines) + 27 photos (3 sizes × 3 formats × 3 photos) + 3 video files (webm/mp4/poster). All scoped to `#fresh-aerials .fa-*` prefix — no new global CSS classes. `og:image`/`twitter:image` meta intentionally unchanged (hero image still LCP candidate; featured video is secondary).
- **Net effect:** Six SLV premium-ZIP location pages now have (1) hero `fetchpriority="high"` for LCP, (2) a navy mid-page CTA inverting the existing gold CTA for 3-CTA layout, (3) a freshly playbook-recipe-written alt on Holladay (other 5 to follow in Phase B alongside Drive-pulled hero swaps). The homepage hero badge swaps two generic claims for specific verifiable facts (review count + accurate city count). The full form-funnel now emits `form_start` so abandonment can be measured + the recovery workflow built. The homepage gains a real-Landon May 2026 Wasatch Front aerial showcase between Quick Answer and Watch Us Work, with conversion-safe mobile CTA fading (gold half only — navy tap-to-call always visible). Five hero photos for SLV blog Posts #2-6 are pre-staged in the repo at their final filenames — drafting next 5 weeks of SLV content can skip the Drive round-trip entirely.
- **Carry-forward (rolling, still open):**
  - SLV location-page **Phase B (hero swaps for Cottonwood Heights / Millcreek / Sandy / Draper / SLC)** — gated on Ryan's Drive pulls landing in `/images/projects/cities/`; Holladay alt is already playbook-recipe-rewritten so its alt will need a second rewrite when its hero swaps too.
  - SLV blog calendar **Posts #2-6 drafts** — hero photos pre-staged (b017481), pipeline ready; queue: #2 Cottonwood Heights mountain-modern, #3 Holladay Mt. Olympus architectural vs metal, #4 SLC East Bench historic, #5 Sandy East Bench premium, #6 Millcreek + Murray mid-tier upgrade.
  - Phase 1 **form-abandonment recovery** Supabase scheduled fn (matches form_start → no form_submit within 5 min → Twilio SMS) — `form_start` is now emitting (PR #42); fn itself not yet written.
  - Gallery expansion (5 more photos + 3 process videos + "Inside a Frame Install" sub-section) — intentionally deferred from PR #40 for cleaner review surface.
  - Level-3 AEO `refresh-stale` fixer (`scripts/aeo-fix-refresh-stale.py`) — **now 5-day-old debt** (uncommitted since 2026-05-07).
  - `data/audits/` raw Lighthouse JSON commit policy still open.
  - GTM container + `track-attribution.js` site-wide rollout in flight (uncommitted).
  - Formspree (account meeroaqa) — retired but not yet deleted; ~30 days as rollback (clock 2026-05-10).
  - Manual publish of the GBP service-area-expansion post (`data/GBP-POST-PUBLISH-READY-2026-05-11.md`) — still needs Landon or Ryan at the GBP browser.
  - GBP Products section still empty in customer view — Landon photos to add.

### 2026-05-11 (mid-morning) — Strategic Refocus on Salt Lake Valley Premium ZIPs + Flagship SLV Reroof Guide Shipped + Review Language Audit + GBP Audit + Heber-Valley-is-ONE-market Rule
Fourteen commits land on `main` between 09:04 → 11:03 MDT — all authored by the `review-bot@frameroofingutah.com` automation identity. Coherent multi-step session: (a) a brand-new Review Language Audit tool was built and run for AIO-friendly attribute coverage, (b) Frame's GBP merchant profile got a live full audit (Products empty, Services misconfigured, Service Area 10/20 cities, site-hours schema drift), (c) Landon ruled out drone marketing entirely, (d) Ryan re-pointed the entire low-traffic-high-revenue strategy from Heber/PC → Salt Lake Valley premium ZIPs after two refinement steps (Heber-Valley-as-brand → SLV-as-brand), and (e) the flagship Salt Lake Valley Premium Roof Replacement Guide (3,057 words) shipped end-to-end with a real-Landon Mt. Olympus aerial hero pulled live from Landon's Drive folder.
- **db96579 — feat: review language audit (AIO attribute coverage scoring).** New `scripts/review-language-audit.py` (215 lines, pure stdlib, no extra SerpAPI cost). 11-attribute taxonomy (Cleanup, Warranty/Trust, Free Estimate/Inspection, Financing/Insurance, Storm/Hail/Leak, Response Speed, Material Quality, Pricing/Value, Professionalism, Communication, Workmanship), 30% coverage threshold. Outputs `data/review-language-audit.{md,json}`. Auto-generates a paste-ready Landon-facing CTA targeting the top-3 lowest-coverage attributes. **Strategic basis:** Bodhi insight (x.com/irentdumpsters/status/2053573105751044495, 2026-05-10) that AI Overviews synthesize review *language* into their answers — attributes customers don't mention in reviews won't surface in AIO for "best roofer Heber City" / "best roofer Salt Lake City" type queries. Chained into `update-google-reviews-cron.sh` to run automatically after 1st/15th SerpAPI sync.
- **e5c7576 — feat: GBP audit doc + full review feed (60 reviews via paginated fetch).** Live GBP inspection through Ryan's authenticated merchant panel surfaced two operational gaps the Bodhi insight predicted: (1) Products section **completely empty** (empty-state intro showing on customers' screens), (2) Services under-stocked + misconfigured (Siding cat with 0 services, General Contractor duplicating Roofing services, Gutter Cleaning with 0 services, no drone/tarping/insurance-claims/gutters/free-estimate under Roofing Contractor). Doc `data/GBP-AUDIT-2026-05-11.md` ships 8 paste-ready Products + Services cleanup items Landon can action directly. Also added `fetch_all_reviews()` to `scripts/update-google-reviews.py` — paginates SerpAPI google_maps_reviews up to 60 reviews → `data/reviews-full.json` (audit-only feed, separate from the 8-review carousel `reviews.json`). Live inspection caught review snippet "cleaned up every nail garbage" which proved our Cleanup-at-0% audit finding was a false zero from the 6-review SerpAPI sample. Audit script updated to prefer `reviews-full.json` with fallback to carousel feed.
- **191ef15 — fix: remove drone from Products + audit taxonomy (Landon directive); first full-corpus audit run.** Landon ruled out drone inspection as a marketed service (2026-05-11). Changes: (a) `GBP-AUDIT-2026-05-11.md` swaps Drone Roof Inspection product → Insurance Claim Assistance, removes "Drone inspection" from Roofing Contractor services-to-add; (b) `review-language-audit.py` renames "Drone / Inspection" attribute → "Free Estimate / Inspection", removes "drone" from match patterns + CTA askable map. **First full-corpus run (20 reviews, not the 6 from carousel):** real gap picture — Cleanup 5% (was false 0%), Warranty/Trust 5%, Free Estimate/Inspection 5%, Financing/Insurance 15%, Storm/Hail/Leak 20% (new gap visible at full sample), Response Speed 25%. Brand-leak filter caught 2 reviews mentioning "Frame Restoration" without "Utah" (Marie Conroe, Sam Thornton) — kept out of carousel, kept in audit feed. **No-drone is now a baked rule** (see also `feedback_no_drone_marketing` memory).
- **bda5310 — docs: GBP service area audit (10-city add list, 10 → 20).** Live GBP inspection (Edit profile → Location → Service area) shows only 10 cities listed despite the description claiming "40+ communities" and the website having 45 location pages. `data/GBP-SERVICE-AREA-AUDIT-2026-05-11.md` ships round-1 paste-ready additions: Provo, Orem, West Valley City, West Jordan, Lehi, American Fork, Cottonwood Heights, Murray, Layton, Ogden. Side findings documented: **Utah DOPL license #14256097-5501** (in GBP description, now canonicalized), **actual hours Mon-Fri 8AM-7PM / Sat 8AM-6PM** (memory + schema had Mon-Sat 8-6 = drift), **GBP merchant profile ID 9902208163385139295**.
- **23ae38a — fix: site hours schema drift + GBP audit completion status.** Closes the schema drift from bda5310. `index.html` openingHoursSpecification split into Mon-Fri (8AM-7PM) + Sat (8AM-6PM); `thank-you.html` copy updated to match. `GBP-AUDIT-2026-05-11.md` marked what was done in-session (Siding deleted, 4 Roofing services added, Service Area expanded to 20 cities) vs. what still needs Landon (Products photos, General Contractor + Gutter Cleaning category decisions).
- **ec1171e — docs: GBP post publish-ready — service area expansion announcement with photo.** `data/GBP-POST-PUBLISH-READY-2026-05-11.md` drafts a Google Post announcing the 20-city service area expansion. Post copy ready; photo recommended (`heber-valley-crew-trucks.jpg`). **Could not auto-publish** — Google's GBP merchant panel renders the post composer in a cross-origin iframe (name='app'), which blocks JS file injection from Chrome MCP; native file picker can't be driven because Chrome is at tier "read" in computer-use. Manual publish path documented in the file (30s at the browser). **Forward rule: every GBP post must include a photo (Ryan directive 2026-05-11)** — the two most-recent text-only posts on Frame's profile (Spring storm + "Now serving 9 cities") are visible without photos and underperform the photo-bearing ones.
- **d808d9f — docs: GBP post backlog targeting low-traffic + high-revenue ZIPs.** `data/GBP-POST-BACKLOG.md` queues 5 publish-ready posts in priority order — initially #2 Park City premium / #3 Midway-Heber Valley / #4 Holladay-CH / #5 Alpine-Highland luxury / #6 Storm damage + insurance. Each post: real-Landon photo from `images/projects/`, city-name in first sentence, free inspection/financing hook, 435-302-4422 CTA, no drone mentions, no text-only. **(NOTE: subsequent commits c6ce67c reset these targets to SLV premium ZIPs — see below.)**
- **37e8d7e + 2e959aa — docs: refocus to Heber Valley as ONE market (Ryan directive 2026-05-11).** Ryan: "midway is part of heber, lets focus on the valley." Heber City + Midway + Wallsburg + Charleston + Daniel are functionally the same market — same elevation (5,600+ ft), same snowload, same drive radius from Frame's HQ at 142 S Main. Don't fragment content. Blog calendar Post #1 swapped from "Midway Roof Replacement Guide" → "Heber Valley Roof Buyer's Guide" (umbrella, all valley communities, 2,200-2,800 words). GBP post #3 reframed valley-wide. **New ❌ rule baked into GBP backlog:** no city-specific Heber/Midway/Wallsburg-only posts; Heber Valley is ONE market.
- **cf6c269 — docs: reframe to Heber Valley as brand, not Heber City.** Ryan refinement: "we want to focus on the valley, so not heber." Blog calendar Post #1 slug moved from `/blog/heber-city/` → new `/blog/heber-valley/` folder. Title drops city name → "Heber Valley Roof Buyer's Guide 2026". Communities reordered Midway > Wallsburg > Charleston/Daniel > Heber (Heber last, no anchor framing). Frame branded as "Heber Valley-based crew." Existing `/blog/heber-city/` posts stay where they are (already indexed) but cross-link UP to the new valley hub. GBP Rules: new ❌ rule — no Heber-City-anchored framing; valley is the brand.
- **c6ce67c — docs: refocus on Salt Lake Valley — premium SLV ZIPs are the focus, not Heber/PC.** Ryan's final clarification: "so the valley i mean salt lake city and the surrounding areas." Full reset — Heber Valley, Park City, Utah Valley, Davis County all demoted (Frame already has visibility there). **New target focus = SLV + surrounding premium ZIPs:** 84093/84121 (Cottonwood Heights), 84117/84121/84124 (Holladay), 84106/84109 (Millcreek east), 84092/84094 (Sandy east bench), 84020 (Draper east bench), 84102/84103/84105/84108 (SLC east bench). Blog calendar reset (6 posts, all SLV): #1 SLV Premium Reroof Guide (flagship), #2 Cottonwood Heights premium aging-home, #3 Holladay architectural vs metal, #4 SLC East Bench (Avenues/Federal Heights/Sugarhouse historic), #5 Sandy East Bench (Pepperwood/Granite/Alta-foothill), #6 Millcreek + Murray mid-tier upgrade. GBP post backlog #2-#6 all pivoted to SLV premium ZIPs. Memory updated (`feedback_frame_low_traffic_high_revenue`): focus = SLV + surrounding premium ZIPs.
- **b6f53f7 — docs: 6-post blog calendar targeting low-traffic + high-revenue ZIPs.** Initial Round-1 file `data/BLOG-CALENDAR-LOW-TRAFFIC-HIGH-REVENUE-2026-05-11.md` (171 lines) — filed before the SLV refocus. Each entry has slug, target keyword, hook, section outline, word count, hero photo recommendation, internal-link plan, and CTA. Build rules enforced: blog-hero/blog-body CSS (per `feedback_blog_template`), unique og:image per post (per `feedback_blog_photo_grep_check`), no drone mentions (per `feedback_no_drone_marketing`), no inline styles, real-Landon photos only. **Cadence: 1 post/week → covers next 6 weeks.** Subsequent commits 37e8d7e / cf6c269 / c6ce67c rewrote the targets in this same file to SLV-focused.
- **34eb621 — feat: publish flagship SLV Premium Roof Replacement Guide 2026.** Round-1 Post #1 of the SLV blog calendar shipped. **3,057 words** at `blog/salt-lake-valley/salt-lake-valley-premium-roof-replacement-2026.html` covering: why east-bench SLV roofs fail faster (Wasatch snowmelt + mature canopy debris + altitude UV); the premium SLV ZIPs; material decision matrix (Class 4 architectural, designer shake, standing-seam metal, synthetic slate, natural slate); snowmelt + tree-debris failure modes; 2026 cost ranges by material ($22-30K → $65K+); HOA + permit realities (Top of the World, Mt. Olympus Cove, Pepperwood, Granite, SunCrest, Federal Heights historic); Frame's premium SLV process; replace-vs-repair guidance; 5-question FAQ block; sources & references. **Template follows `feedback_blog_template` (blog-hero/blog-body CSS, embedded style block, no inline styles).** JSON-LD: BlogPosting + FAQPage. Real-Landon photo, no drone mentions, valley-focus framing. `blog/index.html` updated with card at top. **New blog directory opened: `/blog/salt-lake-valley/`** (parallels prior /blog/heber-valley/ pattern from cf6c269 — both umbrella valley folders).
- **ef749ab — feat: swap SLV flagship hero to Mt. Olympus aerial from Drive pull.** Replaced the generic `salt-lake-city-residential-reroof.webp` with a new image pulled live from Landon's "Pics for Website" Drive folder (IMG_2673) — converted to webp 1600x900 with bottom-bias crop showing Mt. Olympus + autumn canopy + established east-bench neighborhood + a premium home with architectural shingles on the lower right. Much stronger SLV-premium-reroof narrative fit (Mt. Olympus = iconic east-bench landmark; mature autumn canopy = post's tree-debris failure-mode story; white fence + established neighborhood = premium ZIP audience signal; visible architectural shingles + work-in-progress = Frame's craft). Updates: hero img src + alt (Mt. Olympus mentioned for SEO), og:image + twitter:image, JSON-LD BlogPosting.image, blog/index.html card thumbnail, hero max-height 400 → 480px. Source pulled via iframe-download from logged-in Drive session. **Real-Landon photo, no AI, no fabrication.** New asset: `images/projects/cities/salt-lake-valley-east-bench-aerial-2026.webp` (479 KB).
- **Net effect:** Frame's content strategy for the next ~6 weeks is now pointed at SLV premium ZIPs (Cottonwood Heights / Holladay / Millcreek / east-bench Sandy / east-bench Draper / SLC east bench) — premium underserved markets where Frame has low current visibility but high revenue-per-job potential, with the explicit demotion of Heber Valley / Park City / Utah Valley / Davis (already-strong markets). Flagship SLV post live with real-Landon Mt. Olympus aerial. Review Language Audit is wired into the bi-monthly cron with a paste-ready Landon CTA targeting the top-3 AIO-coverage gaps. GBP merchant profile audit ships 8 paste-ready Products + Services cleanup items + 10 service-area-city additions, with hours-schema drift fixed at the source. Drone is permanently out of Frame's marketed services. New ❌ rules baked: no city-anchored Heber framing (valley is the brand), every GBP post must include a photo, Heber Valley is ONE market.
- **Working tree at close (carried forward, NOT touched by this refresh):** ~120 modified HTML files (locations + blog + pages + service pages) plus the same untracked-files set as 2026-05-10 evening close (the GTM container + UTM-tracking rollout still in flight) PLUS this morning's new data/* docs which all committed cleanly. The Heber-Valley-Roof-Buyer's-Guide post (Blog Calendar Round-1 Post #2 in the SLV-refocused calendar — wait, actually after c6ce67c the SLV refocus made Post #1 = SLV flagship which IS shipped; Heber Valley umbrella post got demoted off the active 6-post calendar) is **not** being drafted next; SLV Posts #2-#6 are the next 5 weeks' content queue.
- **Carry-forward (rolling, still open):**
  - Level-3 AEO `refresh-stale` fixer (`scripts/aeo-fix-refresh-stale.py`) — **now 5-day-old debt** (uncommitted since 2026-05-07).
  - `data/audits/` raw Lighthouse JSON commit policy still open.
  - GTM container + `track-attribution.js` site-wide rollout in flight (uncommitted).
  - Formspree (account meeroaqa) — retired but not yet deleted; ~30 days as rollback (clock 2026-05-10).
  - Manual publish of the GBP service-area-expansion post (`data/GBP-POST-PUBLISH-READY-2026-05-11.md`) — needs Landon or Ryan at the GBP browser.
  - GBP Products section still empty in customer view — Landon photos to add.
  - GBP services: Siding deleted ✅, 4 Roofing services added ✅, Service Area 10→20 ✅; still pending: Products photos + General Contractor + Gutter Cleaning category decisions.

### 2026-05-10 (evening) — Park City AEO Close-Out + Ship-Day Handoff Docs (PRs #35, #36)
Five commits land between 21:01 → 21:15 MDT, closing out the 2026-05-10 ship-day. One substantive content fix (PR #35) plus a 4-commit documentation sweep — the latter aligns Frame Utah's handoff convention with Frame TX's `COWORK-HANDOFF-YYYY-MM-DD.md` naming and pre-positions the next session with a clean state snapshot.
- **PR #35 / 65feb7a — fix(park-city): add internal link from Storm Damage card → /pages/storm-damage.** Single-line manual AEO close-out for action `aeo-ef3d8192` (internal-link-boost on `pages/storm-damage.html`, query "storm damage roofing Park City"). `locations/park-city.html` was the missing 28th inbound link — all 3 Park City blog spokes already linked to the hub, only the location page's "Storm Damage & Insurance" service-mini-card was prose-mentioning storm damage without linking. **Why manual instead of via `aeo-fix-internal-links.py`:** today's storm-cluster expansion (PRs #27, #31, #32) touched all 5 candidate files within the auto-fixer's 24-hour race-condition guard window, which correctly prevented the hardened fixer from editing them — single targeted human edit on existing prose is the right path here. Storm-damage hub now has **28 inbound links sitewide**. June 5 AEO monitor expected to close `aeo-ef3d8192` on next evaluation.
- **Commit d05058a — docs(handoff): 2026-05-10 footer on SESSION-2026-05-09 doc.** Appends 208-line correction footer to `data/SESSION-2026-05-09-email-fix-and-site-audit.md`. **Key correction:** the 2026-05-09 body had listed `/seo-report` under "exposed admin pages" — footer flags that as wrong. Both `/seo-report` and the new `/leads` are intentional PIN-gated infra with `noindex,nofollow` + `report_access` table check. **They must NOT be added to `.vercelignore`** (would break Ryan's + Landon's tooling). Footer also confirms: handle-lead v8 deployed (Resend sending domain `frameroofingutah.com` verified, `RESEND_API_KEY` in Supabase secrets); /leads CRM live + lead-crm edge function deployed; 20260510 attribution migration applied to prod; one TEST lead remains in DB (id=43, `weekly-report` filters `TEST*` names).
- **PR #36 / b2a3ec6 — docs: cowork handoff 2026-05-10.** New `data/COWORK-HANDOFF-2026-05-10.md` (159 lines, .vercelignored). Paste-ready handoff for Cowork Claude / Landon / future-sessions covering: live production state (storm cluster: 14 cities; roof-replacement cluster: 8 cities + 1 statewide companion); all 11 PRs shipped 2026-05-09 → 2026-05-10 with cluster-role labels; tooling delta (`blog-target-prioritizer.py` MAX_SPOKES_PER_CITY=2 cap, Ollama daemon parallel-drafting config, `generate-blog-post.py` 900s urllib timeout); hard rules; open work queue (hail self-assessment statewide post, photo refresh for 4 cities still on fallback hero, Frame TX cap-bake before launch); strategic insight (2026 local-SEO sentiment convergence on fewer/deeper). Matches Frame TX convention (`data/COWORK-HANDOFF-2026-05-05.md`).
- **Commits 73c4108 + df5b061 — naming-convention reconciliation.** First commit ships `data/SESSION-2026-05-10-ship-day.md` (150 lines); second removes it 6 minutes later because PR #36's `COWORK-HANDOFF-2026-05-10.md` is the canonical handoff. Net effect: zero — file added then deleted. Documents the decision to align Frame Utah with Frame TX's `COWORK-HANDOFF-*` naming so future sessions land on one canonical doc, not two.
- **Working tree at close (carried forward, NOT touched by this refresh):** identical to 2026-05-10 morning entry — ~120 modified HTML files (locations + blog + pages + service pages) plus 8 untracked items (`assets/_backup-2026-05-06-*`, `data/ADS-LAUNCH-AND-OPTIMIZATION-PLAYBOOK.md`, `data/LANDON-LAUNCH-CHECKLIST.md`, `data/SESSION-2026-05-07-to-10-handoff.md`, `data/SETUP-GTM-CONTAINER.md`, `data/VISION-INTAKE-BACKPORT-SPEC.md`, `data/audits/`, `data/reddit-content-opportunities-2026-05-09.md`, `scripts/aeo-fix-refresh-stale.py`, `supabase/migrations/DEPLOY-attribution.md`, `track-attribution.js`). The HTML modification cluster + `track-attribution.js` + `data/SETUP-GTM-CONTAINER.md` still look like an in-progress GTM container + UTM-tracking rollout aligned with the new attribution columns — **debt growing**, this is now ~1-3 days uncommitted depending on file.
- **Carry-forward (rolling, still open):**
  - Level-3 AEO `refresh-stale` fixer (`scripts/aeo-fix-refresh-stale.py`) — **now 4-day-old debt** (uncommitted since 2026-05-07).
  - `data/audits/` raw Lighthouse JSON commit policy still open.
  - GTM container + `track-attribution.js` site-wide rollout in flight (uncommitted, growing).
  - Formspree (account meeroaqa) — retired but not yet deleted; keep ~30 days as rollback option (clock started 2026-05-10).
  - Hail self-assessment statewide post (queued in `COWORK-HANDOFF-2026-05-10.md`).
  - Photo refresh for 4 cities still on fallback hero (queued in handoff).
- **Net effect:** Ship-day closed with one final hub-link tightening + a clean canonical handoff doc. Storm-damage hub-spoke cluster reaches 28 inbound links from blog + location pages sitewide. Frame Utah now follows Frame TX's `COWORK-HANDOFF-*` naming so a fresh collaborator can pick up either project from a single doc.

### 2026-05-10 — Lighthouse Audit Closure + Lead Pipeline Overhaul: Formspree → Resend (handle-lead v8) + /leads CRM + Attribution Migration
Two commits land on `main`, both authored by the `review-bot@frameroofingutah.com` automation identity (established 2026-05-06). Together they close two of the longest-standing Immediate-TODO items (Lighthouse audit fixes + Formspree retirement) on the same day.
- **Commit 0199efd — perf(bp): fix DOMContentLoaded TypeError + disable PostHog surveys.** Closes 2 of the 3 fixes queued from `data/audits/lighthouse-2026-05-04.md`:
  - **Fix 1 (Best Practices 96 → 100):** `index.html:1692` had a malformed IIFE wrapping `addEventListener('DOMContentLoaded', …)` — the trailing `()` was trying to invoke `addEventListener`'s `undefined` return, throwing a TypeError on every page load (Lighthouse `errors-in-console` flagged it on every audit). Changed `})();` → `});` and added an inline note explaining why.
  - **Fix 3 (saves 27 KB unused JS):** Added `disable_surveys: true` to PostHog init at `index.html:781`. Frame Utah doesn't use PostHog surveys; this prevents `surveys.js` (33 KB total, 27 KB unused = 81% waste) from loading site-wide.
  - **Fix 2 (LCP) was already solved** by the 2026-05-06 video-swap stack (drone + showcase + showreel rehome) — a fresh Lighthouse mobile-perf audit run 2026-05-10 measures **99/100 with LCP 1.5 s**, so no image / `srcset` work was needed. Projected per-audit table: Perf 99/99, Best Practices 96 → 100 (both viewports), A11y 100/100, SEO 100/100. **Closes Immediate TODO #1 entirely** — all three audit fixes have shipped.
- **Commit d9bd585 — feat(crm,leads): Resend swap + /leads CRM page + lead-crm edge function** (763 insertions, 31 deletions across 7 files). Largest LEAD PIPELINE change since the function was first deployed:
  - **`handle-lead` v7 → v8 — Formspree replaced with Resend.** New `sendResendEmail` helper used by both Landon-notification email + Verizon SMS gateway. Resend free tier (100/day, 3000/mo) replaces Formspree free (50/mo). Domain `frameroofingutah.com` to verify in Resend; `RESEND_API_KEY` stored as a Deno secret. Landon notification: `leads@frameroofingutah.com` → `landon@framerestorations.com`, CC `ryanconwell99@gmail.com`. Verizon SMS gateway: `noreply@frameroofingutah.com` → `{digits}@vtext.com`. **Closes Immediate TODO #5** (Direct email — replace Formspree with Resend/SendGrid). CLAUDE.md LEAD PIPELINE block updated to reflect v8 + Resend.
  - **`/leads` CRM page (NEW, 433 lines).** Dark-themed PIN-gated CRM dashboard at `leads.html`, served at `/leads` (cleanUrls). Same PIN system as `/seo-report` (key=`frame-roofing-2026` + per-user PIN against `report_access` table — existing PINs work unchanged). KPIs: new untouched, last 30d, open pipeline, won MTD, 10% commission MTD. Filter / sort / search across the lead list. Click-to-edit panel for status, notes, job_value, commission — auto-fills commission as 10% of job_value and auto-stamps `won_at` when status flips to `won`. `seo-report.html` user-bar gains a "Leads CRM →" button linking to `/leads`. `.vercelignore` header now carries a comment warning that `leads.html` is a public deliverable (to prevent future reflexive ignore).
  - **`lead-crm` Supabase Edge Function (NEW, 127 lines).** PIN-gated reads + writes for the CRM page; auth mirrors `weekly-report`. Source committed at `supabase/functions/lead-crm/index.ts`.
  - **Migration applied to prod — `supabase/migrations/20260510_add_attribution_columns.sql`** (76 lines). Adds `gclid`, `fbclid`, `utm_source`, `utm_medium`, `utm_campaign`, `utm_term`, `utm_content`, `won_at`, `uploaded_to_google_ads`, `uploaded_to_meta_ads` columns to `leads`. Adds `leads_status_check` CHECK constraint. Adds partial indexes for offline-conversion sync. Wires the data plane for future Google Ads / Meta Ads offline-conversion upload (script not yet shipped).
  - **`hero.html` brought in line with `index.html` convention** — legacy `formspree.io/f/meeroaqa` form action replaced with `handle-lead` via `data-endpoint`. Was an orphan file with stale wiring.
- **Origin/main divergence noted during this refresh:** the same `feat(crm,leads)` commit existed twice with identical patches but different SHAs (`d9bd585` local vs `755b5b6` origin) — likely a force-push / rebase elsewhere. Resolved via `git pull --rebase origin main` before pushing the CLAUDE.md update; identical patches dedupe cleanly during rebase.
- **Working tree at close of refresh (NOT touched by this refresh):** ~120 modified HTML files (locations + blog + pages + service pages) plus 8 untracked items — `assets/_backup-2026-05-06-*` (existing), `data/SETUP-GTM-CONTAINER.md` (NEW), `data/VISION-INTAKE-BACKPORT-SPEC.md` (carried from 2026-05-09), `data/audits/` (Lighthouse JSONs, commit-policy still open), `data/reddit-content-opportunities-2026-05-09.md` (carried), `scripts/aeo-fix-refresh-stale.py` (Level-3 fixer, carried from 2026-05-07 — 3-day-old debt), `supabase/migrations/DEPLOY-attribution.md` (NEW), `track-attribution.js` (NEW). The HTML modification cluster + `track-attribution.js` + `data/SETUP-GTM-CONTAINER.md` together look like an in-progress GTM container + UTM-tracking rollout aligned with the new attribution columns — left for the next session to commit cleanly as its own focused PR.
- **Carry-forward (rolling):**
  - Level-3 AEO `refresh-stale` fixer (`scripts/aeo-fix-refresh-stale.py`) — still uncommitted, now 3 days old.
  - `data/audits/` raw Lighthouse JSON commit policy still open (now also has lighthouse-2026-05-10).
  - GTM container + `track-attribution.js` site-wide rollout in flight (uncommitted).
  - Formspree (account meeroaqa) — retired but not yet deleted; keep ~30 days as rollback option.
- **Net effect:** Two of the longest-standing Immediate TODO items closed in a single day (Lighthouse audit pass + Formspree retirement). Frame Utah's lead pipeline now has (1) a unified Resend-backed email path (6× the free-tier cap), (2) a PIN-gated `/leads` CRM with full lead lifecycle + auto-commission accounting, and (3) the attribution columns wired so offline-conversion sync becomes a script-only lift rather than a schema migration. Best Practices Lighthouse projection 96 → 100 mobile + desktop.

### 2026-05-09 (afternoon) — Storm-Damage + Roof-Replacement Cluster Expansion: 10 New Spokes via Higgsfield-Aware Pipeline + Saturation-Cap Optimizer + AEO Fixer Hardening
Eight commits landed `main` between 12:12 → 13:50 MDT (#27 → #34), driving the largest single-day blog-spoke buildout in project history: **10 new posts shipped end-to-end through the new Higgsfield-aware blog generator + traffic-aware prioritizer pipeline** (the pipeline itself only merged 2h earlier in PR #24 / #26). Sitemap grew from 105 → 115 URLs.
- **PR #27 / 8c96711 — post(slc): storm-damage roofing Salt Lake City — depth-play spoke.** First content shipped *through* the new pipeline (was sitting on `post/slc-storm-damage` feature branch at end of morning session). Adds `blog/salt-lake-city/storm-damage-roofing-salt-lake-city.html` + JSON seed at `data/blog-pending/`. Also patches `scripts/generate-blog-post.py` with depth-play scoring tweaks discovered during the live render. Spoke #12+ in the storm-damage hub-spoke cluster — classified as DEPTH PLAY (existing SLC traffic + Reddit storm-damage demand, untapped service angle).
- **PR #28 / 89994ad — seo: integrate SLC storm-damage post into hub-spoke + sitemap.** Wires the new SLC post into `pages/storm-damage.html` (hub), `blog/utah/utah-hail-season-roof-guide-2026.html` (statewide cluster anchor), `locations/salt-lake-city.html` (city page), and `sitemap.xml`. Standard hub-spoke linking pattern: hub points down to spoke, spoke points up to hub + sideways to nearest neighbor. Closes the loop so Google can crawl the new node from three independent paths.
- **PR #29 / cd4c62b — fix(blog): broken Wasatch card layout + add SLC storm-damage card + harden AEO fixer.** Visual fix on `blog/index.html` (Wasatch cluster card layout had broken during the SLC integration in #28); adds the SLC storm-damage card to the index grid; **also hardens `scripts/aeo-fix-internal-links.py`** — patches the runaway-protection guard from 2026-05-07 (`MAX_LLM_CALLS = 8`, `LLM_TIMEOUT_SEC = 30`) plus additional safeguards now that the fixer is producing live PRs (#21, #22 yesterday). Clears the carry-forward "internal-link runaway guard still uncommitted" item from the morning session.
- **PR #30 / 5d8e349 — feat(optimizer): MAX_SPOKES_PER_CITY=2 saturation cap.** Sole change: `scripts/blog-target-prioritizer.py` now caps any single city at 2 spokes per service before the prioritizer marks it saturated and rotates focus elsewhere. Prevents the prioritizer from over-investing in already-strong cities (e.g., SLC with 5 posts) when whitespace cities sit at 0. Discovered as a gap during today's run when SLC kept ranking #1 despite being a depth play, not a whitespace play.
- **PR #31 / ba91e25 — post(holladay): storm-damage roofing — canyon-outflow wind + east-bench neighborhood guide.** WHITESPACE play — Holladay was Tier-2 city with 0 spokes. Adds `blog/holladay/storm-damage-roofing-holladay.html` + JSON seed; integrates into hub (`pages/storm-damage.html`), Wasatch hail anchor (`blog/utah/utah-hail-season-roof-guide-2026.html`), and `locations/holladay.html`. Local SEO angle: canyon-outflow wind events + east-bench neighborhood (Cottonwood Heights border) rich-detail content.
- **PR #32 / 0988011 — post(ch+murray): two whitespace spokes — CH roof-replacement (new cluster) + Murray storm-damage (extend existing).** Two-fer: `blog/cottonwood-heights/roof-replacement-cottonwood-heights-utah.html` opens a brand-new roof-replacement cluster for CH, and `blog/murray/storm-damage-roofing-murray-utah.html` adds Murray to the active storm-damage cluster (Murray was already at 1 post — `signs-you-need-new-roof` — so this hits the new MAX_SPOKES_PER_CITY=2 cap exactly). Both wired into hubs + statewide anchors + location pages. Generator script also touched (small helpers).
- **PR #33 / 297bc0a — post(5x): roof-replacement cluster expansion — Alpine + Highland + South Jordan + Orem + Provo.** Single largest content drop of the day — **5 spokes in one PR**, all targeting the roof-replacement vertical, all WHITESPACE (Tier 2/3 cities at 0 roof-replacement spokes). Each spoke gets: blog post HTML + JSON seed in `data/blog-pending/` + integration into `pages/roof-replacement.html` hub + `locations/<city>.html` page + sitemap entry. Net effect: roof-replacement is now Frame's deepest cluster on `main`, surpassing storm-damage in city coverage.
- **PR #34 / 2c5c69c — post(utah): roof-replacement quote checklist — closes Reddit content gap, compounds aeo-76a17e0e.** Statewide spoke (not city-targeted). New `blog/utah/roof-replacement-quote-checklist-utah-2026.html` + JSON seed + render-skeleton (the file from this morning's untracked pile, `data/blog-skeletons/roof-replacement-quote-checklist-utah-2026.html`, is now `data/blog-published/...-skeleton-2026-05-09.html` — pipeline lifecycle `pending → published`). **Doubly intentional:** (1) Reddit demand signal (top content gap surfaced by yesterday's traffic-aware prioritizer / PR #26), (2) compounds the Level-2 AEO auto-fix from yesterday's PR #21 (aeo-76a17e0e — internal-link target was `roof-replacement-cost-utah-2026`; this checklist is the natural sibling that absorbs follow-on internal-link injections from future AEO runs). Also patches `pages/roof-replacement.html` and the existing `roof-replacement-cost-utah-2026.html` to cross-link.
- **Pipeline validation (this is the headline):** End of morning had pipeline shipped (#24 generator + #26 prioritizer) but only 1 spoke drafted (SLC, on a feature branch). Afternoon ran the full loop **9 more times in 90 minutes** — prioritizer scored targets → generator drafted post + Higgsfield image → renderer produced template-matched HTML → integration into hubs + sitemap → ship. Confirms the cost-tiered routing memo works under volume (Tier-1 Ollama drafts free, Tier-4 Higgsfield only for hero illustration). Saturation cap (PR #30) was discovered + fixed mid-run, which is what robust pipelines look like.
- **Untracked / still uncommitted on 2026-05-09 (afternoon close):**
  - `scripts/aeo-fix-refresh-stale.py` (363 lines) — Level-3 mechanical (no-LLM) auto-fixer for `refresh-stale` AEO actions. Carried forward from 2026-05-07; **still un-shipped after today.**
  - `scripts/aeo-citation-monitor.py` — uncommitted working-dir patch.
  - `data/audits/` (Lighthouse JSONs from 2026-05-04, ~1 MB each) — commit-policy decision still open.
  - `data/VISION-INTAKE-BACKPORT-SPEC.md` (58 lines) — spec doc, not yet referenced by any committed code.
  - `data/reddit-content-opportunities-2026-05-09.md` — scratch from prioritizer's Reddit signal pull; multiple items now consumed (Holladay, CH, Murray, the 5x roof-replacement, the quote checklist), but the file itself stays untracked.
  - Backup dirs from 2026-05-06 video swap unchanged (.vercelignore'd).
- **Net effect:** Blog count 42 → ~52 (10 new spokes); sitemap 105 → 115; storm-damage cluster gains 3 spokes (SLC, Holladay, Murray); roof-replacement cluster gains 6 city spokes + 1 statewide checklist (Alpine, Highland, South Jordan, Orem, Provo, Cottonwood Heights, quote-checklist); new city directories opened: alpine, cottonwood-heights, highland, holladay, south-jordan; AEO internal-link runaway guard now committed (carry-forward cleared).
- **Carry-forward (rolling):** Lighthouse audit fixes (Immediate TODO #1, three fixes) still un-shipped. Level-3 stale-refresh fixer still uncommitted (now 2-day-old debt). `data/audits/` commit policy still open. SLC storm-damage post wants a hero photo upgrade (Higgsfield illustration is fine but SLC has real Frame project photos that would outperform).

### 2026-05-09 — 6-PR Wave Lands the 2026-05-07 Untracked Stack + First Live AEO Auto-Fix PRs + Email Inbox Restoration + Traffic-Aware Blog Prioritizer + SLC Storm Spoke
Six PRs merged to `main` between 11:22 → 11:55 MDT (#21 / #22 / #23 / #24 / #25 / #26). The 2026-05-07 untracked debt mostly cleared, the AEO Level-2 fixer started producing live production PRs, and Frame's primary lead inbox stopped silently bouncing.
- **PR #23 / f0e6215 — chore(handle-lead): commit v7 LLM tier classifier source + strip debug payload.** The deployed `handle-lead` edge function (previously deployed via `bash /tmp/deploy-handle-lead.sh`, source untracked) is now in-tree at `supabase/functions/handle-lead/index.ts` (439 lines) + `DEPLOY.md` (deploy + secret-rotation notes). v7 OpenRouter / Gemini 2.0 Flash tier classifier (emergency / urgent / scheduled / general / spam routing) is the production version. Public response stripped back to `{success, message}` — `LAST_LLM_DEBUG`, `_debug_config_keys`, `_debug_llm.bearer_*` were load-bearing during a 401 hunt earlier in the day (root cause: duplicated `sk-or-` prefix in `app_config.OPENROUTER_API_KEY`, fixed via `UPDATE`; classifier now returns emergency/urgent tiers correctly end-to-end). Adds `supabase/.temp/` to `.gitignore` so CLI cache doesn't land.
- **PR #24 / d946755 — feat(blog): Higgsfield-aware blog generator + Park City storm draft.** The 30 KB `scripts/generate-blog-post.py` from the 2026-05-07 untracked stack shipped, plus the Park City JSON seed at `data/blog-pending/`. Implements the cost-tiered routing memo: Tier-1 Ollama qwen3:8b/mistral-small3.1 ($0) drafts post body + image prompt; Tier-4 Higgsfield Marketing Studio ($$) generates 1 stylized hero illustration; Tier-3 (this script, $0) renders final HTML matching Frame's existing blog template (BlogPosting + FAQPage + HowTo + ImageObject schema). Hard rules baked in (not docs): AI hero gets `ImageObject` with `disambiguatingDescription` (never `Photograph`); `digitalSourceType` flips `compositeSynthetic` vs `humanWritten` based on image source; alt enforces "stylized illustration"; phone CTA hardcoded `+14353024422` (call) — never the SMS-only 435-292-8802; internal `linkify()` validates against whitelist + drops invalid links instead of shipping 404 anchors; brand string locked "Frame Roofing Utah" with explicit anti-Texas guard in prompt. Two new npm scripts wired: `blog:draft` + `blog:render`.
- **PR #21 / b080c46 — feat(aeo): auto-fix aeo-76a17e0e — internal links → blog/utah/roof-replacement-cost-utah-2026.html.** **First live production PR from the Level-2 PR-gated `aeo-fix-internal-links.py` fixer** (closes issue #15). Two anchors added: `blog/bountiful/roof-repair-vs-replacement.html` ("what costs to expect", confidence 0.85) + `blog/west-valley-city/roof-replacement-cost.html` ("roof replacement cost", confidence 0.90). Co-authored-by Review Bot.
- **PR #22 / 931e074 — feat(aeo): auto-fix aeo-a8603ecd — internal links → pages/insurance-claims.html.** Second live AEO auto-fix PR (closes issue #17). Two anchors: `blog/utah/utah-hail-season-roof-guide-2026.html` ("insurance claims process", 0.85) + `blog/index.html` ("insurance claim timing", 0.80). The Level-2 fixer is now actively producing PRs against high-leverage AEO actions filed by the monthly cron — full end-to-end loop confirmed (research → leverage-scored issue → auto-PR → merge).
- **PR #25 / 29a161f — fix: replace dead info@framerestorationutah.com with landon@framerestorations.com.** The `info@framerestorationutah.com` mailbox was never provisioned — contact-form fallback emails + every "email us" link on 15 public pages was silently bouncing. Restored to Landon's real inbox across `index.html`, `terms.html`, `privacy.html`, `pages/about.html`, `llms.txt`, and 10 blog posts (draper, heber-city, herriman, salt-lake-city, sandy×2, utah×2, west-jordan, west-valley-city). **Trade-off explicitly accepted:** re-introduces the `framerestorations.com` (Texas-parent) signal that the 2026-04-22 entity-leak lockdown removed — working inbox > brand isolation. Documented as override in CLAUDE.md PEOPLE block. Note: NEVER use `sales@framerestorations.com` (swept out 2026-04-22 for same Frisco TX cross-resolution reason).
- **PR #26 / 3f51d6e — feat(blog): traffic-aware target prioritizer (PostHog + Reddit + market-intel).** New gate before `npm run blog:draft`. Ranks (city, service) targets by `whitespace × tier × storm-override × aeo-leverage × freshness × revenue-proxy × demand-supply-gap × depth-play`. Three strategic axes surfaced in output: 📍 WHITESPACE (Tier 1/2 city, 0 spokes, 0 traffic — pure coverage gap), 🔥 DEMAND-LED (Reddit signal + low PostHog traffic — demand > supply), 🎯 DEPTH PLAY (existing traffic + Reddit demand, untapped service). Live data sources: PostHog 90d pageviews per `/locations/{city}` (existing weekly-report path), Reddit demand signals, market-intel city tiering. Replaces gut-feel topic selection with data-driven score before any LLM spend.
- **Active feature branch (NOT yet merged to main):** `post/slc-storm-damage` / 082e421 — first piece of content shipped *through* the new Higgsfield-aware pipeline. Adds `blog/salt-lake-city/storm-damage-roofing-salt-lake-city.html` + JSON seed at `data/blog-pending/storm-damage-roofing-salt-lake-city.json`. Spoke #12+ in the storm-damage hub-spoke cluster. PR pending.
- **Untracked / still uncommitted on 2026-05-09:**
  - `scripts/aeo-fix-refresh-stale.py` (363 lines) — Level-3 mechanical (no-LLM) auto-fixer for `refresh-stale` AEO actions. Carried over from 2026-05-07; did not ship in today's wave.
  - `scripts/aeo-fix-internal-links.py` working-dir patch — the 46-min runaway-protection patch (`MAX_LLM_CALLS = 8`, `LLM_TIMEOUT_SEC = 30`) from 2026-05-07. **Bypassed by today's PR runs (#21/#22 each had ≤2 candidates), but should still ship — next runaway will burn 46 min again without it.**
  - `data/audits/` (Lighthouse JSONs from 2026-05-04, ~1 MB each) — commit-policy decision still pending.
  - **NEW today: `data/reddit-content-opportunities-2026-05-09.md`** (152 lines) — likely scratch from the new traffic-aware prioritizer's Reddit signal pull.
  - **NEW today: `data/blog-skeletons/roof-replacement-quote-checklist-utah-2026.html`** (22 KB) — render-ready skeleton from the blog generator, pending review before move to `pages/` or `blog/utah/`.
  - **NEW today: `data/VISION-INTAKE-BACKPORT-SPEC.md`** (58 lines) — spec doc; name suggests porting back a vision/photo-intake pattern (likely from Frame TX).
  - Backup dirs from 2026-05-06 video swap unchanged (.vercelignore'd).
- **Net effect:** 2026-05-07 untracked debt mostly cleared (4 of 4 work streams shipped except Level-3 stale-refresh fixer + runaway-guard patch); AEO automation loop fully end-to-end for `internal-link-boost` actions; Frame's primary lead inbox no longer silently bouncing; blog pipeline now has both a generator (#24) AND a traffic-aware prioritizer (#26) gating it; one new spoke (SLC) already drafted through the pipeline on a feature branch.
- **Carry-forward:** Lighthouse audit fixes (Immediate TODO #1, three fixes) still un-shipped; Level-3 stale-refresh fixer + internal-link runaway guard still uncommitted; commit-policy decision on `data/audits/` raw JSONs still open.

### 2026-05-07 (afternoon) — Untracked Stack: Blog Generator Pipeline + Level-3 AEO Stale-Refresh Fixer + Supabase Edge Source-of-Truth + Internal-Link Runaway Guard
No new commits since the morning's `ef33690` auto-refresh. Working tree shows a meaningful untracked/uncommitted stack landed during the afternoon session (15:11 → 17:55), all building on top of the AEO v2 monitor + PR-gated auto-fixer that committed earlier the same day.
- **NEW `scripts/generate-blog-post.py` (30 KB, untracked, May-7 17:04):** Higgsfield-aware blog-post generator. Tier-based routing per the cost-optimized tool routing memory. Adds two npm scripts to `package.json` (uncommitted): `blog:draft` (`python3 scripts/generate-blog-post.py`) and `blog:render` (`python3 scripts/generate-blog-post.py --render`). Pairs with new `data/blog-pending/` workflow dir (also untracked).
- **NEW `data/blog-pending/storm-damage-roof-repair-park-city.json` (18 KB, May-7 17:09):** First draft from the new generator — Park City storm-damage piece. JSON includes title, slug, excerpt, category=Storm Response, etc. Pending review before render → `pages/storm-damage-roof-repair-park-city.html`. Slot in the storm-cluster hub-spoke that PR #12/#14 built (storm-damage hub already links to 11 city storm/hail/wind posts; this would be #12).
- **NEW `scripts/aeo-fix-refresh-stale.py` (15 KB, untracked, May-7 17:55):** Level-3 auto-fixer that complements the Level-2 LLM-based `aeo-fix-internal-links.py`. Pure mechanical refresh — **NO LLM** — bumps `dateModified` in JSON-LD + visible "Last updated" stamp on a stale page to today's date. Closes `refresh-stale` AEO action types (where the `aeo_actions.py` classifier flagged a high-leverage page as gone-stale). Rounds out the auto-fix coverage: `internal-link-boost` / `authority-boost` → Level-2 LLM fixer; `refresh-stale` → Level-3 mechanical fixer; remaining classifications (`freshness-anchor` / `create-content`) still need human authoring.
- **MODIFIED `scripts/aeo-fix-internal-links.py` (uncommitted, +21/-5 lines):** Runaway-protection patch after a **46-minute hung run** on action `aeo-ef3d8192` (looped through 23 candidates × 120s ollama timeout). Two bounds added: `MAX_LLM_CALLS = 8` (caps candidates evaluated per run) and `LLM_TIMEOUT_SEC = 30` (was implicit 120s; anchor drafting is ~300 token JSON, normally returns 5-15s). Also better progress logging (`[1/8] Trying foo (score 12)…`) and richer no-edits telemetry (`candidates_tried` + `candidates_total`).
- **NEW `supabase/functions/handle-lead/` (untracked dir, May-7 15:11):** Lead-handler Supabase Edge Function source-of-truth pulled into the repo as `index.ts` + `DEPLOY.md`. Previously the function existed only in the deployed Supabase project (referenced in PR #10's "POSTs to `/functions/v1/handle-lead`"); now its source is in-tree. **Note for future commit:** `.vercelignore` should add `supabase/` so the edge-function source doesn't ship to the static site CDN. The `.temp/cli-latest` companion dir is Supabase CLI scratch — should be `.gitignore`d.
- **Backup directories from 2026-05-06 video swap** (`assets/_backup-2026-05-06-drone-swap/` + `assets/videos/_backup-2026-05-06-showcase-swap/`) remain untracked and are already covered by the `.vercelignore` glob `assets/_backup-2026-*` added in commit 3adaf0f. Safe to either keep local-only or commit if cross-machine restore matters.
- **Open question (carried forward from 2026-05-06):** `data/aeo-citations/` rolling JSONs (`_actions.json` / `_filed_issues.json` / `_trend.json`) — still committed-as-bootstrap with parent dir `.vercelignore`d; whether to (a) periodically `git commit` trend data for cross-machine continuity, or (b) `.gitignore` the rolling JSONs and only commit the bootstrap snapshot. Defer 2-3 monthly runs to evaluate JSON drift rate.
- **Lighthouse audit follow-through (re: 2026-05-04):** unchanged — drone + showcase swaps from the morning of 2026-05-06 partially shipped fix #1 (mobile page weight / LCP). PostHog `disable_surveys` and the `index.html:1671` JS TypeError both still pending; no perf push today.
- **Status going into 2026-05-08:** untracked stack is functional but not yet committed. Suggested next-session ship — separate PRs for (a) the supabase/ edge source pull, (b) the blog generator pipeline + first Park City draft, (c) the Level-3 stale-refresh fixer, (d) the internal-link runaway guard. Keeping them separate preserves clean rollback + clean lift attribution.

### 2026-05-06 (evening) — AEO Citation Monitor v2: Repo-Aware Actions + PR-Gated Auto-Fixer + Anchor-Quality Hardening
Four commits (3adaf0f → 762b9b8) on the same day as the morning's drone/showcase/showreel/reviews work, now committing the AEO citation monitor stack that was logged earlier today as "untracked, local-only." Net effect: the prior claude.ai/code AEO monitor trigger is **retired**; replacement is repo-aware, ~$0.23/month, and closes the loop end-to-end (research → leverage-scored action → GitHub issue → optional PR-gated auto-fix).
- **Commit 3adaf0f / repo-aware citation monitor + auto-filed action issues (+1867 lines, 11 files):** The full Level-1 stack moves from untracked to committed.
  - `scripts/aeo-citation-monitor.py` (507 lines) — SerpAPI sweep across 5 high-value queries (local pack + organic + AI Overview), retry-with-backoff, telemetry → `~/.cache/local-llm-telemetry.jsonl`. Optional OpenRouter Perplexity Sonar citation check (skipped if no key). **Fixed local-pack detection bug** — Frame is **#1 for "best roofer Heber City Utah"** (the original organic-only check missed it).
  - `scripts/aeo_actions.py` (421 lines, NEW) — turns research into actionable, leverage-scored work. Per-query lead-value weights (1-10) → `leverage = severity × lead_value`. Repo-aware file mapping: action items reference real file paths with last-modified dates, word counts, current titles. Smart action classification (`freshness-anchor` / `authority-boost` / `refresh-stale` / `internal-link-boost` / `create-content`). Trend tracking → `_trend.json` (24-entry rolling history with sparkline). Persistent action IDs across runs → `_actions.json` shows `weeks_open`.
  - `scripts/aeo-file-issues.py` (242 lines, NEW) — reads `_actions.json` → files new actions as GitHub issues via `gh` CLI. Idempotent (action ID → issue number map in `_filed_issues.json`). Configurable threshold (high/medium/low), cap, dry-run, list modes. Auto-creates `aeo` + `auto-filed` + `priority:*` labels on first use.
  - `scripts/aeo-citation-cron.sh` + `Library/LaunchAgents/com.ryan.frame-roofing-aeo.plist` — replaces the monthly claude.ai/code trigger; runs **5th of month 8:23am**. Telemetry on success/failure.
  - `scripts/update-google-reviews-cron.sh` (108 lines) — cron-only replacement for the bi-monthly review-sync trigger built earlier today. Also includes telemetry. Pairs with the Review Bot identity already wired (commit 21edf38).
  - `.vercelignore` — `data/aeo-citations/` excluded from production deploy (internal monitoring artifacts, not customer-facing); `assets/_backup-2026-*` glob added to ignore the homepage video swap backups from the morning's commits 660ead1 / 64128a4 / 4009ffe.
  - **First run output (committed as bootstrap state):** Score **1/5** (Frame #1 in local pack for "best roofer Heber City Utah"). 3 medium-priority actions filed as GitHub issues **#15-#17**. Real competitor signal logged: `roofingutah.com` cited for Park City storm. State files: `data/aeo-citations/2026-05-06.{json,md}`, `_actions.json`, `_filed_issues.json`, `_trend.json`.
- **Commit 4f03d07 / Level-2 PR-gated auto-fixer (+561 lines):** `scripts/aeo-fix-internal-links.py` reads an open AEO action by `--action <id>` or `--issue <number>`, finds 3-5 high-relevance blog posts (excludes service/location/legal pages), drafts contextual link insertions via local `mistral-nemo:12b` (confidence ≥ 0.6 initial threshold — hardened to 0.75 in 762b9b8; anchor 3-12 words; anchor must exist verbatim in source paragraph). Handles both `internal-link-boost` and `authority-boost` action types — they share the same fix (add inbound internal links to the target page); only the diagnostic differs. Opens a branch `aeo-auto/<action-id>`, commits, pushes, opens a PR — **NEVER auto-merges**. Comments on the original GitHub issue with the PR link. Idempotent: refuses to re-run if branch already exists on origin. Auto-creates `aeo` + `aeo-auto-fix` labels on first use. **Safety constraints baked in:** cap 3 source-page edits per run; excludes `index.html`, `privacy.html`, `terms.html`, `thank-you.html`, all `pages/*`, `locations/*`, `archive/*`, `scripts/*`, `assets/*`, `images/*`; validates anchor text exists verbatim (rejects LLM hallucinations); skips paragraphs already containing `<a>` tags (no double-linking); 24-hour race-condition guard skips files Cowork may be editing. **Demo run:** opened **PR #18** with 3 edits adding internal links to the `roof-replacement-cost-utah-2026` cost guide from related blog posts.
- **Commit 8a1be3f / fix: don't pollute main with edits during PR creation:** Bug discovered the moment PRs #18-#20 ran — `apply_edits()` was writing to filesystem **before** the feature branch existed, leaving duplicate uncommitted changes on `main`. Fix: move the actual write into `open_pr()` (which already had the same logic for re-applying after stash); `apply_edits()` is now a validation-only pass. Also bumped `ls-remote` timeout 10s → 30s and gracefully continue on timeout (GitHub flakiness shouldn't abort a PR-in-flight).
- **Commit 762b9b8 / fix: tighten anchor quality — confidence + topicality:** First batch (PRs #18-#20) produced 3/8 borderline anchors. Two filters added:
  - `MIN_LLM_CONFIDENCE` 0.6 → **0.75** (drops PR #20's "Salt Lake City" anchor at 0.70).
  - New `_topic_overlap()` — anchor must share at least one topic word (length ≥ 4, not in domain stop list) with the target query. Stop words include domain-common terms (`utah`, `roofing`, `roofer`, `roof`, `best`, `top`, `help`) so e.g. "Utah" alone in an anchor doesn't count as topical. Year tokens (2024-2026) and 1-3 char tokens also filtered.
  - **Drops:** "Utah's freeze-thaw cycles" → cost-2026 (0 overlap); "Frame Roofing Utah" → storm damage (0 overlap); "Salt Lake City" → insurance claims (0 overlap); "Utah roof sustains emergency damage" → insurance (0 overlap).
  - **Keeps:** "what costs to expect" (cost match); "insurance claims process" (multi-match); "storm damage repair guide" (multi-match).
  - Net: future runs produce fewer edits per PR but each one is genuinely topical.
- **Open question logged:** `data/aeo-citations/_actions.json` / `_filed_issues.json` / `_trend.json` are committed as bootstrap state but the parent dir is also `.vercelignore`d. Subsequent monthly runs will append to these files locally — open whether to (a) periodically `git commit` trend data for cross-machine continuity, or (b) `.gitignore` the rolling JSONs and only commit the bootstrap snapshot. Defer 2-3 monthly runs to evaluate JSON drift rate.
- **Lighthouse audit follow-through (re: 2026-05-04 entry's 3 fixes):** unchanged from the morning's status — fix #1 (mobile page weight / LCP) partially shipped via the morning's drone + showcase swaps; PostHog `disable_surveys` and the `index.html:1671` JS TypeError both still pending.
- **No site/HTML/schema changes today's evening session** — all four commits are pure tooling. Source code (HTML / CSS / JSON-LD / sitemap) untouched between 21edf38 and 762b9b8.

### 2026-05-06 — Homepage Video Refresh: Drone Swap + Showcase Aerial + Branded Showreel Restore + Reviews → 20 + AEO Citation Monitor (untracked)
Four commits to main today (660ead1 → 21edf38) plus new local cron infrastructure for AEO citation monitoring + Google Reviews auto-sync. Net effect: homepage video stack got a quality + perf bump (partial mobile-LCP win on the 2026-05-04 audit's #2 finding), branded music showreel restored to bottom, automated review feed ticked 19 → 20.
- **Commit 660ead1 / Homepage drone footage swap:** `/assets/drone-footage.mp4` replaced (13.2 MB → **5.2 MB**, H.264 720p, 24.5s) with a fresh upscaled DJI clip (source: `dji_fly_20260504_172428` — real Landon drone, authenticity verified). New poster `/images/projects/heber-valley-drone-poster.webp` (luxury Heber Valley reroof, Wasatch backdrop). Moov-first faststart layout for instant streaming. Old files preserved in `assets/_backup-2026-05-06-drone-swap/` (untracked). **Direct partial fix for the 2026-05-04 audit's 6.1 MB mobile page weight finding** — drone clip was the largest single asset on the homepage at 13.2 MB; cutting it to 5.2 MB removes ~8 MB of mobile bandwidth on first load.
- **Commit 64128a4 / "Watch Us Work" showcase swap:** Top homepage `#watch-in-action` section swapped from the 22 MB / 10.6 MB 60-second narrative to a fresh aerial drone clip (`frame-restoration-showcase.mp4` 22 MB → **5.4 MB** H.264 720p / `.webm` 10.6 MB → 7.5 MB VP9 720p). Poster updated. Video element flipped from `controls` → `autoplay muted loop playsinline` (silent aerial reads better as ambient loop). Copy: "One minute / tear-off to finished" → aerial-pass framing. VideoObject JSON-LD `name`/`description`/`duration`/`uploadDate` updated to match. Meta tag "60-Second Showcase" → "Aerial Showcase". `?v=20260506` cachebuster on all asset URLs to defeat CDN/browser cache. Old files preserved in `assets/videos/_backup-2026-05-06-showcase-swap/` (untracked).
- **Commit 4009ffe / Restore branded 60s music showreel to bottom Video Showcase section:** The old 22 MB 1080p music narrative was NOT deleted — it was repurposed. New files `assets/videos/frame-roofing-showreel.{mp4,webm}` (22.0 MB / 10.7 MB) + poster shipped, and the bottom Video Showcase section now plays the click-to-play branded showreel **with audio**. Top section keeps the new silent DJI aerial loop from 64128a4. Element changed from `autoplay/muted/loop` → `controls` (autoplay-with-sound is browser-blocked; click-to-play unlocks the music). `?v=20260506` cachebuster on all 3 URLs. **Net homepage video stack:** silent aerial loop above the fold (fast LCP) + click-to-play branded music narrative below (full brand experience for engaged scrollers).
- **Commit 21edf38 / Google Reviews → 20 (auto-update):** Review Bot (`review-bot@frameroofingutah.com` automation identity) appended a new review to `data/google-reviews.json` and bumped `reviews.json` count 19 → 20. AggregateRating across LocalBusiness JSON-LD + 45 location pages now shows **20 reviews / 5.0 stars** (was already 20 in copy as of 2026-04-22 — this is the cron catching up the underlying JSON to match). Closes the "20 reviews" claim audit gap.
- **NEW AEO citation monitor cron stack (untracked, local-only):** Three new untracked artifacts on disk: `scripts/aeo-citation-monitor.py` (15.3 KB Python script — likely polls AI engines for Frame Roofing Utah brand mentions / extractions), `scripts/aeo-citation-cron.sh` (1.9 KB launcher), `scripts/update-google-reviews-cron.sh` (3.1 KB — wraps the existing `update-google-reviews.py` for cron). First citation snapshot at `data/aeo-citations/2026-05-06.md` (1.4 KB). All four files are gitignored or pending decision (also: `data/audits/` from the 2026-05-04 Lighthouse drop is still untracked). **Open question logged 2026-05-04 (whether to commit raw Lighthouse JSONs vs. rendered MD only) is still open** — neither path taken yet.
- **Lighthouse audit follow-through status (re: 2026-05-04 entry's 3 fixes):**
  1. Mobile page weight (LCP 7.8 s) — **partially shipped** by today's drone + showcase swaps (≈ −20 MB mobile bandwidth). PostHog surveys disable + JS console TypeError still pending. Re-audit recommended on next perf push.
  2. PostHog `disable_surveys: true` — still pending.
  3. `index.html:1671` JS TypeError — still pending.
- **No other code changes today.** No service-page edits, no schema changes, no sitemap delta. Source code untouched between 21edf38 and the next session.

### 2026-05-04 — Lighthouse Audit Drop from Frame-TX Session (Live Site: 70/100/100/100 mobile · 93/100/96/100 desktop)
No code commits today — but a meaningful artifact landed. A Lighthouse 13.1.0 audit of `https://www.frameroofingutah.com/` was dropped into `data/audits/` (untracked) by a Frame-TX session for the Utah dispatch lane to act on. Three new files (≈1.8 MB total): `lighthouse-2026-05-04-mobile.json`, `lighthouse-2026-05-04-desktop.json`, and a rendered summary `lighthouse-2026-05-04.md`. **The audit is NOT auto-applied** — per the dispatch hard rules in MEMORY (`feedback_frame_roofing_reviews.md`, `feedback_frame_brand_boundary.md`), all Utah HTML edits go through the Utah Cowork lane to avoid race conditions with parallel sessions.
- **Scoreboard:** Desktop **93 / 100 / 96 / 100** (perf / a11y / best practices / SEO). Mobile **70 / 100 / 96 / 100**. A11y 100 + SEO 100 on both viewports — strong line item for Landon's value memo. Mobile perf + best-practices are the only gaps.
- **Mobile CWV:** LCP **7.8 s** 🔴 (target < 2.5 s) — single biggest score-killer. CLS 0, TBT 0 ms, FCP 2.4 s, Speed Index 4.4 s. Mobile LCP > 2.5 s is what's blocking "Good" mobile CWV in GSC — a direct Google ranking signal for "roofer near me" queries.
- **Three specific fixes recommended** to take Utah to 100/100/100/100:
  1. **JS TypeError on every page load** (−4 BP both viewports). `errors-in-console` failing — `index.html:1671` calling `document.addEventListener(...)` where the LHS isn't an EventTarget. Fix: null-check the selector or fix the typo. Estimated 10 min. Flips BP 96 → 100.
  2. **6.1 MB mobile page weight** (LCP 7.8 s → ≤ 2.5 s). 30 requests / 6 scripts / 3 stylesheets / 3 fonts. Likely culprits: hero/photo imagery shipped at desktop res to phones (no `srcset`), PNG/JPEG instead of WebP/AVIF (note: 88 images already converted 2026-04-12 — verify hero set is included), missing `loading="lazy"` below fold, missing `width`/`height` attributes. Estimated 1–2 hr. +15–20 mobile perf pts (70 → 85–90).
  3. **PostHog shipping 65 KB unused JS** (−3–5 mobile perf). `posthog/static/array.js` 37.8 KB unused / `surveys.js` 27.0 KB unused (81%). Frame Utah doesn't use PostHog surveys. Fix: pass `disable_surveys: true` at init OR switch to the `array.no-surveys.min.js` CDN variant. Estimated 5 min. +3–5 mobile perf pts.
- **Combined post-fix projection:** Desktop 98 / 100 / 100 / 100. Mobile 92 / 100 / 100 / 100. 100/100/100/100 is achievable if hero conversion gets total page weight to ≤ 1.5 MB (mobile perf 95+).
- **Suggested next-session ship:** single PR titled `perf: Lighthouse 70→92 mobile + 100/100/100/100 desktop`. Re-audit on Vercel preview before squash-merge. Do NOT bundle with marketing content changes — keep it focused for clean rollback + clean lift attribution in the Friday weekly review.
- **Open question to resolve before commit:** whether to commit the JSONs (~1 MB each, useful for trend tracking against future audits) or `.gitignore data/audits/lighthouse-*.json` and only commit rendered summaries. The audit MD recommends one of those two paths, not pushing the raw JSONs unreviewed.
- **No commit made today** — refresh-only update to capture the audit context for the next Utah-lane session. Source code untouched since PR #14 (commit 818a0bd, 2026-05-01).

### 2026-05-01 — May Lead-Velocity Sprint Day 1: 6-PR Wave (Reviews / noindex Fix / Form CRO / Storm Hub-Spoke / Neighborhood Depth / FAQ AEO)
Six PRs merged to main today (#9 → #14) executing the first wave of the May 2026 lead-velocity sprint (canonical plan: `data/30-day-lead-sprint-2026-05-01.md`, vercelignored). All shipped through normal squash-merge → main → Vercel auto-deploy. Mix of CRO, SEO unblocks, AEO, and topical-cluster depth — no edge-function or schema-breaking changes.
- **PR #9 / Review velocity foundation (commit af15bc8):** Move #1 of the sprint — close the 20→54 review gap vs roofingutah.com (the #1 map-pack ranking factor per the May audit). Three surfaces:
  - **`thank-you.html`** — added a second card below "Message Received" (gold left-border, 5-star badge, "Help the next homeowner") pointing at `/review.html`. Both cards now in a flex stack. New PostHog events: `review_cta_click` (source=thank_you) + `lead_thank_you_view` for funnel attribution.
  - **`index.html` footer** — Contact column gained a gold "★ Leave a Review" link to `/review`. Every visitor (not just post-conversion) now has a review path.
  - **`pages/about.html`** — legal name corrected `Frame Roofing Utah LLC` → `Frame Restoration Utah LLC` in JSON-LD `legalName` + footer copyright. **Resolves the open question logged 2026-04-30** about whether the entity-name correction should land. Confirmed: registered Utah LLC = Frame Restoration Utah LLC (DBA Frame Roofing Utah).
  - **Sprint docs added (vercelignored):** `data/30-day-lead-sprint-2026-05-01.md` (canonical May sprint plan — 5 highest-EV plays w/ effort scores + impact + cadence) + `data/RYAN-VALUE-MEMO-2026-05-01.md` (Landon-facing build receipt + 10% commission cadence ask).
- **PR #10 / 5-week-old noindex bug + form CRO (commit ee88e79) — CRITICAL:** Two service pages had been silently telling Google `noindex,nofollow` for **5 weeks** since commit 53d6352 (2026-03-28 storm-damage consolidation). `pages/storm-damage.html` (highest-CPL page in Frame's vertical — hail / wind / insurance) AND `pages/general-contracting.html` were both in sitemap.xml + canonical to themselves but tagged noindex. Fixed: `noindex,nofollow` → `index,follow,max-snippet:-1,max-image-preview:large` on both. Plus Move #3 from the sprint — **form CRO 6→3 required fields** across all three primary funnels (index hero form, index #contact long form, global-modal.js booking modal). Required: Name + Phone + ZIP. Email/city/service/issue/message moved into a collapsed `<details>` "Add details (optional)" disclosure. Hero subhead changed to "3 fields, 30 seconds. We'll call you back within 15 minutes during business hours." Documented expectation: 20-35% completion lift. Modal also fixes a typo where ZIP wasn't being captured before. Forms still POST to same `/functions/v1/handle-lead` endpoint (handle-lead v5 already tolerates empty optional strings).
- **PR #11 / Visible FAQ blocks matching schema on top 3 service pages (commit a5b2351):** Move #5 from the sprint (AEO citation moat). storm-damage, roof-replacement, and insurance-claims pages all had FAQPage JSON-LD in `<head>` but **zero matching visible HTML** — schema-content mismatch silently disqualified them from FAQ rich-result eligibility, and AI engines (ChatGPT, Perplexity, Google AI Overviews) couldn't extract answers because they only cite from visible content. Fix: 4 visible Q&A blocks per page inserted between `</main>` and `<footer>`, near-verbatim to the JSON-LD (≥70% word overlap, no contradicting facts), reusing residential-roofing.html's existing FAQ block style for visual consistency. Each page +23 lines. Especially high-value for storm-damage now that PR #10 just unblocked it from indexing.
- **PR #12 / Storm-damage hub: link to all 11 city storm/hail/wind blog posts (commit 754114c):** storm-damage.html previously linked to only 2 city posts. Expanded the hub-spoke cluster to all 11 city storm/hail/wind posts (hail pillar + storm checklist + sandy/lehi/herriman/riverton/draper/ogden/bountiful/provo/west-jordan/heber-city). Section retitled "City-Specific Storm & Hail Damage Guides" (H3 → H2). Compounds the noindex unlock — city posts now flow PageRank into the canonical storm-damage page, AND any user landing on a city post has a clear path to the conversion-optimized service page.
- **PR #13 / Review CTAs on 45 location pages + thank-you primary CTA flip (commit 8c57b7a):** Move E + D from the sprint.
  - **Move E:** Each of the 45 location-page footers now carries a gold "★ Leave a Review" link to `/review.html`, inserted into the existing footer link row (Home · Privacy · Terms · phone). Single perl in-place edit; all 45 files match. **45×s the review-acquisition surface area** beyond the homepage footer + thank-you page already shipped in PR #9.
  - **Move D:** thank-you page primary visual CTA flipped from "Back to Home" (wasted action — sending converted leads back to the page they just left) → **gold ☎️ Call 435-302-4422 (tel:)** as the primary CTA. New headline: "Got it — we're on it." Sets explicit 15-minute response expectation tied to business hours. "Back to Frame Roofing Utah" demoted to a small grey link. PR #9's review card preserved as secondary cardstack. New PostHog event `post_lead_call_now` fires on tap so the dashboard can measure submitter→immediate-call conversion (strongest possible lead-quality signal).
- **PR #14 / Storm cluster reverse-links + neighborhood depth on top 3 location pages (commit 818a0bd):** Two compounding moves shipped together.
  - **PART 1 — Reverse storm CTAs on 11 city blog posts:** Closes the loop on PR #12. Every one of the 11 city storm/hail/wind blog posts now has a dark-navy CTA card (with city-personalized headline like "Storm hit your Sandy roof?") pointing back to `/pages/storm-damage`, plus a tap-to-call CTA. Inserted between `</article>` and existing related-articles / blog-cta / author-bio. JSON-LD untouched. None of the 11 had a pre-existing CTA card to /pages/storm-damage — only inline body links — so all 11 got the dedicated card.
  - **PART 2 — Neighborhood-specific depth on 3 highest-traffic location pages:** Audit agent flagged location pages as templated despite the earlier "unique city-specific content" rewrite. Real depth comes from concrete local data AI engines can extract for citation. Added a "{City} Neighborhood-Specific Roofing Notes" section with 4 callout cards each, sourced from real public planning code + IRC + manufacturer specs:
    - **heber-city.html:** Old Town pre-1960s cedar-shake-under-asphalt → tear-off on reroof; 5,600 ft snow load 50-70 psf per IRC Table R301.2(1) + structural review on 60+ yr roofs; mountain ice dam → 3 ft past interior wall I&W per IRC R905.1.2; Wasatch Mountain HOA earth-tone palette (OC TruDefinition Estate Gray / Driftwood).
    - **park-city.html:** Old Town HDC review per Ch. 15.11; Deer Valley Class A + 30-yr warranty (CT Landmark / standing-seam OK, 3-tab disqualified); 7,000 ft + lake-effect 80-100 psf, full-deck I&W shield above 6,500 ft; Empire Pass / Promontory / Glenwild HOA design-review coordination.
    - **salt-lake-city.html:** Avenues HDC permit on 1,300+ contributing structures; Sugar House 1920s-40s bungalows 4/12-6/12 + Class A citywide; Capitol Hill / Federal Heights — copper restricted in some Capitol Hill micro-overlays; SLC heat island west of I-15 → cool-roof shingles or 50%+ ventilation upgrade.
    - Each section closes with a sources attribution line citing the city planning code, IRC R905, manufacturer specs, and Frame's Utah DOPL license #14256097-5501. Reuses existing `.services-mini` / `.service-mini-card` classes (zero new CSS).
- **Net effect:** the 5-week-silent noindex bug is fixed, review-acquisition surface area went from ~2 surfaces to ~48 (homepage footer + thank-you + 45 location footers + about page links), the top 3 lead-volume forms are 50% smaller (6 → 3 required fields), the storm cluster now has a complete bidirectional hub-spoke (1 hub ↔ 11 spokes), top 3 service pages have visible-FAQ AEO citation surface, and the top 3 location pages have concrete neighborhood-specific data AI engines can quote. **Untouched intentionally:** Twilio 10DLC TCR brand-alignment from yesterday (no new fixes there yet); /dashboard/ (no Phase 3 work today); CLAUDE.md self-update sweep (this auto-refresh).

### 2026-04-30 — 10DLC Brand-Alignment Fix: Registered Brand Name in SMS Consent + Privacy + Terms
Single commit (1a15637) closing TCR campaign rejection **30927**: "opt-in consent named a different company than the registered brand 'Frame Restoration LLC'." The registered TCR brand for A2P 10DLC is **Frame Restoration LLC (DBA Frame Roofing Utah)** — but the SMS consent copy across forms, modal, and legal pages had been naming "Frame Roofing Utah" alone. Fix sweeps every SMS-consent surface to use the legally-registered brand string while leaving the consumer-facing "Frame Roofing Utah" brand in place everywhere else. **4 files changed** (`global-modal.js`, `index.html`, `privacy.html`, `terms.html` — 7 insertions, 7 deletions).
- **Forms (3 places — index hero form, index contact form, global modal):** consent copy now reads "I agree to receive SMS/text messages from **Frame Restoration LLC (DBA Frame Roofing Utah)**, sent from +1 435-292-8802, about my inquiry — including appointment confirmations, inspection scheduling, project updates, and service follow-ups." Was "from Frame Roofing Utah (sent from +1 435-292-8802)". Restructured punctuation: parenthetical sending-number → comma-delimited, then em-dash before the message-types list — keeps the registered-brand clause reading cleanly.
- **index.html Contact → Text card microcopy:** "SMS from **Frame Restoration LLC (DBA Frame Roofing Utah)**. Msg & data rates may apply…" (was "SMS from Frame Roofing Utah…").
- **privacy.html §SMS/Text Messaging:** opener rewritten to "SMS/text messaging is a separate, optional service operated by **Frame Restoration LLC (DBA Frame Roofing Utah)**." Both subsequent "from Frame Roofing Utah" mentions in this section now read "from Frame Restoration LLC."
- **terms.html §SMS/Text Messaging Terms:** same swap in the opener; **§Cost** also updated — "Frame Restoration LLC does not charge for text messages…" (was "Frame Roofing Utah does not charge…").
- **What this unblocks:** TCR (The Campaign Registry) CTA verification → Twilio campaign approval → Twilio outbound SMS delivery. Continues the rejection-chain saga (30034 → 30923 → 30927). Verizon email-to-SMS gateway (4353024422@vtext.com) remains the working fallback in the meantime.
- **Untouched intentionally:** marketing copy, hero H1, page titles, footer brand lines — those all keep "Frame Roofing Utah" since the TCR brand-alignment requirement only applies to the SMS-consent context. "Frame Restoration LLC" only appears in SMS-consent strings + SMS-related legal sections.
- **Working-tree note (uncommitted as of refresh):** `pages/about.html` has a 2-line edit changing JSON-LD `legalName` and the footer copyright string from "Frame Roofing Utah LLC" → "Frame Restoration Utah LLC" — a related entity-name correction matching CLAUDE.md's PEOPLE section. Not yet committed. **Open question worth verifying with Landon/TCR docs:** the registered TCR brand is "Frame Restoration LLC" (no "Utah"), but the about.html JSON-LD/footer use "Frame Restoration Utah LLC" (with "Utah"). These may be two distinct strings (LLC filing name vs. TCR brand registration name) — confirm before committing about.html so the schema's `legalName` matches the actual Utah Secretary of State filing.

### 2026-04-28 (afternoon) — /dashboard/ Phase 2 Site Health Tiles + Silent-Webhook-Failure Recovery + PSI Wiring
Three commits (#6, #7, #8) extending `/dashboard/` with SEO/AEO signals beyond Supabase data — and surfacing a Vercel deploy-trust gap in the process.
- **PR #6 / Phase 2 Site Health tiles (commit 50320e1):** New "Site Health" section between Biggest Movers and Top Pages, three tiles pulling SEO/AEO signals beyond the existing Supabase pipeline. **194 lines added across 2 files** (`dashboard/dashboard.js` +178, `data/dashboard-config.json` +16). All client-side; no edge function changes.
  - **⭐ Reviews Velocity** — reads `/reviews.json` (already populated by SerpAPI bi-monthly sync). Total count + rating + days-since-last-review color-coded (green ≤14d, orange ≤30d, red older). Configurable competitor-gap line displays "vs roofingutah.com: gap 34" — surfaces the marketing-build audit's #1 finding (reviews are the actual gap, 20 vs 54). No auth, no spend.
  - **📝 Content Freshness** — parses `/sitemap.xml` lastmod dates. Total indexed pages + days-since-most-recent-update + 7d/30d/90d update counts. Catches the "we haven't touched /pages/storm-damage in 6 months" stale-content drift pattern. No auth, no spend.
  - **⚡ Core Web Vitals** — PageSpeed Insights API integration with 60-min sessionStorage cache (avoids repeated PSI hits within a session). LCP / CLS / INP / TTFB origin-wide field data when available + Lighthouse lab perf score. Graceful fallbacks: no API key → "Add a key to dashboard-config.json"; has key, no field data → "Insufficient field data — site needs more traffic for CrUX inclusion." Frame Utah is currently in the second state (587 impressions/30d too low for CrUX inclusion).
  - **Config:** `dataSources.{reviews, sitemap, psi}` in `dashboard-config.json`. Each tile silent-fails if endpoint unreachable — won't break dashboard if a feed goes down.
- **PR #7 / Force re-deploy + verify-deploy.sh (commit d083be1):** PR #6 (v0.11.0) merged to main 16:24Z but Vercel didn't deploy it — both Vercel projects still served v0.10.0 (26231 bytes) while GitHub raw had the correct v0.11.0 (36230 bytes). The squash-merge → main → Vercel webhook chain failed silently. Fix:
  - Added `deploy-stamp: v0.11.1` comment at top of `dashboard.js` to force a content change Vercel can't dedupe with previous build cache.
  - Added `scripts/verify-deploy.sh` — compares local file size to production after a configurable wait, fails loud on drift. Should run after every PR merge that touches `/dashboard/*` until we trust the auto-deploy chain.
  - **Lesson logged:** declaring "shipped" after `gh pr merge --auto` without verifying production bytes is a real bug — closing the loop with the verify step.
- **PR #8 / Wire PSI API key + dataSources to inline config (commit b6f817c):** Loaders read `CFG.dataSources.*` from `window.DASHBOARD_CONFIG`, but the inline config block in `dashboard/index.html` only had the flat `clientName/supabaseUrl/etc.` fields — no `client.domain` and no `dataSources` block. Result: PSI tile fell through to the configure-key prompt even though `data/dashboard-config.json` had all the wiring. Fix: added `client.domain` (used as PSI origin fallback) + `dataSources` block (stormWatch/reviews/sitemap/psi) mirroring `data/dashboard-config.json` so the loaders actually see config. Live PSI API key now in both files; key restricted to PSI API + HTTP referrer `www.frameroofingutah.com/*` in Google Cloud Console (so even though it's exposed in page source — necessary for browser-side PSI calls — blast radius is limited). Bumped deploy-stamp to **v0.11.2** to force byte-size change so Vercel actually redeploys (vs. the v0.11.0 silent webhook failure discovered earlier the same day).
- **Net effect:** `/dashboard/` now ships its first non-Supabase signal layer — reviews velocity (which directly maps to the marketing-build $1,700/mo reviews acquisition allocation), content freshness (catches stale pages before they drift), and CWV (origin-field where CrUX data exists, lab fallback otherwise). Plus a deploy-trust loop closure (`verify-deploy.sh`) so the next webhook silent-failure gets caught immediately instead of hours later.

### 2026-04-28 — /dashboard/ Polish Stack: Chart-Height Hotfix + Growth-vs-Prior-Period + Biggest Movers + Storm Watch
Three back-to-back commits hardening `/dashboard/` after the 2026-04-27 evening launch. All client-side — no edge function changes, ships immediately on Vercel deploy.
- **Chart canvases full-screen-height hotfix (commit 96f2c56):** Chart.js doughnuts were rendering 1500px+ tall on wide monitors because `maintainAspectRatio:true` (the default) sizes the canvas to match its container width. On a 1500px-wide chart cell, that produced a 1500px-tall doughnut, pushing top pages table + location heatmap + recent leads + insights below the fold. Fix: cap canvas to 280px max via CSS + set `maintainAspectRatio:false` on both Chart.js configs so Chart.js respects the CSS-imposed height. 1 file changed (`dashboard/dashboard.js`).
- **Growth-vs-prior-period comparisons (commit ed493ea):** Time-range buttons changed 7/14/30/90 → **7/30/60/90** (Ryan's preference for SEO/AEO impact tracking). For each click, dashboard now fires TWO parallel fetches (`days=N` and `days=2N`), derives prior-period totals by subtraction (`current_2N − current_N`). Added growth indicators on KPI tiles (Pageviews / Google Organic / Form Leads / Inbound Calls — green=up, red=down, "NEW"=no prior data) + new Growth column on the Top Pages table showing per-page % change vs prior period of same length. Doubles bandwidth per range click (2 fetches instead of 1) — acceptable for the SEO/AEO observability win. If edge function gets slow at days=180, can later add a server-side `compare_days` param to do this in one call. 3 files changed (`dashboard/dashboard.js`, `dashboard/index.html`, `data/dashboard-config.json`).
- **🚀 Biggest Movers section + ⛈️ Storm Watch tile (commit 7a42f1d):** Two compounding wins on top of the growth data.
  - **🚀 Biggest Movers** — between charts and Top Pages, three columns: 📈 Top Growers (top 3 pages by positive growth %), ✨ Fresh Wins (top 3 NEW pages with no prior-period data), 📉 Needs Attention (top 3 pages with negative growth). Each card: page name + view count + growth %, color-coded. Directly extracts the SEO/AEO impact signal from the growth metrics added in #4.
  - **⛈️ Storm Watch** — top of dashboard, polls NWS API (`api.weather.gov`) on every render. Free public API, browser CORS allowed, no auth. Filters to roofing-relevant events (storm, wind, hail, tornado, thunderstorm, hurricane, winter, blizzard, ice, snow, flood) at Moderate/Severe/Extreme severity. **Active alert:** red/orange/gold border + event icon + headline + areaDesc + "Storm-trigger reserve recommended" CTA (ties to the $800/mo storm-trigger reserve recommendation in `data/MARKETING-BUILD-CONTEXT.md`). **No alerts:** green checkmark + "Storm-trigger reserve held; bid back to baseline." Configurable area via `dataSources.stormWatch.area` (default: UT). Silent fail if NWS unreachable so a weather-API outage can't break the dashboard. 2 files changed (`dashboard/dashboard.js` +114 lines, `data/dashboard-config.json` +5 lines).
- **Net effect:** `/dashboard/` is now actually usable on wide monitors (chart fix), shipped its first SEO/AEO observability layer (growth deltas + Biggest Movers), and gained a marketing-intel-aware operations widget (Storm Watch ↔ $800/mo storm reserve). Zero edge-function or schema changes — all four PRs merged today were pure client-side.

### 2026-04-27 (evening) — Dashboard Module + Marketing Build Context + Wave 2 Wiring + /dashboard/ Hotfix
Two PRs merged via release branch `release/2026-04-27-dashboard-and-marketing` plus the immediate hotfix.
- **PR #1 (commit bb48056):** `/dashboard/` productized module + marketing build context + Wave 2 marketing-intel wiring. **27 files changed, +5708/−1.**
  - **`/dashboard/` module** — productized client-dashboard vendored from RCBuild-Kit v0.8.0, replaces standalone `seo-report.html` pattern with a parameterized template. Config-driven via `window.DASHBOARD_CONFIG` (reads `data/dashboard-config.json`). **Same edge function (`/functions/v1/weekly-report`), same PIN system, same Supabase tables — existing PINs continue to work.** Files added: `dashboard/index.html` (118), `dashboard/dashboard.css` (196, with `--brand-accent` CSS variable for white-label), `dashboard/dashboard.js` (353; identical UX to seo-report.html — PIN gate, KPI grid, charts, admin panel), `data/dashboard-config.json` (86, per-client install state).
  - **Migration path:** `/seo-report.html` ships unchanged as fallback. After ~7-day soak verifying `/dashboard/` in production, plan 301 from `/seo-report.html` → `/dashboard/`.
  - **`.vercelignore` cross-state leak guard** — added 15+ Texas-pattern globs (`Fort-Worth-*`, `Dallas-*`, `Frisco-*`, `Plano-*`, `McKinney-*`, `Allen-*`, `Prosper-*`, `Celina-*`, `Lewisville-*`, `Carrollton-*`, `Flower-Mound-*`, `Southlake-*`, `TX-*`, `Texas-*`, `DFW-*`) so TX content can never accidentally deploy on the Utah site. Mirrors the brand-boundary lockdown enforced 2026-04-22.
  - **Marketing build context (canonical entry-point pattern):**
    - `data/MARKETING-BUILD-CONTEXT.md` (308 lines) — single "start here" doc for any agent (Cowork, Claude Code, future-you) working on Frame Utah marketing. Contains active $15K/mo allocation, Wave 1 findings already integrated, Wave 2 fire commands, free actions today, KPI scoreboard, confidence scorecard, full file index.
    - `data/market-intel-audit-2026-04-27.md` (252 lines) — full $15K/mo deep-dive audit: Tier-1-4 city ranking with EV math, channel ranking with Utah-specific CPL, Wave 1-3 confidence scorecard, KPI dashboard, open decisions.
    - `DASHBOARD-SECURITY-CHECKLIST.md` (122 lines) — per-project security verification with active curl tests for RLS posture (vercelignored as `*.md`).
  - **CLAUDE.md self-update (+57 lines):** new "💰 MARKETING BUILD (Active April 2026)" section between PRIORITY TODO and GIT WORKFLOW. Quick-reference allocation, Wave 1 finding (roofingutah.com content-DORMANT since 2024-05-31 → reviews acquisition is the actual gap, not content), npm scripts table, free actions, key files list, operational separation reminder pointing at MARKETING-BUILD-CONTEXT.md as canonical entry.
  - **Wave 2 marketing-intel wiring (~$33 spend pending company CC):**
    - `scripts/market-intel.mjs` + `scripts/market-intel-config.json` — 45 Wasatch Front + Heber Valley cities × 4 tiers, $15K growth tier
    - `scripts/market-intel-kit/` — bundled scoring + allocator + sources from RCBuild-Kit v0.7.0 (kept in-repo so runner works without npm-link)
    - `scripts/competitor-ads.mjs` — SerpAPI sweep on roofingutah.com + utahroofingcompany.com + reignroofing.com (~30 queries × $0.005 = ~$0.15)
    - `scripts/competitor-tiktok.mjs` — Apify TikTok scraper for UT competitor handles (~5 profiles × $0.30 = ~$1.50)
    - `scripts/supabase-pipeline-query.sql` — ready-to-paste SQL for real per-city job-value + close-rate from `leads` table (Tier C → A unlock)
    - All scripts dry-run when env vars (`SERPAPI_KEY`, `APIFY_TOKEN`, `DATAFORSEO_*`, `CENSUS_API_KEY`) are missing — print queries + estimated spend, exit 0, no API calls.
    - First-run output committed: `data/market-intel-report.md` (449), `data/market-intel-allocation.json` (2099) — offline mode all-neutral factors → all-LSA naive allocation; documents the concentration argument from the audit.
  - **`package.json`:** 4 npm scripts wired — `npm run market-intel`, `market-intel:dataforseo`, `competitor-ads`, `competitor-tiktok`. `scripts/*` and `*.md` vercelignored; `data/market-intel-allocation.json` deploys but contains no PII (internal scoring snapshot only).
  - **Side update:** `directory-tracker.html` — BuildZoom directory submission marked Live with profile URL (`https://www.buildzoom.com/contractor/frame-restoration-utah-llc`).
- **PR #2 / hotfix (commit 801bc59):** Fix `/dashboard/` — use absolute paths for CSS + JS. **Bug:** Vercel's `cleanUrls` + `trailingSlash:false` combo redirects `/dashboard/` → `/dashboard` (no trailing slash). With relative paths, `dashboard.css` resolved to `/dashboard.css` (sibling, 404) instead of `/dashboard/dashboard.css` (child, the actual file). Cascade: `dashboard.js` never loaded → `window.__dashboard` undefined → form's `onsubmit` handler threw `TypeError` → form submitted natively → page reloaded with no auth attempted. User saw "PIN didn't work" because the JS path that would have called the edge function never executed. **Fix:** switch `<link>` / `<script>` tags to absolute paths so resolution doesn't depend on the request URL's trailing-slash state. Same fix applied upstream in RCBuild-Kit `core/client-dashboard/template/`.

### 2026-04-27 — Blog Hero Image Uniqueness + Template Parity + Eager-Load LCP Fix
Three commits closing residual blog image-collision gaps left over from the 2026-04-20 hero photo rotation pass and a follow-up perf fix for blog hero LCP.
- **Reassign 3 duplicate blog hero images to unique real-Landon shots (commit a89192e):** Audit found 35/38 unique og:image entries across blog — three posts were still sharing hero photography. Fixes:
  - `blog/heber-city/reroofing-complete-guide.html` — `frame-restoration-08` → `heber-custom-reroof-aerial-1` (updated body `<img>`, og:image, and JSON-LD schema image — all 3 references aligned)
  - `blog/utah/how-to-choose-a-roofer-utah.html` — `frame-restoration-18` → `heber-valley-crew-rooftop` (og:image only at this commit)
  - `blog/utah/spring-roof-inspection-utah.html` — `heber-city-residential-reroof-2026` → `salt-lake-city-residential-reroof-2026` (og:image + twitter:image)
  - Result: **38/38 unique og:image entries**, all authentic real-Landon photography. Eliminates cross-post visual collision in social shares + Discover feed.
- **Bring how-to-choose-a-roofer-utah to template parity (commit 4efcc14):** Follow-up on the prior commit. The post had an inconsistency the audit didn't catch on the first pass — body `<img>` and JSON-LD `image` field weren't aligned with the new og:image. Fixes:
  - Added missing JSON-LD `BlogPosting.image` field (was absent from schema entirely)
  - Updated body hero `<img>` + alt text from `salt-lake-city-residential-reroof-2026` (which is now spring-roof-inspection-utah's hero — would have re-introduced a collision) → `heber-valley-crew-rooftop` to match og:image
  - All 3 image references now aligned (og:image + body img + JSON-LD image) — full template parity restored
- **Eager-load blog hero images for faster LCP (commit 40b6073):** User-reported visible blank gap on first paint where blog hero images appeared missing. Root cause: `figure.blog-featured-image` heroes were marked `loading="lazy"` even though they sit just below the dark page header — still in the initial viewport on most devices. Lazy loading was deferring fetch past first paint and dragging LCP. Swapped `loading="lazy"` → `loading="eager" fetchpriority="high"` across all 23 blog posts using the template — web-standard pattern for above/near-the-fold heroes. **0 remaining lazy-loaded heroes** in the blog hero figure template. Affects 23 files (1 line each, +23/−23). Visible-on-paint blog heroes; cleaner LCP signal for Core Web Vitals.

### 2026-04-24 (afternoon) — AEO Audit Gap Closure + llms.txt Markdown Polish + .vercelignore Incident #3
Three commits closing gaps surfaced by the 2026-04-24 Perfect Stack AEO audit (score 76/B → projected 91/A-) plus a repeat of the `.vercelignore` sweep booby-trap.
- **.vercelignore incident #3 (commit f51a24c):** `directory-blitz-tool.html` was 404ing in production — Landon is actively working through its "What We Need From You" todo block. Same root cause as the 2026-04-22 seo-report.html and reviews.json incidents: the audit commit swept it into the "Internal-only HTML" ignore bucket by name. While in, also unblocked the two siblings shipped by the same bug: `backlink-playbook.html` (12 backlink opportunities, shared with Landon) and `directory-tracker.html` (interactive React tracker, 38 directories) — both 404 live for the same reason. Expanded the `.vercelignore` warning header with the directory-blitz-tool incident note. **Working heuristic (third time confirmed):** "is this file fetched by the site OR shared with a customer/partner? If yes, LEAVE IT." The name-based "internal-looking" heuristic has failed three times — retire it.
- **AEO audit gap closure (commit ba20b2e):** Closed 4 of 6 Perfect Stack gaps flagged in the 2026-04-24 run.
  - **index.html hero `<p>`:** prepended "Frame Roofing Utah is a family-owned, licensed roofing contractor based in Heber City" (definition pattern — AI systems extract direct answers from "[Entity] is a [class] [qualifier]" structures). Fixed "10-year" (non-breaking hyphen) → "10 years" so the AEO-audit regex that counts numeric specifics actually matches.
  - **.quick-answer-strip section** added between hero and `#watch-in-action` — reinforces the definition + specifics (20 reviews / 5.0 stars / 45 cities / BBB A+) and carries a visible "Last updated: April 24, 2026" freshness stamp. The 87 blog + location pages already had last-updated stamps from 2026-04-22; the homepage itself was the gap.
  - **LocalBusiness JSON-LD:** added `"dateModified": "2026-04-24"` (was absent — maintenance-layer scored 0/2, now 2/2).
  - **llms.txt Texas-brand disambiguation:** rewrote "is a different business" → "not to be confused with … distinct from" phrasing (AEO entity-disambiguation checks look for "not to be confused" / "distinct from" / "different from" markers — the prior phrasing bypassed the heuristic).
  - **Intentionally untouched:** review count (owned by scheduled trigger `trig_01NZuRWGiWRybBqCYgrG5zcC`), .vercelignore hygiene (Cowork's territory), CWV field-data (needs real-user PSI data).
- **llms.txt A-grade markdown polish (commit 58d07f1):** audit's `llms_txt_quality` check counts `](http` markers as a proxy for llmstxt.org format (title + blockquote + sections + links). File had zero markdown-style `[title](url)` links — all Blog Library + People/Owner entries used `- Title — url`. Converted 11 lines to `- [Title](url)`. No content loss; links now crawlable as markdown for AI agents honoring the llmstxt.org spec. Score delta: `llms_txt_quality` 2/5 → 5/5 projected; overall 88 → 91 (A-).

### 2026-04-24 — 10DLC Fix: Publish Twilio Sending Number (435-292-8802) on Site + Forms + Privacy + Terms
Single commit (30b0215) to satisfy **TCR (The Campaign Registry) CTA verification** for A2P 10DLC. Carriers (T-Mobile / AT&T) were rejecting the campaign because the sending number wasn't disclosed on the website or in the consent flow. Fix: publish 435-292-8802 everywhere SMS-related, keep 435-302-4422 as the voice number.
- **index.html:**
  - Hero form + contact form — SMS consent checkbox copy expanded: names the sending number "(sent from +1 435-292-8802)", enumerates message types (appointment confirmations, inspection scheduling, project updates, service follow-ups), adds "Msg frequency varies (up to 5/month)" and "Reply STOP to opt out, HELP for help"
  - Contact section — new **Text** contact card added next to Phone card with `sms:+14352928802` link + inline microcopy "SMS from Frame Roofing Utah. Msg & data rates may apply. Reply STOP to opt out."
  - Footer — split single phone line into two lines: "Call: 435-302-4422" (tel:) + "Text: 435-292-8802" (sms:)
- **global-modal.js** — booking modal's SMS consent copy upgraded to match the expanded version (sending number + frequency + STOP/HELP)
- **privacy.html §SMS/Text Messaging** — names "+1 (435) 292-8802" as the sending number; adds "inspection scheduling" to the message-type list
- **terms.html §SMS/Text Messaging** — same: names the sending number explicitly + expanded message types
- **data/google-reviews.json** — refreshed (23-line diff from routine scraper run)
- **screenshots/mobile-audit/** — 9 new mobile-audit captures + diagnostics.json committed (home / heber-city / roof-replacement / blog-storm above-the-fold + full + nav-open variants)
- **CLAUDE.md rule updated** — the old "ONE number everywhere, never use 435-292-8802" rule is obsolete. Replaced with role-separation: Call=302-4422 (tel:), Text=292-8802 (sms:). See CRITICAL RULES section.
- **What this unblocks:** TCR CTA verification → Twilio campaign activation → Twilio outbound SMS delivery (currently blocked by 10DLC error 30034). Verizon email-to-SMS gateway remains the working fallback in the meantime.

### 2026-04-22 (evening) — Mobile Polish: 3-Tier Pass + Swipe-Driven Reviews Carousel
Four focused commits cleaning up the mobile experience after the morning audit + afternoon hub-spoke work. No new features — just fixing what was already there but broken on phones.
- **Tier 1 — perf + touch targets (commit 95d04ff):** Killed the Mapbox 4s timeout fallback — IO + scroll triggers already cover real users, and the timeout was firing during Lighthouse audits on non-scrolling viewports, dragging 7s of Mapbox JS eval + 2.3s TBT into the initial-load measurement window. Null-safed the mobile nav toggle (`menuBtn.addEventListener` was throwing silently on homepage when element wasn't in expected state, breaking subsequent handler binding). SMS consent checkbox on hero + contact forms upgraded to WCAG-compliant tap target — label now min-height 44px with padding, input bumped browser-native ~13px → 22×22px, font 13→14px.
- **Tier 2 — type sizes + Quick Answer visibility + hamburger (commit eee3c00):** Homepage mobile ≤600px: hero-badge 10→12px (trust strip + BBB line was unreadable), hero p 14→16px (was below body-copy minimum), hero-points 13→15px. ≤900px: hero p 15→16px for consistency. Quick Answer boxes on 4 service pages (roof-replacement, storm-damage, insurance-claims, residential-roofing) — swapped #FAF9F5 cream bg → #EEF4F9 navy-tint (cream-on-cream page was nearly invisible), bumped border 4→6px, added subtle shadow for card affordance. Hamburger (.mobile-btn) was 33×24 — added min-width/min-height 44px + flexbox centering + padding 10px (WCAG 2.5.5 / Fitts' Law).
- **Tier 3 — legibility + thumb-zone polish (commit eef8145):** `.last-updated` freshness stamp inside blog/location hero backgrounds — force high-contrast rgba(255,255,255,0.82) via global CSS override (inline #6B7280 was rendering as faded grey on dark-navy heroes, missing the freshness signal Google + readers should see). `.content-section p` unified at 16px mobile (was special-cased at 15px for no reason). Homepage hero subhead trimmed 5 sentences → 2 (family-owned + trust triad) — phone already lives in CTA button + sticky mobile bar, and the 5-line wrap was crowding the primary CTA out of the first scroll.
- **Mobile reviews carousel — swipe-driven, content-sized (commit 764fdc9):** Three problems at once — short reviews rendered as tall empty boxes (min-height on slides), three long quotes stretched slides past 500px (no line clamp), and prev/next arrows were 32×32 — under the 44px touch minimum. Fixes: removed min-height on .rc-slide at ≤600px; clamped review body to 7 lines; hid .rc-arrow on ≤600px (dots remain for tap-to-page). Added touchstart/touchend swipe handler on `.rc-viewport` — 40px horizontal threshold, only fires when |dx| > |dy| so vertical page scroll still works; auto-advance pauses during touch. `touch-action: pan-y` lets browser own vertical scroll while handler owns horizontal.
- **Untracked in repo (FYI):** `data/` and `screenshots/` directories not tracked.

### 2026-04-22 (afternoon) — Close Audit Follow-Ups: Hail Hub-Spoke + Brand-Safe Review Sync + reviews.json Unblock
Cleared the two "Not auto-fixed" items from the morning audit, plus a second `.vercelignore` booby-trap that surfaced same day.
- **Hail/storm cluster cannibalization resolved (commit b51894f):** The Utah hail pillar (`blog/utah/utah-hail-season-roof-guide-2026`) was competing with 8 city-level hail/storm posts for statewide queries. Fixed via **hub-spoke internal linking** (not canonicalization — city posts keep their long-tail traffic, pillar becomes authority):
  - Added "Statewide context" callout (`aside.pillar-callout`) at the top of 8 city posts: bountiful, draper, herriman, lehi, ogden, provo, riverton, sandy — each links to the pillar with city-specific "stay here for..." framing
  - Added "City-Specific Hail & Storm Guides" spoke block to the pillar linking all 8 cities (above the Service Areas H2)
  - Idempotent markers (`pillar-callout`, `city-guide-spokes`) — safe to re-run
- **reviews.json / GBP verification complete (commit b51894f):** Ran `scripts/update-google-reviews.py` against live SerpAPI pinned to the GBP data_id. Aggregate verified: **20 reviews, 5.0 stars (unchanged)**. Pulled 8 fresh snippets — 2 filtered because they used the old "Frame Restoration" brand wording (would have undone the morning's brand-leak lockdown).
  - **Script hardening:** new `_has_brand_leak()` drops bare "Frame Restoration" (allows "Frame Restoration Utah" LLC name); new `_merge_reviews()` preserves `city` metadata from prior curated feed when SerpAPI returns a known author; guardrail falls back to existing feed if filter nukes all fresh reviews
  - index.html schema `ratingValue` normalized "5" → "5.0"
- **.vercelignore booby-trap #2 (commit 565d920, incident 2026-04-22b):** Audit sweep had lumped `reviews.json` into "Data files not meant for public serving" — but index.html **fetches /reviews.json at runtime** to populate the live carousel. Carousel was sitting invisible (data-ready=0 → opacity:0) for JS-enabled visitors. Fix: removed from ignore list; added explicit entry to warning header; broadened the check from "is this shared with a customer" → "is this **fetched by the site** OR shared with a customer" (same root cause as the morning's seo-report.html incident).
- **Still open (minor):** 4 new-reviewer cities (Arikka Von, LeeAndra Jones, Yonghong Xu, Arianne Kaspar) render as "Google Review" until we backfill cities manually or SerpAPI starts exposing reviewer location.

### 2026-04-22 (hotfix) — Unblock seo-report.html + .vercelignore guardrail
- **Production 404 on /seo-report fixed:** The morning's audit commit (28f0dfa) had swept `seo-report.html` into `.vercelignore` as "internal", but it's the PIN-gated live SEO & lead tracker shared with Landon. Removed from ignore list; Vercel redeployed it (commit 30bad68).
- **Guardrail added:** 13-line warning header prepended to `.vercelignore` listing customer-facing HTML deliverables that must NEVER be ignored (seo-report.html, directory-blitz-tool.html, backlink-playbook.html, directory-tracker.html, review/, etc.). Prevents future audit-cleanup passes from re-blocking shared tools (commit b01847a).

### 2026-04-22 — Full Audit Fix: Brand-Leak Lockdown + AEO/Schema Pass + Perf
**101 files changed.** Fixes surfaced by the 2026-04-22 full site + ops audit.
- **Texas-brand entity leak lockdown (critical):** Google was cross-resolving Frame Roofing Utah into Landon's unrelated framerestorations.com (Frisco TX). Replaced sales@framerestorations.com → info@framerestorationutah.com across index, legal pages, and 10 blog posts. Removed parentOrganization block from index LocalBusiness schema (it's a DBA, not a subsidiary). Deleted broken "Part of Frame Roofing Utah" Texas-domain footer link. Changed about page foundingLocation schema from Dallas-Fort Worth → Heber City; rewrote "Our Story" H2 + lede around Heber City origin. Dropped "same standards from DFW" copy from services.html source fragment. Narrowed homepage services sub from "all of Utah" → "Wasatch Front + Heber Valley".
- **Stop deploying internal files (critical):** New `.vercelignore` blocks CLAUDE.md (contains Supabase anon key + rules), *.md strategy docs, *.py scripts, drive-photos.zip (8.5 MB customer photos), archive/, tmp-*/, internal audit/scratch HTML files.
- **Content leakage:** Removed two `EDITOR CHECK` placeholder paragraphs that had shipped to production on blog/park-city/ice-dam-winter-roof-damage.html. Grep-swept rest of blog — clean.
- **Schema + AEO pass:** Added FAQPage JSON-LD + visible Quick Answer passages to 4 service pages (roof-replacement, storm-damage, insurance-claims, residential-roofing). Added AggregateRating (20 reviews, 5.0 stars) to all 45 location pages. Added SpeakableSpecification to homepage FAQPage. Backfilled dateModified on two 2026 posts that still showed 2026-03-27. Added visible "Last updated" stamp under H1 on 87 blog + location pages. Fixed malformed stray parentOrganization block on west-valley-city.html.
- **Performance:** Removed double-loading Google Fonts <link> (self-hosted WOFF2 already wired in global.css — kills render-blocking + double FOUT). Changed drone-loop video preload from "metadata" → "none" (13 MB clip was loading eagerly on mobile). Added width/height to 6 service-card imgs (CLS fix on lazy-load).
- **Dedup + hygiene:** Standardized "24/7 emergency response" across homepage FAQ/schema + services.html. Deleted privacy-policy.html and terms-of-service.html duplicates; added 301 redirects to /privacy and /terms in vercel.json. Fixed privacy.html/terms.html canonicals to use www subdomain. Removed Disallow /privacy + /terms from robots.txt (they're already noindex via meta). Regenerated sitemap.xml: **126 URLs → 105**, pruned dupes, removed robots-disallowed paths and Vercel-redirected routes, fixed missing trailing slash on root.
- **Not auto-fixed (resolved afternoon of 2026-04-22 in commit b51894f):**
  - ~~8-post hail/storm city cluster cannibalizes the Utah-wide pillar~~ ✅ Fixed via hub-spoke internal linking (see 2026-04-22 afternoon log)
  - ~~reviews.json entries need 1:1 verification against the pinned GBP data_id~~ ✅ Verified 20 reviews / 5.0 stars; script hardened with brand-leak filter + fallback

### 2026-04-21 — Homepage Video Quality Bump + Untracked Repo Cleanup
- **Showcase video upscaled** 1024x576 → 1920x1080 (music kept bit-perfect)
- `drone-footage.mp4` replaced with stabilized + color-graded version; new `drone-footage-clip2.mp4` added as second drone asset; posters regenerated from new sources
- **Committed previously-untracked April work:** SEO directory blitz scripts, DieselEye GTM HTMLs in images/projects, contact photos. Also updated `.gitignore` to exclude node_modules, __pycache__, tmp-*.

### 2026-04-20 (late) — Heber Valley Drone Job + Blog Hero Photo Rotation
- **Heber Valley custom reroof drone capture (in-progress job):** 4 drone photos (panorama, aerial-1, aerial-2, front-view) at 3 responsive sizes each (WebP + JPG fallback) + drone flyover video (MP4 + WebM + poster). Gallery now leads with FEATURED IN-PROGRESS PROJECT section; 4 new residential cards added at top of filtered grid. heber-city.html hero, og:image, and Article schema image all swapped to new aerial panorama. Labeled "in progress" — post-completion shots follow once job closes; photo-authenticity policy preserved (no fabricated before/afters).
- **Blog hero photo rotation (42 unique images, zero duplicates):** sandy-storm-damage-reroof-2025-oct had been repeating across 3 posts; heber-city-residential-reroof-2026 + frame-restoration-18 each duplicated; 6 posts had no hero image at all. Swapped to unique shots + added heroes to the 6 bare posts (ogden/wind-damage, riverton/hail-inspection, utah/commercial-roofing, utah/gutter-installation, utah/roof-financing, utah/spring-inspection). The mountain-grade-roofing-materials post now showcases the new Wasatch-Back drone aerial.

### 2026-04-20 — Homepage Video Showcase + 2 SEO/AEO Blog Posts
- **#watch-in-action homepage section** added directly after hero (above the fold) — 60-second Heber Valley reroof showcase
  - Click-to-play with poster image (preload=none) to keep LCP clean
  - Dual source: MP4 (H.264, faststart) + WebM (VP9) for broad browser support
  - VideoObject JSON-LD schema for Google video rich results
  - Existing Gallery, Video Showcase (drone loop), and Trust Bar sections untouched
  - Assets added: `/assets/videos/frame-restoration-showcase.mp4` (11.7 MB), `.webm` (10.6 MB), poster `.jpg`
- **2 new blog posts (42 total, +2 sitemap URLs → 126):**
  - `/blog/utah/april-2026-wasatch-storm-roof-checklist.html` — post-storm 7-point inspection checklist after the April 2 Wasatch Front storm; Article + HowTo + FAQ schema; TL;DR answer box optimized for AI Overviews / extractive snippets; targets Salt Lake / Davis / Weber / Utah County post-storm searches
  - `/blog/bountiful/davis-county-wind-damage-guide.html` — Davis County wind-corridor geography + roof construction specs; targets Bountiful, Centerville, Farmington, Kaysville local SEO; Article + FAQ schema with city-specific areaServed; historical wind-event data card; Class H asphalt + standing seam recommendations
  - Blog index features both posts at top of grid; sitemap lastmod bumped 2026-04-20
- **Series plan:** 4 more posts scheduled (drought/snowpack, wildfire, hail season, city-specific)
- **Untracked in repo (FYI, not committed):** DieselEye HTML project files in `images/projects/`, test/helper scripts (blitz-test-brownbook.js, convert-webp.sh, directory-blitz.py, directory-blitz-prompt.md), node_modules/, package.json/lock, 2 contact screenshots

### 2026-04-17 (late) — Mapbox Service Map + Design System Sweep + Live Review Carousel + Auto Review Count
- **Interactive Mapbox GL JS 3D Wasatch Front service-area map** replaced the original SVG map (CSP updated to allow api.mapbox.com + tiles + events + worker blobs; bulletproof lazy-load with scroll + timeout fallbacks; 3 parallax silhouette mountain layers; self-hosted Tippy + Popper; per-city labelAnchor collision avoidance; tier dots + paint-order halos + progressive disclosure)
- **12 service pages redesigned** via global CSS overrides (no HTML changes needed — design system applied globally)
- **About/Meet Landon page redesigned** to match Frame Roofing design system
- **Before/after drag slider** in gallery (3 project pairs) — added then **pulled same day (commit 2861f0b)** because the before/after pairs were not same-roof; reinstate once true same-roof pairs are sourced
- **Live review carousel** reading `/reviews.json` on homepage
- **Review scraper switched from Google Places → SerpAPI**, made self-healing; auto-updates Google review count twice monthly
- **Review count bumped 19 → 20** in schema + hero CTA + hero subtitle (synced by self-healing scraper)
- **Dual sticky mobile CTA:** Call | Free Inspection (upgraded from single-button sticky call bar)
- **Replaced 14 stock images with real Landon job photos**; fixed og:image logo paths
- **Re-crop + relabel service tiles** for optimal visualization
- **Directory blitz tool:** added top-of-page "What We Need From You" todo block for Landon + solo-todo + GoDaddy delegate workaround; GoDaddy DNS item marked done
- **Fixed SMS consent label:** overflow on narrow viewports + broken label on hero + lead forms
- **Removed GitHub Action**, switching to Claude scheduled trigger for automation

### 2026-04-17 — H1 Rebrand + AEO llms.txt + 10DLC Consent + Reddit Scanner Hardening
- **Homepage H1 rebranded:** "The Gold Standard In Utah Roofing" → **"Mountain-Grade Roofing, Valley-Wide"** (positions Frame Roofing for both mountain-country Heber/Park City AND Wasatch Front valley markets)
- **llms.txt upgraded for AEO (Answer Engine Optimization):** 10 passage-citable Q&A blocks, 45 service areas categorized by county, verified-credentials-only policy (no BBB/certs not confirmed)
- **10DLC fix (error 30923):** Added optional SMS consent checkbox to lead form + linked consent text to privacy/terms policies (required for Twilio A2P 10DLC carrier compliance)
- **Reddit-scanner edge function hardened:** robust response parsing — HTTP status checks, empty body handling, retry logic, typed error returns

### 2026-04-12/13 — Performance + IndexNow + Sitemap Cleanup + Directory Blitz v4
- **Converted 88 images to WebP** and updated all HTML references (major perf win on LCP for location + service pages)
- **IndexNow integration** for bulk URL indexing (pushes updated URLs to Bing/Yandex/IndexNow-compatible engines immediately on publish)
- **Sitemap expanded 107 → 124 URLs** — added 17 missing pages Google couldn't discover
- **Directory blitz tool v4** with full blitz results; Angi moved to "Already Done" on 2026-04-13

### 2026-04-10 — Content + Schema + Analytics + Review Pipeline
- **6 new blog posts** added (5 long-tail SEO posts + spring roof inspection checklist) — blog count 34 → 40
- **HowTo JSON-LD schema complete across all 39 blog posts** (11 added earlier, gap closed today)
- **Blog↔Location crosslinks:** added internal blog links + city crosslinks to all 45 location pages, internal location links into 9 blog posts
- **Live SEO & Lead Attribution Tracker page launched** — Chart.js dashboards, Supabase-backed, default 30-day view
- **PIN login gate + multi-user admin panel** for SEO report (blocks public access, per-user credentials)
- **PostHog tracking rolled out:** init snippet on 107 pages, click event tracking on 47; CSP updated to allow us-assets.i.posthog.com + Chart.js CDN + Supabase edge function
- **Review landing page + `review-request` edge function** deployed for automated post-job review collection
- **Backlink acquisition playbook** (12 opportunities with step-by-step instructions) + **directory blitz tool v3**

### 2026-04-08 — Brand Sweep + FAQ/Schema Expansion + Security
- **Site-wide brand fix:** "Frame Restoration" → "Frame Roofing Utah" on all public pages (catches from earlier rebrand)
- **FAQ schemas expanded 5 → 12 city-specific per location page**, Article schema + direct-answer blocks added to all 45 location pages
- **Author bios, digitalSourceType, comparison tables** added to blog + service pages
- **Removed deprecated HowTo schema** from 4 blog posts that didn't fit the pattern
- **HSTS header added + PostHog reverse proxy** configured
- Sitemap lastmod bumped to 2026-04-08 for Google re-crawl
- Internal cross-links added between 6 priority cities + service pages

### 2026-04-07 (late evening) — SEO + A11y + Perf Sweep
- **HowTo schemas, CSP header, fixed broken JSON-LD** across site
- Fixed WebSite schema trailing comma + trimmed meta descriptions to 143 chars
- **301 redirects for old domain** (framerestorationutah.com → www.frameroofingutah.com) added in code
- **Perf + a11y:** compressed 24 images (-6.8MB), fixed aria-label roles, heading order, select label, added sr-only class
- BBB URL corrected to profile 90056184 (badge + schema sameAs both fixed)
- **SEO upgrade:** schema areaServed with 20 cities, social sameAs profiles, initial WebP conversion, Claude Code hooks for agent quality gates

### 2026-04-07 (evening) — Location Rewrite + Agent v2.2
- **All 44 location pages rewritten** with unique city-specific content (weather patterns, neighborhoods, housing stock, elevation, roofing challenges) — eliminates thin content / duplicate content SEO penalty risk
- **BBB A+ badge added to homepage trust bar** (SVG star icon, linked to correct BBB profile 90056184)
- Fixed broken BBB image tag the scheduled agent had left on the live page
- Corrected BBB URL across trust bar AND schema sameAs (both now profile 90056184)
- **Landon Message Agent upgraded to v2.2** — SKILL.md now 578 lines, 10/10 readiness on all 9 classification categories:
  - certification: SVG icon bank (8 icons), exact trust-item HTML, schema update, Lighthouse post-deploy
  - photos: iMessage extraction, sips auto-resize, Ollama alt-text, exact gallery HTML for both pages, fixed-grid warning
  - service_change: full 14-page inventory, exact service-card HTML, remove=escalate, Lighthouse verify
  - testimonial: copy-paste HTML template with star SVGs, reviewCount update, content verification
  - directory_update: curl HTTP pre-check, exact sameAs format, footer pattern, schema validation
  - repo_or_code: default=escalate, diagnostic toolkit (Formspree test, Lighthouse mobile, Playwright screenshots, git revert)
  - question: quick-reference cheat sheet (15 items)
  - excitement_or_status: soft follow-up awareness for potential content
  - general: Google Calendar MCP for scheduling
- **Global Post-Deploy Verification Protocol** added: HTTP 200 check → content grep → Lighthouse a11y+SEO ≥ 0.9 → auto-revert if dropped
- Agent SKILL.md location: `/Users/agenticmac/.claude/skills/landon-message-agent/SKILL.md`

### 2026-04-07 (earlier)
- BBB accreditation confirmed by Landon — removed from false claims list, added to confirmed list
- Updated Facebook link in footer: old /framerestorationutah → new /61572258054303 (Landon's new FB page)
- Apple Maps Business Connect rejected listing — service-area business without storefront, not actionable
- Landon now communicates primarily on Twilio number +14353024422 (not personal +18014103280)
- Landon Message Agent skill built and scheduled (every 15 min) — auto-processes Landon's iMessages
- Privacy & Terms pages updated with SMS/texting compliance for 10DLC
- 10DLC campaign resubmitted (expect 1-5 business days)

### 2026-04-01
- Phone number standardized to 435-302-4422 across all 103 HTML pages (index, locations, blog, services, archive, projects, legal pages) — ensures consistent NAP for local SEO signals
- Removed false BBB A+ and NRCA membership claims from all pages (these were AI-hallucinated trust badges that were never true)
- Added ⛔ CRITICAL RULES section to top of CLAUDE.md to prevent future false credential/certification claims

### 2026-03-30 (afternoon)
- Added About Us page (`pages/about.html`) with Landon Yokers owner bio, headshot (`images/landon-yokers-headshot.jpg`), and E-E-A-T trust signals
- Also created root-level `about.html`
- Site now has 115 HTML files, 102 sitemap URLs

### 2026-03-30 (morning)
- Landon completed 301 domain forwarding: framerestorationutah.com + www → frameroofingutah.com (permanent, path forwarding enabled)
- Updated robots.txt with AI crawler directives (GPTBot, ClaudeBot, PerplexityBot, Google-Extended) and blocked bad bots (Bytespider, AhrefsBot)
- GSC audit: 19 indexed, 89 not indexed. Performance: 10 clicks, 587 impressions, 1.7% CTR, avg pos 23.2
- Submitted indexing requests for 8 service pages (roof-repair, residential-roofing, roof-replacement, commercial-roofing, insurance-claims, gutters, emergency-tarping, solar-installation)
- Found and fixed redirect loop in vercel.json: /pages/storm-damage ↔ /pages/storm-damage-restoration was an infinite loop. Removed the wrong-direction rule.
- Discovered insurance-claims and emergency-tarping pages missing from sitemap

### 2026-03-29
- Auto-refresh: No new commits or file changes since 2026-03-28
- Corrected CLAUDE.md inaccuracies: location count 44→45, added /blog/utah/ (10 statewide posts), added gallery.html to pages list

### 2026-03-28
- Fixed Yelp URL in index.html footer (/biz/frame-restoration-utah → /biz/frame-restoration-heber-city)
- Updated homepage H1 from "For Your Utah Home" to "In Utah Roofing"
- Consolidated storm-damage-restoration → storm-damage (301 redirect, canonical, sitemap)
- Verified all location/service/blog pages already had meta descriptions, canonicals, OG tags
- Pushed to GitHub, confirmed live on frameroofingutah.com via Chrome
- Created this CLAUDE.md
- Set up auto-update scheduled task for memory refresh

### Previous sessions (summary)
- Created 6 new blog posts, audited 30 existing
- Built directory tracker (38 directories)
- Full SEO audit (interactive HTML report)
- Full rebrand Frame Restoration → Frame Roofing (all 86 HTML files)
- Built Supabase lead pipeline + edge function
- Set up Yelp listing for Landon
- Migrated all canonical/OG URLs to frameroofingutah.com
- Built OG image, service card images, project city photos
