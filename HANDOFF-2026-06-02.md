# Frame Roofing Utah — Full Handoff (2026-06-02)

Supersedes `HANDOFF-2026-06-01.md` and the earlier build-ready draft. Pick up cold from here. Branch every new task off `origin/main`.

## Snapshot
- **Repo:** `~/projects/frame-restoration-utah` · GitHub `Rconman99/frame-restoration-utah` · static HTML, Vercel auto-deploy from `main`.
- **Live:** `https://www.frameroofingutah.com` (canonical). Supabase ref `hdcflshhomzildwqlmwh`.
- **main:** all of today's PRs (#64–#74) merged. **All 5 compliance gates are now BLOCKING + green** (no more advisory/UNSTABLE): compliance-words · JSON-LD · link-integrity · CTA-integrity · doorway/thin-page.
- **Owner:** Landon Yokers. **Canonical brand:** "Frame Roofing Utah." Legal entity "Frame Restoration LLC."
- **Phones:** public/NAP `435-292-8802` (Twilio tracking) · `435-302-4422` = LANDON_PHONE, **internal-only, NEVER publish** (the CTA-integrity gate blocks it on customer pages).

## What shipped this session (11 PRs)
**Reviews / CTA defense (the big arc):**
- Root cause: the review CTA pointed at a **dead Google listing** (`cid=16833…`, which is actually this profile's internal location ID, not a duplicate). Real GBP = `cid=8458659884566588108` / place_id `ChIJCT6-aZD1TYcRzCpwX1kyY3U` / "Frame Restoration Utah," Heber City.
- **#67** repointed the CTA; **#70** upgraded to a one-tap write-review link (`search.google.com/local/writereview?placeid=ChIJCT6-aZD1TYcRzCpwX1kyY3U`).
- **3-layer defense (live on UT AND TX):** `#68` keystone `data/route-factory/business.json` + blocking `scripts/audit-cta-integrity.mjs`; `#69` weekly liveness monitor `scripts/check-cta-liveness.py` + `cta-liveness.yml`. (TX got the same via TX PRs #184/#186.)
- `SERPAPI_KEY` is now set in BOTH repos' Actions secrets → liveness Check B (GBP-alive) active.

**GBP enforcement watch (the real reason reviews were held):**
- Frame's GBP is **on Google enforcement watch** from a prior posting violation (a post that mentioned reviews/stars = DPNB), plus live policy-violating content pushed 2026-05-11: an **"Insurance Claim Assistance"** service (unlicensed-adjusting language) + a **"Free Roof Estimate"** service ("Free" = auto-reject).
- **#71** scrubbed the GBP draft docs (9× internal-phone leak fixed, adjusting language removed, services renamed, post rules added, correction banner on GBP-AUDIT).
- **Landon's live-dashboard fixes (status as of handoff):** description "insurance claim navigation" ✅ removed · "Insurance Claim Assistance" service ✅ renamed · "Free" in the Mar-31 post = optional/minor · **reinstatement/support note drafted** — Landon still needs to SEND it.

**Utah content + gates:**
- **#72** deepened the last 15 thin location pages past the 500-unique-word floor (560–622 words) with city-specific FAQ content (15 parallel agents, grounded with real facts, compliance-clean).
- **#73** flipped the doorway/thin-page guard advisory → **blocking** (all 45 location pages pass).
- **#74** consolidated duplicate about pages onto `/pages/about` (sitemap + 301 redirect; `pages/about.html` is canonical, richer, nav-linked).
- Also: #64 (Twilio-status doc), #65 (blog meta), #66 (Orem video caption).

## ⚠️ Open obligations (human / off-repo)
1. **Landon → send the GBP reinstatement note** (drafted in chat + memory `frame-gbp-enforcement-watch-2026-06-01`) via business.google.com → Support, + the "missing reviews" form. This is what should lift the enforcement watch and release held reviews.
2. **Optional GBP polish:** Mar-31 post's "Free inspections available" → "Roof inspections available" (behind the "…more" truncation).
3. **Watch:** reviews should reappear once the watch lifts — re-run `python3 scripts/check-cta-liveness.py` anytime.

## Backlog — next moves (prioritized, all branch off origin/main)
1. **PR #46 (schema entity-graph)** — now UNBLOCKED (`/pages/about` is canonical and #46 edits `pages/about.html`). It's ~50 commits behind with likely conflicts → **rescue = re-apply its `@id` entity-graph + Landon Person schema fresh off main, then close #46.** Don't merge the stale branch directly.
2. **Lighthouse cleanup** — `label-content-name-mismatch` (a11y); `errors-in-console` + `inspector-issues` (likely PostHog/CSP — verify); agentic-browsing 67 / `agent-accessibility-tree not well-formed`.
3. **New location pages: Fruit Heights + Spanish Fork** — served areas whose dead links were removed in #60; build real pages (use the deepened 15 as the depth/structure template — 2+ city-specific FAQ Q&As, ≥500 unique words, or the doorway gate now BLOCKS them).

## Key gotchas / cold-start notes
- **GBP edit access:** `ryanconwell99@gmail.com` DOES manage the profile (business.google.com/locations shows it Verified w/ Edit controls) — the earlier "no owner controls" was a Maps-side-panel quirk. But **don't pile automated edits on a watched profile** — remedial GBP edits go through a human login (Landon).
- **CTA-integrity gate** reads `data/route-factory/business.json`; scoped to deployed (`.vercelignore`) ∩ customer-facing (sitemap + core routes), strips HTML comments, checks writereview `placeid` == canonical, walk-skips `.claude/`.
- **City-quality gate is now BLOCKING** — any new/edited location page must clear 500 unique words + 30% city-uniqueness or it won't merge. Real city facts, not padding.
- **Compliance (Utah §31A-26-201):** never publish adjusting language (fight/handle/manage/navigate/negotiate/file/settle/adjust the claim, "maximize payout", public adjuster). Safe: "we document the damage, provide a line-item scope your adjuster can read, meet your adjuster on site — the claim stays between you and your insurer; we don't adjust."
- **Branch off `origin/main`** — older feature branches are stale.

## Memory (loaded each session)
- `frame-gbp-enforcement-watch-2026-06-01` — enforcement watch + live fixes owed
- `frame-cta-integrity-gate-2026-06-01` — review-link defense (3 layers, both sites) + place_ids
- `frame-10dlc-lessons-2026-06-01` — Utah Twilio SMS live
- `frame-utah-systems-port-2026-06-01` — TX→UT port baseline
