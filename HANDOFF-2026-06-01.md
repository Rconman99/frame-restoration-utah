# Frame Roofing Utah — Session Handoff (2026-06-01)

Full context to pick up cold. Pairs with `data/SITE-AUDIT-2026-06-01.md`,
`data/BRAND-DECISION-2026-06-01.md`, `data/NAP-DIRECTORY-CHECKLIST-2026-06-01.md`,
and memory `~/.claude/projects/-Users-agenticmac-projects-frame-restoration-texas-v2/memory/frame-utah-systems-port-2026-06-01.md`.

## Project facts
- **Repo:** `~/projects/frame-restoration-utah` · GitHub `Rconman99/frame-restoration-utah` · static HTML, Vercel auto-deploy from `main`.
- **Live:** `https://www.frameroofingutah.com` (canonical). `framerestorationutah.com` (old) 301→canonical. Vercel projects: `frameroofingutah` + `frame-restoration-utah`.
- **Owner:** Landon Yokers. **Legal:** "Frame Restoration Utah LLC" (BBB) / "Frame Restoration LLC" (state record — names disagree, confirm before DBA).
- **Public phone (NAP):** `435-292-8802` (Twilio tracking; `handle-call` forwards to `LANDON_PHONE = 435-302-4422`, which is **internal-only, never publish**).
- **DOPL:** S280 Roofing Contractor, license `#14256097-5501`. **GBP:** name "Frame Restoration Utah", data_id `0x874df59069be3e09:0x756332595f702acc` (cid `8458659884566588108`), 5.0/20 reviews. **Supabase ref:** `hdcflshhomzildwqlmwh`.
- **Canonical brand DECIDED:** "**Frame Roofing Utah**" (Option B — align GBP/directories to the website, not the reverse).
- **SMS stack: LIVE (Twilio, 10DLC approved).** All four `TWILIO_*` creds set in `app_config`; `handle-lead` (v28) fires owner SMS + customer speed-to-lead auto-text via Twilio. `send-message` (Cyborg-Ryan ad-hoc sender) deployed v3. Verizon vtext gateway is now a redundant owner-alert path, not the primary. (Older `DEPLOY.md`/10DLC docs that say "pending" are stale — corrected 2026-06-01.)

## What shipped this session (all MERGED to main/prod)
| PR | What |
|----|------|
| **#59** | TX→UT systems port: 6 compliance-gate scripts + `compliance-gate.yml`; Utah `compliance-words.json` (legal basis in `UTAH-COMPLIANCE-LAW.md` — §31A-26-201 unlicensed adjusting, §13-50-302 deductible-rebate, DOPL S280, **not** TX §4102); rating-integrity fix (removed cloned AggregateRating from 45 location pages); `google-reviews-sync.yml` (wraps UT's existing Python SerpAPI scraper); `deploy-edge-function.yml`. **Site-wide compliance copy remediation (~60 files)**: "we handle the insurance / manage the claim / fight for / maximize payout / work directly with insurer" → "we document, the claim stays between you and your insurer — we don't adjust." Plus `llms.txt` phone → 292-8802, `hasMap` on homepage, real Review schema + visible cards on Sandy + Millcreek. Skipped verify-pin/_shared/track-analytics (TX-specific dead code). |
| **#60** | Audit fixes: 17→0 broken links (path bugs + dead-link removal); **link-integrity gate → `--strict` blocking**; 49 titles + 47 metas trimmed to length; 5 og:image added; sitemap orphan resolved. Added `scripts/audit-full-site.mjs` + `data/SITE-AUDIT-2026-06-01.md`. |
| **#61** | FAQ + FAQPage schema on the 3 thin service pages (general-contracting, solar-installation, water-fire-flood-restoration). |
| **#62** | One accurate, city-specific paragraph on all 32 thin location pages. |
| **#63** | Second local technical paragraph on 27 of those → 15/32 now clear the 500-unique-word floor. |

## Current site health
LCP **291ms** · CLS **0.02** · SEO **100** · Accessibility **100** · JSON-LD 100% valid (364 blocks) · 0 broken links · FAQ on 102/116 pages · all **blocking** gates green.

## Gate suite (`.github/workflows/compliance-gate.yml`)
- **BLOCKING:** compliance-words, jsonld-validity, link-integrity (`--strict`).
- **ADVISORY (`continue-on-error`):** city-quality (shows "fail" in checks on the 17 thin pages — does NOT block merge). This is why merged PRs show `UNSTABLE` mergeStateStatus; that's expected.

## Backlog (prioritized, for a fresh session)
**On-repo content/decisions:**
1. **17 location pages at 484–498 unique words** — clear the advisory floor with a real city FAQ or project example per page (NOT vocabulary padding). Pages: american-fork, bluffdale, clearfield, kaysville, kearns, lindon, magna, north-salt-lake, payson, santaquin, south-salt-lake, springville, syracuse, wallsburg, west-bountiful, west-valley-city, woods-cross.
2. `pages/about.html` self-canonicals and may duplicate `/about` — owner decision: add to sitemap or canonical to `/about`.
3. **Lighthouse items:** `label-content-name-mismatch` (a11y); `errors-in-console` + `inspector-issues` (likely PostHog/CSP — verify); **agentic-browsing 67** / `agent-accessibility-tree not well-formed` (AI-agent crawlability).
4. **Fruit Heights + Spanish Fork** — served areas with no location page (dead links were removed in #60); candidates for new pages.

**Off-repo (human / can't be done from code):**
5. **Repo secrets** (so the new workflows run): `SERPAPI_KEY` (reviews sync), `SUPABASE_ACCESS_TOKEN` (edge-fn deploy). Both workflows skip gracefully until set.
6. **NAP / brand cleanup** (`data/NAP-DIRECTORY-CHECKLIST-2026-06-01.md`): send Landon the cleanup text (drafted in chat); confirm exact registered LLC name; **file DBA "Frame Roofing Utah"** → then edit GBP name (reviews persist); update name + phone on listings. Verified wrong-name listings: BBB, Nextdoor, BuildZoom, GBP. Unverified (blocked to auto-check): HomeAdvisor, Yelp, FB/IG. `framerestorations.com/utah-home` returns 404 (stale-indexed — redirect or remove).
7. ~~Commit `supabase/functions/send-message/`~~ **DONE** — verified 2026-06-01: tracked + committed (`57bd993`) and deployed live (Supabase v3). No drift; nothing to do.

## Gotchas / notes
- `audit-full-site.mjs` had an apostrophe-truncation bug in meta extraction (falsely reported "broken/dup metas") — fixed to delimiter-aware + entity-decoded (rendered) length. Re-run is trustworthy.
- `audit-links` is scoped to **served pages** (ignores archive/, data/, images/, build-intelligence/, dashboard/ + `index-redesign.html`, `leads.html`, `directory-blitz-tool.html`, `directory-tracker.html`). The original "17 broken" was mostly archive/draft noise.
- `/innovate` methodology finding (verified primary source): Google Search Central May-2026 = "**AEO/GEO is just SEO**" — no llms.txt / AI-files / special schema needed for its AI features. Don't chase AEO gimmicks; do keep clean schema + citable FAQ + NAP consistency for the non-Google engines (ChatGPT/Perplexity differ).
- Working branches for the merged PRs still exist locally (`feat/tx-systems-port`, `fix/*`) — safe to delete.
- This repo is separate from the cwd project (`frame-restoration-texas-v2`); Utah was originally the template TX was cloned from, now improved independently.
